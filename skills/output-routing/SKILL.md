---
name: rbtv-output-routing
description: "Post-install setup — interactively write `## File Routing` blocks into workspace CLAUDE.md files so the `rbtv-output-resolution` rule has routing to read at runtime. Use when setting up a new RBTV install or when routing needs to change."
---

# Output Routing Setup

**CRITICAL — Execute these steps in order. Load persona/workflow files FULLY before acting.**

1. Read and execute the workflow at `{rbtv_path}/workflows/output-routing/workflow.md`.
