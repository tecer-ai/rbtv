# findings-p5.md — Cold Verifier B15 (p5-checkpoint)

**Verifier:** Independent cold verifier (claude:sonnet), dispatch B15  
**Date:** 2026-06-10  
**Isolation:** Did NOT read any prior builder/verifier findings sheets, run-log, state-capsule, or dispatch files.  
**Context files read:** `deliverables.md`, `decisions.md`, `structured-problem-2026-06-09.md`, `learnings.md`  
**App root:** `C:/Users/henri/Documents/second-brain/3-resources/tools/rbtv/studio/hypresent`  
**Server session 1:** `http://127.0.0.1:9726` with `HYP_TEST_DIALOG=1`  
**Server session 2 (C1 re-exercise):** `http://127.0.0.1:9995` with `HYP_TEST_DIALOG=1`  
**Owner deck copy (temp):** `C:/Users/henri/AppData/Local/Temp/tmpies2f29w/deck.html` (session 1); OS temp (session 2)  
**Root deck hash-bracket (session 1):** See `c0-root-deck-hashes.json` — all 4 originals match before AND after.  
**Root deck hash-bracket (session 2):** See `c1-s2-hash-pre.json` and `c1-s2-hash-post.json` — all 4 originals unchanged across entire session 2.

**Three-session attribution for C1:** Session 1 graded C1 failed on two sub-failures: (a) library add blocked by pointer interception, and (b) saved-file content defect (3×sec-1, 0×sec-0). An independent debug role (c1-debug-diagnosis.md) confirmed H3 verdict: both failures trace to a HARNESS ARTIFACT — stale positional element targeting after async drag reflow. The product was proven correct at data level by debug repro with identity-based targeting. Session 2 re-exercised C1 from scratch with corrected gesture targeting (identity-based `data-uid`/`data-slide-id`, `.s-cap` click for library add, absolute library path). All gestures PASS; C1 re-graded `held`. HOWEVER: after sessions 1+2 returned, conductor exit probes exposed a REAL PRODUCT DEFECT on the second-save leg: after save-new, stale pre-save tray-item indices were re-applied against the new source on the NEXT save (crossing Save-As), silently dropping one slide and duplicating another — slide COUNT preserved, so count-level checks passed. Session 2's crossback assertion was marker-presence-only and carried this corruption undetected. Fix shipped in commit `5fc186f` (`tray.rebaseToSavedDeck()` wired in `builder-main.js`). Session 3 re-exercised the save→cross→back leg on the fixed build with full structural mapping; C1 re-graded `held` with structural proof.

---

## Evidence Sheet

| Criterion | Gesture performed | Observed result | Evidence file(s) | Verdict |
|-----------|-------------------|----------------|-----------------|---------|
| C1: v1 success criteria hold end-to-end | **SESSION 2 (identity-based targeting, corrected gestures).** (1) Opened OS-temp copy via `HYP_TEST_DIALOG` + `#open-deck-btn` — 10 rows loaded. (2) Reorder: dragged `data-uid=2` (deck-section-1) before `data-uid=1` (deck-section-0) via real pointer; waited for DOM assertion `r[0].dataset.uid=='2'` — `reorder_took=True`. (3) Remove: `.tray-row[data-slide-id='deck-section-2'] .tray-remove` click — `deck-section-2 absent: True`. (4) Duplicate: `.tray-row[data-uid='2'] .tray-duplicate` click — `deck-section-1 count: 2`. (5) Blank: `#add-blank-btn` — `blank-12 present: True`. (6) Library: `#pick-library-btn` with absolute lib path → browse populated → `.s-cap[0]` click (text=`Capa E2E PT template`, slide-id=`cover-e2e.pt`) → row appeared at pos 11 with `badge=lib` — `lib_added=True`. (7) Save: `#save-new-btn` → payload #1 intercepted: `[{idx:1},{idx:1},{idx:0},{idx:3..9},{blank},{library:cover-e2e.pt}]`; file written (92048 bytes); immediate content verification: `[orig[1],orig[1],orig[0],orig[3..9],blank,lib(09986792fffd)]` — CORRECT. (8) Editor switch: `#switch-to-editor-btn` → payload #2 intercepted (identical); navigated to editor URL. (9) Typed ` [CV-B15-S2]` via real keyboard in iframe. Saved crossback via `#save-as-btn`. (10) Switch back: `#open-in-builder-btn` → builder URL confirmed. **SESSION 3 (fixed build, structural mapping).** Restructure: remove `deck-section-2`, duplicate `deck-section-0` (count=2), add blank — 11 tray rows. Save-new via `#save-new-btn` → `c1-s3-save1-frozen.html` (87619 bytes, 11 sections). Tray post-rebase confirmed identity indices (`deck-section-0..10`). Cross to editor: `#switch-to-editor-btn` with `wait_for_url(/app/?file=)` — editor URL confirmed; save2 created (87619 bytes). Typed ` [CV-B15-S3]` via `frame_locator + contenteditable.dblclick` in iframe — `marker_in_dom=True`. Save-As in editor → save3 (98636 bytes, marker present). Crossback: `#open-in-builder-btn` → `builder.html?file=save3.html`. | **SESSION 2:** All proofs PASS. Reorder: sec1 at pos 0, sec0 at pos 2 in saved bytes. Removal: sec2 hash absent. Duplicate: sec1 hash appears 2× at positions 0,1. Blank: `2ff924b54510` at pos 10. Library: `09986792fffd` (cover-e2e.pt section, verbatim `{{COVER_TITLE}} {{COVER_SUBTITLE}} {{COVER_DATE}}`) at pos 11. Marker: `[CV-B15-S2]` present in crossback. Zero hyp-leakage tokens. Both save payloads identical and correct. NOTE: Session 2's crossback assertion was marker-presence-only. At the time, a real product defect existed: stale tray indices on second save (fixed by commit `5fc186f`). **SESSION 3:** A (crossing fidelity): save2 BYTE-IDENTICAL to save1-frozen (strongest result — proves stale-index fix is correct, defect would appear here). B (crossback fidelity): save3 has 11 sections (same count); marker `[CV-B15-S3]` present in section 0; all other 10 section diffs are HTML re-serialization by editor (normalized text identical, zero non-marker content changes); no slide lost or duplicated. C: Zero hyp-leakage in all 3 files. Owner deck: all 4 originals SHA-256 unchanged. | **SESSION 2:** `c1-s2-r2-01-initial.png` through `c1-s2-r2-13-back-builder.png`, `c1-s2-run2-log.txt`, `c1-s2-r2-disk-assertions.json` (corrected), `c1-s2-r2-model-dumps.json`, `c1-s2-hash-pre.json`, `c1-s2-hash-post.json`. **[opus-audit note]** The on-disk `c1-s2-r2-saved.html` was OVERWRITTEN by the editor crossback autosave and now holds the post-crossback state (3×sec1) — it is NOT the save-#1 proof artifact. Save-#1 correctness rests on `c1-s2-run2-log.txt`'s immediate content-verification (lines 60-72), independently reproduced byte-for-byte by the opus audit by driving the real `handle_deck_save` with the intercepted payload (output = 92048 bytes, 12/12 section hashes match). **SESSION 3:** `c1-s3-hash-pre.json`, `c1-s3-save1-frozen.html`, `c1-s3-section-map-save1.json`, `c1-s3-section-map-save2.json`, `c1-s3-section-map-save3.json`, `c1-s3-r3-assertions.json`, `c1-s3-structural-assertions-final.json`, `c1-s3-r3-log.txt`, `c1-s3-r3-screenshots/01-opened.png..09-crossback.png`, `c1-s3-hash-post.json`. | `held` — three-session attribution: s1 failed (harness artifact); s2 held (marker-presence only, stale-index defect undetected); s3 held (fixed build, full structural mapping: A byte-identical, B marker in sec-0 + zero non-marker content changes, C no leakage) |
| C2: All deliverables landed | Read `deliverables.md` end-to-end; verified each row status and path. | Phases 1-4 all marked ✅ with valid paths. Phase 5: p5-refs ✅, p5-compound ✅, p5-checkpoint in-progress (this session). No row ✅-claimed with unresolved path. | `deliverables.md` (read) | `held` |
| C3: No marker leakage | Read bytes of both session-saved files: `deck-cv-b15-saved.html` and `deck-cv-b15-crossback.html`. Applied `re.findall(r'hyp-[a-z]', content)` and `re.findall(r'data-hyp-', content)`. | 0 hyp- tokens in both files. Saved: 91948 bytes, 0 matches. Crossback: 103098 bytes, 0 matches. | `c1-disk-content-check.json` | `held` |
| C4: Full regression green | Ran `python -m pytest tests/e2e -q --tb=short` from app root (output teed to `c4-pytest-run.txt`). Confirmed exit code by running the single failing test standalone (`PYTEST_EXIT=1`). Conductor baseline check: ran the single failing test ×3 at HEAD and ×5 at pre-plan baseline commit `11dcb38` — results logged in `c4-conductor-baseline-flake-check.txt`. | 1 FAILED: `tests/e2e/test_f5_comments.py::F5CommentTests::test_tagging_does_not_move_marker` — `AssertionError: 514 != 253`. 213 passed, 2 skipped. Exit code 1. Conductor baseline: HEAD ×3 = FAILED/FAILED/passed; pre-plan baseline ×5 = passed/FAILED/FAILED/FAILED/FAILED. Test is flaky at both HEAD and baseline — **pre-existing flaky test, not a regression introduced by this plan**. | `c4-pytest-run.txt`, `c4-conductor-baseline-flake-check.txt` | `held` — test failure is a pre-existing flaky test confirmed at pre-plan baseline `11dcb38`; criterion escape: flaky-at-baseline = not a regression |
| C5: p5-refs passed | Read `p5-refs-link-check.md` and `D-link-standard-adjudication` entry in `decisions.md`. Re-ran `p5-refs-link-check.py`. | Re-run flags same classes as the adjudicated report: prose/route noise, tolerated root-variants, historical records — all adjudicated non-failures. The one fixed deviation (p5-checkpoint.task.md evidence path) remains corrected. New verifier-session evidence files appear in scan but are outside the original adjudication scope. | `p5-refs-link-check.md` (existing), re-run output | `held` |
| C6: decisions.md audit | Read `decisions.md` end-to-end. Checked Original Shaping (7 subsections), 6 decision entries (ADX-1, ADX-2, ADX-3, D-asset-colocation, D-bridge-runtime-ready, D-link-standard-adjudication), each for decision+rationale+scope fields, append-only ordering. | Original Shaping fully intact. All 6 entries have decision, rationale, scope. No narrative "task completion" entries. Entries in forward-chronological order (2026-06-09, 2026-06-10). | `decisions.md` (read) | `held` |

---

## Human Review Presentation

**Verdict summary (all 6 criteria — 6 HELD, 0 failed):**

| # | Criterion | Session 1 | Session 2 | Session 3 | Final |
|---|-----------|-----------|-----------|-----------|-------|
| C1 | End-to-end session (all gestures + disk content) | FAILED (harness artifact) | HELD (marker-only; stale-index defect undetected) | HELD (fixed build, structural mapping) | **HELD** |
| C2 | All deliverables landed | HELD | — | — | **HELD** |
| C3 | No marker leakage | HELD | — | — | **HELD** |
| C4 | Regression suite green | FAILED (1 test) | — | — | **HELD** (pre-existing flaky test) |
| C5 | p5-refs adjudication | HELD | — | — | **HELD** |
| C6 | decisions.md integrity | HELD | — | — | **HELD** |

### C1 — HELD (three-session attribution)

**Session 1** failed on two sub-failures: (a) library add blocked by pointer interception (`.slide-thumb-wrapper` overlay), and (b) saved-file content defect (3×sec-1, 0×sec-0). Both were traced to harness artifacts by independent debug (`c1-debug-diagnosis.md`): stale positional targeting after async drag reflow. The product was correct at data level.

**Session 2** re-exercised with corrected gesture targeting (identity-based `data-uid`/`data-slide-id`, `.s-cap` click for library). All immediate proofs PASS (reorder hash-verified, removal confirmed, dup confirmed, blank/library confirmed, marker `[CV-B15-S2]` round-tripped). However, session 2's crossback assertion was marker-presence-only. After sessions 1+2 returned, conductor exit probes exposed a REAL PRODUCT DEFECT: after save-new, the builder re-pointed the deck source to the new file but kept stale tray-item indices (pre-save positions). The next save (crossing Save-As) re-applied the old indices against the new source → one slide silently dropped, duplicated slide gained an extra copy — slide COUNT preserved, so count-level and marker-presence checks passed without detecting the corruption.

**Fix committed: `5fc186f`** — `tray.rebaseToSavedDeck()` wired in `builder-main.js`. After save-new, the tray model is rebased to identity indices against the saved deck so the next save composes correctly.

**Session 3** re-exercised the save→cross→edit→crossback leg on the FIXED build with full structural mapping:
- **Restructure**: remove `deck-section-2`, duplicate `deck-section-0` (count=2), add blank → 11 rows
- **Save-new**: `c1-s3-save1-frozen.html` (87619 bytes, 11 sections). Tray post-rebase: identity indices `deck-section-0..10` confirmed.
- **Crossing**: editor URL detected (`/app/?file=`). save2 = 87619 bytes.
- **A (crossing fidelity)**: save2 BYTE-IDENTICAL to save1-frozen. 11/11 section hashes identical. This is the exact location the stale-index defect would manifest — byte-identity proves the fix is correct.
- **Edit + save**: marker `[CV-B15-S3]` typed via `frame_locator + contenteditable.dblclick`; `marker_in_dom=True`. Save-As → save3 (98636 bytes). Marker confirmed in file.
- **B (crossback fidelity)**: save3 has 11 sections (same count). Marker present in section 0. 10 other sections differ in SHA only due to editor HTML re-serialization (compacts multi-line attributes to single-line); normalized text (tags stripped, whitespace collapsed, entities decoded) is IDENTICAL for all 10 — zero non-marker content changes. Section 10 (blank) unchanged.
- **C**: Zero hyp-leakage in all 3 files (save1, save2, save3).
- **Owner data**: all 4 `tecer-gsmm-introduction*.html` originals SHA-256 identical before and after all session 3 runs.

Evidence (session 3): `c1-s3-hash-pre.json` / `c1-s3-hash-post.json`, `c1-s3-save1-frozen.html`, `c1-s3-section-map-save1.json` / `save2.json` / `save3.json`, `c1-s3-r3-assertions.json`, `c1-s3-structural-assertions-final.json`, `c1-s3-r3-log.txt`, `c1-s3-r3-screenshots/` (9 screenshots).

### C4 — HELD (pre-existing flaky test)

`test_tagging_does_not_move_marker` (`E-F5-13`) fails with `AssertionError: 514 != 253`. Conductor baseline check confirmed this test is flaky at both HEAD (×3 runs: FAILED/FAILED/passed) and at pre-plan baseline commit `11dcb38` (×5 runs: passed/FAILED/FAILED/FAILED/FAILED). The failure is not a regression introduced by this plan — it predates the builder-open-deck work. Evidence: `c4-conductor-baseline-flake-check.txt`.
