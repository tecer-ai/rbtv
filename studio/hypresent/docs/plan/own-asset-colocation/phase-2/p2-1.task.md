---
task_id: p2-1
status: pending
complexity_score: 13
human_review: optional
---

# Task p2-1: Headed done-gate proof — real deck renders own images after save-to-new-dir

## Goal

On a REAL deck copy with its own `assets/*` images (including a name-collision case), restructure in the builder and save to a NEW directory; the own-slide images render when the saved deck is reopened in the builder AND when it is opened in the editor. This is the done-gate floor for the feature (spec Test Plan row 7).

**Path anchor:** `tests/…`, `server/…` relative to the hypresent app dir `3-resources/tools/rbtv/studio/hypresent/`.

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| `../specs/own-asset-colocation-spec.md` | Test Plan row 7 + Fidelity floor / Evidence plausibility — the proof bar |
| `../decisions.md` | Evidence lands in-plan at `./phase-2/done-gate-evidence/`; collision decision (B) to exercise |
| `tests/e2e/test_pb11_deck_save.py` | Headed harness reference — `_copy_deck`, `_open_deck`, `H.set_fake_dialog`, `#save-new-btn` |
| `tests/e2e/conftest_helpers.py`, `tests/e2e/builder_helpers.py` | Server start + library-pick helpers for driving the builder/editor headed |

## Tools

| Tool | Purpose |
|------|---------|
| `rbtv-done-gate` | Invoke and follow it exactly — it owns the exercise discipline, evidence-sufficiency bar, and evidence routing. `.claude/skills/rbtv-done-gate/` |
| Playwright | Headed browser driving (the e2e harness uses `playwright.sync_api`) |

## Execution Flow

### Phase: Understand
1. Read every Context File. Invoke `rbtv-done-gate` and follow it. Mark this task in progress (Close stamp, `--status in_progress`).
2. Prepare a REAL deck copy that references its own `assets/*` images (use a real tecer deck copy with its `assets/`, or augment a `tecer-gsmm-introduction-test-v3.html` copy with a real image asset referenced by a section). Prepare a collision case: an added library fragment whose asset basename matches an own-asset, different image.

### Phase: Execute (real gestures, visible browser)
1. Open the deck copy in the builder; restructure (reorder/remove/duplicate + add the colliding library slide); Save-As to a NEW directory via the dialog.
2. Verify on disk: the saved `out_dir/assets/` holds the own images; the colliding own-asset landed under a renamed name; the saved section's ref was rewritten to match (grep the saved HTML).
3. Reopen the saved deck in the builder → assert each own-slide `<img>` renders (`naturalWidth > 0`), and the collision slide shows the CORRECT image.
4. Cross to the editor (open the saved file via the editor `?file=` path) → assert the same images render.
5. Capture evidence DURING the exercise: screenshots of both views, the `out_dir/assets/` directory listing, the rewritten-ref grep, and a console-log capture. Reject implausibly fast timings.

### Phase: Validate
1. Confirm every spec Test Plan row 7 sub-claim has a corresponding evidence file on disk. A claim without a file is not proven — mark `unexercisable` with the concrete blocker rather than asserting it.

### Phase: Close (standalone)
1. Write the evidence sheet + captures under `./phase-2/done-gate-evidence/` (per `rbtv-done-gate`).
2. Append a `decisions.md` discovery entry only if the exercise surfaced something that changes future work (e.g. an editor-side asset-resolution gap).
3. Present a brief summary; await approval.
4. After approval, stamp complete (run from the vault root; `<plan-dir>` = this plan's folder, holding the plan file + `deliverables.md`):
   `python 3-resources/tools/rbtv/orchestration/skills/orchestrating/scripts/stamp.py --plan-dir <plan-dir> --task p2-1 --status completed --scope worker`

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Done-gate evidence sheet + captures | `./phase-2/done-gate-evidence/` | markdown sheet + screenshots/logs |

## Return Contract

Return `status` · `landed` (evidence files) · `validation` (each row 7 sub-claim → evidence file + WALL_MS) · `concerns` · `open_questions`.

> `decisions.md` entries: decision + rationale + scope ONLY (+ optional one-word `compoundable` marker for harvest-worthy findings) — never file-lists or N→M narratives; supersede by appending, never rewrite.
