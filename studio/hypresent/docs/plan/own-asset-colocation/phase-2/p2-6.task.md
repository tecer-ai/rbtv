---
task_id: p2-6
status: completed
complexity_score: 13
human_review: required
---

# Task p2-6: OWNER's real save STILL copies no assets after p2-4+p2-5 — find the divergence no probe replicated

## Symptom (owner re-test, 2026-06-12 ~16:15Z)

After p2-4 (missing-source warning) and p2-5 (head-CSS chrome colocation) landed in the working tree — both conductor-validated green and headed-proven — the owner re-ran his real save: **same deck, same images, still no assets copied**. Owner instruction: capture-not-solve; this task carries EVERYTHING this session reasoned and tried so the next session starts from here, not from scratch.

## Path anchor

`server/…`, `app/…`, `tests/…` relative to the hypresent app dir `3-resources/tools/rbtv/studio/hypresent/` (inside the rbtv repo `3-resources/tools/rbtv/`, a nested git repo). Server: `python server/server.py` → `127.0.0.1:8765`, stdlib http.server, **NO auto-reload — a running server keeps executing OLD code until restarted**. Tests: `python -m pytest` from the hypresent app dir.

## State of the code (CRITICAL — read first)

- Committed `3ce0400` (now under HEAD `08206c9`+, branch `master`): own-asset SECTION colocation + collision rename/rewrite in `server/deck_api.py`/`server/recompose.py`.
- **UNCOMMITTED in the working tree** (validated 67/67 green, opus-reviewed, NOT yet committed — protect from parallel-session `git restore`):
  - p2-4: `assets_missing` response field (`server/deck_api.py`) + builder warning (`app/js/builder/builder-main.js` + `deck-save.js`) — warning text: `⚠ N own-asset ref(s) not colocated — source files not found beside the deck`.
  - p2-5: chrome scan `_html_outside_spans` in `server/deck_api.py` — head-CSS `url("assets/…")` refs now copied / reported missing.
  - Plus: `tests/test_deck_api.py` (+7 tests), `tests/e2e/test_pb11_deck_save.py` (+1 own-asset e2e test), `tests/e2e/p2_3_live_repro.py` (headed repro driver).
- First action on resume: `git -C 3-resources/tools/rbtv status` — confirm these files still carry the deltas (parallel sessions exist; if wiped, see `../decisions.md` and the run-log for what they contained — or re-derive from the task specs `p2-4.task.md`/`p2-5.task.md`).

## Everything PROVEN WORKING this session (do not re-probe blindly — all evidence on disk)

| Probe | Result | Evidence |
|-------|--------|----------|
| Direct `handle_deck_save` on real `…/gsmm/presentations/2026-06-02-introduction/tecer-gsmm-introduction.html` (temp copy WITH assets) | 5 section assets copied, STATUS 200 | session transcript probe (2026-06-12) |
| Direct call on real `…/2026-06-11-board/tecer-gsmm-board-v8.html` (temp copy WITH assets) | 5 copied, STATUS 200 | same |
| Headed BUILDER UI (open → restructure → `#save-new-btn`) on real gsmm intro deck copy, with p2-5 code | **all 8 assets copied** (5 section + 3 head-CSS backgrounds), editor renders cover-bg `naturalWidth=1600` | `./done-gate-evidence/p2-5-real-gsmm-proof.json` + `p2-5-real-gsmm-editor.png` |
| Headed BUILDER UI on small-deck-v3 copy | 5 copied | `./done-gate-evidence/p2-3-repro-results.json` + `p2-3-live-payloads.jsonl` |
| Headed BUILDER UI on the assets-less root copy (`tecer-gsmm-introduction-test-v3.html`) | 0 copied + **warning fires**: `⚠ 5 own-asset ref(s) not colocated…` | `./done-gate-evidence/p2-4-warning-probe.json` + `p2-4-warning-headed.png` |
| Named suite | 67 passed EXIT 0 | run-log gate rows |

## DISPROVEN suspects (with the disproving evidence)

- Handler logic wrong for the gsmm deck shape — disproven by the direct calls above.
- Live builder→server path diverges from direct calls — disproven by the headed UI probes (payload capture in `p2-3-live-payloads.jsonl`: `source_path` correct, `items` all `{kind:'existing'}`, `source_root_assets_exists:true` → 5 copied).
- Restructured `items` shape breaks the loop — disproven (captured items after drop+duplicate are well-formed).
- Stale `__pycache__` / wrong checkout — cleared and `deck_api.__file__` confirmed during p2-3.
- Head-CSS refs never colocated — WAS REAL (p2-5 fixed it; backgrounds were the most visible breakage on the real deck), but the owner still reproduces the failure post-p2-5.

## The three OPEN unknowns — investigate in this order

1. **Was the owner's server running the NEW (uncommitted) code?** The server does NOT auto-reload; the p2-4/p2-5 code exists only in the working tree. If his re-test reused a server started before the changes (or after but from a different checkout), it ran OLD code — which exactly reproduces "same issue" AND would show NO warning. **Decisive check:** with his server running, `curl -s -X POST http://127.0.0.1:8765/api/deck-save -d '{...}'` style probe or simply ask: did the status bar show the `⚠` warning or an `Assets copied: N` count? Old code shows NEITHER message fragment.
2. **Which save entry point does the owner actually use?** EVERY probe this session drove the BUILDER's `#save-new-btn`. The original vault task names the bite point as "the builder↔editor BRIDGE (every crossing is a Save-As to a NEW file)". If the owner saves from the EDITOR (or a bridge dialog), that flow may use a DIFFERENT endpoint/handler that has NO colocation at all — never instrumented, never probed. **Check `server/api.py` / `server/server.py` for save endpoints other than `/api/deck-save`, and `app/js/` editor code for its save path.**
3. **Which file does the owner open?** "Same deck" — but two fingerprint-identical, assets-less copies of the gsmm deck sit at the hypresent repo ROOT (`tecer-gsmm-introduction.html`, `tecer-gsmm-introduction-test-v3.html` — KEEP them until this closes, they are repro material). The real decks (`5-workbench/tecer-biz/prospects/gsmm/presentations/2026-06-02-introduction/` and `2026-06-11-board/`) have full `assets/` folders. With the NEW code, opening a root copy → warning fires (proven); so if the owner saw NO warning and NO copy, suspects 1 or 2 dominate.

## CONFIRMED LEAD (2026-06-12, post-halt owner exchange) — two separate Save-As endpoints; only one colocates

Conductor verified in source: the BUILDER's Save-As posts to `/api/deck-save` → `deck_api.handle_deck_save` (`server/server.py:191`) — the ONLY path with asset colocation; ALL session probes/tests drove this. The EDITOR's Save-As — and the leave-guard confirm-modal "Save As…" button (`app/js/shell/confirm-modal.js`, the builder↔editor bridge) — posts to `/api/save-as` / `/api/dialog-save-as` → `api.handle_save_as` (`server/api.py:162`, payload `{path, html}`), which **writes the HTML and copies NOTHING** — no assets, no warning. An owner flow through the editor/bridge Save-As reproduces the symptom exactly, on any deck, regardless of server freshness — this likely closes unknowns #1/#3. Owner also reports the saved deck does not "correctly load" — verify on reopen what breaks beyond images. Fix direction: give `handle_save_as` (and `handle_dialog_save_as`/the `/api/save` overwrite path where applicable) the same own-asset colocation + `assets_missing` semantics as `handle_deck_save` — factor the shared logic, don't duplicate it; same-dir overwrite stays a no-op. Owner confirmation of his exact button (Step 0) still settles it conclusively before coding.

## Step 0 — ask the owner FIRST (cheapest decisive evidence)

Before any code work, ask him to narrate ONE save end-to-end and answer:
- Which file he opens (full path) and HOW (builder open button? editor? recent list?).
- Which button performs the save (builder `#save-new-btn`? an editor menu? the bridge prompt?).
- What the status bar said verbatim after the save (with the new code it MUST say either `Assets copied: N` or the `⚠ … not colocated` warning — if it says neither, the server ran old code or a different endpoint handled the save).
- Whether he restarted `server/server.py` after 2026-06-12 ~16:00Z.

## Then

- If old-code server → restart, re-test, done (and COMMIT the wave via `rbtv-commit`, explicit pathspecs, after re-checking foreign-dirty — HEAD moves under parallel sessions).
- If a different endpoint → instrument it, capture the payload (mirror `tests/e2e/p2_3_live_repro.py`'s approach), extend colocation to that handler at the root cause, unit + headed proof.
- If root-copy file → the warning now surfaces it; decide with the owner whether to delete/relocate the trap copies.
- Re-validate at the done-gate floor: the OWNER's own narrated flow, executed headed, lands `assets/` with all images and renders in the editor.

## Context files

| File | Purpose |
|------|---------|
| `../decisions.md` | Full decision/discovery trail (read end-to-end — five 2026-06-12 entries cover this saga) |
| `./p2-3.task.md`, `./p2-4.task.md`, `./p2-5.task.md` | The three predecessor specs + errata |
| `server/deck_api.py` `handle_deck_save` | The (working-tree) handler with section + chrome colocation + assets_missing |
| `server/api.py`, `server/server.py` | Hunt for non-`/api/deck-save` save endpoints (open unknown #2) |
| `tests/e2e/p2_3_live_repro.py` | Working headed driver — adapt for any new scenario |
| `./done-gate-evidence/` | All probe evidence named above |

## Criteria

The owner's OWN flow (as he narrates it, executed by him or reproduced headed exactly) saves a deck to a new directory with `assets/` + all referenced images on disk and rendering in the editor; root cause of the re-test failure named with captured evidence; the uncommitted p2-4/p2-5 wave committed (or superseded) via `rbtv-commit`; suite green.

## Return contract

`status` · `landed` · `validation` (commands + EXIT + WALL_MS + captured payloads/answers) · `concerns` · `open_questions`.
