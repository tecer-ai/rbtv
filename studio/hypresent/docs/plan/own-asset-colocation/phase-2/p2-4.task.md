---
task_id: p2-4
status: completed
complexity_score: 7
human_review: required
---

# Task p2-4: missing-source warning — `assets_missing` in `/api/deck-save` response + non-blocking builder warning

## Goal

When a save-to-new-directory preserves sections referencing own `assets/*` files that do NOT exist beside the source deck, the save still succeeds exactly as today (spec Behavior row 5 copy semantics unchanged) — but the response carries the missing refs in a new `assets_missing` list and the builder status surface shows a non-blocking warning, so the owner is never silently shipped broken image refs (owner ruling, decisions.md 2026-06-12 "option B").

**Path anchor:** `server/…`, `app/…`, `tests/…` relative to the hypresent app dir `3-resources/tools/rbtv/studio/hypresent/`. Run `python -m pytest` from there.

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| `server/deck_api.py` `handle_deck_save` own-asset block (~L297-358) | The loop that already computes referenced assets per preserved section and hits `if not src_asset.exists(): continue` — the silent-skip point to instrument |
| `app/js/builder/deck-save.js` | Builds the save status-bar message from `assets_copied`/`assets_skipped` — extend for `assets_missing` |
| `../specs/own-asset-colocation-spec.md` | Behavior row 5 (missing source tolerated) — COPY semantics stay; only the response/UI surface grows |
| `../decisions.md` | The owner ruling driving this task + all prior invariants |
| `tests/test_deck_api.py` | Unit-test home — own-asset tests to mirror |

## Behavior

1. In the own-asset loop, when a referenced own asset's source file does not exist at `source_root`, record the deck-relative ref (e.g. `assets/gsmm-logo.png`) in an `assets_missing` list — deduplicated, stable order — instead of (not in addition to) any current silent `continue` bookkeeping. Copy/rename/rewrite behavior for EXISTING assets is byte-for-byte unchanged.
2. `handle_deck_save` response gains `"assets_missing": [...]` (empty list when none). Existing response fields unchanged.
3. `deck-save.js`: when `assets_missing` is non-empty, the post-save status message appends a warning fragment — e.g. `⚠ N own-asset ref(s) not colocated — source files not found beside the deck` — non-blocking (save still reports success), styling consistent with the existing status bar.
4. Library-asset behavior untouched; no warning for library refs.

## Constraints

- Minimal surgical diff; reuse the existing loop's ref discovery — do NOT add a second HTML scan.
- Non-colliding sections + separators stay byte-identical (existing faithfulness tests must stay green).
- No new endpoints, no config flags, no UI beyond the status-bar fragment.

## Test Plan

- Unit (`tests/test_deck_api.py`): (a) deck whose section references an own asset absent on disk → response `assets_missing == ["assets/<name>"]`, save succeeds, no `assets/` dir created (when nothing else copies); (b) mixed deck (one existing + one missing own asset) → existing one copied, missing one listed, HTML rewrite behavior unchanged; (c) no missing assets → `assets_missing == []`.
- Existing suite green: `python -m pytest tests/test_deck_api.py tests/test_recompose.py -q` then full `tests/e2e/test_pb11_deck_save.py`.

## Criteria

`assets_missing` returned exactly per the unit tests above; builder status bar shows the non-blocking warning on a real save of an assets-less deck (headed proof — one screenshot or DOM-text capture into `./done-gate-evidence/`); full named suite green; spec Behavior row 5 copy semantics unchanged.

## Return contract

`status` · `landed` · `validation` (commands + EXIT + WALL_MS) · `concerns` · `open_questions`.

## ADX-2 erratum (2026-06-12, conductor — appended)

Behavior item 3's warning surface: the builder status message is constructed in `app/js/builder/builder-main.js`, not in `deck-save.js` alone. Implement the warning fragment in whichever of `deck-save.js` / `builder-main.js` constructs the post-save status message (minimal diff across the two); both files are in the allowlist.
