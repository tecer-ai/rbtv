status: landed

landed: Applied Fix A and Fix B only in `app/js/shell/file-controls.js` and `tests/e2e/test_f5_comments.py`. No commit or push run.

validation: Ran `git --no-pager diff -- app/js/shell/file-controls.js tests/e2e/test_f5_comments.py`

```diff
diff --git a/studio/hypresent/app/js/shell/file-controls.js b/studio/hypresent/app/js/shell/file-controls.js
index 12b38f3..4076f92 100644
--- a/studio/hypresent/app/js/shell/file-controls.js
+++ b/studio/hypresent/app/js/shell/file-controls.js
@@ -43,7 +43,7 @@
-      setStatusSetter()('No document open.', 'error');
+      getStatusSetter()('No document open.', 'error');
@@ -53,16 +53,17 @@
+      getStatusSetter()('Choose where to save the presentation — it then opens in the Builder.', '');
@@
-        setStatusSetter()('Save failed.', 'error');
+        getStatusSetter()('Save failed.', 'error');
@@
-      setStatusSetter()('Save failed: ' + err.message, 'error');
+      getStatusSetter()('Save failed: ' + err.message, 'error');
diff --git a/studio/hypresent/tests/e2e/test_f5_comments.py b/studio/hypresent/tests/e2e/test_f5_comments.py
@@ -104,17 +104,22 @@
-    def test_plain_enter_inserts_newline(self):            # E-F5-3
+    def test_enter_submits_shift_enter_newline(self):      # E-F5-3 (Enter submits; Shift+Enter = newline)
...
+        self.page.keyboard.press("Shift+Enter")
...
+        self.page.keyboard.press("Enter")
+        self.page.wait_for_timeout(300)
+        self.assertIsNone(self.page.query_selector(".hyp-comment-composer"))
+        cards = self.page.query_selector_all("#comment-threads .comment-thread")
+        self.assertEqual(len(cards), 1)
```

EXIT: `0`

Also checked `file-controls.js` contains no remaining `setStatusSetter`; all four call sites now use `getStatusSetter`.

concerns: Git emitted CRLF warnings for both modified files. No e2e tests were run because the task specified `test_command: NONE`.

open_questions: none