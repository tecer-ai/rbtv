---
task_id: T12
status: pending
phase: execute
complexity_score: 11
human_review: required
---

# Task T12: Resize — Flow-Aware (D1)

## Goal

CREATE flow-aware resize: mount Moveable on the selected element inside the iframe; on resize-end, write `width`/`height`/`flex-basis`/grid track based on the element's layout role; NEVER convert to `position:absolute`.

---

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| ../../../spec/04-implementation-plan.md | T12 row: contract, acceptance (V-RSZ-1/2/3, V-RSZ-NEG). |
| ../../../spec/01-architecture.md | §2 (Moveable runs in iframe, A5); §3 resize-commit command. |
| ../../../spec/03-module-map.md | §4 resize contract. |
| ../../decision-log.md | D1 + Move/Resize Reconciliation (axis selection, absolute targets). |
| ../shape.md | Constraint: `position:absolute` may be semantic content (read role first). |

---

## Execution Flow

### Phase: Understand
1. MUST read every Context File.
2. Mark T12 in progress in BOTH locations.
3. Confirm axis selection: flex-row child→`flex-basis`/width; flex-column child→height; grid child→track; absolute→`width`/`height` (+`top`/`left` only if explicitly dragged). Never force absolute.

### Phase: Execute
1. CREATE `runtime/js/resize.js`: `begin(hypId)` mounts Moveable (resizable) on the iframe element; `end()` reads `roleOf` and writes the correct sizing property; builds `commands.resize(before,after)` pushed to `history`; emits `geometry-changed`. Control box is `hyp-`classed.
2. UPDATE `app/js/shell/toolbar.js`: add the resize tool toggle (`set-tool resize`).

**Discovery Handling:** simple → resolve + shape.md; complex → add task, notify.

### Phase: Validate
1. V-RSZ-1 (DECK grid child) + V-RSZ-2 (flex child): correct sizing property changes; layout stays responsive; `position` not absolute.
2. V-RSZ-3 (REPORT semantic absolutely-positioned node): absolute role → width/height (+top/left if dragged) change; element remains absolute; `left:%` anchor not destroyed unless explicitly resized.
3. V-RSZ-NEG: no element force-converted to `position:absolute`.

### Phase: Close
1. Append decision/discovery to shape.md (standalone) or defer (orchestration).
2. `human_review: required` → emit the Human Review Presentation block (format + flag criteria per `3-resources/tools/rbtv/orchestration/workflows/planning/templates/plan-task-microstep-template.md` § Human Review Presentation and `3-resources/tools/rbtv/orchestration/workflows/_shared/authoring/human-review-criteria.md`) — D1 is a locked, non-negotiable behavior.
3. Mark T12 complete in BOTH locations after acceptance passes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Resize module | `runtime/js/resize.js` | ES module per §4 contract |
| Toolbar resize tool | `app/js/shell/toolbar.js` (UPDATE) | ES module |

---

## Revolving Plan Rules

- Append discoveries to shape.md; complex → add task; `PLAN MODIFIED:` in output.
- NOTE: `toolbar.js` is shared with T11/T13/T14; if concurrent edits risk collision, coordinate the `toolbar.js` UPDATE per `04` Parallelization note.
