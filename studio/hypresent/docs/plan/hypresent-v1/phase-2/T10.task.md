---
task_id: T10
status: pending
phase: execute
complexity_score: 8
human_review: optional
---

# Task T10: Text Edit (contenteditable lifecycle)

## Goal

CREATE text editing: double-click a text-editable element to enter `contenteditable` (recording prior state), commit on blur/Esc as an undoable text command.

---

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| ../../../spec/04-implementation-plan.md | T10 row: contract, acceptance (V-TXT-1/2, V-TXT-NEG). |
| ../../../spec/02-html-convention.md | §1 text-editability baseline, H4 opt-in/out + fallback (auto-detected flag). |
| ../../../spec/03-module-map.md | §4 text-edit + commands + history contracts. |
| ../shape.md | A12 namespacing; SVG path excluded from text. |

---

## Execution Flow

### Phase: Understand
1. MUST read every Context File.
2. Mark T10 in progress in BOTH locations.
3. Confirm: only operate on elements the registry marks text-editable; record original `contenteditable` so the serializer can restore it.

### Phase: Execute
1. CREATE `runtime/js/text-edit.js`: on double-click of a text-editable element, set `contenteditable=true` (record prior); on blur/Esc, build a `commands.text(before,after)` command and push via `history`.
2. Exclude SVG `<text>`/`<path>` and registry-locked nodes (H4 `data-hyp-text="false"`).

**Discovery Handling:** simple → resolve + shape.md; complex → add task, notify.

### Phase: Validate
1. V-TXT-1 (DECK): edit a slide subheading element; class preserved; one history entry.
2. V-TXT-2 (REPORT): edit a lead paragraph; the document's own reveal-animation state classes untouched.
3. V-TXT-NEG: CSS-generated label / counter spans not offered as editable, or editable + cleanly undoable with no JS-state breakage.

### Phase: Close
1. Append decision/discovery to shape.md (standalone) or defer (orchestration).
2. Mark T10 complete in BOTH locations after acceptance passes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Text edit | `runtime/js/text-edit.js` | ES module per §4 contract |

---

## Revolving Plan Rules

- Append discoveries to shape.md; complex → add task; `PLAN MODIFIED:` in output.
