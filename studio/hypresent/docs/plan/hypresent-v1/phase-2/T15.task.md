---
task_id: T15
status: pending
phase: execute
complexity_score: 12
human_review: required
---

# Task T15: Comments — Embedded JSON Island + Google-Slides UX (D4)

## Goal

CREATE comments: an in-memory thread store, read/write of the `<script type="application/json" id="hyp-comments">` island, element anchoring by the collision-resistant 5-field anchor key (`01` §6.1; never loses a thread — unresolvable → "unanchored"), add/reply/resolve, a one-time author-name prompt, an element-anchored marker→popover, and a side panel.

---

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| ../../../spec/04-implementation-plan.md | T15 row: contract, acceptance (V-CMT-1..5). |
| ../../../spec/01-architecture.md | §6.1 comment-anchor key (the concrete 5-field collision-resistant contract: build, match priority order, unanchored fallback); §5 island re-embed after strip. |
| ../../../spec/03-module-map.md | §4 comments + comment-panel contracts. |
| ../../decision-log.md | D4 (hidden JSON island, Google-Slides UX, author name once in localStorage). |
| ../shape.md | A12 namespacing; the island is the only `hyp-` survivor in output. |

---

## Execution Flow

### Phase: Understand
1. MUST read every Context File.
2. Mark T15 in progress in BOTH locations.
3. Confirm: anchors keyed by the collision-resistant 5-field key stored IN the island record (NOT `data-hyp-id`), so they survive the serializer strip and re-anchor on open. The bare tag+nth+id fingerprint COLLIDES on the repeated identical siblings BOTH fixtures contain (each fixture has repeated identical sibling cards with no id) — the content-hash + same-key sibling-index fields disambiguate them; unresolvable threads are never lost (→ "unanchored").

### Phase: Execute
1. CREATE `runtime/js/comments.js`: `load(islandJson)`, `toJson()`, `add(hypId,body,author)`, `reply(commentId,...)`, `resolve(commentId)`, `threads()`, `anchorRect(commentId)`, `buildAnchorKey(el)`, `matchAnchor(anchor)`; implement the anchor key (build, match priority order, unanchored fallback) per `01` §6.1 VERBATIM; each mutation is an undoable command (via `commands`/`history`); emit `comment-anchor-clicked`/`comment-requested`. Render comment text via `textContent` (XSS-safe; DOMPurify on comment text is OPTIONAL only — A11).
2. CREATE `app/js/shell/comment-panel.js`: side panel thread list; anchored popover (add/reply/resolve); prompt for author name ONCE, store in `localStorage`, reuse thereafter.

**Discovery Handling:** simple → resolve + shape.md; complex → add task, notify.

### Phase: Validate
1. V-CMT-1: first comment prompts name once (stored); marker anchored; thread in panel.
2. V-CMT-2: reply + resolve reflected; name not re-asked.
3. V-CMT-3: add → Save As → re-open: island present; thread re-anchors to the same element.
4. V-CMT-4: open the saved file WITHOUT the editor: island invisible; document normal.
5. V-CMT-5 (REPORT): after document-JS class mutation + reload, anchor re-resolves or thread shown "unanchored" (never lost).

### Phase: Close
1. Append decision/discovery to shape.md (standalone) or defer (orchestration).
2. `human_review: required` → emit the Human Review Presentation block (format + flag criteria per `3-resources/tools/rbtv/orchestration/workflows/planning/templates/plan-task-microstep-template.md` § Human Review Presentation and `3-resources/tools/rbtv/orchestration/workflows/_shared/authoring/human-review-criteria.md`) — verify island portability + invisibility + re-anchoring.
3. Mark T15 complete in BOTH locations after acceptance passes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Comments module | `runtime/js/comments.js` | ES module per §4 contract |
| Comment panel UI | `app/js/shell/comment-panel.js` | ES module |

---

## Revolving Plan Rules

- Append discoveries to shape.md; complex → add task; `PLAN MODIFIED:` in output.
