# DISPATCH — p3-4 (kimi executor) — WAVE 5 (single task, LAST writer of the shared files)

You are a bounded code executor. Implement EXACTLY the task below. You do NOT reason about intent or design — every decision is pre-resolved. If a code anchor excerpt does not match the live file, HALT and return `DOUBT_ESCALATED` with the precise mismatch — never guess.

## ⚠️ COMMIT OVERRIDE — DO NOT COMMIT (overrides the task's "Commit Rule")
A parallel session is ACTIVELY writing this shared repo. To avoid your commit sweeping foreign pre-staged files, **DO NOT run `git add` or `git commit`.** Run ALL validation, then leave your 4 allowlisted changes UNCOMMITTED in the working tree. The conductor commits with explicit pathspec. In `landed`, list the files you changed and write `commit: none (conductor commits with pathspec)`.

## ⚠️ E2E DRIVABILITY OVERRIDE (binding — supersedes the task's "Send real key + mouse events" for Ctrl+C/V/Shift+V)
Under browser automation on this platform, a synthetic `Ctrl+C` / `Ctrl+V` / `Ctrl+Shift+V` keydown does NOT cross into the slide iframe document (established this run: `Ctrl+B` crosses, the copy/paste combos do NOT). Therefore your `tests/e2e/test_copy_paste.py` MUST exercise copy/paste through the **registered bridge commands** — the GENUINE production path the keys call into — NOT through synthetic copy/paste keystrokes, and NEVER through a `window.*` test hook (the review WILL reject a test hook, exactly as it removed `window.__deleteComponentById` from p2-1):
- Copy: drive `bridge.command("copy")` (or the harness's command helper) after selecting a component — the SAME `register("copy", …)` you add in runtime-main.js step C.
- Paste float: drive `bridge.command("paste", { x, y })` with explicit slide-relative client coords.
- Paste into layout: drive `bridge.command("paste-into-layout", { x, y })`.
- Undo/serialize: drive the existing `undo` / `serialize` bridge commands.
This is the real path (clipboard.js → paste.js → commands.paste → history) — it is production drive, not a stub. (The Ctrl+C/V/Shift+V KEY routing itself is exercised headed at the p3-checkpoint by the conductor driving from shell focus — do NOT try to prove key-crossing in this automated e2e.) Mouse `mousemove` to set `lastPointer` for the KEY path may still be sent as a real event if you also want to smoke the pointer tracker, but the copy/paste ASSERTIONS run off the bridge commands.

## Binding obligations
- **Allowlist boundary:** Modify ONLY (work-dir-relative): `runtime/js/shortcuts.js` (✎), `runtime/js/runtime-main.js` (✎), `docs/spec/03-module-map.md` (✎); Create `tests/e2e/test_copy_paste.py` (✚). Any other file write is a violation. Do NOT modify `clipboard.js`/`paste.js`/`commands.js` — consume them as-is.
- **Bounded reads (NOT open exploration):** You MAY read these live files in the work-dir to confirm anchors + import surfaces — nothing broader: `runtime/js/shortcuts.js` + `runtime/js/runtime-main.js` (the files you extend — VERIFY the p2-1 anchors below match; HALT if not), `runtime/js/clipboard.js` + `runtime/js/paste.js` (the public surfaces you wire — note paste.js exports ONLY `pasteFloat`/`pasteIntoLayout`), `docs/spec/03-module-map.md` (the map you extend), and the existing `tests/e2e/` harness files (e.g. `test_shortcuts.py`, `test_r3_delete.py`, `conftest.py`/helpers) to MIRROR the harness + the bridge-command helper. Do NOT read other task files, the plan, or decisions.md.
- **Stray-file ban:** NEVER write scratch/log/summary files anywhere outside the allowlist.
- **Doubt policy:** `halt`. **Swarm policy:** `disabled` — do NOT launch any subagent.
- **Forbidden ops:** git push · git commit (conductor commits) · writes outside allowlist · destructive git reset · external production API calls.
- **Evidence:** capture validation output; the `validation` field cites EACH command (`node --check` ×2 + the pytest) with its `EXIT` + `WALL_MS`, and `SKIPPED_COUNT` + per-skip reason.

## Return schema — return EXACTLY these five named fields (not prose)
- `status`: `DONE` · `DONE_WITH_NOTES` · `BLOCKED` · `DOUBT_ESCALATED` · `NEEDS_CONTEXT`
- `landed`: files modified/created (NO commit — write `commit: none (conductor commits with pathspec)`)
- `validation`: each command, its `EXIT`, its `WALL_MS`; `SKIPPED_COUNT` + per-skip reason
- `concerns`: risks/smells/adjacent issues noticed
- `open_questions`: anything unresolved (the precise blocker if you halt)

## Inherited decisions (load-bearing — already reflected in the task; build EXACTLY as written)
- **Import ONLY `pasteFloat`/`pasteIntoLayout` from `paste.js`** — `pasteRegion` is intentionally module-internal (a `wasRegion` clipboard reaches the whole-slide path via delegation INSIDE `pasteFloat`/`pasteIntoLayout`). Do NOT import `pasteRegion`.
- **Pass LIVE cursor client-coords** to the paste handlers — `paste.js` resolves the target slide via `document.elementFromPoint(x,y)`, so the key path must read the current `lastPointer` (set by the `mousemove` tracker), and the bridge `paste` command takes explicit `{x,y}`.
- **`Ctrl+C` is a copy ONLY when a component is selected AND not editing AND no real text selection** (D4) — otherwise native copy (do nothing, no preventDefault). The handler returns boolean "did it copy"; `preventDefault()` only when it returned true.
- **Agent-native parity:** the `copy`/`paste`/`paste-into-layout` bridge `register(...)` calls (step C) exist so the shell OR an agent can drive paste, not just the keyboard — this is also what your e2e drives (see the E2E override). Keep them.
- **`byId` is already imported in `runtime-main.js`** (the p2-1 delete handler uses it) — the `copy` handler's `byId(info.hypId)` resolves; do NOT add a duplicate import.
- **Module map truthfulness** is a repo rule — add all 4 new modules (`shortcuts.js`, `clipboard.js`, `paste.js`, `shortcuts-help.js`) to `docs/spec/03-module-map.md` per the task.

---

## INLINED CONTEXT — `docs/plan/shortcuts-copypaste/specs/copypaste-spec.md` (authoritative; Ctrl+C guard rows + D4)

| # | When (gesture) | Then (observable result) |
|---|----------------|--------------------------|
| 1 | Component selected, not editing, `Ctrl+C` | clipboard slot stores a deep clone with `data-hyp-*` stripped, `wasRegion` set, `cascade=0` |
| 2 | `Ctrl+C` while editing / real text selection | NOT intercepted — native text copy (D4) |

D4: `Ctrl+C` while typing stays native text copy; it copies the COMPONENT only when a component is selected and NOT editing (and no non-collapsed text selection exists). D1: `Ctrl+V` floats a copy (no reflow); `Ctrl+Shift+V` inserts into the row/grid (reflows). Empty clipboard → no-op.

---

## TASK PAYLOAD — `phase-3/p3-4.task.md` (verbatim — implement exactly this, EXCEPT the Commit Rule + the e2e drive-path are overridden above)

---
task_id: p3-4
allowed_workdir: html/hypresent
allowlist:
  - runtime/js/shortcuts.js
  - runtime/js/runtime-main.js
  - docs/spec/03-module-map.md
  - tests/e2e/test_copy_paste.py
test_command: node --check runtime/js/shortcuts.js && node --check runtime/js/runtime-main.js && python -m pytest tests/e2e/test_copy_paste.py -q
doubt_policy: halt
swarm_policy: disabled

# Task p3-4: Wire copy/paste keys + register bridge commands + module map

## Goal

Wire the copy/paste keys and pointer tracking into the runtime, register the `copy`/`paste`/`paste-into-layout` bridge commands, and update the module map. Behaviour source of truth: the INLINED copypaste-spec above (the guards D4 + bindings). Depends on p2-1 (`shortcuts.js`, the `initShortcuts` wiring), p3-1 (`clipboard.js`), p3-2 (`commands.paste`), p3-3 (`paste.js`) — all landed when you run. This is the LAST writer of `runtime-main.js` (`p1-1 → p2-1 → p3-4`) and `shortcuts.js` (`p2-1 → p3-4`).

## Acceptance criteria (owner-observable)
- `Ctrl+C` copies the selected component (NOT while editing or with a real text selection — then native copy). `Ctrl+V` float-pastes under the cursor; `Ctrl+Shift+V` inserts into layout. Empty clipboard → no-op. (C6–C9 end-to-end.)
- The module map lists `shortcuts.js`, `clipboard.js`, `paste.js`, `shortcuts-help.js`.

## `runtime/js/shortcuts.js` edits (extend the p2-1 module)
1. **Pointer tracker:** add a `document` `mousemove` listener storing `lastPointer = { x: e.clientX, y: e.clientY }` (module-scope, init `{x:0,y:0}`).
2. **Handler bag:** `initShortcuts` now also reads `handlers.copy` (returns boolean: did it copy), `handlers.pasteFloat`, `handlers.pasteLayout`.
3. **Keydown cases** (add to the existing listener). Let `editing` be the existing contenteditable check and `textSelected = !!(window.getSelection() && !window.getSelection().isCollapsed)`:
   - `Ctrl/Meta+C` (no Alt/Shift): if `editing || textSelected` → do nothing (native copy). Else `if (handlers.copy && handlers.copy()) e.preventDefault();` (D4).
   - `Ctrl/Meta+V` without Shift: if `editing || textSelected` → native paste. Else `e.preventDefault(); handlers.pasteFloat(lastPointer.x, lastPointer.y);`.
   - `Ctrl/Meta+Shift+V`: if `editing || textSelected` → native. Else `e.preventDefault(); handlers.pasteLayout(lastPointer.x, lastPointer.y);`.

## `runtime/js/runtime-main.js` edits
### A. Imports — add after the `initShortcuts` import (from p2-1):
```js
import { initShortcuts } from "./shortcuts.js";
```
→
```js
import { initShortcuts } from "./shortcuts.js";
import { copy as clipboardCopy, hasContent as clipboardHasContent } from "./clipboard.js";
import { pasteFloat as doPasteFloat, pasteIntoLayout as doPasteLayout } from "./paste.js";
```
### B. Extend the `initShortcuts` call (created by p2-1; locate by exact text) to add the three handlers:
```js
  initShortcuts({
    bold: () => applyFormat("bold", current()),
    italic: () => applyFormat("italic", current()),
    deleteComponent: () => { const info = current(); if (info && info.hypId) deleteComponentById(info.hypId); },
    requestComment: () => emit("shortcut", { action: "comment" }),
    requestShortcutsHelp: () => emit("shortcut", { action: "show-shortcuts" }),
  });
```
→ add, inside the object:
```js
    copy: () => { const info = current(); if (!info || !info.hypId) return false; const el = byId(info.hypId); if (!el) return false; clipboardCopy(el); return true; },
    pasteFloat: (x, y) => doPasteFloat(x, y),
    pasteLayout: (x, y) => doPasteLayout(x, y),
```
### C. Register bridge commands (agent-native parity — the shell or an agent can drive paste too). Add near the other `register(...)` calls inside `boot()`:
```js
  register("copy", () => { const info = current(); if (!info || !info.hypId) return { copied: false }; const el = byId(info.hypId); if (!el) return { copied: false }; clipboardCopy(el); return { copied: true }; });
  register("paste", (payload) => { doPasteFloat((payload && payload.x) || 0, (payload && payload.y) || 0); return { ok: true, hasContent: clipboardHasContent() }; });
  register("paste-into-layout", (payload) => { doPasteLayout((payload && payload.x) || 0, (payload && payload.y) || 0); return { ok: true }; });
```

## `docs/spec/03-module-map.md` edits (keep it truthful)
1. In the file tree, under `runtime/js/`, add (after `serializer.js`):
   ```
   │     ├─ shortcuts.js          # in-iframe keydown router (bold/italic/delete/copy/paste) via DI from runtime-main; pointer tracker; forwards shell actions via emit("shortcut",{action})
   │     ├─ clipboard.js          # single in-memory component clipboard slot {node (data-hyp-* stripped), wasRegion, cascade}
   │     └─ paste.js              # float-paste (out-of-flow, no reflow) + insert-paste (FLIP reflow) + grid fallback + whole-slide duplicate; one history command per paste
   ```
   and under `app/js/shell/`, add: `│     │  ├─ shortcuts-help.js   # cheat-sheet overlay (grouped shortcut list; Esc/outside-click close)`.
2. In §4 (Iframe Runtime Modules) add a 2–3 line subsection for each of `shortcuts.js`, `clipboard.js`, `paste.js` (purpose + public surface), and in §3 (App-Shell Modules) one for `shortcuts-help.js`. Match the existing terse style.

## Self-verifiable checks
- `node --check runtime/js/shortcuts.js && node --check runtime/js/runtime-main.js` → EXIT 0.
- `python -m pytest tests/e2e/test_copy_paste.py -q` → passes.

## E2E test — `tests/e2e/test_copy_paste.py` (CREATE)
Mirror the `tests/e2e/` harness. Unique port `8813`. Use `copy_synthetic("flow-grow")` (flex row) and `copy_synthetic("grid-healthy")` (grid) where available. Cover: (a) **C6** select a component, `Ctrl+C`, move mouse over the slide, `Ctrl+V` → a copy exists AND each OTHER component's `getBoundingClientRect` is unchanged (assert via `doc_eval`); (b) **C7** flex: `Ctrl+Shift+V` inserts a sibling and a neighbour's measured position changes; grid: `Ctrl+Shift+V` produces an absolutely-positioned floating copy (assert `position:absolute`), the grid is not broken; (c) **C9** one Undo removes the pasted copy (Redo re-adds), and `bridge.command("serialize")` after a paste returns non-null HTML. Send real key + mouse events.
> **(Overridden by the E2E DRIVABILITY OVERRIDE at the top of this dispatch: drive copy/paste via the `copy`/`paste`/`paste-into-layout` BRIDGE COMMANDS, not synthetic Ctrl+C/V keystrokes, and NEVER a `window.*` hook. The assertions — siblings unchanged for C6, sibling moved / grid float for C7, undo+serialize for C9 — are unchanged.)**

## Execution Flow
- **Understand:** read the Context Files; confirm the `shortcuts.js`/`runtime-main.js` anchors match the post-p2-1 files (HALT on mismatch).
- **Execute:** extend `shortcuts.js` + `runtime-main.js`; update the module map; create the e2e.
- **Validate:** `node --check` ×2 + pytest, all EXIT 0.
- **Close:** local commit on `master`; return the contract.

## Forbidden paths / ops
- Touch ONLY the 4 allowlist files. Do NOT modify `clipboard.js`/`paste.js`/`commands.js` (consume them as-is). No scratch files at repo root. No `git push`/reset/amend. No external API calls.

## Commit rule
After checks pass: local commit on `master`, subject starting `[p3-4]`. Stage ONLY the 4 allowlist files. Never push. Return the hash in `landed`.

## Return contract
`status` · `landed` (files + commit hash) · `validation` (commands + EXIT + WALL_MS; skips with reasons) · `concerns` · `open_questions`.

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.

## Validate then STOP (commit is the conductor's — see COMMIT OVERRIDE at top)
After `node --check` ×2 + `python -m pytest tests/e2e/test_copy_paste.py -q` all EXIT 0, STOP. Do NOT commit. Return the five-field contract with `commit: none (conductor commits with pathspec)`. If a `shortcuts.js`/`runtime-main.js` anchor does not match the live post-p2-1 file, HALT with `DOUBT_ESCALATED` rather than guess.
