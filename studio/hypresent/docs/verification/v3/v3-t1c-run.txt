V3-T1c Verification Run — minimal pointer-events rule (replaces over-broad 5-selector rule)
============================================================================================
Date: 2026-06-04
Task: KIMI TASK V3-T1c — R2 final product fix, FIX SPEC v2 (a) only.
File modified: runtime/js/interaction.js (allowlist only).

---

1. SYNTAX CHECK
   Command: node --check runtime/js/interaction.js
   Result:  PASS (exit 0, no syntax errors)

2. INJECTED STYLE RULE VERIFICATION
   Command: rg -n "pointer-events" runtime/js/interaction.js
   Output:
     151:// pointer-events:none wrapper, and Moveable's compensating pointer-events CSS is
     152:// :host-scoped (shadow-DOM only), so the controls inherit pointer-events:none and
     154:// stylesheet that re-enables pointer-events:auto on Moveable's own control
     155:// elements scoped under the wrapper. The wrapper STAYS pointer-events:none so
     163:    "#hyp-interaction-wrapper .moveable-control.moveable-direction { pointer-events: auto; }";
     249:    el.style.removeProperty("pointer-events");
   Interpretation:
     - The ONLY pointer-events:auto grant in the injected stylesheet is the
       single selector `.moveable-control.moveable-direction` scoped under
       `#hyp-interaction-wrapper`.
     - No `.moveable-area`, `.moveable-line`, `.moveable-control-box`, bare
       `.moveable-control`, or `.moveable-origin` grants remain.
     - Line 249 is the onDragEnd temporary toggle (pointer-events:none restore),
       unchanged from before this task.

3. WRAPPER / REMOUNT / onDragEnd BYTE-IDENTICAL CHECK
   Command: git diff HEAD -- runtime/js/interaction.js | rg -A2 -B2 "createWrapper|remount|onDragEnd|teardown"
   Observed: The diff shows ONLY the style.textContent replacement in
             ensureInteractionStyle() (lines 162-163). All other lines
             (createWrapper position:absolute sizing, remount wrapper resize,
             onDragEnd toggle logic) are identical to the pre-task state.
   Result: PASS — no byte changes outside the injected stylesheet text.

4. SCOPE CHECK — git diff confined to allowlist
   Command: git diff --name-only HEAD
   Output (pre-existing modifications from earlier tasks shown for context):
     changelog.md
     runtime/js/interaction.js
     tests/e2e/test_f2_select_guides.py
   Note: The ONLY change introduced by THIS task is inside
         runtime/js/interaction.js (the style rule). The other modified files
         were already dirty before V3-T1c started (changelog.md and
         test_f2_select_guides.py from prior tasks). No new files were created
         at the workspace root.

5. EXACT DIFF OF THE CHANGE
   diff --git a/hypresent/runtime/js/interaction.js b/hypresent/runtime/js/interaction.js
   index acf1ec9..a62312a 100644
   --- a/hypresent/runtime/js/interaction.js
   +++ b/hypresent/runtime/js/interaction.js
   @@ -159,11 +159,8 @@ function ensureInteractionStyle() {
      if (document.getElementById(INTERACTION_STYLE_ID)) return;
      const style = document.createElement("style");
      style.id = INTERACTION_STYLE_ID;
      style.textContent =
   -    "#hyp-interaction-wrapper .moveable-control-box," +
   -    "#hyp-interaction-wrapper .moveable-control," +
   -    "#hyp-interaction-wrapper .moveable-line," +
   -    "#hyp-interaction-wrapper .moveable-area," +
   -    "#hyp-interaction-wrapper .moveable-direction { pointer-events: auto; }";
   +    "#hyp-interaction-wrapper .moveable-control.moveable-direction { pointer-events: auto; }";
      document.head.appendChild(style);
    }

---

ACCEPTANCE SUMMARY
- [x] node --check runtime/js/interaction.js passes.
- [x] Injected style grants pointer-events:auto ONLY to .moveable-control.moveable-direction;
      no .moveable-area / .moveable-line / .moveable-control-box / bare .moveable-control /
      .moveable-origin grants remain.
- [x] Wrapper absolute sizing + remount re-sync + onDragEnd toggle unchanged.
- [x] Product change confined to runtime/js/interaction.js.
- [x] Evidence file written to docs/verification/v3/v3-t1c-run.txt.

STOP.
