Read and execute `{rbtv_path}/studio/workflows/studio-loop/workflow.md`.

**Intake routing gate (run BEFORE executing the workflow).** Inspect the request for the signal **an EXISTING artifact file (deck/site/app HTML) + review comments or targeted fixes to address on it** (an owner hands a built `.html` and asks to apply comments, implement feedback, address review notes, or make targeted edits). On that signal you MUST NOT enter blank-slate beat-01 (which discards the existing structure and locks a fresh narrative). Route to the TWO-PHASE SURGICAL-REVISION flow instead: **PHASE 1** — the Strategist revises the message/narrative working FROM the existing artifact + its comments (surgical, structure-preserving, produces NO HTML); **PHASE 2** — the Designer implements via `{rbtv_path}/studio/workflows/hypresent-comments/comment-implementation.md`. Follow `workflow.md` § Intake Routing Gate for the exact procedure; NEVER skip Phase 1 by routing straight to the Designer, and NEVER improvise a blank-slate build.

Optional toggle: set `critic: on` in the design-state frontmatter to enable the v1.1 structural critic's comparative pass at the design beats. Default is OFF; the critic is advisory and NEVER gates the loop.
