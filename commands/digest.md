Read and execute the workflow at `{rbtv_path}/workflows/digest/workflow.md`.

If the user invoked the command with `yolo` as an argument (e.g., `/digest yolo`), pass `yolo_mode = true` into step-01. Otherwise `yolo_mode = false`.
