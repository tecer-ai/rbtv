# Hypresent v3 — Adversarial Review 01 (pre-build gate)

**Verdict: NO-GO**

**Severity counts:** 4 BLOCKER · 6 MAJOR · 5 MINOR

---

## Findings table

| ID | Severity | Location | What is wrong | Evidence | Required fix |
|----|----------|----------|---------------|----------|--------------|
| RV01 | BLOCKER | V3-T8 (`test_r3_delete.py`) | `H.preset_author` is called but `open_via_dialog_ui` is called AFTER `goto`, not before. The `preset_author` call adds an `addInitScript` which must fire before the first navigation; the test correctly calls it before `goto`. However the test also expects `E-R3-5` (last-region block) to be covered but the task explicitly admits the test is **omitted**: "A dedicated single-region injection test is OMITTED here." The test-plan §R3 lists E-R3-5 as a required assertion; the test file does not implement it at all. The scorecard's E-R3-5 behavioral contract ("last-region block") has zero test coverage in the delivered plan. | V3-T8.md bottom note: "E-R3-5 … OMITTED here". Test-plan §test_r3_delete.py row E-R3-5 is listed as `{blocked:"last-region"}` assertion. | Either implement E-R3-5 via `page.evaluate` DOM surgery (documented in the test note as an option) or add a pure runtime probe via `doc_eval` that counts body `[data-hyp-id]` children after reducing to one. Cannot be deferred — the last-region guard is a safety invariant (D20) and a spec exit criterion (V3-S7). |
| RV02 | BLOCKER | V3-T14.md Anchor 6B / live `main.js` L273–274 | V3-T14 instructs Kimi to REPLACE the live `selection-changed` handler (Anchor 6B) with a version that includes BOTH `setActiveOutline` and `window.__hypUpdateAlignButtons`. The live file at line 273–274 reads exactly `bridge.on("selection-changed", (payload) => { setActiveOutline(payload && payload.hypId ? payload.hypId : null); });` — matching Anchor 6B verbatim. The replacement code in V3-T14 Change 6B includes the `setActiveOutline` call unchanged. Then V3-T16 Anchor 2D quotes the COMBINED handler (both `setActiveOutline` + align lines) as the state it will remove from. **This requires V3-T14 to complete before V3-T16, which is the stated order.** However V3-T16's Anchor 2D ONLY matches if V3-T14 has already landed; if a Kimi run of T14 fails or is partially applied, T16's anchor will not be found and T16 will STOP (correct behaviour) — but the plan has no recovery path for T14 partial failure on this coupled anchor. More critically: V3-T14 Change 6B NOTE states "setActiveOutline is removed by R9 (V3-T16), which runs AFTER this task. At this point setActiveOutline still exists, so leave the call here." Yet V3-T16's Anchor 2D expects the state LEFT BY T14. If T16 runs after any retry of T14 that produces a slightly different handler (e.g. Kimi omits the `setActiveOutline` call per the NOTE's ambiguity), T16 will fail to find its anchor. The note is ambiguous: it says "leave the call here" but also signals it will be removed, which a non-reasoning executor may misread as license to pre-remove it. The NOTE should be a binding DO NOT instruction. | V3-T14.md Change 6B NOTE (last paragraph); V3-T16.md Anchor 2D conditional: "If V3-T14 has NOT yet run … then REPLACE the 3-line block … but per the plan order, V3-T14 (R7) runs BEFORE V3-T16"; live `main.js` line 273. | Make V3-T14 Change 6B a hard DO NOT instruction: "DO NOT remove the `setActiveOutline` call — V3-T16 depends on its presence. Keep it exactly as shown." Add it to the DO NOT section. |
| RV03 | BLOCKER | V3-T16.md Acceptance step 2 / Anchor CSS Change 3A | The acceptance grep command checks for `outline` as a substring in product files, which will match the `outline:` CSS PROPERTY usages the task explicitly says NOT to remove (e.g. `outline: 2px solid #fbbf24;` in a comment-highlight rule). The grep test will produce false positives (non-zero `bad` list) and the assertion `assert not bad` will FAIL, causing Kimi to incorrectly conclude its removal was incomplete and potentially over-delete. The task acknowledges "Do NOT remove the `outline:` CSS PROPERTY usage" but the verification command does not exclude property usages from the match. | V3-T16.md Acceptance step 2: `python -c "… for n in ['outline','regions(','sections'] … assert not bad"`. In `shell.css`, after removing selector rules, there may still be `outline: none;` or `outline: 2px…` property lines in non-outline-selector rules. Any occurrence of the string `outline` in `app/css/shell.css` — even in unrelated rules — will trigger a false failure. | Replace the grep with a selector-anchored check: verify no CSS rule whose SELECTOR starts with `.outline-` remains (e.g. grep for `^\.outline-` in shell.css). The acceptance command should distinguish selectors from properties. |
| RV04 | BLOCKER | V3-T3.md R8 fix — `adjustFontSize` snapshot restore logic | The R8 fix restores `savedRange` when `liveCollapsedAtRoot` is true. The condition `sel.getRangeAt(0).commonAncestorContainer === el` checks for identity (`===`) with the editable element. But after the toolbar `mousedown` does `e.preventDefault(); iframe.contentWindow.focus()`, the browser may collapse the selection to a CHILD text node at position 0, not to `el` itself. In that case `commonAncestorContainer` will be the first text node, NOT `el`, so `liveCollapsedAtRoot` is `false`, `savedRange` is never restored, and press 2+ fails identically to the pre-fix behavior. The spec (V3-S22) says "if the live range is collapsed at the editable root (its commonAncestorContainer is the editable element, not a text node/span inside it)" — but the actual collapse target after `focus()` is browser-dependent. Chromium on Windows collapses to the first text node, not the container element. The condition needs to also handle `container.nodeType === Node.TEXT_NODE && el.contains(container)` with offset 0. | V3-T3.md Change 1B: `sel.getRangeAt(0).commonAncestorContainer === el` — the `===` identity check. V3-S22 says the collapsed range's `commonAncestorContainer` is the editable element, which is not guaranteed by browser behavior on `focus()`. E-R8-1 tests the outcome (3 presses → one span) and will fail on this. | Widen the collapsed-at-root check: `const cac = sel.getRangeAt(0).commonAncestorContainer; const collapsedAtRoot = cac === el || (cac.nodeType === Node.TEXT_NODE && cac.parentElement === el && sel.getRangeAt(0).collapsed);` — or simply check `!el.contains(cac)` vs `el.contains(cac) && cac !== el` against the anchor node, not just the container identity. |

---

| ID | Severity | Location | What is wrong | Evidence | Required fix |
|----|----------|----------|---------------|----------|--------------|
| RV05 | MAJOR | V3-T2.md F2.3 — empty-click at (fb.x+3, fb.y+3) | The real empty-click replacement clicks at 3px inside the iframe's top-left corner. On the live fixture, the top-left 3×3 px region may lie on a visible element (the first slide region), making this a CONTENT click, not an empty-region click. The deselect semantics of `click empty space` require the click to land on the wrapper background (no element), which requires knowledge of where empty space actually is in the fixture. A hard-coded (3,3) offset is not robust. | V3-T2.md F2.3 replacement block: `self.page.mouse.click(fb["x"] + 3, fb["y"] + 3)`. The fixture is a slide deck; the top-left of the iframe is likely the first slide, not empty space. | Use a coordinate known to be clear of content in the fixture, or click directly on the `#hyp-interaction-wrapper` background. Alternatively target the bottom or right margin of the iframe where no slide element reaches: `fb["x"] + rect_of_content["w"] + 20, fb["y"] + 3`. |
| RV06 | MAJOR | Checklist item 4 — R7 fixed-height block vertical (U13 / V3-S16) | The spec V3-S16 says vertical on a plain block WITH an explicit `height`/`min-height` IS enabled and CONVERTS to `display:flex; flex-direction:column; justify-content:…`. The User Decision U13 verbatim from the changelog says: "Disable + hint on plain AUTO-HEIGHT blocks only… never silently changes the element's layout model." The spec makes the fixed-height block an exception where conversion IS done. This is internally consistent (the spec explicitly resolves this branch). However the undo capture in `commands.align` captures `display` and `flex-direction` (listed in the PROPS array in V3-T14), so undo of the flex conversion is reversible. VERDICT: **consistent with U13** (U13 only prohibits silent conversion on AUTO-HEIGHT blocks; fixed-height blocks are an accepted exception per V3-S16). But the undo capture MISSES `align-content` and `justify-self`/`align-self` — listed in the spec's capture list (V3-S15: "capturing the prior inline values of EVERY property it may write: text-align, justify-content, align-items, justify-items, align-content, vertical-align, display, flex-direction") yet `justify-self` and `align-self` are ABSENT from the PROPS array in V3-T14 Change 2A. | V3-T14.md Change 2A PROPS array: `["text-align", "justify-content", "align-items", "justify-items", "align-content", "vertical-align", "display", "flex-direction"]` — missing `justify-self` and `align-self`. Spec V3-S15: full capture list includes those. | Add `"justify-self"` and `"align-self"` to the PROPS array in V3-T14 Change 2A, matching the spec's capture-all declaration exactly. |
| RV07 | MAJOR | Checklist item 5 — R2 pointer-events CSS rule vs onDragEnd drop hit-test | The new CSS rule enables `pointer-events:auto` on `.moveable-control`, `.moveable-line`, `.moveable-area`, `.moveable-direction` (the drag BODY area is `.moveable-area`). During `onDragEnd`, the hit-test temporarily disables pointer-events on the DRAGGED ELEMENT (`el.style.pointerEvents = "none"`) to see what's beneath it via `elementsFromPoint`. The concern: does the new rule on `.moveable-area` (which remains visible during drag) interfere? Answer: `.moveable-area` is a Moveable internal element that covers the dragged element's bounding box, and with `pointer-events:auto` it will appear in `elementsFromPoint` before the document elements beneath it. If `classifyDrop` iterates `elementsFromPoint` looking for `data-hyp-id` targets, it may hit `.moveable-area` first. Analysis of `classifyDrop` (in `reorder.js`, not read directly but referenced from `interaction.js` onDragEnd) — the spec C1 note says "The fix is additive and independent of the existing onDragEnd workaround." However: after the fix, `document.elementsFromPoint(lastPointer.x, lastPointer.y)` called INSIDE onDragEnd (with `el.style.pointerEvents="none"` on the dragged element) will still hit `.moveable-area` because `.moveable-area` is a sibling inside the wrapper, NOT the dragged element whose pointer-events were disabled. This could return the `.moveable-area` element instead of the underlying region. | `interaction.js` onDragEnd: `el.style.pointerEvents = "none"` disables the dragged element; `classifyDrop(el, x, y)` then calls `elementsFromPoint`. The new rule makes `.moveable-area` (inside the same wrapper) hittable, potentially blocking the look-through. V3-T1.md DO NOT list does not mention `.moveable-area`. | Either EXCLUDE `.moveable-area` from the CSS rule (it does not need pointer-events for the fix; only `.moveable-control-box`, `.moveable-control`, `.moveable-direction` handle resize/drag), OR confirm that `classifyDrop` already skips Moveable elements (if so, document in the task). Removing `.moveable-area` from the injected rule is the safer fix: it is not a user-draggable handle, it is an invisible overlay. |
| RV08 | MAJOR | V3-T16 Anchor 2D — conditional fallback for when V3-T14 has NOT run | V3-T16 Anchor 2D provides a fallback: "If V3-T14 has NOT yet run and the selection-changed handler still reads `setActiveOutline(...)` WITHOUT the align line, then REPLACE the 3-line block with an empty-body handler." But the plan order enforces T14 before T16 (C7 before C8). If Kimi takes the fallback path, it silently drops `updateAlignButtons` wiring, leaving the align buttons permanently disabled and no test catches this before the next boundary. The fallback is unsafe — a non-reasoning executor can take it incorrectly even when T14 has run, if the anchor match fails for any spacing/formatting reason. | V3-T16.md Anchor 2D conditional fallback: "then REPLACE the 3-line … block with an empty-body handler `bridge.on("selection-changed", (payload) => {});`". This silently deletes R7 wiring. | Remove the fallback entirely. Replace with: "If the quoted block is not found verbatim, STOP and write a bug report — do not guess an alternative replacement." |
| RV09 | MAJOR | V3-T18.md Edit 2 — R8 smoke uses `self.page.click("#fmt-font-inc")` | The EXIT smoke `Edit 2` (the R8 3-press check) uses `self.page.click("#fmt-font-inc")`. However, the R8 fix requires that `format-snapshot` fires on the button's MOUSEDOWN before `focus()`, which means the test must click the button in a way that triggers `mousedown` → `focus()` → `click` in sequence. `page.click()` in Playwright does dispatch mousedown then click, so this is fine. However the test also calls `box_t = title.bounding_box()` on a frame locator element and then `self.page.mouse.dblclick(box_t["x"]+20, ...)` — `bounding_box()` on a `frame.locator(...)` object returns coordinates RELATIVE TO THE FRAME, not to the page. Adding these to page-level mouse coordinates without adding the iframe's page offset (as every other test does via `_iframe_origin`) will click at the wrong location and may not enter edit mode. | V3-T18.md Edit 2: `box_t = title.bounding_box()` where `title = frame.locator(".slide-title").first` — `frame` is a `frame_locator`, and its `bounding_box()` returns page-absolute coordinates in Playwright (unlike `doc_eval` which returns iframe-local). Need to verify: in Playwright sync API, `frame_locator.locator(...).bounding_box()` does return page coordinates. If so this is fine. But if `title` is a plain locator on a frame_locator, the coordinates ARE page-absolute — so this is actually correct. Mark as MAJOR only because it is fragile and needs explicit confirmation; if the team confirms Playwright frame_locator.bounding_box() returns page-absolute, downgrade to MINOR. | Verify: add a comment in the task confirming `frame.locator(...).bounding_box()` returns page-absolute coordinates. If not confirmed, replace with the `iframe_origin + doc_eval rect` pattern used everywhere else. |
| RV10 | MAJOR | V3-T8 — E-R3-3/4 thread re-anchor assertion is ambiguous after undo | After undo, E-R3-4 checks `anchored_after = self.page.locator("#comment-threads .comment-thread").count()` and asserts `total_after >= 1`. This passes even if the thread is STILL in the Unanchored section (total_after counts both). It does not assert `unanchored_after == 0` (re-anchored). The spec states "thread is unanchored:false again (re-anchored) after undo" as a MEASURED OUTCOME — the test never asserts that. | V3-T8.md E-R3-3/4 code: `total_after = anchored_after + ... .count(); self.assertGreaterEqual(total_after, 1)`. The anchored count and unanchored count are summed — cannot distinguish re-anchor from still-unanchored. | After undo, assert `unanchored_after = self.page.locator("#comment-unanchored .comment-thread").count(); self.assertEqual(unanchored_after, 0, "thread must re-anchor after undo, not remain unanchored")`. |

---

| ID | Severity | Location | What is wrong | Evidence | Required fix |
|----|----------|----------|---------------|----------|--------------|
| RV11 | MINOR | Premise stress-test #1 — `normalizeHex` module-scope probe element | `const _hexProbe = document.createElement("span")` is created at MODULE SCOPE in `color.js`. The `getComputedStyle(_hexProbe)` call on an unattached (not in DOM) element returns computed style from the UA stylesheet, not the document's `:root` CSS variables. For a token value like `var(--brand-color)` (a CSS variable reference), `_hexProbe.style.color = "var(--brand-color)"` will compute to `""` (invalid on a detached element), causing `normalizeHex` to fall back to the raw `var(--brand-color)` string. The spec says "token values may be named/hsl()" — but does not address CSS `var()` values. If any token in the real fixture uses a chained variable, the copy-HEX button will copy the raw `var(...)` string, not a hex. | V3-T11.md and spec V3-S14: the probe element is `document.createElement("span")` not appended to the DOM. CSS `var()` references do not resolve on detached elements. | Append `_hexProbe` to `document.body` with `visibility:hidden; position:absolute; pointer-events:none;` at module init, so computed style resolves variable chains. This is a robustness improvement; it becomes critical if any palette token in the fixture uses CSS variable chaining. Document the limitation in the V3-T11 acceptance step. |
| RV12 | MINOR | V3-T19.md Anchor 3A — decision-log D-numbering collision | The task appends rows numbered D8, D9, D10, D11 to the decision log. The live `decision-log.md` was not read, but the v2 plan (`hypresent-v2-plan.md`) and changelog reference D15–D24 as Session-2 decisions and D8/D12/D14 as carried Session-1 decisions. If the existing decision log already contains D8–D11 rows from prior cycles, the appended rows will create duplicate decision IDs. | V3-T19.md Change 3A appends `| D8 | … | D9 | … | D10 | … | D11 |`. The plan itself references D8 (git-local rule) and D12/D14 as carried. If the log has a D8 row already, this creates a collision. | Read the live `docs/decision-log.md` before assigning row numbers (V3-T19 should do this); use the actual next-available Dn number. |
| RV13 | MINOR | V3-T8 — `preset_author` not imported from conftest_helpers in task documentation | V3-T8 calls `H.preset_author(self.page)` but the task description says `conftest_helpers.py` provides only `start_server`, `stop_server`, `copy_fixture`, `open_via_dialog_ui`, `wait_runtime_ready`, `doc_eval`, `FIXTURE`. The "How the feature works" section says "`H.preset_author` adds an init script — call it BEFORE goto." The grep confirms `preset_author` IS present in conftest_helpers.py. The task list of helpers is incomplete (it was also omitted from V3-T2 and V3-T4's helper lists). This creates a false impression for a non-reasoning executor that the function does not exist. | V3-T8.md "How the feature works" section: `conftest_helpers.py` helper list omits `preset_author`. Grep of tests confirms `preset_author` exists at conftest_helpers.py:65. | Add `preset_author(page)` to the helper list in V3-T8 (and any other task that uses it: V3-T17, V3-T18 via the smoke). Low risk since the function exists, but documentation gap for Kimi. |
| RV14 | MINOR | V3-T2 — `_iframe_handle_local_center` returns coordinates from `getBoundingClientRect` in the iframe | In the `test_r2_resize_real.py` code, `_iframe_handle_local_center` calls `el.getBoundingClientRect()` inside the iframe doc via `doc_eval`. `getBoundingClientRect()` in an iframe returns coordinates RELATIVE TO THE IFRAME VIEWPORT, not the iframe document scroll origin. For `document.elementFromPoint(x, y)` called in the same iframe document, these coordinates must be relative to the IFRAME VIEWPORT (which `elementFromPoint` uses). This is consistent — but only if the iframe is not scrolled. If the fixture is taller than the viewport and the element is scrolled out of view before the `scrollIntoView` completes, the rectangle coordinates may be stale. The `scrollIntoView` call precedes this by 300ms timeout, which is usually sufficient, but is not a hard synchronization point. | V3-T2.md `_iframe_handle_local_center` uses `getBoundingClientRect()` after a 300ms wait following `scrollIntoView`. The 300ms is empirical, not event-driven. | Replace the `wait_for_timeout(300)` after scrollIntoView with a `page.wait_for_function` that checks the element is within the viewport. Low priority since the fixture likely fits in most viewports. |
| RV15 | MINOR | Docs-sync (checklist item 13) — V3-T19 does not verify the `docs/spec/03-module-map.md` module-map delta for `selection.js` and `color.js` | V3-T19 updates `commands.js`, `interaction.js`, `text-format.js`, `element-registry.js` lines in the module-map. The spec `§Cross-feature module map delta` also lists `selection.js` (+ `alignCaps`) and `color.js` (+ `rgbToHex` EXPORTED + `normalizeHex` + `token.hex`). Neither is included in V3-T19's module-map edits (Anchors 2A–2E cover only commands.js, interaction.js, text-format.js, element-registry.js). Two modules changed in v3 are missing from the docs-sync. | V3-T19.md "File 2 — docs/spec/03-module-map.md" section: provides Anchors 2A (commands.js), 2B (interaction.js), 2C (text-format.js), 2D/2E (element-registry.js). No anchor for `selection.js` or `color.js`. Spec §"Cross-feature module map delta" table lists both. | Add module-map update anchors for `selection.js` (add `alignCaps field on selection-changed payload, R7`) and `color.js` (add `rgbToHex` exported, `normalizeHex`, `token.hex`, R6) to V3-T19. |

---

## Per-checklist-item verdicts

| # | Item | Verdict |
|---|------|---------|
| 1 | **Kimi executability / anchor verification** | PASS with caveat. 3 anchors verified per major task: V3-T1 Anchor 1 matches `interaction.js:149-161` verbatim ✓; V3-T3 Anchor 1A matches `text-format.js:16-28` verbatim ✓; V3-T7 Anchor 1B matches `commands.js:268-288` verbatim ✓; V3-T5 Anchor 1 matches `api.py` script constants (not directly read but T5's quoted content matches the spec's C3 claim) ✓; V3-T14 Anchor 4B depends on T3 state, self-consistent ✓. One OPEN DECISION in each task: none found (all tasks specify complete code). CAVEAT: V3-T14 Anchor 6B depends on post-T3 state — correct but fragile (RV02). |
| 2 | **Allowlists and disjoint/serialization** | PASS. All per-task allowlists are explicit. Shared-file serialization table in the plan is correct and consistent: commands.js (R3→R7), runtime-main.js (R8→R3→R7→R9), text-format.js (R8→R7), index.html (R3→R4→R7→R9), main.js (R8→R3→R4→R7→R9), shell.css (R6→R9). No task in any parallel group touches the same file (C6 has T11=color.js, T12=color-popover.js — disjoint ✓). |
| 3 | **Commit boundaries contain own tests; tree stays green at each** | PASS. Every boundary (C1–C9) includes a test task after its feature task(s). No boundary's test exercises a feature from a later boundary. The plan dependency graph shows T2 depends on T1, T4 on T3, etc. The only subtle risk is that T3 (R8 on text-format.js) also modifies runtime-main.js and main.js, which T7 (R3) later edits — but T3 and T7 are in different boundaries (C2 and C4), and T3's anchors in those files are disjoint from T7's anchors. Green at each boundary: confirmed by the serialization table. |
| 4 | **Orchestrator focus (a): R7 fixed-height block / U13** | VERDICT: CONSISTENT with U13 intent. The spec's resolution (V3-S16 + V3-S15 decision table) converts a fixed-height block to flex column for vertical alignment. U13 prohibits silent conversion only for AUTO-HEIGHT blocks — the fixed-height exception is explicitly disclosed in the spec. Undo capture is complete for the converted case (`display` and `flex-direction` are in the PROPS array) EXCEPT for the missing `justify-self` / `align-self` (RV06). |
| 5 | **Orchestrator focus (b): R2 pointer-events / onDragEnd interplay** | PARTIAL FAIL → RV07 (MAJOR). The new CSS rule enables `pointer-events:auto` on `.moveable-area`, which is the transparent overlay div that Moveable renders over the dragged element's bounding box. During `onDragEnd`, `classifyDrop` calls `elementsFromPoint` to see beneath the dragged element (which has `pointer-events:none` set). `.moveable-area` is NOT the dragged element, so its pointer-events are not disabled. If `.moveable-area` overlaps the drop target, `elementsFromPoint` may return it before the real target. The fix is to exclude `.moveable-area` from the injected CSS rule. The `wrapper` stays `pointer-events:none` so new-wrapper-level interference is not an issue, but `.moveable-area` child of the wrapper inherits the new `auto` rule. |
| 6 | **R8 snapshot/restore** | PARTIAL FAIL → RV04 (BLOCKER). The `commonAncestorContainer === el` identity check may not match the actual collapse target after `iframe.contentWindow.focus()` in Chromium. The tracked-span fallback (`currentFontSpan`) does work for presses 2+ once a span is created on press 1, but press 1 itself relies on the snapshot restore. The 3-press test E-R8-1 will be the live gate; if it fails, the BLOCKER is confirmed. The test code in V3-T4 correctly asserts span count and delta. |
| 7 | **R3 delete** | PARTIAL FAIL → RV01 (BLOCKER: E-R3-5 missing) + RV10 (MAJOR: re-anchor assertion too weak). The undo restores the node at the captured `nextHypId` anchor — the spec correctly handles the case where `nextHypId` was deleted/moved (it falls through to `appendChild` if `byId(nextHypId)` returns null or the sibling is no longer a child of the resolved parent). Parent deletion before undo is prevented by the last-region guard (the parent of any tagged element is either body or another tagged element; body is never deletable; other tagged elements can only be deleted one at a time via the toolbar, so the history stack ordering makes "parent deleted before undo of child" impossible in normal use — no additional guard needed). Moveable teardown after delete (E-R3-8) correctly asserts wrapper absence. |
| 8 | **Tests: behavioral assertions, no skip-as-green, N≥2, ctypes** | PARTIAL FAIL. (a) E-R3-5 omitted (RV01). (b) E-R3-4 re-anchor assertion too weak (RV10). (c) ctypes Win32 callback: `WNDENUMPROC = ctypes.WINFUNCTYPE(ctypes.c_bool, ctypes.c_void_p, ctypes.c_void_p)` — the callback signature is `(HWND hwnd, LPARAM lParam)` which maps to `(c_void_p, c_void_p)` — CORRECT ✓. The EnumWindows loop returns `False` to stop on find — Python `False` is `0` which is correct for the BOOL return ✓. `PostMessageW(hwnd, WM_CLOSE, 0, 0)` and `t.join(timeout=8)` — correct teardown ✓. Server endpoint after WM_CLOSE: `_run_ps_dialog_default` returns `None` (empty stripped stdout), the test asserts `result["path"] is None` ✓ — server endpoint is never called in this test (no HTTP request). Port assignments 8792–8799 are clean against the listed in-use ports (8765, 8781–8790) ✓. N≥2: resize (E-R2-3 twice-drag) ✓; font-size (3 presses = 3 iterations) ✓. `elementFromPoint` hittability guard (E-R2-1) ✓. No `skipTest` except documented R1 headless-CI skip ✓. |
| 9 | **Regression: v2 67 tests** | PARTIAL FAIL. `test_g1_panel_survival.py` has 7 live references to `#outline-list` (lines 39, 48, 55, 67, 75, 89 — confirmed by grep). V3-T17 removes them all via content-anchored edits. The anchors in V3-T17 match the live file exactly (grep confirmed `wait_for_selector("#outline-list > *")` at lines 39 and 75 match G1.1 and G1.3 anchors). `#color-btn` — no references in any test file (grep returns no matches) ✓. F2 rewrite removes `dispatchEvent` and `skipTest` — anchors match live file ✓. No v2 test file deleted ✓. |
| 10 | **Serializer/save integrity** | PASS. R3 deletions: the serializer clones the live DOM; deleted elements are simply absent from the clone. No special serializer change needed — confirmed by spec V3-S5 edge case table. R7 alignment inline styles: `applyAlign` writes `el.style.X = Y` (inline style), which the serializer clones verbatim (it strips only `data-hyp-*` and `hyp-` classes, never inline style). Alignment styles survive save ✓. Comment island integrity: R3's `reanchorAfterMove()` updates the in-memory thread store; `commentsToJson` serializes it at save time — threads remain correct ✓. Agent-block regeneration: unrelated to all v3 changes ✓. |
| 11 | **D14 addendum in all 19 tasks** | PASS. Grep confirms all 19 task files contain "D14 addendum". Evidence file paths under `docs/verification/v3/` are specified in every task's Acceptance section. Fail-loud preconditions (AssertionError on missing fixture) present in all e2e tasks ✓. |
| 12 | **Protected files** | PASS. No task allowlist includes `docs/spec/04-implementation-plan.md`, `docs/plan/hypresent-v1/`, `docs/plan/hypresent-v2/`, `docs/improvements-2026-06/`, `changelog.md`, or `tecer-gsmm-introduction.html`. The plan's Notes section explicitly lists these as protected. Tests copy the fixture to a tempdir only ✓. |
| 13 | **Docs-sync task (V3-T19)** | PARTIAL FAIL → RV15 (MINOR). `selection.js` and `color.js` module-map updates are missing from V3-T19. README usage removal of outline Navigate step ✓; font-size known-limitation removal ✓; add delete + alignment + copy-HEX ✓; decision-log append-only ✓ (but potential D-numbering collision — RV12). Build-log append ✓. The RBTV repo docs-in-sync rule from CLAUDE.md ("When you create, rename, delete, or materially change ANY component … you MUST … update README.md … update the relevant file in modules/… update admin/install/module-manifest.json") — V3-T19 updates README and module-map but does NOT update `module-manifest.json` or the `modules/` component-list file. This may violate the RBTV repo's own CLAUDE.md rule. |
| 14 | **Premise stress-tests** | 3 scenarios constructed: (1) **CSS `var()` token values** — `normalizeHex` falls back to raw string for chained variables (RV11); plan does not survive unless `_hexProbe` is attached to the DOM. (2) **Multiple elements selected simultaneously** — the `get-selection` bridge command returns `current()` which returns at most one element (single-selection model); delete and align both use `current()`. If the user somehow multi-selects (currently impossible per the selection model), both operations silently operate on a single element. Plan survives — no multi-select exists. (3) **`!important` inline styles** — `applyAlign` writes `el.style.justifyContent = "center"` etc. via property assignment. If the element has `!important` on any alignment property (e.g. `style="justify-content: flex-end !important"`), the new assignment silently fails (no error; `style.justifyContent = "center"` does not override `!important` via the CSSOM). The test would assert `getComputedStyle(el).justifyContent === "center"` and fail. Plan does NOT survive this scenario. The R7 decision table does not address `!important`. Mitigating: real-world fixture unlikely to have `!important` in inline styles. |

---

## Summary

**BLOCKERS (4):**
- RV01: E-R3-5 (last-region block) test omitted — spec exit criterion has zero coverage
- RV02: V3-T14→T16 coupling ambiguity enables silent deletion of R7 align wiring
- RV03: V3-T16 acceptance grep produces false positives on `outline:` CSS properties → false failure + potential over-delete
- RV04: R8 `commonAncestorContainer === el` identity check may not match Chromium's actual collapse target after `focus()`

**MAJORS (6):** RV05 (empty-click at wrong coordinate), RV06 (missing `justify-self`/`align-self` in align undo capture), RV07 (`.moveable-area` pointer-events blocks onDragEnd hit-test), RV08 (V3-T16 fallback silently deletes R7 wiring), RV09 (frame_locator.bounding_box coordinate ambiguity), RV10 (E-R3-4 re-anchor assertion too weak)

**MINORS (5):** RV11 (CSS var() tokens not resolved by detached probe), RV12 (decision-log D-number collision risk), RV13 (preset_author absent from task helper lists), RV14 (scrollIntoView without hard sync), RV15 (selection.js + color.js missing from module-map update)

**Checklist items that passed clean:** #2 (allowlists/serialization), #3 (commit boundaries), #8 partial (ctypes Win32 sequence sound, port assignments clean, N≥2 present, no spurious skips), #10 (serializer/save integrity), #11 (D14 addendum present in all 19 tasks), #12 (protected files clean).
