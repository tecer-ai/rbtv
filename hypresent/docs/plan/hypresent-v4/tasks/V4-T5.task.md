---
task_id: V4-T5
title: "R11 RED — test_r11_resize_guides_equal_size.py, run vs post-R12 master"
r_item: R11
kind: RED
execution_kind: code
executor: kimi
reviewer: claude-opus
allowed_workdir: "C:\\Users\\henri\\Documents\\second-brain\\3-resources\\tools\\rbtv\\hypresent"
allowlist:
  - "tests/e2e/test_r11_resize_guides_equal_size.py"   # CREATE
  - "docs/verification/v4/v4-t5-red.txt"               # CREATE
commit_policy: local-only      # RED task does NOT commit; rides the V4-T6 GREEN commit
test_command: "python -m pytest tests/e2e/test_r11_resize_guides_equal_size.py -v"   # run vs post-R12 master (R11 fix NOT applied)
doubt_policy: halt
swarm_policy: disabled
max_kimi_subagents: 0
forbidden_ops:
  - "edit any file outside the allowlist"
  - "edit any product/runtime/app file (TEST-ONLY task)"
  - "edit or recreate the fixtures or conftest_helpers.py (reuse the committed flow-grow.html read-only)"
  - "weaken, xfail, or skip any assertion to make a test pass"
  - "touch docs/plan/hypresent-v1, docs/plan/hypresent-v2, or docs/plan/hypresent-v3"
  - "touch tecer-gsmm-introduction*.html"
  - "git push, git merge, git reset --hard, or any history rewrite"
  - "create any file at the workspace root"
---

# V4-T5 — R11 RED: `test_r11_resize_guides_equal_size.py` (run vs post-R12 master)

You are Kimi, a non-reasoning code executor. Do EXACTLY what this file says. Read NOTHING outside this file. Output ONLY the files in the allowlist. Depends on V4-T4 (R10+R12 landed; `flow-grow.html` + `copy_synthetic`/`SYN_DIR` exist, reused READ-ONLY).

## D14 discipline (binding)
On a RED/GREEN mismatch vs the lists below, STOP and write `docs/verification/v4/v4-t5-BUG.md`. NEVER weaken a test to pass. NEVER patch product files. NEVER create files at the workspace root.

## What this task is
RED half of the R11 pair. CREATE `test_r11_resize_guides_equal_size.py` and RUN it vs CURRENT (post-R12) master — the R11 equal-size feature is NOT applied. Expected: equal-size SNAP/HINT cases FAIL (no equal-size logic); the position-guide verification case (E-R11-1) PASSES (R10 already makes the flex-grow edge move, so the existing position guides fire). You apply NO product fix (that is V4-T6).

> Sequencing (AD7): R11 lands AFTER R10. E-R11-1 depends on R10 making edges move.

---

## Harness + helpers (transcribe VERBATIM)
Same conventions as the R10/R12 suites. `PORT = 8811`. Inline the SAME module-level helpers (`_iframe_origin`, `_rect_in_iframe`, `_scroll_into_view`, `parse_px`, `bridge_get_selection`, `_HANDLE_SELECTOR`) and the SAME TestCase methods (`select_by_native_id`, `edge_handle_screen`, `rendered`) used by the R10 suite — copy them verbatim (the exact bodies are in V4-T1 STEP 3; reproduce them here so this test module is self-contained). `setUpClass` starts the server + Chromium; fixture-existence guard via `os.path.exists(os.path.join(H.SYN_DIR, "flow-grow.html"))`; REAL input only; D14 guard via `select_by_native_id`.

> The `flow-grow.html` fixture's `twin` is `width:300px` (the out-of-flow equal-size target); `node-a`/`node-b` are same-parent flex-grow siblings of `node-accent` (their rendered widths are grow-share output, NOT author-set sizes — they must be EXCLUDED from equal-size candidates).

---

## Test cases (implement exactly)
Each opens `flow-grow.html` via `H.open_via_dialog_ui(self.page, self.base, H.copy_synthetic("flow-grow.html"))`.

**E-R11-1 — Position guides FIRE on the flex-grow element post-R10:**
- `self.select_by_native_id("node-accent")`.
- E-handle drag in two phases (mirror `test_resize_shows_guidelines`): `hx,hy = self.edge_handle_screen("e")`; `self.page.mouse.move(hx,hy); self.page.mouse.down()`; `for i in range(1,6): self.page.mouse.move(hx+i*6, hy); self.page.wait_for_timeout(30)`; PROBE `found = H.doc_eval(self.page, "return doc.querySelectorAll('.moveable-line').length >= 1;")`; then `for i in range(6,11): self.page.mouse.move(hx+i*6, hy); self.page.wait_for_timeout(30)`; `before_w` recorded BEFORE the gesture, `after_w` after `self.page.mouse.up()`.
- Assertions: `self.assertTrue(found, "position guides must appear during resize on the flex-grow element post-R10")`; `self.assertNotAlmostEqual(after_w, before_w, delta=2)` (edge actually moved).

**E-R11-2 — Equal-size SNAP lands ON the matched dimension (snap to out-of-flow `twin`):**
- `twinW = self.rendered("twin")["w"]` (≈300); `self.select_by_native_id("node-accent")`; `accW0 = self.rendered("node-accent")["w"]`.
- Compute the cursor travel needed to bring the accent width to within ~4px of 300 (drag toward `twinW`): a positive E-handle drag of `target_travel = twinW - accW0` (clamp to a sane positive number; do it in 10 steps). After `mouse.up()`, `accW1 = self.rendered("node-accent")["w"]`. Also compute `raw_cursor_target = accW0 + target_travel` (the un-snapped value).
- Assertions: `self.assertAlmostEqual(accW1, twinW, delta=0.5)` (LANDED EXACTLY on the twin width — snapped, not merely within 4) AND `self.assertNotAlmostEqual(accW1, raw_cursor_target, delta=0.5)` (contrast: differs from the un-snapped value → snap fired).

**E-R11-3 — Equal-size HINT renders during the snap, removed at end, positioned over the match (G5):**
- As E-R11-2, but PROBE mid-drag (while snapped, at the step nearest 300): `hint = H.doc_eval(self.page, "return doc.querySelectorAll('[class*=\\"hyp-size-hint\\"]').length;")`; `pe = H.doc_eval(self.page, "const h=doc.querySelector('[class*=\\"hyp-size-hint\\"]'); return h?getComputedStyle(h).pointerEvents:null;")`; `pos = H.doc_eval(self.page, "const h=doc.querySelector('[class*=\\"hyp-size-hint\\"]'); const t=doc.querySelector('#twin'); if(!h||!t) return null; const hr=h.getBoundingClientRect(), tr=t.getBoundingClientRect(); return {dx:Math.abs(hr.left-tr.left), dy:Math.abs(hr.top-tr.top)};")`. After `mouse.up()` + `self.page.wait_for_timeout(200)`, `hint_after = H.doc_eval(self.page, "return doc.querySelectorAll('[class*=\\"hyp-size-hint\\"]').length;")`.
- Assertions: `self.assertGreaterEqual(hint, 1)`; `self.assertEqual(pe, "none")`; `self.assertIsNotNone(pos)` AND `self.assertLessEqual(pos["dx"], 8)` AND `self.assertLessEqual(pos["dy"], 8)`; `self.assertEqual(hint_after, 0)`.

**E-R11-4 — Equal-size hint is serializer-EXEMPT:**
- Open `flow-grow.html`; perform E-R11-2's equal-size gesture; then `H.set_fake_dialog(self.base, out)` where `out` is a fresh tempfile path; `self.page.click("#save-as-btn")`; `self.page.wait_for_timeout(600)`.
- Read `out`; `self.assertNotIn("hyp-size-hint", text)` AND `self.assertEqual(re.findall(r'class="[^"]*hyp-size-hint', text), [])`.

**E-R11-5 — NO phantom snap to a solver-derived flex-grow sibling width (F5 guard):**
- `siblW = self.rendered("node-a")["w"]` (the frozen grow-share width); `accW0 = self.rendered("node-accent")["w"]`; `self.select_by_native_id("node-accent")`.
- Compute a cursor travel that brings the accent's width to within ≈1px of `siblW` (drag toward `siblW`, NOT toward the 300px `twin` — choose a target so the accent's width passes through `siblW` but stays >4px from 300). E-handle drag that amount in 12 SLOW steps (`self.page.wait_for_timeout(40)` each); at the step whose cumulative travel lands the accent nearest `siblW`, probe `mid = self.rendered("node-accent")["w"]` and `hint = H.doc_eval(self.page, "return doc.querySelectorAll('[class*=\\"hyp-size-hint\\"]').length;")`. `mouse.up()`; `accW1 = self.rendered("node-accent")["w"]`.
- Assertions: `self.assertGreater(abs(accW1 - siblW), 1.0)` (did NOT pin to the sibling grow-share width) AND `self.assertEqual(hint, 0)` (no hint while passing through the sibling width — sibling is not a candidate).

End with `if __name__ == "__main__": unittest.main()`.

---

## RUN — against post-R12 master and record red-first evidence
Run `python -m pytest tests/e2e/test_r11_resize_guides_equal_size.py -v`.

### Expected RED on post-R12 master (R11 equal-size is NOT wired)
- **E-R11-2** — no equal-size logic exists, so the accent lands at the RAW cursor value, NOT exactly on the twin's 300px → `assertAlmostEqual(accW1, twinW, delta=0.5)` FAILS (and the contrast `assertNotAlmostEqual(accW1, raw_cursor_target, …)` ALSO fails because `accW1 == raw_cursor_target` pre-fix — record both).
- **E-R11-3** — no `hyp-size-hint` overlay element is ever created → `hint` count is 0 → `assertGreaterEqual(hint, 1)` FAILS.

### Expected GREEN already on post-R10/R12 master
- **E-R11-1** — position guides fire on the flex-grow element because R10 made the edge move (the existing `snappable:true`+`elementGuidelines` wiring is untouched). MUST PASS. If RED → R10/R12 regressed the position guides → STOP + BUG.
- **E-R11-4** — there is nothing to strip (no hint), so the save contains no `hyp-size-hint` → the assertions PASS VACUOUSLY pre-fix. Record as PASS (it becomes load-bearing post-fix).
- **E-R11-5** — no candidate logic at all → no snap to the sibling → `abs(accW1-siblW) > 1` holds and `hint == 0` → PASSES VACUOUSLY pre-fix. Record as PASS (becomes the load-bearing F5 guard post-fix).

### Red-first GATE (binding)
If **E-R11-2** OR **E-R11-3** does NOT fail on post-R12 master → STOP + write `docs/verification/v4/v4-t5-BUG.md` (the equal-size feature must be absent pre-fix). If **E-R11-1** FAILS → STOP + BUG (R10's edge-movement, the position-guide prerequisite, regressed).

Write `docs/verification/v4/v4-t5-red.txt` with the full `pytest -v` output and a one-line observed note for E-R11-2 (observed `accW1` vs `twinW`) and E-R11-3 (observed `hint` count).

## Acceptance (verify yourself)
1. `test_r11_resize_guides_equal_size.py` exists with E-R11-1/-2/-3/-4/-5 + the verbatim helpers; REAL input only; D14 guard on every selection-dependent case.
2. Run completed vs post-R12 master; E-R11-2/-3 RED; E-R11-1 GREEN; E-R11-4/-5 GREEN (vacuous).
3. `docs/verification/v4/v4-t5-red.txt` written.
4. `git diff --stat` shows changes ONLY in the allowlist paths.

## Return format (exact)
```
DONE V4-T5
files-changed: <list>
red-result: E-R11-1 <…>, E-R11-2 <PASS/FAIL+observed accW1/twinW>, E-R11-3 <…>, E-R11-4 <…>, E-R11-5 <…>
red-first-gate: <SATISFIED | BUG written>
concerns: <none | …>
```

## Do NOT
- Do NOT apply any product fix (interaction.js is NOT in this allowlist — that is V4-T6).
- Do NOT recreate or edit the fixtures or conftest_helpers.py.
- Do NOT weaken/xfail/skip any assertion.
- Do NOT commit (V4-T6 commits these test files with the fix).
- Do NOT create any file at the workspace root.
