---
task_id: V4-T8
title: "R13 GREEN — comment edit+delete (4 ops + regs + panel affordances + composer edit mode); rerun + commit"
r_item: R13
kind: GREEN
execution_kind: code
executor: kimi
reviewer: claude-opus
allowed_workdir: "C:\\Users\\henri\\Documents\\second-brain\\3-resources\\tools\\rbtv\\hypresent"
allowlist:
  - "runtime/js/comments.js"                # MODIFY — 4 ops + editedAt in writeIsland/toJson
  - "runtime/js/runtime-main.js"            # MODIFY — 4 command registrations + imports
  - "app/js/main.js"                        # MODIFY — createThreadEl Edit/Delete affordances
  - "app/js/shell/comment-composer.js"      # MODIFY — edit mode
  - "docs/verification/v4/v4-t8-run.txt"    # CREATE
commit_policy: local-only      # commit [v4-t8] R13: ... on master; NEVER push
test_command:
  targeted: "python -m pytest tests/e2e/test_r13_comment_edit_delete.py -v"
  full: "python -m pytest tests/ -q"
doubt_policy: halt
swarm_policy: disabled
max_kimi_subagents: 0
forbidden_ops:
  - "edit any file outside the allowlist (tests are FROZEN — V4-T7 wrote them)"
  - "weaken, xfail, or skip any test to make the suite pass"
  - "copy the !payload.author check from add-comment/reply-comment into the edit/delete validators (G3)"
  - "set thread.editedAt to undefined on undo (must DELETE the key when it was absent — G4)"
  - "add a separate comment-panel.js module or inline (non-composer) editing (AD5)"
  - "change buildAgentBlock structurally (R14 owns the anchor-line rewrite)"
  - "touch docs/plan/hypresent-v1, docs/plan/hypresent-v2, or docs/plan/hypresent-v3"
  - "touch tecer-gsmm-introduction*.html"
  - "git push, git merge, git reset --hard, or any history rewrite"
  - "create any file at the workspace root"
---

# V4-T8 — R13 GREEN: comment edit+delete (verbatim change-map) + rerun + commit

You are Kimi, a non-reasoning code executor. Do EXACTLY what this file says. Read NOTHING outside this file (you MAY read the four allowlisted product files to locate anchors and confirm the EXISTING patterns: `makeCommentCommand`/the reply/resolve command shape in `comments.js`, the `reply-comment` registration + validators in `runtime-main.js`, `createThreadEl`'s actions row in `main.js`, and `openComposer`'s mode handling in `comment-composer.js`). Output ONLY the files in the allowlist. Depends on V4-T7 (R13 RED tests landed; FROZEN).

## D14 discipline (binding)
On a blocking product bug, STOP and write `docs/verification/v4/v4-t8-BUG.md`. NEVER weaken/xfail/skip a test. NEVER edit the test files. NEVER create files at the workspace root.

## Edit-anchoring rule (binding)
Anchor every edit on quoted code CONTENT, never a line number. Mirror the EXISTING `reply`/`resolve` undoable-command pattern (`makeCommentCommand` do/undo closures → `writeIsland()` + marker update + `emit("dirty-changed")`). If an existing pattern (e.g. `makeCommentCommand`, `writeIsland`, `updateMarkerState`, `historyPush`, `reanchorAll`) is named differently in the live source, use the live name. If a referenced anchor is not found, STOP and write the BUG file.

## Goal (R13 — edit AND delete, for root comments AND replies; island + agent-block sync)
Four undoable store ops in `comments.js`, each via the existing command factory; `editedAt` ISO stamp on edited items; four bridge registrations in `runtime-main.js`; Edit/Delete affordances in `main.js` `createThreadEl` (root AND each reply); composer `edit` mode in `comment-composer.js` (pre-filled, For-agents checkbox hidden). NO `buildAgentBlock` structural change — it already emits `instruction:` from `t.body` and `reply:` from `r.body`, so edits/deletes flow through automatically (R14 separately rewrites its anchor line).

---

## FILE 1 — `runtime/js/comments.js` — four ops + editedAt persistence

### Op A — NEW `editComment(commentId, newBody)`
Find the thread by id; capture `before = {body: thread.body, editedAt: thread.editedAt}`; build via `makeCommentCommand("edit-comment", do, undo)`:
- `do`: `thread.body = newBody; thread.editedAt = new Date().toISOString(); writeIsland(); updateMarkerState(commentId); emit("dirty-changed",{dirty:true});`
- `undo`: `thread.body = before.body;` then the EXACT G4 conditional (DELETE the key when it was absent pre-edit; never set it to `undefined`):
  ```js
  if (before.editedAt === undefined) delete thread.editedAt; else thread.editedAt = before.editedAt;
  ```
  then `writeIsland(); updateMarkerState(commentId); emit("dirty-changed",{dirty:true});`
- `historyPush` the command. Return `{commentId}`.

### Op B — NEW `deleteComment(commentId)`
Capture the thread object + its index `idx` in `threadStore`; build via `makeCommentCommand`:
- `do`: `removeMarker(id); threadStore.splice(idx,1); writeIsland(); emit("dirty-changed",{dirty:true});`
- `undo`: `threadStore.splice(idx,0,saved); writeIsland(); reanchorAll(); emit("dirty-changed",{dirty:true});` (the re-anchor re-renders the marker).
- `historyPush`.

### Op C — NEW `editReply(commentId, replyIndex, newBody)`
Capture `before = {...thread.replies[replyIndex]}` (a shallow-clone snapshot — required for G4-safe undo); build via `makeCommentCommand`:
- `do`: set `thread.replies[replyIndex].body = newBody; thread.replies[replyIndex].editedAt = new Date().toISOString(); writeIsland(); updateMarkerState(commentId); emit("dirty-changed",{dirty:true});`
- `undo`: `thread.replies[replyIndex] = before;` then `writeIsland(); updateMarkerState(commentId); emit("dirty-changed",{dirty:true});` (full-object replace → G4-safe).
- `historyPush`.

### Op D — NEW `deleteReply(commentId, replyIndex)`
Capture `saved = thread.replies[replyIndex]` + the index; build via `makeCommentCommand`:
- `do`: `thread.replies.splice(replyIndex,1); writeIsland(); updateMarkerState(commentId); emit("dirty-changed",{dirty:true});`
- `undo`: `thread.replies.splice(replyIndex,0,saved); writeIsland(); updateMarkerState(commentId); emit("dirty-changed",{dirty:true});` (S3-11 index-safe `splice(i,0,saved)` — restore at the ORIGINAL ordinal, NOT append).
- `historyPush`.

EXPORT all four (`export function editComment`/`deleteComment`/`editReply`/`deleteReply` — match the module's existing export style; if the module uses a single `export { … }` block, add them there).

### `writeIsland` + `toJson` — persist `editedAt`
LOCATE the field set that `writeIsland`/`toJson` build for each thread (and each reply). ADD `editedAt: t.editedAt` to the persisted/serialized thread field set so the stamp round-trips. Replies persist verbatim already (passthrough) — `editedAt` on a reply rides along; if replies are mapped to an explicit field set, add `editedAt: r.editedAt` there too. `editedAt` is ADDITIVE (an extra key) — existing `test_f5`/`test_g2` assertions check key PRESENCE, not absence, so they stay green.

### `buildAgentBlock` — NO change in R13
It already emits `instruction:` from `t.body` and `reply:` from `r.body`. Do NOT touch it. (R14 rewrites its anchor line separately.)

---

## FILE 2 — `runtime/js/runtime-main.js` — register four commands (G3)

LOCATE the existing `reply-comment` registration. Register four NEW commands NEXT TO it, importing the four new fns from `comments.js`. Each validator validates ONLY the fields in its payload and throws on missing — **the edit/delete validators MUST NOT require `author`** (the composer edit-mode `onSubmit(text)` passes no author; an `author` requirement would break every edit). Validate `replyIndex` as a NUMBER including `0` via `typeof payload.replyIndex !== "number"` (NOT a falsy check, or index 0 throws):

- `edit-comment {commentId, body}` → `editComment(commentId, body)`. Validate `commentId` truthy + `body` is a string (NOT `!body` — an empty body is handled by the composer no-op, but here require `typeof body === "string"`). Do NOT require `author`.
- `delete-comment {commentId}` → `deleteComment(commentId)`. Validate `commentId` truthy. Do NOT require `author`.
- `edit-reply {commentId, replyIndex, body}` → `editReply(commentId, replyIndex, body)`. Validate `commentId` truthy + `typeof replyIndex === "number"` + `typeof body === "string"`. Do NOT require `author`.
- `delete-reply {commentId, replyIndex}` → `deleteReply(commentId, replyIndex)`. Validate `commentId` truthy + `typeof replyIndex === "number"`. Do NOT require `author`.

Add the imports to the existing `comments.js` import block (e.g. `editComment as editComment, deleteComment as deleteComment, editReply as editReply, deleteReply as deleteReply` — match the existing aliasing style). Do NOT re-import `historyPush`/`byId`/`clear` if already imported.

---

## FILE 3 — `app/js/main.js` — Edit/Delete affordances in `createThreadEl`

LOCATE `createThreadEl`. On the ROOT actions row (near the existing Reply/Resolve buttons):
- add an **Edit** button → opens the composer in `mode:'edit'` with `initialText: thread.body` and `onSubmit:(text)=> bridge.command("edit-comment",{commentId:thread.id, body:text}).then(()=>refreshCommentPanel())`.
- add a **Delete** button → `bridge.command("delete-comment",{commentId:thread.id}).then(()=>refreshCommentPanel())`.

Inside the replies loop, PER reply, add **Edit**/**Delete** buttons wired to `edit-reply`/`delete-reply` with that reply's index:
- reply Edit → composer `mode:'edit'`, `initialText: reply.body`, `onSubmit:(text)=> bridge.command("edit-reply",{commentId:thread.id, replyIndex:i, body:text}).then(()=>refreshCommentPanel())`.
- reply Delete → `bridge.command("delete-reply",{commentId:thread.id, replyIndex:i}).then(()=>refreshCommentPanel())`.

ALL new buttons call `e.stopPropagation()` in their click handler (so clicking them does not bubble to a row-select). Match the EXISTING button-creation style in `createThreadEl` (the existing Reply/Resolve buttons show the pattern: class names, `addEventListener`, the composer open call). Use the EXISTING `refreshCommentPanel`/composer-open function names. The composer open for edit calls the shell composer's open function with the new `mode:'edit'` option added in FILE 4.

---

## FILE 4 — `app/js/shell/comment-composer.js` — `edit` mode

LOCATE `openComposer` (the composer-open function that already handles `mode:'new'|'reply'`). EXTEND it to accept `mode:'edit'` + `initialText`:
- when `mode === 'edit'`: set `textarea.value = initialText` (prefill); set the textarea placeholder to the string `Edit comment`.
- HIDE the For-agents checkbox in edit mode: the checkbox is built only for `mode === 'new'` — ensure the guard that builds the For-agents checkbox is `mode === 'new'` ONLY (so `'edit'` and `'reply'` never render it). If the guard currently builds the checkbox for any non-reply mode, tighten it to `mode === 'new'`.
- `submit()` calls `onSubmit(text)` (the agent arg is irrelevant in edit). The viewport flip/clamp (D12) is independent of `mode` — edit reuses the same positioning, so a low anchor stays in-viewport (E-R13-7). Empty edit: the existing `submit()` no-op on empty (`if (!text) { closeActive(); return; }`) means an empty edit closes without mutating — leave that behavior.

---

## RUN — targeted then full suite
1. `node --check` on all four files (`runtime/js/comments.js`, `runtime/js/runtime-main.js`, `app/js/main.js`, `app/js/shell/comment-composer.js`).
2. Targeted: `python -m pytest tests/e2e/test_r13_comment_edit_delete.py -v` — ALL pass (edit/delete root+reply; undo fidelity incl. delete-reply at original index; agent-block reflects edited instruction; edit composer in-viewport + no For-agents checkbox).
3. **Comment regression:** `python -m pytest tests/e2e/test_f5_comments.py tests/e2e/test_g2_save_with_comments.py -v` — STILL green (`editedAt` is additive; REG-4/REG-5).
4. Full suite: `python -m pytest tests/ -q` — green. Any regression → STOP + `docs/verification/v4/v4-t8-BUG.md`.

Write `docs/verification/v4/v4-t8-run.txt` with the four `node --check` results, the targeted R13 + comment-regression run tails, and the full-suite tail.

## COMMIT (local-only, on master)
After all runs green, stage the R13 fixture + tests (from V4-T7) + these four product files + evidence and commit:
```
git add tests/e2e/fixtures/agent-comments.html tests/e2e/test_r13_comment_edit_delete.py runtime/js/comments.js runtime/js/runtime-main.js app/js/main.js app/js/shell/comment-composer.js docs/verification/v4/v4-t7-red.txt docs/verification/v4/v4-t8-run.txt
git commit -m "[v4-t8] R13: comment edit+delete for roots and replies (undoable, island + agent-block sync)"
```
NEVER push/merge/amend. Capture the commit hash.

## Acceptance (verify yourself)
1. `node --check` passes on all four files.
2. `comments.js` exports `editComment`/`deleteComment`/`editReply`/`deleteReply`, each undoable via the existing command factory with the G4 undo-delete-key + S3-11 index-safe delete-reply; `writeIsland`/`toJson` persist `editedAt`.
3. `runtime-main.js` registers the four commands WITHOUT an `author` requirement and with `typeof replyIndex === "number"` validation (G3).
4. `main.js` `createThreadEl` has Edit/Delete on the root row AND per reply (all `e.stopPropagation()`); `comment-composer.js` supports `mode:'edit'` (prefill + no For-agents checkbox).
5. `test_r13_comment_edit_delete.py` all pass; `test_f5_comments.py` + `test_g2_save_with_comments.py` green; full suite green.
6. Commit message exactly `[v4-t8] R13: ...`; local-only.

## Return format (exact)
```
DONE V4-T8
files-changed: <list>
tests-run: targeted test_r13_...py = <N passed>; comment regression = <N passed>; full suite = <N passed / OK>
commit-hash: <sha>
concerns: <none | …>
```

## Do NOT
- Do NOT edit any test file (FROZEN from V4-T7).
- Do NOT copy the `!payload.author` check into the edit/delete validators (G3).
- Do NOT set `editedAt` to `undefined` on undo — DELETE the key when absent (G4).
- Do NOT change `buildAgentBlock` structurally (R14 owns that).
- Do NOT add a separate panel module or inline (non-composer) editing (AD5).
- Do NOT push/merge/amend/reset.
- Do NOT create any file at the workspace root.
