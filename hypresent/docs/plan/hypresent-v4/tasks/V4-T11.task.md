---
task_id: V4-T11
title: "T-final — docs-sync (README/module-map/decision-log/build-log) + full suite + clean server run"
r_item: docs
kind: docs
execution_kind: code
executor: kimi
reviewer: claude-opus
allowed_workdir: "C:\\Users\\henri\\Documents\\second-brain\\3-resources\\tools\\rbtv\\hypresent"
allowlist:
  - "README.md"                              # MODIFY — usage deltas
  - "docs/spec/03-module-map.md"             # MODIFY — module deltas
  - "docs/decision-log.md"                   # MODIFY — APPEND-ONLY rows
  - "docs/build-log.md"                      # MODIFY — append v4 cycle entry
  - "docs/verification/v4/result.md"         # CREATE — exit evidence
  - "docs/verification/v4/v4-t11-run.txt"    # CREATE — raw run outputs
commit_policy: local-only      # commit [v4-t11] docs: ... on master; NEVER push
test_command:
  full: "python -m pytest tests/ -q"
  server: "python -c \"import subprocess,sys,time,urllib.request; p=subprocess.Popen([sys.executable,'server/server.py','127.0.0.1','8765']); time.sleep(2)\\ntry:\\n  r=urllib.request.urlopen('http://127.0.0.1:8765/app/'); print('GET /app/', r.status)\\nfinally:\\n  p.terminate()\""
doubt_policy: halt
swarm_policy: disabled
max_kimi_subagents: 0
forbidden_ops:
  - "edit any file outside the allowlist"
  - "edit any EXISTING decision-log row (APPEND-ONLY — existing rows byte-identical)"
  - "edit any product/runtime/app/test file (docs-only task; if the suite fails, the cause is a prior task gap → BUG, do not patch)"
  - "touch docs/plan/hypresent-v1, docs/plan/hypresent-v2, docs/plan/hypresent-v3, or docs/spec/04-implementation-plan.md"
  - "touch changelog.md (Registrar-owned)"
  - "touch tecer-gsmm-introduction*.html"
  - "git push, git merge, git reset --hard, or any history rewrite"
  - "create any file at the workspace root"
---

# V4-T11 — docs-sync + EXIT: README + module-map + decision-log + build-log + full suite + clean server run

You are Kimi, a non-reasoning code executor. Do EXACTLY what this file says. Read NOTHING outside this file (you MAY read the four allowlisted docs to locate anchors, and `docs/decision-log.md` to derive the next D-numbers). Output ONLY the files in the allowlist. Depends on V4-T10 (R10–R14 all landed + committed; the orchestrator R14 fresh-agent legibility gate recorded PASS).

## D14 discipline (binding)
On a blocking problem (e.g. the full suite is NOT green — meaning a prior task left a gap), STOP and write `docs/verification/v4/v4-t11-BUG.md` naming the failing test; do NOT patch product/test files (this is a docs-only task). NEVER create files at the workspace root.

## Edit-anchoring rule (binding)
Anchor every edit on the EXACT quoted string, NOT a line number. If a quoted anchor is not found verbatim, STOP and write the BUG file (the doc drifted). For the decision-log, DERIVE the next D-numbers from the live file (Step 3 below) — never hardcode.

## Goal
Sync the docs to the v4 (Session-3 R10–R14) behavior changes; then run the FULL suite + a clean server run on the synthetic fixtures and record the exit evidence. The behavior-change delta list (derived from spec.md R10–R14):
- **R10** — resize: the dragged edge tracks the cursor 1:1 on flex-grow items (kills amplification + dead zone); grow-neutralized `flex-basis` from `beforeRect+dist`; undo restores all three flex longhands.
- **R12** — Alt held during resize = symmetric center-origin on any element (Moveable `fixedDirection:[0,0]`); auto-centered elements still mirror without Alt (honest layout).
- **R11** — resize guides: position alignment lines now fire during resize (restored by R10) PLUS equal-size matching (snap + `hyp-`namespaced dashed hint when a dimension equals a same-section element's).
- **R13** — comments: edit AND delete, for root comments AND replies; undoable; island + agent `<head>` block stay in sync.
- **R14** — agent anchors: the saved file stamps `data-hyp-agent="<id>"` on each unresolved agent-tagged element; the `<head>` block references a copy-pasteable attribute selector with a self-cleanup directive; resolved/deleted threads don't stamp; live DOM stays clean after save.

---

## FILE 1 — `README.md` (usage deltas)

LOCATE the usage steps and APPLY these deltas (anchor on the live text — the section numbers/wording may have drifted from v3; match on the descriptive phrases):

1. **Resize / Move / Delete / Align step** (the step describing resize + the alignment toolbar from v3): UPDATE the resize sentence to note that the dragged edge tracks the cursor exactly in every layout context (flow/flex/grid), and ADD: holding **Alt** during a resize grows the element symmetrically from its center (any element); without Alt, resize is one-sided (auto-centered elements still mirror — honest layout). ADD that resize now shows **equal-size matching** — when a dimension comes within a few px of a nearby element's width/height it snaps to it exactly and shows a dashed hint with the matched value.
2. **Comments step** (the step describing adding comments): ADD that posted comments (root AND replies) can be **edited** and **deleted** from the comment panel (full undo; edits propagate to the saved JSON island and, for agent-tagged comments, to the `<head>` agent block).
3. **Save / agent-block step** (the step describing the saved file's agent `<head>` block, if present; else add a short note): UPDATE/ADD that agent-tagged comments stamp a `data-hyp-agent="<id>"` attribute on the target element in the saved file and the `<head>` block references it via a copy-pasteable `[data-hyp-agent~="<id>"]` selector (robust to DOM shifts) with a self-cleanup directive; resolved/deleted agent comments leave no stamp.

Keep the edits SURGICAL — extend the existing sentences; do not restructure the usage list. If a "Known Limitations" bullet describes any of these now-fixed behaviors (e.g. "resize amplification", "no guides during resize", "comments not editable", "agent anchor goes stale"), REMOVE that bullet (the limitation is fixed in v4).

---

## FILE 2 — `docs/spec/03-module-map.md` (module deltas)

LOCATE each module's tree comment / contract line and UPDATE (anchor on the live text):
- **`interaction.js`** — note the R10/R11/R12 changes: dragged-edge-1:1 resize (grow-neutralized flex-basis from beforeRect+dist), Alt-held symmetric resize via Moveable `fixedDirection`, equal-size candidate cache + `hyp-size-hint` overlay.
- **`comments.js`** — note R13 (`editComment`/`deleteComment`/`editReply`/`deleteReply` undoable ops + `editedAt` round-trip) and R14 (`buildAgentBlock` emits a `[data-hyp-agent~=…]` target selector + self-cleanup directive; new `agentStampMap()` helper).
- **`serializer.js`** — note R14: `serialize()` transient live-stamp of `data-hyp-agent` (clear→stamp→clone→strip-exempt→finally-unstamp); `stripClone` exempts `data-hyp-agent`.
- **`runtime-main.js`** — note R13: `edit-comment`/`delete-comment`/`edit-reply`/`delete-reply` registrations.
- **`comment-composer.js`** (if it appears in the module map) — note R13: `edit` mode (prefill, For-agents checkbox hidden).

Extend the existing comment strings surgically; do not restructure the tree.

---

## FILE 3 — `docs/decision-log.md` (APPEND-ONLY)

### Step 3.0 — READ the live decision-log FIRST and derive the next D-numbers (mandatory)
Before appending, READ `docs/decision-log.md`, find the HIGHEST existing `Dn` row number in the "Locked Product Decisions" table, and use the next consecutive numbers. Run:
```
python -c "import pathlib,re; s=pathlib.Path('docs/decision-log.md').read_text(); nums=[int(m) for m in re.findall(r'(?m)^\|\s*D(\d+)\s*\|', s)]; print('existing:', sorted(set(nums))); print('highest:', max(nums)); print('next five:', [max(nums)+i for i in (1,2,3,4,5)])"
```
The v3 docs-sync appended D8–D11, so the live highest is EXPECTED to be **D11** and the next five **D12–D16** — but DERIVE from the print above; if the live highest differs, use the printed "next five" and note the renumber in your evidence file. NEVER duplicate a D-number; NEVER edit an existing row.

### Step 3.1 — APPEND five rows AFTER the last existing row (one per R10–R14)
APPEND (using the derived numbers; bodies below assume D12–D16). Each row: `| D<n> | **<title>.** <decision> | <rationale + evidence>. |`

- **D12 — R10 resize tracks the cursor 1:1.** Resize neutralizes `flex-grow`/`flex-shrink` on the resized main axis and drives `flex-basis` from `beforeRect[axis] + e.dist[axis]`, so the dragged edge tracks the cursor exactly in both the slack (amplification) and no-slack (dead-zone) regimes; the healthy width/grid/absolute paths are untouched; undo restores all three flex longhands incl. the absent-inline case. Rationale: Session-3 R10 / D1; the only primitive that tracks 1:1 in both regimes; `flex-shrink:0` is a layout-necessary D1-extension scoped to the resized axis. Evidence: `docs/improvements-2026-06-r3/spec.md` R10 + S3-1..S3-4.
- **D13 — R12 Alt-held symmetric resize.** Holding Alt during a resize sets Moveable `fixedDirection:[0,0]` (center-origin) for that gesture, so the box grows symmetrically (dragged edge 1:1, opposite edge mirrors, width 2Δ); without Alt it is one-sided; auto-centered elements still mirror without Alt (honest layout, no compensation CSS); the anchor resets at `resizeEnd` even on a thrown gesture. Rationale: Session-3 R12 / D1; native PowerPoint semantics; composes with R10 unchanged (dist pre-doubled under the center anchor). Evidence: `docs/improvements-2026-06-r3/spec.md` R12 + S3-5/S3-6.
- **D14v3 — R11 resize guides + equal-size matching.** Position alignment guides fire during resize again (restored for free once R10 makes the flex edge move — no new snap config); net-new equal-size matching snaps the resized dimension to a same-section visible element's dimension within 4px and shows a `hyp-`namespaced dashed hint, excluding solver-derived flex-grow siblings from the candidate set. Rationale: Session-3 R11 / D2; Moveable has no size-equality snap; the exclusion prevents phantom snaps to grow-share widths. Evidence: `docs/improvements-2026-06-r3/spec.md` R11 + S3-7..S3-9. (NOTE: if the derived number collides with the historical anchor-discipline D14, use the DERIVED next number for THIS row — this bullet's label is illustrative; the row's D-number is whatever Step 3.0 printed.)
- **D15v3 — R13 comment edit + delete.** Four undoable store ops (`editComment`/`deleteComment`/`editReply`/`deleteReply`) for root comments and replies; edited items gain an `editedAt` ISO stamp that round-trips in the island; the panel exposes Edit/Delete on the root and each reply; editing reuses the anchored composer in an `edit` mode (For-agents checkbox hidden); the agent `<head>` block regenerates from the store automatically. Rationale: Session-3 R13 / D3; mirrors the existing reply/resolve undoable-command pattern; delete-reply undo restores at the original index. Evidence: `docs/improvements-2026-06-r3/spec.md` R13 + S3-10..S3-12.
- **D16v3 — R14 agent-anchor stamping + attribute-selector head block.** The saved file stamps `data-hyp-agent="<id>"` (space-separated for multiple threads on one element) on each unresolved agent-tagged element via a transient live-stamp that is reverted in a `finally` (live DOM stays clean between saves); the `<head>` block references the element via a copy-pasteable `[data-hyp-agent~="<id>"]` selector with a self-cleanup directive; resolved/deleted threads don't stamp; stamping is idempotent across saves and high-confidence-only (no fuzzy resolution stamps). Rationale: Session-3 R14 / D4; replaces the stale structural-path anchor that broke after the agent's first DOM edit; attributes are not nodes → the serializer node-count guard is unaffected. Evidence: `docs/improvements-2026-06-r3/spec.md` R14 + S3-13..S3-16.

(The `v3`-suffixed labels above are illustrative names — the ACTUAL appended D-numbers are the consecutive integers Step 3.0 printed; write `| D12 | ... |` etc. with the derived integers and clean titles, NO `v3` suffix in the file.)

Verify the diff after appending: `git diff -- docs/decision-log.md` shows ONLY added lines (no `-` lines on existing content).

---

## FILE 4 — `docs/build-log.md` (APPEND a v4 cycle entry)

LOCATE the END of the file (the last bullet of the most recent cycle entry — the v3 "v3 improvement cycle (improvements-2026-06-r2 — Session 2)" section's last line). APPEND a new section AFTER it:

```

---

## v4 improvement cycle (improvements-2026-06-r3 — Session 3)

- **Scope:** owner hands-on feedback on the v3 build (R10–R14, all must-have for exit — D5): R10 resize amplification + dead-zone fix · R11 resize guides + equal-size matching · R12 Alt-held symmetric resize · R13 comment edit + delete · R14 data-hyp-agent stamping + rewritten head block + fresh-agent legibility.
- **Key changes:**
  - Runtime `interaction.js`: resize neutralizes flex-grow/flex-shrink on the resized main axis and drives flex-basis from `beforeRect+dist`, so the dragged edge tracks the cursor 1:1 in both the slack and no-slack regimes (R10); Alt held sets Moveable `fixedDirection:[0,0]` for the gesture (center-origin symmetric), reset at resizeEnd above the early-return guard (R12); equal-size candidate cache (excluding solver-derived flex-grow siblings) + a `hyp-size-hint` dashed overlay that snaps the resized dimension to a same-section element within 4px (R11). Position alignment guides fire again for free once R10 makes the flex edge move.
  - Runtime `comments.js`: four undoable ops `editComment`/`deleteComment`/`editReply`/`deleteReply` (delete-reply undo restores at the original index; `editedAt` round-trips) (R13); `buildAgentBlock` now emits a `target: [data-hyp-agent~="<id>"]` selector + `context:` line + a self-cleanup preamble directive, and a new `agentStampMap()` (high-confidence-only) drives stamping (R14).
  - Runtime `serializer.js`: `serialize()` transient live-stamp of `data-hyp-agent` (clear → stamp by matchAnchor identity → clone → strip-exempt-this-pass → `finally` unstamp), so the saved clone carries the stamp and the live DOM ends each save clean; `stripClone` exempts `data-hyp-agent` from the `data-hyp-*` strip. Attributes are not nodes, so the node-count guard is unaffected (R14).
  - Runtime `runtime-main.js`: `edit-comment`/`delete-comment`/`edit-reply`/`delete-reply` registrations (no `author` requirement; `replyIndex` validated as a number incl. 0) (R13).
  - Shell `main.js` + `comment-composer.js`: Edit/Delete affordances on the root comment row and each reply; the composer gains an `edit` mode (prefilled, For-agents checkbox hidden, viewport flip/clamp preserved) (R13).
- **Tests:** TDD at task granularity — each R landed a RED task (synthetic fixtures + real-input behavioral tests run against master to prove the bug, recorded in `docs/improvements-2026-06-r3/red-first-log.md`) then a GREEN task (verbatim product fix, targeted + full suite). New synthetic fixtures `flow-grow.html`/`flow-grow-deadzone.html`/`grid-healthy.html`/`agent-comments.html` (committed; never the gitignored tecer sample — AD8); new suites `test_r10_resize_flex.py`, `test_r12_alt_symmetric.py`, `test_r11_resize_guides_equal_size.py`, `test_r13_comment_edit_delete.py`, `test_r14_agent_stamping.py`, `test_r14_legibility.py`. Full suite green per `docs/verification/v4/result.md`. App stays dependency-free (Playwright dev-only).
- **Commit cadence:** per-R RED→GREEN, one local commit per R (`[v4-t<N>] R<n>: ...`), branch `master` continued, local-only (no push — D6).
```

---

## RUN the full suite + clean server run; write the evidence
After the doc edits, run:
1. `python -m pytest tests/ -q` (or the unittest discover equivalent `python -m unittest discover -s tests -p "test_*.py" -v`). Capture the FULL output. It MUST end green (zero failures). The R10–R14 suites + all carried suites (esp. `test_r2_resize_real.py`, `test_f2_select_guides.py`, `test_f5_comments.py`, `test_g2_save_with_comments.py`) must pass.
2. Clean server run (mirror v3's exit check):
```
python -c "import subprocess,sys,time,urllib.request; p=subprocess.Popen([sys.executable,'server/server.py','127.0.0.1','8765']); time.sleep(2)
try:
  r=urllib.request.urlopen('http://127.0.0.1:8765/app/'); print('GET /app/', r.status)
finally:
  p.terminate()"
```
Expect `GET /app/ 200`.
3. Zero-editor-console-error evidence on the synthetic fixtures: this is captured by the `test_no_console_errors`-style cases inside the R10/R13 suites (REG-8 runs on `flow-grow.html` + `agent-comments.html`). Confirm those cases are green and note it. (If you want an explicit probe, you MAY open `flow-grow.html` and `agent-comments.html` via a headless Playwright page in a small script and assert zero editor-origin console errors — but the suite already covers this; do NOT add a new test file.)

Write `docs/verification/v4/result.md` (mirror `docs/verification/v3/result.md` shape) with: the full-suite tail (`Ran N tests … OK` / `N passed`), a per-scorecard mapping (R10/R11/R12/R13/R14 each → its suite + PASS), the `GET /app/ 200` confirmation, the zero-editor-console-error note (REG-8 green on the synthetic fixtures), and a one-line confirmation that the orchestrator R14 fresh-agent legibility gate passed (the orchestrator records the gate result; reference it). Write `docs/verification/v4/v4-t11-run.txt` with the raw suite + server outputs.

## COMMIT (local-only, on master)
After the suite is green and the evidence is written, stage the four docs + the two evidence files and commit:
```
git add README.md docs/spec/03-module-map.md docs/decision-log.md docs/build-log.md docs/verification/v4/result.md docs/verification/v4/v4-t11-run.txt
git commit -m "[v4-t11] docs: sync v4 docs (usage, module map, decision log, build log) and close the Session-3 cycle"
```
NEVER push/merge/amend. Capture the commit hash.

## Acceptance (verify yourself)
1. README usage reflects R10 (1:1 resize) + R12 (Alt symmetric) + R11 (equal-size matching) + R13 (comment edit/delete) + R14 (agent attribute-selector anchors); any now-fixed known-limitation bullet removed.
2. `03-module-map.md` reflects the changed `interaction.js`, `comments.js`, `serializer.js`, `runtime-main.js` (and `comment-composer.js` if present).
3. `docs/decision-log.md` has FIVE new appended rows at the next consecutive D-numbers (derived from Step 3.0); `git diff -- docs/decision-log.md` shows ONLY additions (existing rows byte-identical).
4. `docs/build-log.md` has a new "v4 improvement cycle" section appended; prior content unchanged.
5. Full suite green; `GET /app/` → 200; REG-8 zero-editor-console-error cases green on the synthetic fixtures; `docs/verification/v4/result.md` + `v4-t11-run.txt` written.
6. `git diff --stat` shows changes ONLY in the six allowlisted paths; commit message exactly `[v4-t11] docs: ...`; local-only.

## Return format (exact)
```
DONE V4-T11
files-changed: <list>
decision-log: appended D<a>..D<e> (existing rows byte-identical)
tests-run: full suite = <N passed / OK>; GET /app/ = <200>
commit-hash: <sha>
concerns: <none | …>
```

## Do NOT
- Do NOT edit any EXISTING decision-log row (APPEND-ONLY).
- Do NOT edit any product/runtime/app/test file (docs-only; a failing suite is a prior-task gap → BUG, not a patch here).
- Do NOT touch `changelog.md`, `docs/spec/04-implementation-plan.md`, or any v1/v2/v3 plan folder.
- Do NOT push/merge/amend/reset.
- Do NOT create any file at the workspace root.
