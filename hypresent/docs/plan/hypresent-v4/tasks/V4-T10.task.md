---
task_id: V4-T10
title: "R14 GREEN — data-hyp-agent stamping + rewritten head block (verbatim); rerun + commit"
r_item: R14
kind: GREEN
execution_kind: code
executor: kimi
reviewer: claude-opus
allowed_workdir: "C:\\Users\\henri\\Documents\\second-brain\\3-resources\\tools\\rbtv\\hypresent"
allowlist:
  - "runtime/js/comments.js"                # MODIFY — buildAgentBlock anchor-line rewrite + agentStampMap
  - "runtime/js/serializer.js"              # MODIFY — serialize() transient stamp + stripClone exemption
  - "docs/verification/v4/v4-t10-run.txt"   # CREATE
commit_policy: local-only      # commit [v4-t10] R14: ... on master; NEVER push
test_command:
  targeted: "python -m pytest tests/e2e/test_r14_agent_stamping.py tests/e2e/test_r14_legibility.py -v"
  full: "python -m pytest tests/ -q"
doubt_policy: halt
swarm_policy: disabled
max_kimi_subagents: 0
forbidden_ops:
  - "edit any file outside the allowlist (tests are FROZEN — V4-T9 wrote them)"
  - "weaken, xfail, or skip any test to make the suite pass"
  - "use the index-based resolveCloneNode for stamping (unsound — F3; use the transient live-stamp)"
  - "change thread.anchor or the runtime island format (S3 contract 8)"
  - "change the sentinel ===== HYPRESENT AGENT INSTRUCTIONS ===== (REG-5)"
  - "stamp on a fuzzy (level 3/4/5) anchor resolution (G6 high-confidence-only)"
  - "touch docs/plan/hypresent-v1, docs/plan/hypresent-v2, or docs/plan/hypresent-v3"
  - "touch tecer-gsmm-introduction*.html"
  - "git push, git merge, git reset --hard, or any history rewrite"
  - "create any file at the workspace root"
---

# V4-T10 — R14 GREEN: `data-hyp-agent` stamping + rewritten head block (verbatim) + rerun + commit

You are Kimi, a non-reasoning code executor. Do EXACTLY what this file says. Read NOTHING outside this file (you MAY read `runtime/js/comments.js` and `runtime/js/serializer.js` to locate anchors and confirm names: `buildAgentBlock`'s anchor line, `matchAnchor`, the thread store, `escapeAgentBlock`, `serialize()`'s `cloneNode` call + node-count guard, and `stripClone`'s Phase-B `data-hyp-*` removal loop). Output ONLY the files in the allowlist. Depends on V4-T9 (R14 RED tests landed; FROZEN).

## D14 discipline (binding)
On a blocking product bug, STOP and write `docs/verification/v4/v4-t10-BUG.md`. NEVER weaken/xfail/skip a test. NEVER edit the test files. NEVER create files at the workspace root.

## Edit-anchoring rule (binding)
Anchor every edit on quoted code CONTENT, never a line number. If a referenced name (`matchAnchor`, `escapeAgentBlock`, `stripClone`, `buildAgentBlock`) is not found verbatim, STOP and write the BUG file.

## Goal (R14 — stamp `data-hyp-agent` on each unresolved agent-tagged element in the SAVED file; rewrite the head block to an attribute selector with a self-cleanup directive)
Strategy = **transient live-stamp + strip-exempt-this-pass** (S3-13): `serialize()` stamps the LIVE element by `matchAnchor` identity BEFORE cloning, exempts `data-hyp-agent` from the strip for that pass, and unstamps the live DOM in a `finally` — so the SAVED clone carries the stamp and the live DOM is clean again before `serialize()` returns (history-safe). The index-based `resolveCloneNode` is NOT used for stamping (it desyncs against the strip's removal of body-appended `hyp-` chrome — F3). `thread.anchor` and the runtime island format are UNCHANGED (S3 contract 8). Two files: `comments.js` (block rewrite + the stamp-map helper), `serializer.js` (the transient stamp + the strip exemption).

---

## FILE 1 — `runtime/js/comments.js`

### EDIT 1A — `buildAgentBlock`: rewrite the per-thread anchor line + self-cleanup preamble

LOCATE the per-thread anchor line in `buildAgentBlock`. On master it emits a single line of the form:

```
anchor: ${path} | id="${nid}" | "${ctx}"
```

REPLACE that single line with TWO lines (a `target:` selector line + a `context:` line). All interpolations pass through the existing `escapeAgentBlock`:

```
target: [data-hyp-agent~="${escapeAgentBlock(t.id)}"]
context: ${escapeAgentBlock(tagAndClasses)} | "${ctx}"
```

where `tagAndClasses` is the thread's anchored-element tag name + its non-`hyp` class tokens, resolved via `matchAnchor(t.anchor)`:
- resolve `el = matchAnchor(t.anchor)`;
- if `el`: `tagAndClasses = el.tagName.toLowerCase() + classTokens` where `classTokens` is the space-joined non-`hyp-`-prefixed class tokens, each prefixed with a dot (e.g. `p .lead`; for an element with classes `lead foo` → `p .lead .foo`; filter OUT any class starting with `hyp-`). The exact format the test asserts is `context: p .lead | "<excerpt>"` for `<p class="lead">` — so produce `<tag> .<class1> .<class2>` (tag, then each non-hyp class as `.<name>`, space-separated).
- if `el` is null (unresolved): emit `context: (unresolved) | "${ctx}"`.

The `${ctx}` excerpt (≤80 chars of the element's text) is the existing context excerpt — keep it. `buildPath`/`anchor.path` are NO LONGER emitted (they remain in the store for `matchAnchor`).

Update the block PREAMBLE (the header text before the per-entry list) to ADD a one-time **self-cleanup directive** instructing the consuming agent to REMOVE the `data-hyp-agent` token for an id AND that id's block entry after applying its change. The directive MUST contain the stable substring `remove the data-hyp-agent` (the test pins this substring; you may add surrounding wording). Example preamble addition: `After applying an instruction, remove the data-hyp-agent token for that id from the target element and delete this entry.`

The block iterates `agentInstruction === true && resolved !== true` (resolved/deleted threads are not in the set — S3-16). The sentinel `===== HYPRESENT AGENT INSTRUCTIONS =====` is UNCHANGED. Each entry keeps its `[agent:<id>]` marker, `instruction:` line, optional `reply:` lines, `author:`/`date:` lines.

### EDIT 1B — NEW exported helper `agentStampMap()` (high-confidence-only, G6)

ADD and EXPORT `agentStampMap()` returning `Map<Element, string[]>`: for each thread with `agentInstruction === true && resolved !== true`, resolve `el = matchAnchor(t.anchor)`; if `el`, push `t.id` into the map entry for `el`. Resolved/deleted/unanchored threads contribute nothing.

**G6 high-confidence gate (binding):** `matchAnchor` may return a FUZZY match (its level-4 same-parent scan or level-5 relaxed tail) that could be a wrong sibling. `agentStampMap` MUST stamp ONLY on a high-confidence resolution — accept the element ONLY if the anchor resolves via level 1 (unique `data-hyp-hook`) or level 2 (`nativeId`+path walk with `contentHash` equality); if resolution falls through to level 3/4/5, treat the thread as unanchored for stamping (contribute nothing → no stamp; the block still emits its `context:` excerpt as the human fallback). If `matchAnchor` does not expose its match level, add a high-confidence variant (e.g. re-run the level-1/level-2 resolution inline and return null on fall-through) — do NOT relax to fuzzy. Match the existing `matchAnchor` internals; if it already returns a confidence/level, gate on it.

```js
export function agentStampMap() {
  const map = new Map();
  for (const t of threadStore) {
    if (!(t.agentInstruction === true && t.resolved !== true)) continue;
    const el = matchAnchorHighConfidence(t.anchor); // level 1 or 2 ONLY; null on fall-through
    if (!el) continue;
    if (!map.has(el)) map.set(el, []);
    map.get(el).push(t.id);
  }
  return map;
}
```

(`matchAnchorHighConfidence` is the level-1/level-2-only resolver — implement it from `matchAnchor`'s existing level-1/level-2 logic, returning null when those two levels do not resolve. Use the existing `threadStore` name.)

---

## FILE 2 — `runtime/js/serializer.js`

### EDIT 2A — `serialize()`: transient live-stamp wrapped in try/finally (S3-13)

Import `agentStampMap` from `comments.js` (add to the existing comments import in serializer.js, e.g. `agentStampMap as agentStampMap`).

LOCATE `serialize()`'s clone line:

```js
const clone = document.documentElement.cloneNode(true);
```

(The exact target may be `document.documentElement` — match the live source.) Wrap the stamping in a try/finally around the clone+strip so the live DOM is restored even on error. The EXACT sequence:

1. `const stampMap = agentStampMap();` (BEFORE the clone)
2. Then, still BEFORE `cloneNode`:
   - (1) clear stale/reopened residue: `for (const el of document.querySelectorAll("[data-hyp-agent]")) el.removeAttribute("data-hyp-agent");`
   - (2) stamp current unresolved ids: `for (const [liveEl, ids] of stampMap) liveEl.setAttribute("data-hyp-agent", ids.join(" "));`
3. clone (the existing `cloneNode(true)`).
4. `stripClone(clone)` (now preserves `data-hyp-agent` via the EDIT-2B exemption).
5. In a `finally` that wraps EVERYTHING from step (1) onward: `for (const el of document.querySelectorAll("[data-hyp-agent]")) el.removeAttribute("data-hyp-agent");` — so the LIVE DOM ends with ZERO `data-hyp-agent`.

Concretely (transcribe the shape, adapting variable names to the live source):

```js
const stampMap = agentStampMap();
let clone;
try {
  for (const el of document.querySelectorAll("[data-hyp-agent]")) el.removeAttribute("data-hyp-agent");
  for (const [liveEl, ids] of stampMap) liveEl.setAttribute("data-hyp-agent", ids.join(" "));
  clone = document.documentElement.cloneNode(true);
  stripClone(clone);
} finally {
  for (const el of document.querySelectorAll("[data-hyp-agent]")) el.removeAttribute("data-hyp-agent");
}
```

Everything that previously followed the clone (the node-count guard on the clone, the agent-block `insertAdjacentHTML` into `<head>`, `stripIds`, the final serialization to string) runs AFTER `stripClone(clone)` as before — the block's `target:` selectors match the stamped clone attributes. The node-count guard is computed on the clone AFTER stamping; ATTRIBUTES ARE NOT NODES, so `preCount`/`postCount` are unaffected (Risk-3 — save succeeds with N stamps). Do NOT move the guard relative to the clone; only the stamp+strip is wrapped.

> If `serialize()` already has a try/finally or a different clone variable, fold steps (1)+(2) before the existing clone and add the unstamp loop to the existing/`new` `finally`. The load-bearing invariants: stamp BEFORE clone; unstamp in a `finally`; the clone is taken WHILE the stamps are live.

### EDIT 2B — `stripClone`: exempt `data-hyp-agent` from the `data-hyp-*` removal

LOCATE the Phase-B attribute-removal test in `stripClone` (the loop that removes every `data-hyp-*` attribute). On master it is:

```js
if (attr.name.startsWith("data-hyp-")) {
```

CHANGE it to exempt `data-hyp-agent`:

```js
if (attr.name.startsWith("data-hyp-") && attr.name !== "data-hyp-agent") {
```

The exemption is SAFE because `serialize()` step (1) cleared all live `data-hyp-agent` and step (2) re-applied only current-unresolved-thread ids — so the only `data-hyp-agent` on the clone are the just-applied current stamps; a reopened file's prior `data-hyp-agent` was cleared by step (1) BEFORE the clone, so it never reaches the clone (S3-16 idempotence holds). `stripIds`/`data-hyp-id` removal is UNCHANGED — do NOT touch it.

---

## RUN — targeted then full suite
1. `node --check runtime/js/comments.js` and `node --check runtime/js/serializer.js` — must pass.
2. Targeted: `python -m pytest tests/e2e/test_r14_agent_stamping.py tests/e2e/test_r14_legibility.py -v` — ALL pass (stamp + selector resolves the right element; multi-thread space-separated; resolved/deleted don't stamp; live DOM clean after save; block format complete incl. self-cleanup directive; idempotent across saves; node-count guard safe; non-first element via markers-live; fresh-agent resolves every target incl. after DOM shift; self-cleanup per-id non-destructive).
3. **R13 + save regression:** `python -m pytest tests/e2e/test_r13_comment_edit_delete.py tests/e2e/test_g2_save_with_comments.py -v` — STILL green (the sentinel is unchanged — REG-5; R13's edit→block sync still works).
4. Full suite: `python -m pytest tests/ -q` — green. Any regression → STOP + `docs/verification/v4/v4-t10-BUG.md`.

Write `docs/verification/v4/v4-t10-run.txt` with the two `node --check` results, the targeted R14 + R13/g2 run tails, and the full-suite tail.

## COMMIT (local-only, on master)
After all runs green, stage the R14 tests (from V4-T9) + these two product files + evidence and commit:
```
git add tests/e2e/test_r14_agent_stamping.py tests/e2e/test_r14_legibility.py runtime/js/comments.js runtime/js/serializer.js docs/verification/v4/v4-t9-red.txt docs/verification/v4/v4-t10-run.txt
git commit -m "[v4-t10] R14: data-hyp-agent stamping + attribute-selector head block (fresh-agent legible)"
```
NEVER push/merge/amend. Capture the commit hash.

## Acceptance (verify yourself)
1. `node --check` passes on both files.
2. `buildAgentBlock` emits `target: [data-hyp-agent~="<id>"]` + `context: <tag> .<classes> | "<excerpt>"` (or `(unresolved)`) + the self-cleanup preamble substring `remove the data-hyp-agent`; the sentinel is unchanged; `buildPath` no longer emitted.
3. `agentStampMap()` exported, high-confidence-only (level 1/2; null on fall-through — G6).
4. `serialize()` does clear→stamp→clone→strip-exempt→finally-unstamp (stamp BEFORE clone, unstamp in `finally`); `stripClone` exempts `data-hyp-agent` (`attr.name !== "data-hyp-agent"`); node-count guard unmoved; `thread.anchor`/island format unchanged.
5. `test_r14_agent_stamping.py` + `test_r14_legibility.py` all pass; `test_r13_comment_edit_delete.py` + `test_g2_save_with_comments.py` green; full suite green.
6. Commit message exactly `[v4-t10] R14: ...`; local-only.

## Return format (exact)
```
DONE V4-T10
files-changed: <list>
tests-run: targeted R14 = <N passed>; R13/g2 regression = <N passed>; full suite = <N passed / OK>
commit-hash: <sha>
concerns: <none | …>
```

## Do NOT
- Do NOT edit any test file (FROZEN from V4-T9).
- Do NOT use the index-based `resolveCloneNode` for stamping (F3 — use the transient live-stamp).
- Do NOT stamp on a fuzzy (level 3/4/5) anchor resolution (G6).
- Do NOT change `thread.anchor` or the runtime island format (S3 contract 8).
- Do NOT change the sentinel string (REG-5).
- Do NOT push/merge/amend/reset.
- Do NOT create any file at the workspace root.
