---
task_id: V4-T2
title: "R10 GREEN — interaction.js dragged-edge-tracks-cursor 1:1 fix; rerun targeted + full suite; commit"
r_item: R10
kind: GREEN
execution_kind: code
executor: kimi
reviewer: claude-opus
allowed_workdir: "C:\\Users\\henri\\Documents\\second-brain\\3-resources\\tools\\rbtv\\hypresent"
allowlist:
  - "runtime/js/interaction.js"             # MODIFY — the only product file
  - "docs/verification/v4/v4-t2-run.txt"    # CREATE — run evidence
commit_policy: local-only      # commit [v4-t2] R10: ... on master; NEVER push
test_command:
  targeted: "python -m pytest tests/e2e/test_r10_resize_flex.py -v"
  full: "python -m pytest tests/ -q"
doubt_policy: halt
swarm_policy: disabled
max_kimi_subagents: 0
forbidden_ops:
  - "edit any file outside the allowlist (tests are FROZEN — V4-T1 wrote them; never edit them here)"
  - "weaken, xfail, or skip any test to make the suite pass"
  - "touch docs/plan/hypresent-v1, docs/plan/hypresent-v2, or docs/plan/hypresent-v3"
  - "touch tecer-gsmm-introduction*.html"
  - "edit the else/block-grid branch or the absolute branch of applyVisualResize (R10 touches ONLY the flex-child branch)"
  - "write position:absolute or translate anywhere (I1/I2)"
  - "git push, git merge, git reset --hard, or any history rewrite"
  - "create any file at the workspace root"
---

# V4-T2 — R10 GREEN: `interaction.js` resize 1:1 fix (verbatim) + rerun + commit

You are Kimi, a non-reasoning code executor. Do EXACTLY what this file says. Read NOTHING outside this file (you MAY read `runtime/js/interaction.js` to locate the anchors). Output ONLY the files in the allowlist. Depends on V4-T1 (the R10 RED fixtures + tests landed; they are FROZEN — never edit them).

## D14 discipline (binding)
On a blocking product bug, STOP and write `docs/verification/v4/v4-t2-BUG.md` naming the failing assertion + observed-vs-expected numbers. NEVER weaken/xfail/skip a test to make the suite pass. NEVER edit the test files (they are the contract). NEVER create files at the workspace root.

## Edit-anchoring rule (binding)
Every edit is anchored on the EXACT quoted code string below, NOT a line number. LOCATE each block by matching the quoted code verbatim in the live `interaction.js`. If a quoted anchor is not found verbatim, STOP and write the BUG file — do NOT guess.

## Goal (R10 — the dragged edge tracks the cursor 1:1 on flex-grow items, in every layout context)
The fix neutralizes flex-grow on the resized MAIN axis and drives `flex-basis` from `beforeRect[axis] + e.dist[axis]`, killing BOTH the slack-region amplification AND the no-slack dead zone in one write — while leaving the already-correct width/grid/absolute paths byte-identical. Three edits in `interaction.js`. NOTHING else changes.

---

## EDIT 1 — `captureSizingState`: add `flex-grow` + `flex-shrink` to the flex-child branch

LOCATE the `captureSizingState` function's `role === "flex-child"` branch. On the CURRENT source it captures `flex-basis`, `width`, `height` only (the exact property names captured may be keyed via `s.getPropertyValue("flex-basis")` etc. into a map `m`). ADD the two longhand captures alongside the existing flex-basis capture in that branch:

```js
m["flex-grow"] = s.getPropertyValue("flex-grow") || "";
m["flex-shrink"] = s.getPropertyValue("flex-shrink") || "";
```

Key them through the SAME map and the SAME `getPropertyValue` mechanism the existing `flex-basis` capture uses, so the `onResizeEnd` before/after change-detection sees a symmetric key set (this is what makes undo restore the absent-inline case as `""` → `removeProperty`). Do NOT change `commands.js` — its `applyStyleMap` already maps `""`→`removeProperty`, which restores the absent-inline case. (Captured BEFORE the gesture writes, so the pristine fixture captures `""` for all three.)

## EDIT 2 — `onResize(e)`: pass `e.dist` through to `applyVisualResize`

LOCATE the current `onResize` handler:

```js
  onResize(e) { applyVisualResize(el, roleOf(el), e.width, e.height, e.direction); }
```

(The exact surrounding form may differ slightly — the load-bearing call is `applyVisualResize(el, roleOf(el), e.width, e.height, e.direction)`.) REPLACE that call to ALSO pass `e.dist`:

```js
  onResize(e) { applyVisualResize(el, roleOf(el), e.width, e.height, e.direction, e.dist); }
```

`e.dist = [distWidth, distHeight]`; `dist[0]` is the cursor-true x delta, `dist[1]` the y delta. (Under the default anchor Moveable's `b = 2/|direction − fixedDirection| = 1`, so `e.dist[0] = Δcursor` exactly.)

## EDIT 3 — `applyVisualResize`: gain the `dist` param and replace ONLY the flex-child branch body

LOCATE the `applyVisualResize` signature and add the `dist` parameter:

```js
function applyVisualResize(el, role, width, height, direction, dist) {
```

(Match the existing signature `applyVisualResize(el, role, width, height, direction)` and append `, dist`.)

Then LOCATE the `role === "flex-child" && parent` branch body (the branch that currently writes `el.style.flexBasis = width + "px"` for a flex-child whose parent is the resize axis). REPLACE ONLY that branch's body with the VERBATIM two-branch block below. Grow-neutralize the MAIN axis ONLY (never both axes). The CROSS axis is a plain `width`/`height` write with NO `top`/`left` compensation (the flex item is in normal flow — Moveable's `fixedDirection` holds the opposite edge — so do NOT add the absolute-branch top/left comp here).

```js
const mainDim = isFlexRow ? "width" : "height";
const mainAxisIdx = isFlexRow ? 0 : 1;
el.style.flexGrow = "0";
el.style.flexShrink = "0";
el.style.flexBasis = (beforeRect[mainDim] + dist[mainAxisIdx]) + "px";
if (isFlexRow) {
  el.style.height = (beforeRect.height + dist[1]) + "px";
} else {
  el.style.width = (beforeRect.width + dist[0]) + "px";
}
```

Notes (binding):
- `beforeRect[mainDim]` uses the literal STRING key `"width"`/`"height"` (NOT a numeric index); `dist[mainAxisIdx]` uses the numeric index `0`/`1`.
- `isFlexRow` is the existing local that determines whether the flex parent's main axis is the row (width) — reuse the EXISTING `isFlexRow` computation in this branch; if the branch computes flex-row direction under a different local name, use that name. Do NOT introduce a new direction probe.
- `beforeRect` is the module-level rect captured at `onResizeStart` (`beforeRect = el.getBoundingClientRect()`), already read inside `applyVisualResize`. Confirm it is in scope; if the branch references a differently-named start-rect, use that name.
- The `else` (block/grid) and `absolute` branches are UNTOUCHED — they keep consuming `width`/`height`. Do NOT modify them.

> If R11's equal-size override is later layered on (V4-T6), it replaces the `(beforeRect[mainDim] + dist[mainAxisIdx])` term BEFORE the write — that is a SEPARATE task. For THIS task, write exactly the block above.

`onResizeStart` needs NO change (`beforeRect` already captured). `commands.js` needs NO change.

---

## RUN — targeted then full suite

1. `node --check runtime/js/interaction.js` — must pass.
2. Targeted: `python -m pytest tests/e2e/test_r10_resize_flex.py -v` — ALL cases must PASS now (E-R10-1/-2/-2b flip from RED→GREEN; E-R10-3/-4/-5 stay GREEN; E-R10-6 GREEN; G-R10-UNDO restores all three longhands incl. absent-inline; G-R10-REDO idempotent).
3. Full suite: `python -m pytest tests/ -q` — must be green. Watch especially `test_r2_resize_real.py` (REG-2 — the healthy width path must NOT be perturbed) and `test_f2_select_guides.py` (REG-3). If ANY previously-green test fails, the fix touched a healthy branch — STOP and write `docs/verification/v4/v4-t2-BUG.md`.

Write `docs/verification/v4/v4-t2-run.txt` with: the `node --check` result, the targeted run tail (per-test PASS), and the full-suite tail (`N passed` / OK line).

## COMMIT (local-only, on master)
After BOTH runs are green, stage the R10 fixtures + tests (from V4-T1) + this fix + the evidence, and commit LOCAL-ONLY on `master`:
```
git add tests/e2e/fixtures/flow-grow.html tests/e2e/fixtures/flow-grow-deadzone.html tests/e2e/fixtures/grid-healthy.html tests/e2e/test_r10_resize_flex.py tests/e2e/conftest_helpers.py runtime/js/interaction.js docs/verification/v4/v4-t1-red.txt docs/verification/v4/v4-t2-run.txt docs/improvements-2026-06-r3/red-first-log.md
git commit -m "[v4-t2] R10: resize dragged edge tracks cursor 1:1 (grow-neutralized flex-basis from beforeRect+dist)"
```
NEVER push, NEVER merge, NEVER amend. Verify the working tree has no unstaged product changes outside the allowlist before committing. Capture the commit hash.

## Acceptance (verify yourself)
1. `node --check runtime/js/interaction.js` passes.
2. `captureSizingState` flex-child branch now captures `flex-grow` + `flex-shrink` (keyed through the same map as `flex-basis`).
3. `onResize` passes `e.dist` as the 6th arg; `applyVisualResize` signature gained `dist`; the flex-child branch body is EXACTLY the verbatim two-branch block; the `else`/`absolute` branches are byte-identical to before.
4. `test_r10_resize_flex.py` ALL pass; full suite green; `test_r2_resize_real.py` + `test_f2_select_guides.py` still green.
5. `git diff --stat HEAD~1` (or the staged diff before commit) shows changes ONLY in the allowlist + the V4-T1 test/fixture files; the commit message is exactly `[v4-t2] R10: ...`.
6. `docs/verification/v4/v4-t2-run.txt` written; commit is local (no push).

## Return format (exact)
```
DONE V4-T2
files-changed: <list>
tests-run: targeted test_r10_resize_flex.py = <N passed>; full suite = <N passed / OK>
commit-hash: <sha>
concerns: <none | …>
```

## Do NOT
- Do NOT edit any test file or fixture (FROZEN from V4-T1).
- Do NOT touch the `else`/block-grid or `absolute` branch of `applyVisualResize`.
- Do NOT write `position:absolute` or `translate` anywhere (I1/I2).
- Do NOT change `commands.js` or `captureSizingState`'s non-flex branches.
- Do NOT push/merge/amend/reset.
- Do NOT create any file at the workspace root.
