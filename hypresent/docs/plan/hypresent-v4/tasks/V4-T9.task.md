---
task_id: V4-T9
title: "R14 RED — test_r14_agent_stamping.py + test_r14_legibility.py, run vs master"
r_item: R14
kind: RED
execution_kind: code
executor: kimi
reviewer: claude-opus
allowed_workdir: "C:\\Users\\henri\\Documents\\second-brain\\3-resources\\tools\\rbtv\\hypresent"
allowlist:
  - "tests/e2e/test_r14_agent_stamping.py"     # CREATE
  - "tests/e2e/test_r14_legibility.py"         # CREATE
  - "docs/verification/v4/v4-t9-red.txt"       # CREATE
commit_policy: local-only      # RED task does NOT commit; rides the V4-T10 GREEN commit
test_command: "python -m pytest tests/e2e/test_r14_agent_stamping.py tests/e2e/test_r14_legibility.py -v"   # run vs current master (R14 fix NOT applied)
doubt_policy: halt
swarm_policy: disabled
max_kimi_subagents: 0
forbidden_ops:
  - "edit any file outside the allowlist"
  - "edit any product/runtime/app file (TEST-ONLY task)"
  - "edit or recreate agent-comments.html or conftest_helpers.py (reuse the committed ones read-only)"
  - "weaken, xfail, or skip any assertion to make a test pass"
  - "touch docs/plan/hypresent-v1, docs/plan/hypresent-v2, or docs/plan/hypresent-v3"
  - "touch tecer-gsmm-introduction*.html"
  - "git push, git merge, git reset --hard, or any history rewrite"
  - "create any file at the workspace root"
---

# V4-T9 — R14 RED: `test_r14_agent_stamping.py` + `test_r14_legibility.py` (run vs master)

You are Kimi, a non-reasoning code executor. Do EXACTLY what this file says. Read NOTHING outside this file (you MAY read `tests/e2e/test_g2_save_with_comments.py` to copy the EXISTING save/read patterns ONLY). Output ONLY the files in the allowlist. Depends on V4-T8 (R13 landed; `agent-comments.html` + `copy_synthetic`/`SYN_DIR` exist, reused READ-ONLY).

## D14 discipline (binding)
On a RED/GREEN mismatch vs the lists below, STOP and write `docs/verification/v4/v4-t9-BUG.md`. NEVER weaken a test to pass. NEVER patch product files. NEVER create files at the workspace root.

## What this task is
RED half of the R14 pair. CREATE the two R14 test files and RUN vs CURRENT master — no `data-hyp-agent` stamping exists; the head block emits the stale `anchor: ${path}` line. Expected: the stamping/selector/legibility cases FAIL (no attribute is ever emitted, the path-based block fails the new format assertions). You apply NO product fix (that is V4-T10).

> `test_r14_legibility.py` is the DETERMINISTIC CI harness (a structural test of the saved artifact, NOT an LLM call). It models "an agent with zero project context" as a fresh Playwright page that loads the saved file as a PLAIN document and is given ONLY the head-block text. The separate ORCHESTRATOR fresh-agent legibility GATE (in the plan index) is the human-judgement seal layered on top of this harness — NOT part of this task.

---

## Harness conventions (binding)
- `unittest.TestCase`; `setUpClass` starts the server + Chromium headless; `tearDownClass` closes both. Use `PORT = 8814` for `test_r14_agent_stamping.py`, `PORT = 8815` for `test_r14_legibility.py`. Fixture-existence guard via `os.path.exists(os.path.join(H.SYN_DIR, "agent-comments.html"))`.
- `setUp`: open a page, `H.preset_author(self.page)` BEFORE goto, navigate to `self.base + "/app/"`, then `H.open_via_dialog_ui(self.page, self.base, H.copy_synthetic("agent-comments.html"))`.
- REAL input only; D14 guard on selection.
- Saving: `H.set_fake_dialog(self.base, out)` (out = a fresh tempfile path) + `self.page.click("#save-as-btn")` + `self.page.wait_for_timeout(600)`; then read `out` as text. Reopening a saved file: `H.open_via_dialog_ui(self.page, self.base, out)` + wait 500.
- Inline the `_select`, `_add_comment(text, agent=False)`, `_add_reply(text)`, `_read_island`, and `bridge_command` helpers EXACTLY as defined in V4-T7 STEP 2 (reproduce them here so each module is self-contained). For agent-tagging a fresh comment, prefer checking the per-thread agent toggle `#comment-threads .comment-agent-toggle input` on its row after `_add_comment(text)`, OR the composer For-agents checkbox during `_add_comment(text, agent=True)`.

---

## STEP 1 — `tests/e2e/test_r14_agent_stamping.py` (E-R14-1..8)

Implement E-R14-1 .. E-R14-8 exactly per the test-plan. Key parse helper: from a saved `out` text, extract the head comment block (the region between the sentinel `===== HYPRESENT AGENT INSTRUCTIONS =====` and the end of that HTML comment) via a stdlib regex; resolve which element carries a `data-hyp-agent` value by parsing the saved HTML with a regex/stdlib scan.

**E-R14-1 — Single agent thread: stamp + selector resolves the right element:** open fixture; `self._select("p-lead")`; `self._add_comment("fix lead", agent=True)` (id0); save to `out1`. Assertions: exactly one `data-hyp-agent=` occurrence in `out1`; its value contains `id0`; the stamped element is the `<p id="p-lead">` (parse the saved HTML); head block contains `target: [data-hyp-agent~="<id0>"]` (or `[data-hyp-agent="<id0>"]`). Reopen-resolve: `H.open_via_dialog_ui(self.page, self.base, out1)`; `H.doc_eval(self.page, "const m=doc.querySelectorAll('[data-hyp-agent~=\\"<id0>\\"]'); return {n:m.length, text:m[0]?m[0].textContent.slice(0,30):null};")` → `n == 1` and `text` matches the lead excerpt. (Substitute the real `id0` value into the selector string.)

**E-R14-2 — Two agent threads on the SAME element → space-separated ids, both selectors resolve:** open fixture; `self._select("p-lead")`; `_add_comment("a", agent=True)` (id0); `self._select("p-lead")` again; `_add_comment("b", agent=True)` (id1); save to `out`. Assertions: the lead element's `data-hyp-agent` value == `"<id0> <id1>"` (order = thread order); two block entries; reopening, `querySelectorAll('[data-hyp-agent~="<id0>"]')` and `[data-hyp-agent~="<id1>"]` EACH return exactly the single shared element.

**E-R14-3 — Resolved/deleted threads don't stamp:** open fixture; add agent comment on `#p-lead` (id0) and `#p-arch` (id1). Action A (resolve): `bridge_command(self.page, "resolve-comment", {"commentId": id0, "resolved": True})`; save to `outR`. Action B (delete): continue on the live doc — delete id1 (`bridge_command(self.page, "delete-comment", {"commentId": id1})`); save to `outD`. Assertions: `outR` contains NO `data-hyp-agent` for id0 and no block entry for id0 (id1 still stamped); `outD` contains NO stamp/entry for id1.

**E-R14-4 — Live DOM stays clean after save:** open fixture; add agent comment on `#p-lead`; save to `out`. Assertion: immediately after save, `H.doc_eval(self.page, "return doc.querySelectorAll('[data-hyp-agent]').length;") == 0`.

**E-R14-5 — Block format completeness:** open fixture; `self._select("p-lead")` (a `<p class="lead">`); `_add_comment("rename heading", agent=True)`; `_add_reply("also bold it")`; save to `out`. Read the head block `block_text`. Assertions (each `assertIn`): `"target: [data-hyp-agent"`; `'context: p .lead | "'`; `"instruction: rename heading"`; `"reply: also bold it"`; `"author:"`; `"date:"`; `"remove the data-hyp-agent"` (the self-cleanup directive substring).

**E-R14-6 — Stamping idempotence across multiple saves:** open fixture; add agent comment on `#p-lead` (id0); save to `out2`. Reopen `out2` (`H.open_via_dialog_ui` + wait 500); save to `out3`. Assertions: `out3` has exactly ONE `data-hyp-agent` occurrence (`text3.count("data-hyp-agent") == 1`), its value is exactly `id0`, and exactly ONE block entry.

**E-R14-7 — Node-count guard safe with stamps:** open fixture; add 3 agent comments on 3 DISTINCT elements (`#p-lead`, `#p-arch`, `#li-2`); save to `out`. Assertions: save SUCCEEDS — `#shell-status` contains `"Saved"`; `os.path.exists(out)` and size > 0; the saved file has 3 `data-hyp-agent`-bearing elements.

**E-R14-8 — Stamp a NON-FIRST element with selection + markers live (F3 desync guard):** open fixture; agent-tag `#p-arch` (`_add_comment("fix arch", agent=True)`, id0); then agent-tag `#li-2` (`_add_comment`, id1) so a marker for id0 is already appended to `document.body`. Re-select `#li-2` (`self._select("li-2")`) so the `hyp-interaction-wrapper` + the id0/id1 markers are all live body children. Confirm pre-save: `H.doc_eval(self.page, "return doc.querySelectorAll('.hyp-comment-marker, #hyp-interaction-wrapper').length;") >= 2`. Save to `out`; reopen. Assertions: in `out` text, the `data-hyp-agent` whose value contains id0 sits on `<p id="p-arch">` (text starts "Componentes desacoplados"), and id1's stamp sits on `<li id="li-2">` (text "concilia"); reopen-resolve `H.doc_eval(self.page, "const m0=doc.querySelectorAll('[data-hyp-agent~=\\"<id0>\\"]'); const m1=doc.querySelectorAll('[data-hyp-agent~=\\"<id1>\\"]'); return {n0:m0.length, t0:m0[0]?m0[0].id:null, n1:m1.length, t1:m1[0]?m1[0].id:null};")` → `n0==1 and t0=="p-arch"` AND `n1==1 and t1=="li-2"`; live DOM clean after save (`[data-hyp-agent]` count == 0).

End with `if __name__ == "__main__": unittest.main()`.

## STEP 2 — `tests/e2e/test_r14_legibility.py` (G-R14-LEGIBILITY-1/-2)

**G-R14-LEGIBILITY-1 — Every target resolvable via the head block alone:**
- Setup: open `agent-comments.html`; add 2+ agent-tagged comments on DISTINCT elements (`#p-lead` "fix lead", `#li-2` "renumber item"); save to `out`.
- Extract phase (the ONLY input the "fresh agent" gets): parse `out` with a stdlib regex to pull the head comment block text; from it extract every `[agent:<id>]` entry's `target:` selector and `instruction:` line. The test MUST NOT read the island, the `data-hyp-*` mapping, or any structural path — ONLY the block's `target:` strings.
- Resolve phase (fresh, context-free): launch a NEW Playwright page; `page.goto` the saved file served as a PLAIN document (via the `/doc/` route — `self.base + "/doc/?path=" + urllib.parse.quote(out)` if that route exists, ELSE `file://` + the abspath); for EACH extracted `target:` selector run `page.evaluate("(sel)=>{const m=document.querySelectorAll(sel); return m.length===1 ? m[0].outerHTML.slice(0,120) : {n:m.length};}", selector)`.
- Pass criteria (ALL must hold): (1) the count of `[agent:` markers == 2; (2) EVERY `target:` selector resolves to EXACTLY ONE element (`m.length === 1`); (3) each resolved element's text/native-id matches the comment's intended anchor (the `#p-lead` selector resolves the lead paragraph; the `#li-2` selector resolves the "concilia" list item — assert the resolved `outerHTML` contains the expected native id or text excerpt); (4) **Position-independence:** BEFORE resolving, mutate the fresh document `document.querySelector('#sec-ops').insertAdjacentHTML('afterbegin','<p>injected</p>')` (shifts nth-child indices), then re-run all `target:` selectors and assert they STILL each resolve to exactly one correct element.

**G-R14-LEGIBILITY-2 — Self-cleanup directive present and actionable:**
- Assertion: the extracted block preamble contains the self-cleanup directive (the substring `"remove the data-hyp-agent"`); a follow-up `evaluate` confirms that removing a given id's `data-hyp-agent` token (`element.setAttribute('data-hyp-agent', value.replace(id,'').trim())`, or attribute removal when it was the only id) leaves the OTHER targets still resolvable (cleanup is per-id, non-destructive to siblings).

End with `if __name__ == "__main__": unittest.main()`.

---

## RUN — against current master and record red-first evidence
Run `python -m pytest tests/e2e/test_r14_agent_stamping.py tests/e2e/test_r14_legibility.py -v`.

### Expected RED on master (no `data-hyp-agent` stamping; stale path block)
- **E-R14-1/-2/-5/-8** FAIL — `stripClone` removes every `data-hyp-*`, so NO `data-hyp-agent` appears in any save (`out.count("data-hyp-agent") == 0`); the block emits the stale `anchor: ${path} | id="…" | "…"` line, so `target: [data-hyp-agent~=…]` is ABSENT and `querySelector('[data-hyp-agent~="…"]')` returns 0. E-R14-5's new-format `assertIn` substrings (`target:`, `context: p .lead`, self-cleanup directive) are absent.
- **E-R14-6/-7** FAIL — no stamp count to satisfy (`== 1` fails with 0; the 3-stamp count fails with 0).
- **G-R14-LEGIBILITY-1/-2** FAIL — no `target:` selectors to extract / they resolve nothing (criterion 2 `m.length === 0`); the self-cleanup directive is absent.
- **E-R14-3/-4** may PASS VACUOUSLY (no stamp ever applied → "no stamp for resolved id" and "live DOM clean" hold trivially). Record as PASS (they become load-bearing post-fix).

### Red-first GATE (binding)
If a `data-hyp-agent` ALREADY appears in a save on master (E-R14-1's count > 0) → the feature partially exists → STOP + write `docs/verification/v4/v4-t9-BUG.md`. The correct RED outcome is: E-R14-1/-2/-5/-6/-7/-8 + both legibility cases FAIL; E-R14-3/-4 pass vacuously.

Write `docs/verification/v4/v4-t9-red.txt` with the full `pytest -v` output and a one-line note per case (e.g. "E-R14-1: out.count('data-hyp-agent') == 0 → FAIL as expected").

## Acceptance (verify yourself)
1. `agent-comments.html` is NOT recreated (reused from V4-T7); both test files exist with all cases + inlined helpers; REAL input only; D14 guard on selection.
2. Run completed vs master; E-R14-1/-2/-5/-6/-7/-8 + legibility cases RED; E-R14-3/-4 pass vacuously.
3. `docs/verification/v4/v4-t9-red.txt` written.
4. `git diff --stat` shows changes ONLY in the allowlist paths.

## Return format (exact)
```
DONE V4-T9
files-changed: <list>
red-result: E-R14-1..8 = <list>; LEGIBILITY-1/-2 = <…>
red-first-gate: <SATISFIED | BUG written>
concerns: <none | …>
```

## Do NOT
- Do NOT apply any product fix (no product file is in this allowlist — that is V4-T10).
- Do NOT recreate or edit `agent-comments.html` / conftest_helpers.py.
- Do NOT weaken/xfail/skip any assertion.
- Do NOT commit (V4-T10 commits these test files with the fix).
- Do NOT create any file at the workspace root.
