# V2-T21 — Docs-sync: build-log, decision-log (D6 supersession), module-map, README

You are Kimi, a code executor. Do EXACTLY what this file says. Read nothing else. Output ONLY the files in the allowlist. Depends on V2-T20 (verification green).

## Goal
Sync the docs to the v2 changes: append a build-log entry, APPEND a decision-log row recording the D6 supersession of v1-D2's letter (append-only — never edit existing rows), update the module-map for new/changed/removed modules, and update the README "How to use" + repository-layout to reflect dialogs, modeless interaction, border color, and agent comments.

---

## 1) `docs/decision-log.md` — APPEND the D6 supersession row (CRITICAL, append-only)

The decision-log's "Locked Product Decisions" table currently ends at row D6. Its exact current tail is:
```
| D6 | **Recolor = BOTH.** (a) A palette editor mutates the document's CSS theme variables (recolors every element bound to that token) AND (b) a per-element color override. Both paths MUST also handle colors set via inline `style=` that sit OUTSIDE the `:root` variable system. | The deck fixture binds ~80% of color to `:root` variables; the report fixture carries layout-and-color values in inline `style=`. Covering only variables would miss real colors (see `fixture-profiles.md`). |

---

## Move / Resize Reconciliation (locked)
```
**APPEND a new row to the "Locked Product Decisions" table, immediately AFTER the D6 row and BEFORE the `---` that precedes "## Move / Resize Reconciliation".** Use the next decision id `D7`:
```
| D7 | **Move writes the CSS `translate` property, not `transform: translate()`.** Session decision D6 (improvements-2026-06) SUPERSEDES THE LETTER of D2 (which mandated `transform: translate()`) while preserving D2's intent: moves stay non-destructive, reversible, and undoable. The CSS individual `translate` property composes independently with any document-owned `transform: rotate()/scale()`, eliminating the silent transform-loss risk (recon Risk 3). Resize, out-of-flow flagging, serialization preservation, and undo are otherwise unchanged. | `translate` is an independent inline property; the serializer preserves it automatically (it strips only `data-hyp-*`/`hyp-` classes, never `style`); D2's non-destructive/reversible intent is fully retained. Evidence: `docs/improvements-2026-06/research-03-reorder-drag.md` §5, `docs/improvements-2026-06/spec.md` S1. |
```
> DO NOT edit the existing D1–D6 rows. DO NOT renumber. The new row is D7 in the product-decisions table (distinct from session register D7 in changelog.md — that is a separate document). If you prefer to avoid id confusion, you MAY label it `D2a` instead of `D7`; either is acceptable, but it MUST be a NEW appended row and MUST state it supersedes the LETTER of D2 while preserving its intent.

## 2) `docs/build-log.md` — APPEND a v2 entry

The build-log currently ends with a "## Run command (for reference)" section. APPEND a new section BEFORE or AFTER it (after is fine) with this structure (one entry summarizing v2):
```
---

## v2 improvement cycle (improvements-2026-06)

- **Scope:** F1 native OS open/save dialogs · F2 modeless interaction + alignment guides · F3 move/reorder/re-parent on drop (FLIP) · F4 border color · F5 agent-tagged comments + derived head instruction block · G1/G2 verified (already fixed in v1 CP2) + hardened.
- **Key changes:**
  - Server: new `/api/dialog-open`, `/api/dialog-save-as`, `/api/save`; PowerShell `-STA` dialog launcher with an injectable seam (`api.set_dialog_launcher`); open-path tracking. Toolbar lost both path inputs; gained a Save button.
  - Runtime: NEW `interaction.js` (one combined Moveable: drag+resize+Slides snapping/guides; on-drop hit-test → reorder/re-parent/keep-translate with FLIP); NEW pure `reorder.js`; `resize.js` + `move.js` REMOVED (folded in). Moves now write the CSS `translate` property (D6/D2-letter supersession).
  - Comments: `agentInstruction` flag, `setAgentInstruction`, derived `buildAgentBlock` (head-first-child HTML comment, `-->`→`--&gt;`), `reanchorAfterMove` after DOM moves.
  - Serializer: agent block inserted as first child of `<head>`; node-count guard extended with an `agentBlockCount` term + a pre-existing-block sweep (prevents duplication on re-save).
  - Color: per-element Border row with auto-`1px solid` on borderless elements (U6) and a `mixed` state for differing per-side colors.
  - Shell: NEW `comment-composer.js` anchored popover (replaces `prompt()` for add + reply); per-thread "For agents" toggle.
- **Tests:** stdlib `unittest` server suites + Playwright (Python, headless Chromium) behavioral suites per feature; full suite green per `docs/verification/v2/result.md`. Dev-only deps: `playwright` (app stays dependency-free).
- **Note:** G1 (panel survival) and G2 (serializer island count) were already fixed during v1 CP2 re-verify; v2 locked them with regression tests and extended the serializer guard for the new agent block. The v1 recon/scorecard rows for G1/G2 were stale.
```

## 3) `docs/spec/03-module-map.md` — update the module tree + add contracts

In the file/folder tree (section 1) and the runtime modules section (section 4):
- ADD `runtime/js/interaction.js` — "single combined Moveable (drag+resize+snap+Slides guides); on-drop hit-test → reorder/re-parent/keep-translate (FLIP); commits one history command. begin via `mount(hypId)`; `unmount/suspend/resume/remount`."
- ADD `runtime/js/reorder.js` — "pure drop-classification helpers: `classifyDrop`, `isContainer`, `dominantAxis`, `midpointBefore`, `axisFromDisplay`."
- MARK `runtime/js/resize.js` and `runtime/js/move.js` as REMOVED in v2 (folded into `interaction.js`). (Either delete their rows with a note, or annotate "(removed in v2 → interaction.js)".)
- ADD `app/js/shell/comment-composer.js` — "anchored comment composer popover (textarea + For-agents toggle + Save/Cancel); replaces prompt()."
- NOTE on `runtime/js/comments.js`: "+ `agentInstruction` flag, `setAgentInstruction`, `buildAgentBlock`, `reanchorAfterMove`."
- NOTE on `runtime/js/serializer.js`: "+ agent-block head insertion + revised node-count guard (agentBlockCount + pre-existing-block sweep)."
- NOTE on `runtime/js/color.js`: "+ border-color routing (auto-1px, U6), `readElementBorder`/`readElementColors`."
- NOTE on `runtime/js/commands.js`: "+ `reorder`, `colorBorder`; `move` now writes the CSS `translate` property."
- NOTE on `server/api.py`: "+ dialog handlers, open-path tracking, injectable dialog launcher."
- NOTE on `server/server.py`: "+ `/api/dialog-open`, `/api/dialog-save-as`, `/api/save` routes (+ test-only `/api/_test/set-dialog`)."
> Keep edits additive and consistent with the file's existing table/prose style. Do not rewrite unrelated sections.

## 4) `README.md` — update usage + layout (README EXISTS — confirmed)

In "## How to use":
- Step 1 (Open): replace the "type the absolute path" instruction with: "Click **Open…** to pick a file via a native OS file dialog."
- Add a **Save** step: "Click **Save** to silently overwrite the currently-open file (or **Save As…** to choose a new path via a native save dialog)."
- Update the Resize/Move steps to the modeless model: "Select an element and its drag + resize handles appear immediately (no tool switch). Drag the body to move/reorder/re-parent; drag a handle to resize. Alignment guides appear like Google Slides during drag and resize."
- Add to the Recolor step: "including a **Border** color row (applies to all sides; adds a 1px border if the element has none)."
- Update the Comment step: "Adding or replying to a comment opens an anchored composer popover (not a prompt). Tag a thread **For agents** to emit a machine-readable instruction block at the top of the saved file's `<head>` for AI coding agents."

In "## Repository layout" table:
- ADD rows for `runtime/js/interaction.js`, `runtime/js/reorder.js`, `app/js/shell/comment-composer.js`.
- Update/REMOVE the `runtime/js/resize.js · move.js` row (now `interaction.js`).

In "## Known limitations" (optional but recommended): add "Reorder of grid items with explicit `grid-column/row` placement changes DOM order but may not change visual order (CSS grid honors explicit placement)."

## File allowlist
- ✎ MODIFY `docs/decision-log.md`
- ✎ MODIFY `docs/build-log.md`
- ✎ MODIFY `docs/spec/03-module-map.md`
- ✎ MODIFY `README.md`
- Nothing else. (Do NOT touch `docs/spec/04-implementation-plan.md` — it is the read-only v1 plan.)

## Acceptance (verify yourself)
1. `docs/decision-log.md` has a NEW appended row recording the D6/`translate` supersession of v1-D2's letter; `git diff docs/decision-log.md` shows ONLY an addition (existing rows untouched).
2. `docs/build-log.md` has the new v2 section.
3. `docs/spec/03-module-map.md` lists `interaction.js`, `reorder.js`, `comment-composer.js` and notes the removals/changes.
4. `README.md` "How to use" reflects dialogs, modeless interaction, Border row, and agent comments; the layout table lists the new modules.
5. `git diff --stat` shows ONLY the four allowlisted files.

## Do NOT
- Do NOT edit existing decision-log rows (append-only).
- Do NOT touch `docs/spec/04-implementation-plan.md`.
- Do NOT rewrite unrelated README/module-map sections — keep edits surgical and additive.
