---
task_id: T17
status: pending
phase: execute
complexity_score: 8
human_review: optional
---

# Task T17: Save-As Wiring End-to-End

## Goal

UPDATE the shell to wire Save As end-to-end: serialize the document, POST to `/api/save-as`, write to disk, and reflect dirty-state + error handling.

---

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| ../../../spec/04-implementation-plan.md | T17 row: contract, acceptance (V-SAVE-1/2/3). |
| ../../../spec/01-architecture.md | §4 Save-As data flow; §7 error handling (500 on write fail, no silent damaged file). |
| ../../../spec/05-verification-plan.md | §4 Save-As validity gate. |
| ../../../spec/03-module-map.md | file-controls (save path), api-client, serializer integration. |

---

## Execution Flow

### Phase: Understand
1. MUST read every Context File.
2. Mark T17 in progress in BOTH locations.
3. Confirm: serialize is requested from the runtime via the bridge (`serialize` command); the server writes to the chosen path.

### Phase: Execute
1. UPDATE `app/js/shell/file-controls.js` (save path): on Save As, `bridge.command('serialize')` → `api-client.saveAs(path, html)`; reflect `dirty-changed`; on 500, keep dirty + show error + offer retry/alternate path per `01` §7.
2. UPDATE `app/js/main.js`: wire dirty-state indicator + Save As control into the shell lifecycle.

**Discovery Handling:** simple → resolve + shape.md; complex → add task, notify.

### Phase: Validate
1. V-SAVE-1 (DECK) + V-SAVE-2 (REPORT) + V-SAVE-3 (both): edit, Save As, file written; chrome-free; document JS retained; re-opens.
2. A write failure keeps dirty state and surfaces an error (no silent success).

### Phase: Close
1. Append decision/discovery to shape.md (standalone) or defer (orchestration).
2. Mark T17 complete in BOTH locations after acceptance passes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Save-As flow | `app/js/shell/file-controls.js` (UPDATE) | ES module (save path) |
| Shell wiring | `app/js/main.js` (UPDATE) | ES module |

---

## Revolving Plan Rules

- Append discoveries to shape.md; complex → add task; `PLAN MODIFIED:` in output.
