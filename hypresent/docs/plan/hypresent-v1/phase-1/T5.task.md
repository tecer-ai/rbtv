---
task_id: T5
status: pending
phase: execute
complexity_score: 11
human_review: required
---

# Task T5: Element Registry — Detection, data-hyp-id Tagging, Role/Regions

## Goal

CREATE the element registry: detect editable elements from the live DOM (no class presupposition), assign an ADDITIVE `data-hyp-id`, record original `contenteditable`/`id` state, resolve id↔node, report layout role + regions, and strip ids on demand.

---

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| ../../../spec/04-implementation-plan.md | T5 row: contract, acceptance (V-BOOT-3). |
| ../../../spec/02-html-convention.md | §1 baseline detection, §4 region heuristic, H1/H3/H4/H7 hints + fallbacks. |
| ../../../spec/01-architecture.md | §6 coexistence/namespacing (why `data-hyp-id` is additive + stripped on save; anchor key lives in the island per §6.1, not on the element). |
| ../../../spec/03-module-map.md | §4 registry public surface. |
| ../shape.md | A12 namespacing; constraints (native ids + native `data-*` are read-only). |

---

## Execution Flow

### Phase: Understand
1. MUST read every Context File.
2. Mark T5 in progress in BOTH locations.
3. Confirm: detection MUST work on BOTH fixtures with zero hints; native `id`/`data-*`/classes are read-only.

### Phase: Execute
1. CREATE `runtime/js/element-registry.js` with: `tag()` (assign `data-hyp-id` additively; never replace native `id`), `byId(hypId)`, `idOf(el)`, `roleOf(el)` (`flex-child`/`grid-child`/`absolute`/`block` from computed style), `regions()` (per `02` §4 heuristic, honoring H3 when present), `stripIds(clone)`.
2. Honor hints when present (H1 `data-hyp-hook`, H3 `data-hyp-region`, H4 `data-hyp-text`, H7 decorative) and fall back per `02` §3 when absent.
3. Exclude SVG `<path>`/geometry from text-editability; record original `contenteditable` per element for serializer restore.

**Discovery Handling:** simple → resolve + shape.md; complex → add task, notify.

### Phase: Validate
1. V-BOOT-3 on both fixtures: every editable element has a `data-hyp-id`; REPORT native ids + native `data-*` unchanged.
2. `stripIds` removes all `data-hyp-id` from a clone.
3. `roleOf` returns `absolute` for the REPORT fixture's semantic absolutely-positioned nodes and grid/flex roles for DECK cards.

### Phase: Close
1. Append decision/discovery to shape.md (standalone) or defer (orchestration).
2. `human_review: required` → emit the Human Review Presentation block (format + flag criteria per `3-resources/tools/rbtv/workflows/planning/templates/plan-task-microstep-template.md` § Human Review Presentation and `.../data/plan-creation-rules.md` § Human Review Flag Criteria) — this module is the namespacing/coexistence linchpin.
3. Mark T5 complete in BOTH locations after acceptance passes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Element registry | `runtime/js/element-registry.js` | ES module per §4 contract |

---

## Revolving Plan Rules

- Append discoveries to shape.md; complex → add task; `PLAN MODIFIED:` in output.
