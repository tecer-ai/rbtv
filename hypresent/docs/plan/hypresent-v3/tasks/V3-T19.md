# V3-T19 — docs-sync: README + module-map + decision-log + build-log

You are Kimi, a code executor. Do EXACTLY what this file says. Read nothing else. Output ONLY the files in the allowlist. Depends on V3-T18 (full suite green).

## D14 addendum (binding)
On a blocking product bug, STOP and write a bug-report file under `docs/verification/v3/` — never patch outside your allowlist. NEVER create files at the workspace root — scratch goes under a tempdir.

## Edit-anchoring rule (binding)
Every edit is anchored on the EXACT quoted code string, NOT a line number. LOCATE each block by matching the quoted code verbatim. If not found, STOP and report.

## Goal
Sync the docs to the v3 changes: README usage (remove outline navigate; remove the fixed font-size known-limitation; add delete + alignment + tooltip/copy-HEX; the #color-btn was never in usage so nothing to remove there); `docs/spec/03-module-map.md` (the changed modules); `docs/decision-log.md` APPEND-ONLY rows for the four v3 product decisions; `docs/build-log.md` append a v3 cycle entry. Decision-log existing rows MUST stay byte-identical (append only).

---

## File 1 — `README.md`

### Anchor 1A — usage step 9 (the outline navigate; CURRENT, verbatim):
```markdown
9. **Save / Save As.** Click **Save** to silently overwrite the currently-open file (or **Save As…** to choose a new path via a native save dialog). The output is chrome-free (no `hyp-` classes/ids, no `data-hyp-*` attributes, no injected scripts/styles) except for the one inert comment island, and the document's own scripts still run.
9. **Navigate.** The outline panel lists detected regions (e.g. the deck's slides, the report's sections); click an item to select it in the document.
```
> NOTE: the source has two lines numbered "9." (a save step and a navigate step). REPLACE the whole Anchor-1A block with (drop the Navigate line, renumber save to 9, and ADD a step for delete + alignment):
```markdown
9. **Save / Save As.** Click **Save** to silently overwrite the currently-open file (or **Save As…** to choose a new path via a native save dialog). The output is chrome-free (no `hyp-` classes/ids, no `data-hyp-*` attributes, no injected scripts/styles) except for the one inert comment island, and the document's own scripts still run. Native dialogs appear **on top of** the browser window.
```

### Anchor 1B — usage step 4 (Resize/Move; add delete + alignment mentions). CURRENT, verbatim:
```markdown
4. **Resize / Move.** Select an element and its drag + resize handles appear immediately (no tool switch). Drag the body to move/reorder/re-parent; drag a handle to resize. Alignment guides appear like Google Slides during drag and resize. Resize is **flow-aware**: it edits `width` / `height` / `flex-basis` / grid tracks in place and never force-converts an element to absolute positioning, so the layout stays responsive. Moves write **only** the CSS `translate` property (non-destructive and fully reversible); a badge appears if the element visually leaves its flow box.
```
REPLACE with (add: delete via toolbar; alignment toolbar):
```markdown
4. **Resize / Move / Delete / Align.** Select an element and its drag + resize handles appear immediately (no tool switch). Drag the body to move/reorder/re-parent; drag a handle to resize. Alignment guides appear like Google Slides during drag and resize. Resize is **flow-aware**: it edits `width` / `height` / `flex-basis` / grid tracks in place and never force-converts an element to absolute positioning, so the layout stays responsive. Moves write **only** the CSS `translate` property (non-destructive and fully reversible); a badge appears if the element visually leaves its flow box. Click the **🗑** toolbar button to **delete** the selected element (full undo; deleting the last remaining region is blocked). Use the **alignment toolbar group** to align text inside the selected element's box — horizontal (left/center/right) is always available; vertical (top/middle/bottom) is enabled only for flex/grid containers, table cells, or fixed-height blocks (it never silently converts a plain block's layout).
```

### Anchor 1C — usage step 5 (Recolor; add tooltip + copy-HEX). CURRENT, verbatim:
```markdown
5. **Recolor.** Open the color popover to change a theme **token** (recolors every element bound to that `:root` variable in one operation) or apply a **per-element** color override, including a **Border** color row (applies to all sides; adds a 1px border if the element has none). Colors set via inline `style=` are handled too. Color picking uses Coloris.
```
REPLACE with (add the tooltip + copy-HEX):
```markdown
5. **Recolor.** The color popover changes a theme **token** (recolors every element bound to that `:root` variable in one operation — a ⓘ tooltip on the Palette Tokens header explains this) or applies a **per-element** color override, including a **Border** color row (applies to all sides; adds a 1px border if the element has none). Each palette token has a discreet **copy-HEX** button that copies the normalized `#rrggbb` to the clipboard. Colors set via inline `style=` are handled too. Color picking uses Coloris.
```

### Anchor 1D — the Known Limitations font-size note (now FIXED; CURRENT, verbatim):
```markdown
- **Font-size caret artifact.** Repeatedly applying A+/A- without re-selecting the word between presses can produce sibling font-size spans rather than editing one in place (a synthetic-caret artifact, not nesting; cosmetic). Re-selecting the word between presses updates a single span. See `docs/verification/cp2/result.md`.
```
REPLACE (delete this bullet entirely — the limitation is fixed in v3). Remove the whole Anchor-1D line.

---

## File 2 — `docs/spec/03-module-map.md`

### Anchor 2A — the module-tree comment for `commands.js` (CURRENT, verbatim):
```
│     ├─ commands.js          # command factory: builds {do, undo} for each op type + `reorder`, `colorBorder`; `move` now writes the CSS `translate` property
```
REPLACE with (add deleteElement + align):
```
│     ├─ commands.js          # command factory: builds {do, undo} for each op type + `reorder`, `colorBorder`, `deleteElement`, `align`; `move` writes the CSS `translate` property
```

### Anchor 2B — the `interaction.js` tree comment (CURRENT, verbatim):
```
│     ├─ interaction.js       # single combined Moveable (drag+resize+snap+Slides guides); on-drop hit-test → reorder/re-parent/keep-translate (FLIP); commits one history command. begin via `mount(hypId)`; `unmount/suspend/resume/remount`
```
REPLACE with (note the R2 pointer-events fix):
```
│     ├─ interaction.js       # single combined Moveable (drag+resize+snap+Slides guides); on-drop hit-test → reorder/re-parent/keep-translate (FLIP); injects a hyp-scoped style re-enabling pointer-events on Moveable controls (R2); begin via `mount(hypId)`; `unmount/suspend/resume/remount`
```

### Anchor 2C — the `text-format.js` tree comment (CURRENT, verbatim):
```
│     ├─ text-format.js       # bold/italic/font-size via execCommand + Selection
```
REPLACE with (note R8 + R7):
```
│     ├─ text-format.js       # bold/italic/font-size via execCommand + Selection (font-size: range snapshot/restore + tracked-span, R8); text alignment caps + apply (computeAlignCaps/applyAlign, R7)
```

### Anchor 2D — the `element-registry.js` tree comment (CURRENT, verbatim):
```
│     ├─ element-registry.js  # detects editable elements, assigns/strips data-hyp-id
```
REPLACE with (note regions() removed):
```
│     ├─ element-registry.js  # detects editable elements, assigns/strips data-hyp-id (regions() removed with the outline panel, R9)
```

### Anchor 2E — the `element-registry.js` §4 public-contract line (CURRENT, verbatim):
```
- **Outputs:** `tag()`, `byId(hypId) → Element|null`, `idOf(el) → hypId`, `roleOf(el) → 'flex-child'|'grid-child'|'absolute'|'block'`, `regions() → [{hypId,label}]`, `stripIds(clone)`. NEVER mutates non-`hyp` attributes.
```
REPLACE with (drop `regions()`):
```
- **Outputs:** `tag()`, `byId(hypId) → Element|null`, `idOf(el) → hypId`, `roleOf(el) → 'flex-child'|'grid-child'|'absolute'|'block'`, `stripIds(clone)`. NEVER mutates non-`hyp` attributes. (`regions()` was removed in v3 with the outline panel — R9.)
```

### Anchor 2F — the `selection.js` tree comment (CURRENT, verbatim — RV15, per the spec cross-feature module-map delta):
```
│     ├─ selection.js         # current selection state + visual hyp- selection ring
```
REPLACE with (note the `alignCaps` field added to the `selection-changed` payload, R7):
```
│     ├─ selection.js         # current selection state + visual hyp- selection ring; + `alignCaps` field on the `selection-changed` payload (R7 capability bridge)
```

### Anchor 2G — the `color.js` tree comment (CURRENT, verbatim — RV15, per the spec cross-feature module-map delta):
```
│     ├─ color.js             # palette token mutation + per-element + inline-style color (D6) + border-color routing (auto-1px, U6), `readElementBorder`/`readElementColors`
```
REPLACE with (note `rgbToHex` exported + `normalizeHex` + `token.hex`, R6):
```
│     ├─ color.js             # palette token mutation + per-element + inline-style color (D6) + border-color routing (auto-1px, U6), `readElementBorder`/`readElementColors`; + `rgbToHex` EXPORTED, `normalizeHex`, `token.hex` in `readPalette` (R6 copy-HEX)
```

---

## File 3 — `docs/decision-log.md` (APPEND-ONLY)

### Step 3.0 — READ the live decision-log FIRST and confirm the next-available D-number (mandatory, RV12)
Before appending anything, READ `docs/decision-log.md` and find the HIGHEST existing `Dn` row number in the "Locked Product Decisions" table. The four new rows MUST use the next four consecutive numbers after that highest existing number — do NOT hardcode `D8` if the live file already has a `D8` row. Run:
```
python -c "import pathlib,re; s=pathlib.Path('docs/decision-log.md').read_text(); nums=[int(m) for m in re.findall(r'(?m)^\|\s*D(\d+)\s*\|', s)]; print('existing D-numbers:', sorted(set(nums))); print('highest:', max(nums)); print('next four:', [max(nums)+i for i in (1,2,3,4)])"
```
On the CURRENT source the highest is **D7**, so the next four are **D8, D9, D10, D11** — and the row bodies below are written for exactly that. If the read shows the highest is NOT D7 (the log gained rows since this task was written), RENUMBER the four appended rows to the printed "next four" values, keep their bodies/order identical, and note the renumber in your evidence file. NEVER create a duplicate D-number; NEVER edit an existing row. (Anchor 3A below is the verbatim D7 row that MUST be the last existing row when the highest is D7; if it is not the last row, the log changed — re-derive the numbers from the print above.)

### Anchor 3A — the END of the "Locked Product Decisions" table (the D7 row is last; CURRENT, verbatim — the D7 row text):
```
| D7 | **Move writes the CSS `translate` property, not `transform: translate()`.** Session decision D6 (improvements-2026-06) SUPERSEDES THE LETTER of D2 (which mandated `transform: translate()`) while preserving D2's intent: moves stay non-destructive, reversible, and undoable. The CSS individual `translate` property composes independently with any document-owned `transform: rotate()/scale()`, eliminating the silent transform-loss risk (recon Risk 3). Resize, out-of-flow flagging, serialization preservation, and undo are otherwise unchanged. | `translate` is an independent inline property; the serializer preserves it automatically (it strips only `data-hyp-*`/`hyp-` classes, never `style`); D2's non-destructive/reversible intent is fully retained. Evidence: `docs/improvements-2026-06/research-03-reorder-drag.md` §5, `docs/improvements-2026-06/spec.md` S1. |
```
### Change 3A — APPEND four new rows AFTER the D7 row (do NOT edit the D7 row or any existing row):
```
| D8 | **Outline panel removed.** The region-detection outline panel is removed end-to-end (markup, shell wiring, CSS) along with `element-registry.regions()` and the `ready` event's `sections` payload. The detector only worked for a single-dense-content-root "report" shape and showed "no regions detected" for the dominant slide-deck shape; the owner judged it broken/useless. Grep confirmed `regions()`/`sections` had no consumer other than the outline. | Session-2 R9 / U12. A non-functional panel is worse than none; nothing else consumed `regions()`. Evidence: `docs/improvements-2026-06-r2/diagnosis.md` R9, `docs/improvements-2026-06-r2/spec.md` V3-S23/S24. |
| D9 | **`#color-btn` toolbar button removed.** The 🎨 toolbar button is removed (button + its empty toolbar group + handler). It only opened the first palette token's Coloris picker — redundant with the always-visible Palette Tokens list, and targeted an arbitrary token regardless of selection. | Session-2 R4 / U11. No capability lost; the palette/element color UI fully covers it. Evidence: `docs/improvements-2026-06-r2/diagnosis.md` R4. |
| D10 | **Text alignment is display-branched and never silently converts layout.** A new `align` command writes `text-align` (horizontal, always available) and, for vertical, `justify-content`/`align-items` (flex by direction), `justify-items`/`align-items` (grid), or `vertical-align` (table-cell); a plain auto-height block has vertical DISABLED with a tooltip hint (it is only converted to a flex column when the user already set a fixed height). The toolbar's 6 buttons reflect the selected element's capabilities via an `alignCaps` field on the `selection-changed` payload. | Session-2 R7 / U13/U16/D21. Honest affordances; preserves the project's flow-preservation ethos (no silent flex conversion of responsive blocks). Evidence: `docs/improvements-2026-06-r2/spec.md` R7 decision table. |
| D11 | **Element deletion is toolbar-button-only with full undo; threads on a deleted element go "Unanchored".** A 🗑 toolbar button deletes the selected element (no Delete/Backspace key path). Undo restores the exact subtree at its original position (captured node + parent + next-sibling). Anchored comment threads on the deleted element are kept and shown "Unanchored" (re-anchored on undo), never lost. Deleting the only remaining top-level region is blocked with a status message; deleting while text-editing is blocked. | Session-2 R3 / U14/U15/D20. Mirrors move/reorder thread semantics; toolbar-only avoids collisions with text-edit character deletion. Evidence: `docs/improvements-2026-06-r2/spec.md` R3. |
```

---

## File 4 — `docs/build-log.md` (APPEND a v3 entry)

### Anchor 4A — the END of the file (the v2 cycle entry's last bullet; CURRENT, verbatim):
```
- **Note:** G1 (panel survival) and G2 (serializer island count) were already fixed during v1 CP2 re-verify; v2 locked them with regression tests and extended the serializer guard for the new agent block. The v1 recon/scorecard rows for G1/G2 were stale.
```
### Change 4A — APPEND a new section AFTER that line:
```

---

## v3 improvement cycle (improvements-2026-06-r2 — Session 2)

- **Scope:** owner hands-on feedback on the v2 build — R1 dialogs appear on top · R2 resize/move actually work · R3 element delete · R4 remove redundant #color-btn · R5 palette-token tooltip · R6 per-token copy-HEX · R7 text alignment (H+V, display-branched) · R8 repeatable font-size · R9 remove the broken outline panel.
- **Key changes:**
  - Server: `_OPEN_PS`/`_SAVE_PS` create a hidden `TopMost` owner Form passed to `ShowDialog($owner)`; the unreliable `ShowHelp` hack removed (R1, dialogs now render above Chrome).
  - Runtime `interaction.js`: injects a `hyp-`scoped stylesheet re-enabling `pointer-events:auto` on Moveable's control elements under the wrapper — the handles were unhittable (light-DOM vs `:host`-scoped vendor CSS), so resize/move were dead (R2). The wrapper stays `pointer-events:none`.
  - Runtime `text-format.js`: font-size snapshots the live Selection range on the toolbar `mousedown` (before the focus collapse) and restores it, plus a tracked-span fallback, so A+/A− bump ONE span repeatably (R8); added `computeAlignCaps`/`applyAlign` for display-branched alignment (R7).
  - Runtime `commands.js`: `deleteElement` (full-undo, mirrors reorder's place()) + `align` (capture-all undo). Runtime `runtime-main.js`: `delete-element`, `align`, `format-snapshot` registrations; dropped `regions`/`sections` from the `ready` emit (R9). `selection.js`: `alignCaps` on the `selection-changed` payload (R7). `color.js`: exported `rgbToHex` + `normalizeHex` + `token.hex` (R6). `element-registry.js`: removed `regions()` + its orphan helpers (R9).
  - Shell: `#delete-btn` 🗑 + 6 alignment buttons added; `#color-btn` + the outline panel removed; the Palette-Tokens ⓘ tooltip + per-token copy-HEX button added (R3/R4/R5/R6/R7/R9).
- **Tests:** real-input behavioral suites (geometry deltas, DOM mutation, span counts — never artifact presence); the F2 suite rewritten to use real pointer input and hard outcome assertions (no skip-as-green); a stdlib `ctypes` Win32 z-order test drives the REAL dialog and asserts foreground/topmost (R1); full suite green per `docs/verification/v3/result.md`. Dev-only deps unchanged (Playwright + stdlib ctypes; app stays dependency-free).
- **Commit cadence:** feature-serial (per-feature: implement + tests + gate + commit), branch `hypresent-v2` continued, local-only.
```

## File allowlist
- ✎ MODIFY `README.md`
- ✎ MODIFY `docs/spec/03-module-map.md`
- ✎ MODIFY `docs/decision-log.md`
- ✎ MODIFY `docs/build-log.md`
- Nothing else.

## Acceptance (verify yourself)
1. README usage no longer mentions the outline ("Navigate"/"outline panel lists detected regions" is gone); the "Font-size caret artifact" known-limitation bullet is gone; step 4 mentions delete + alignment; step 5 mentions the tooltip + copy-HEX; the dialogs-on-top note is present.
2. `03-module-map.md` reflects the changed `commands.js`, `interaction.js`, `text-format.js`, `element-registry.js` (regions removed), `selection.js` (+`alignCaps`, RV15), and `color.js` (+`rgbToHex` exported/`normalizeHex`/`token.hex`, RV15) — all six modules from the spec's cross-feature module-map delta.
3. `docs/decision-log.md` has FOUR new appended rows at the next-available consecutive D-numbers (Step 3.0 — on the current source that is D8 outline-removal, D9 color-btn-removal, D10 alignment, D11 delete; if the live highest was not D7, the printed "next four" instead). NO duplicate D-number exists (verify the printed `existing D-numbers` list had no overlap with the four you appended). EXISTING rows (D1–D7…, A1–A12, the reconciliation/vendored tables) are BYTE-IDENTICAL — verify the diff shows only additions:
   ```
   git diff -- docs/decision-log.md
   ```
   shows only added lines (no `-` lines on existing content).
4. `docs/build-log.md` has a new "v3 improvement cycle" section appended; the prior content is unchanged.
5. `git diff` shows changes ONLY in the four allowlisted files.
6. Write evidence to `docs/verification/v3/v3-t19-run.txt`.

## Do NOT
- Do NOT edit any EXISTING decision-log row (append only — D1–D7, A1–A12, the tables stay byte-identical).
- Do NOT touch `docs/spec/04-implementation-plan.md` or any v1/v2 plan folder.
- Do NOT create any file at the workspace root.
