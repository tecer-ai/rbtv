v3-t19b docs-sync completion evidence
======================================

1. README.md line ~55 (repository layout table)
-----------------------------------------------
BEFORE:
  | `app/js/main.js` | Shell bootstrap: bridge wiring, toolbar, comment panel, outline, Save As |

AFTER:
  | `app/js/main.js` | Shell bootstrap: bridge wiring, toolbar, comment panel, Save As |

2. docs/decision-log.md appended rows (verbatim)
------------------------------------------------
| D12 | **Comment composer gains a bottom-viewport flip/clamp.** A latent v1/v2 bug let low anchors push the composer (incl. the For-agents checkbox) below the fold; it now flips above the anchor on bottom overflow, else clamps. | Session-2 EXIT gate finding; evidence `docs/verification/v3/v3-t18-debug.md` blocker C. |
| D13 | **history.push round-trips the edited element through innerHTML, DETACHING live node references.** Any feature tracking a node across history-recorded operations MUST use a re-findable DOM marker (pattern: `data-hyp-fontspan` in `text-format.js`), never an object reference. | R8 root cause #2; evidence `docs/verification/v3/v3-t4-debug.md`. |
| D14 | **selection-dependent e2e tests MUST pin a unique id on the exact target element and assert the runtime-selected hypId equals it BEFORE any outcome assertion.** Composite components register their children, so coordinate clicks select the nearest registered descendant (this produced three separate red herrings in Session 2). Reference pattern: the probe-based `_select` in `test_r7_alignment.py`. | D30; evidence `v3-t8-debug.md`, `v3-t15-debug.md`, `v3-t18-debug.md`. |

3. git-diff file list
---------------------
 hypresent/README.md            | 10 ++++------
 hypresent/docs/decision-log.md |  7 +++++++
 2 files changed, 11 insertions(+), 6 deletions(-)

4. Append-only confirmation
---------------------------
This task modified ONLY the two allowlisted files (README.md, docs/decision-log.md).
For docs/decision-log.md, the v3-t19b edit is strictly append-only: three rows
(D12, D13, D14) were inserted after D11 with zero existing rows removed or altered.
No files were created at workspace root.
