# V4-T2 Post-Commit Review — R10 flex-child resize 1:1 (ADX-1/ADX-2)

**Commit:** `0381c4785374ebc6e3480c04c4fd31964b35b613` (rbtv master)
**Reviewer:** post-commit Opus-grade scrutiny, READ-ONLY on code.
**Scope of commit:** R10 ONLY — `interaction.js` flex-child branch (grow-neutralize + ADX-1 box-sizing conversion) + 9 R10 tests + 3 synthetic fixtures + `copy_synthetic`/`SYN_DIR` helper. R11/R12/R13/R14 are NOT in this commit (correct per AD7 per-fix sequencing; `agent-comments.html` intentionally absent).
**Method:** static review against `spec.md` (R10 + ADX-1/ADX-2), `test-plan.md` (R10 cases + coverage matrix), `diagnosis.md` (§risks, healthy paths, R10c), and the committed red/run evidence. No live browser used (one live-probe recommendation noted under F-2).

---

## Verdict

**CLEAN** — product code faithfully implements the spec-as-amended (verbatim flex branch + ADX-1 conversion, else/absolute branches byte-untouched), undo capture/restoration is correct including the absent-inline case, and the 9 tests encode the ADX-2 contracts with un-weakened anti-amplification/anti-dead-zone width-delta kill assertions. The findings below are all MINOR/NOTE and do not block the commit. Two test-coverage gaps (corner-on-flex-grow; deadzone magnitude/duplication) are documented for the next live pass, not defects in product code.

---

## A — Product code vs spec verbatim block + ADX-1 (PASS)

1. **(NOTE) Flex branch matches spec block as amended by ADX-1.** `interaction.js:111-135`. `mainDim`/`mainAxisIdx` derivation, `el.style.flexGrow="0"`, `flexShrink="0"`, `flexBasis = toContent(beforeRect[mainDim]+dist[mainAxisIdx], mainDim)+"px"`, and the `isFlexRow ? height : width` cross-axis write all transcribe the spec §R10 change-map block (line 208) with the ADX-1 `toContent` wrapper layered on. `beforeRect[mainDim]` uses the literal string key (`"width"`/`"height"`), `dist[mainAxisIdx]` uses the numeric index — exactly as the spec mandates.
2. **(PASS) ADX-1 conversion is correct.** `toContent` (`:116-127`) reads `getComputedStyle(el).boxSizing` (computed, not inline), subtracts the AXIS-CORRECT pair (width → paddingLeft+Right + borderLeft+Right; height → paddingTop+Bottom + borderTop+Bottom), floors at `Math.max(0, …)`, and is a NO-OP for `border-box`. Box-sizing is NEVER mutated. Matches ADX-1 verbatim.
3. **(PASS) Else / absolute branches byte-untouched.** Diff replaced ONLY the flex-child branch body + the two signatures (`applyVisualResize` gains `dist`, `onResize` passes `e.dist`). The `else` branch (`:136-138`) and the absolute `top`/`left` edge-compensation (`:139-144`, runs only for `role==='absolute'`) are identical to pre-commit. `roleOf` (`element-registry.js:139-145`) returns `grid-child`/`block` for non-flex parents, so grid/block elements take the `else` branch and never the flex branch — the healthy-path isolation is structural, not incidental.
4. **(PASS) dist plumbing is the only signature change.** `onResize` (`:225`) now forwards `e.dist`; no other call site of `applyVisualResize` exists.

## B — Undo correctness (PASS)

1. **(PASS) Snapshot captures all three longhands BEFORE the write.** `captureSizingState` flex-child branch (`:97-101`) adds `m["flex-grow"]` and `m["flex-shrink"]` alongside `flexBasis`/`width`/`height`. It is invoked in `onResizeStart` (`:220`, `beforeSizing = captureSizingState(...)`) which fires BEFORE any `onResize` write — so the snapshot is the true pre-gesture inline state. Absent-inline → `getPropertyValue` returns `""` (stored verbatim).
2. **(PASS) Undo restores absent-inline to ABSENT, not empty-string artifacts.** `commands.js applyStyleMap` (`:36-45`) runs `kebabCase(prop)` on EVERY key, so the mixed-case snapshot (`flexBasis` camel + `flex-grow`/`flex-shrink` kebab) all normalize: `flexBasis→flex-basis`, `flex-grow→flex-grow` (idempotent), `flex-shrink→flex-shrink`. For a captured `""` it calls `el.style.removeProperty(key)` — restoring the property to genuinely absent (no `="0"` or `=""` residue). This is the highest-value hazard (diagnosis Risk-2) and it is correctly handled; `commands.resize` needed no change exactly as S3-4/L1 predicted.
3. **(PASS) G-R10-UNDO asserts longhand restoration explicitly.** `test_r10_resize_flex.py:306-308`:
   ```python
   self.assertEqual(post["inlBasis"], pre["inlBasis"])
   self.assertEqual(post["inlGrow"], pre["inlGrow"])
   self.assertEqual(post["inlShrink"], pre["inlShrink"])
   ```
   `pre` is captured on the pristine fixture where all three are `""` (`:293`), so the assertion proves the absent-inline round-trip (`""`→write→undo→`""`). `inlGrow`/`inlShrink`/`inlBasis` read `e.style.*` (inline, not computed) via `rendered()` (`:161`), so a stray `inlGrow="0"` after undo would fail. Genuine guard, not masked.

## C — Tests vs ADX-2 contracts (PASS, with two MINOR deviations)

1. **(PASS) Width-delta kill assertions present and UN-weakened.** E-R10-1 `after["w"]-before["w"]==60, delta=2` (`:177`); E-R10-2 `==-60, delta=2` (`:195`); E-R10-2b `==-60, delta=2` (`:213`). All three also assert `inlGrow==0.0` + `inlShrink==0.0` and the ADX-1 box-sizing-converted inline basis (`before.w ± 60 - 48, delta=1` — the `-48` correctly encodes the content-box padding subtraction). These are the anti-amplification (E-R10-1) and anti-dead-zone (E-R10-2/2b) assertions ADX-2 mandates stay mandatory; tolerances are the tight ±2/±1, NOT weakened.
2. **(PASS) Edge-position assertions live ONLY on start-pinned contexts (ADX-2 rule iv).** Reflow-coupled accent tests (E-R10-1/2/2b) carry NO right-edge assertion — correct, the dragged-edge property is geometrically unsatisfiable there (ADX-2). Position assertions appear only on the grid tests: E-R10-4 `before.left-after.left==0, delta=2` (`:251`) and E-R10-5 `abs(after.left-before.left)<=3` (`:269`) — both grid columns are start-pinned in the fixed-track fixture.
3. **(MINOR) E-R10-2 magnitude reduced from test-plan −120 to −60, and E-R10-2b reduced from −100 to −60 — the two tests are now IDENTICAL.** Both (`:182-216`) open `flow-grow-deadzone.html`, drag `hx - i*6` (i=1..10 ≈ −60 cursor), and assert the same three things. Test-plan §E-R10-2 specified a −120 sweep / −120 assertion and §E-R10-2b a −100 sweep / −100 assertion (coverage-matrix R10-2 maps both). The kill THRESHOLD (width-delta == cursor-delta ±2) is intact, so the contract is not weakened in kind — but E-R10-2b no longer adds coverage beyond E-R10-2 (it was meant to prove first-gesture escape at a DIFFERENT magnitude). The committed red log (`v4-t1-red.txt:25`) still references the −100 escape, so the docs and the test drifted. Impact: redundant test, lost magnitude diversity; not a correctness hole. Recommend restoring distinct magnitudes (or documenting the −60 collapse as intentional).
4. **(MINOR) E-R10-1 dropped the test-plan's right-edge kill assertion.** Test-plan §E-R10-1 (line 126) mandated `after["right"]-before["right"]==60, delta=2` as a co-equal kill assertion; the committed test (`:177-180`) keeps only the width-delta + basis + grow/shrink assertions. This is ADX-2-rule-(iv) COMPLIANT (the accent is reflow-coupled; the right-edge tracks-cursor property is unsatisfiable there per ADX-2 / v4-t2-BUG.md §assertion-3), so dropping it is the correct resolution of the halt — flagged only because it is a deviation from the un-amended test-plan that a reader cross-checking the matrix would notice.

## D — T1 flag (a): E-R10-4 fixed-track grid, no center-shift (PASS, with NOTE)

1. **(PASS) E-R10-4 meaningfully guards the healthy width path.** `:248-251` asserts `w-delta==61, delta=2` (1:1 rendered width) + `inlBasis==""` + `inlGrow==""` (NO flex writes on a grid child) + `before.left-after.left==0, delta=2` (no left shift). `roleOf(grid-mid)==grid-child` → `else` branch → plain `width` write, no flex props. The 1:1 + no-flex-write guard is real and correct.
2. **(NOTE) The `left==0` assertion is correct for THIS fixture but CONTRADICTS the spec prose.** `grid-healthy.html:4` uses `grid-template-columns: 408px 606px 408px` — FIXED pixel tracks. Growing `#grid-mid`'s inline width does not push the fixed 408px side columns, so its left edge is pinned (`left==0` is right). BUT spec §grid-healthy.html (line 179) claims `#grid-mid` "sits in the centered middle column (equal side columns absorb → x shifts Δ/2, real grid centering)" and test-plan §E-R10-4 (line 165) asserts `before.left-after.left==30, delta=4`. Those describe a `1fr`-track (live tecer) grid, not this synthetic fixed-track fixture. The committed test correctly matches the fixture it ships with; the SPEC/TEST-PLAN prose is the stale artifact. The fixture has NO inline comment documenting "fixed-track by design / no center-shift" — adding one (or correcting the spec line) would remove the apparent contradiction. No code/test defect; doc-fixture mismatch only.

## E — T1 flag (b): corner-handle coverage on a FLEX-GROW element — STATIC ASSESSMENT

1. **(PASS, code) Corner drags on a flex-grow element receive the SAME fixed contract.** Static trace of a NE-corner (`direction=[1,-1]`) drag on a flex-row child: `roleOf=flex-child`, `isFlexRow=true`, `mainDim="width"`. Main axis (`:130`): `flexBasis = toContent(beforeRect.width + dist[0], "width")` with `flexGrow=0`/`flexShrink=0` — grow-neutralized, box-sizing-converted, per ADX rules. Cross axis (`:131-132`): `el.style.height = toContent(beforeRect.height + dist[1], "height")` — a PLAIN box-sizing-aware height write with NO `top` compensation (the `direction[1]===-1` top-comp at `:142-143` is gated to `role==='absolute'`, so a north-edge flex resize never adjusts `top`; Moveable's `fixedDirection` holds the bottom edge, matching diagnosis R10c). The code is axis-symmetric and role-correct: main = ADX rules, cross = plain height. **No code defect.**
2. **(MINOR, coverage gap) NO committed test drives a corner on a flex-grow element.** E-R10-6 (`:272-287`) — the only corner test — targets `#twin`, a BLOCK element (`roleOf=block` → `else` branch height write), NOT the flex branch. So the flex-branch cross-axis height write (`:131-132`) is never exercised under a real vertical `dist[1]` combined with a simultaneous main-axis basis write. The diagnosis names `.flow-node--accent` corner (G7) as the live anomaly surface; statically it is fixed, but it is UNVERIFIED by test. **Live probe must check:** on `flow-grow.html`, select `#node-accent`, drag the NE (or SE) corner with a meaningful vertical component (e.g. +ΔY ≈ +30 down on the north edge) AND a horizontal component; assert (i) rendered width-delta tracks `dist[0]` within ±2 / inline basis = box-sizing-converted (`flexGrow==0`, `flexShrink==0`), (ii) rendered height shrinks toward but not past the content floor and `inlHeight` ≈ box-sizing-converted `beforeRect.height + dist[1]`, (iii) the element's top/`rect.y` does NOT jump (no spurious top-comp; Moveable holds the bottom edge), (iv) undo restores all three longhands AND the inline `height`. If (iii) shows a top jump or (i)/(ii) show amplification, the flex corner path has a defect the static read missed.

## F — Regression sweep (PASS)

1. **(PASS) Only `interaction.js` changed in product code.** `git diff-tree` confirms the commit touches `interaction.js` + tests + fixtures + the two verification docs ONLY. No serializer, comments, drag/move (`onDragStart`/`onDragEnd`/`commitDrop`), guides (`getElementGuidelines`/`buildMoveable`), history, or `text-format` files touched. Diagnosis "healthy paths to preserve" (R2 pointer-events, onDragEnd toggle, FLIP reorder, translate-only move) are all in untouched files/branches → invariants I2/I5 hold.
2. **(NOTE) The unconditional cross-axis write fires on every flex resize, including pure-horizontal drags.** On an E-handle drag, `:131-132` still writes `el.style.height = toContent(beforeRect.height + dist[1])` with `dist[1]≈0` — i.e. it pins an explicit content-box height on the accent even when the user only dragged horizontally. This is spec-faithful (the verbatim block at spec line 208 has the same unconditional cross write) and the tests pass, but it converts a previously height-implicit flex item to height-explicit after any width resize. Captured for undo (height is in the snapshot), so it round-trips. Behavior-equivalent for the fixtures; flagged as a NOTE in case a downstream element relied on auto cross-axis height. Not a defect against the spec.
3. **(PASS) Evidence chain is internally consistent.** Red-first (`v4-t1-red.txt`): 3 deadzone/amplification tests RED on master for the right reason (E-R10-1 Δright≈+246 / Δw≈+246 vs expected 60; E-R10-2/2b Δw≈0 vs expected −60/−60), 6 healthy/undo GREEN — a valid red-first gate. Post-fix (`v4-t2-run.txt`): 9/9 R10 green, full suite 124 passed / 1 skipped / 0 failed, `test_r2_resize_real.py` + `test_f2_select_guides.py` green (REG-2/REG-3 held). `v4-t2-BUG.md` halt chain closes with "RESOLVED by ADX-2". (The +246 red number vs the +108 in BUG.md reflects different fixture stages — final committed fixture vs an intermediate ADX-1 state — not a contradiction.)

---

## Findings summary (by severity)

| # | Sev | Finding |
|---|-----|---------|
| C-3 | MINOR | E-R10-2 (−120→−60) and E-R10-2b (−100→−60) reduced to identical −60 gestures; lost magnitude diversity, E-R10-2b adds no coverage beyond E-R10-2. Kill threshold intact. |
| C-4 | MINOR | E-R10-1 dropped the test-plan's right-edge kill assertion. ADX-2-rule-(iv) compliant (reflow-coupled), but a deviation from the un-amended test-plan. |
| E-2 | MINOR | No committed test drives a corner on a flex-grow element; the flex-branch cross-axis height write is statically correct but test-unverified. Live probe spec'd in §E. |
| D-2 | NOTE | E-R10-4 `left==0` is correct for the fixed-track `grid-healthy.html`, but spec §grid-healthy.html line 179 + test-plan §E-R10-4 prose claim Δ/2 centering (a `1fr` grid). Doc-fixture mismatch; correct the spec line or add a fixture comment. |
| F-2 | NOTE | Cross-axis height write fires unconditionally on pure-horizontal flex resizes (spec-faithful; round-trips via undo). |

No BLOCKER or MAJOR findings. Product code is spec-conformant and undo-correct.
