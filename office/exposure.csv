# ⚠ INTERIM — module-root placement, matching `ignite/exposure.csv` (no component.md exists in
# this repo; entry points are MODULE-RELATIVE, prefixes drop when the CMP-5 tree materializes).
# Authored by task 7.647, 2026-08-10, under the mandatory first-party tool inventory
# (`1-projects/rbtv-sb-merge-refactor-core-build/decisions.md#d-tool-inventory-exposure-rows`
# + FIRST-PARTY scope correction + `#d-tool-inventory-rbtv-cli-column`): `part-kind=tool`,
# `method=path`, `description` EMPTY, `rbtv-cli` filled IFF
# `core/capabilities/rbtv-cli/tool/lib/verbs.js` ROUTES routes to the tool — checked 2026-08-10:
# no office tool is routed, so the cell is empty.
#
# BOUNDARY (the ignite worked example's rule — first-party invocables with their own agent-facing
# command surface): office ships exactly ONE, the doc-export DOCX engine below (usage block,
# invoked by hand per the doc-export workflow). NOTE: it requires third-party libraries
# (python-docx, PyYAML) but is itself first-party — module-authored. Everything else in the
# module is harness-exposed markdown, which this inventory does not cover.
part-id,part-kind,method,rbtv-cli,entry-point,description,write-roots
md-to-docx,tool,path,,workflows/doc-export/engines/docx/md-to-docx.py,,
