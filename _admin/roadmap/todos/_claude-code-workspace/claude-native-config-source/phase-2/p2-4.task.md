---
task_id: p2-4
status: pending
phase: understand
complexity_score: 10
human_review: none
---

# Task p2-4: Update Admin Mode Flow

## Goal

UPDATE `_config/install-rbtv.py` — rewrite the admin mode installation flow to use `_admin/.claude/` and `_config/.claude/` as source, deriving `.cursor/` artifacts.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_config/install-rbtv.py` | Full installer — understand current admin mode flow |
| `_admin/roadmap/todos/_claude-code-workspace/claude-native-config-source/prd-claude-native-config-source.md` | PRD section "Update admin mode flow" |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review shape.md execution log for prior task context.
4. Identify the admin mode entry point function and trace its flow.
5. Map current admin flow to new flow: `_config/.claude/` + `_admin/.claude/` as sources, derive `.cursor/`.
6. Note: admin mode applies path substitution (`{project-root}/_bmad/rbtv/` → ``) and appends reinforcement reminders.

### Phase: Execute

1. Update admin mode function to:
   - Copy `_config/.claude/` → rbtv root `.claude/` (with path substitution and reinforcement)
   - Copy `_admin/.claude/rules/` → rbtv root `.claude/rules/`
   - Replicate `.claude/commands/` → `.cursor/commands/`
   - Replicate `.claude/rules/` → `.cursor/rules/` (with `_convert_claude_rule_to_mdc`)
   - Replicate `.claude/agents/` → `.cursor/agents/` (with `_convert_claude_agent_to_cursor`)
   - Replicate `.claude/skills/` → `.cursor/skills/`
2. Update admin source directory constants to reference `_admin/.claude/` instead of `_admin/.cursor/`.
3. Preserve path substitution logic and reinforcement reminder injection.

### Phase: Validate

1. Verify admin mode references `_config/.claude/` and `_admin/.claude/` as sources.
2. Verify replication calls go `.claude/` → `.cursor/`.
3. Verify path substitution and reinforcement logic preserved.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user (max 2000 chars).
3. After approval, set task to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated admin mode flow in installer | `_config/install-rbtv.py` | Python |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
