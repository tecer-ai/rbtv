---
task_id: p2-2
status: pending
phase: understand
complexity_score: 11
human_review: none
---

# Task p2-2: Reverse Replication Functions (Claude → Cursor)

## Goal

UPDATE `_config/install-rbtv.py` — reverse all `ide_replicate_*_to_claude` functions to `ide_replicate_*_to_cursor` equivalents that copy from `.claude/` source and derive `.cursor/` output.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_config/install-rbtv.py` | Full installer — understand current replication functions and their callers |
| `_admin/roadmap/todos/_claude-code-workspace/claude-native-config-source/prd-claude-native-config-source.md` | PRD section on function renaming and direction reversal |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review shape.md execution log for prior task context.
4. Map existing replication functions to their replacements:
   - `ide_replicate_commands_to_claude` → `ide_replicate_commands_to_cursor` (`.claude/commands/` → `.cursor/commands/`, direct copy)
   - `ide_replicate_rules_to_claude` → `ide_replicate_rules_to_cursor` (`.claude/rules/*.md` → `.cursor/rules/*.mdc`, using `_convert_claude_rule_to_mdc`)
   - `ide_replicate_agents_to_claude` → `ide_replicate_agents_to_cursor` (`.claude/agents/` → `.cursor/agents/`, using `_convert_claude_agent_to_cursor`)
   - `ide_replicate_skills_to_claude` → `ide_replicate_skills_to_cursor` (`.claude/skills/` → `.cursor/skills/`, strip Claude-only frontmatter fields)
5. Identify all callers of the existing functions (IDE mode, admin mode).

### Phase: Execute

1. Rename and rewrite `ide_replicate_commands_to_claude` → `ide_replicate_commands_to_cursor`: source is `.claude/commands/`, destination is `.cursor/commands/`.
2. Rename and rewrite `ide_replicate_rules_to_claude` → `ide_replicate_rules_to_cursor`: source is `.claude/rules/*.md`, destination is `.cursor/rules/*.mdc`, call `_convert_claude_rule_to_mdc` on each file.
3. Rename and rewrite `ide_replicate_agents_to_claude` → `ide_replicate_agents_to_cursor`: source is `.claude/agents/`, destination is `.cursor/agents/`, call `_convert_claude_agent_to_cursor` on each file.
4. Rename and rewrite `ide_replicate_skills_to_claude` → `ide_replicate_skills_to_cursor`: source is `.claude/skills/`, destination is `.cursor/skills/`, strip Claude-only frontmatter fields from SKILL.md files.
5. Update all call sites to use the new function names.

### Phase: Validate

1. Verify all old function names are gone (no references remain).
2. Verify new functions exist with correct source/destination directions.
3. Verify each function calls the appropriate conversion helper.
4. Verify all callers updated.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user (max 2000 chars).
3. After approval, set task to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated installer with reversed replication functions | `_config/install-rbtv.py` | Python |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
