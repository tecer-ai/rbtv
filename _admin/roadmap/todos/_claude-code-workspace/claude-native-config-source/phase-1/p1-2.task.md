---
task_id: p1-2
status: pending
phase: understand
complexity_score: 9
human_review: none
---

# Task p1-2: Convert Rules from .mdc to Claude .md Format

## Goal

CREATE `_config/.claude/rules/` containing all 7 rule files converted from Cursor `.mdc` format to Claude Code `.md` format with correct frontmatter transformation.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_config/.cursor/rules/` (all 7 `.mdc` files) | Source rules to convert |
| `.claude/rules/bmad-rbtv-reasoning.md` | Reference for target Claude `.md` format (installed copy) |
| `_config/install-rbtv.py` (function `_convert_mdc_frontmatter_to_claude`, ~line 376) | Existing conversion logic to use as reference for field mapping |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review shape.md execution log for prior task context.
4. Study the `.mdc` frontmatter format: `description`, `globs`, `alwaysApply` fields.
5. Study the Claude `.md` frontmatter format: `description`, `paths` (YAML array). Note: `alwaysApply` is stripped; `globs` (comma string) becomes `paths` (YAML array).

### Phase: Execute

1. CREATE directory `_config/.claude/rules/`.
2. For each `.mdc` file in `_config/.cursor/rules/`:
   - Read the `.mdc` source file.
   - Convert frontmatter: `globs: "pattern1, pattern2"` → `paths:\n  - pattern1\n  - pattern2`. Remove `alwaysApply` field.
   - Change file extension from `.mdc` to `.md`.
   - Write converted file to `_config/.claude/rules/{name}.md`.
3. Preserve all body content exactly as-is (only frontmatter changes).

### Phase: Validate

1. Verify all 7 files exist in `_config/.claude/rules/`.
2. Verify each converted file has valid YAML frontmatter with `description` and `paths` (where applicable).
3. Verify no `.mdc`-specific fields remain (`alwaysApply`, `globs`).
4. Verify body content is identical to source.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user (max 2000 chars).
3. After approval, set task to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| 7 converted rule files | `_config/.claude/rules/*.md` | Claude Code `.md` with YAML frontmatter |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
