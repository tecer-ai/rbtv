---
task_id: p1-2
status: pending
phase: understand
complexity_score: 4
human_review: none
---

# Task p1-2: UPDATE name Field in All 7 Existing SKILL.md Files

## Goal

Fix the `name` field in YAML frontmatter of all 7 SKILL.md files in `_config/.cursor/skills/` to match their parent folder name.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `.cursor/plans/align-skills-anthropic-spec/shape.md` | Plan context — name mapping table |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.

### Phase: Execute

Update `name:` in YAML frontmatter for each file in `_config/.cursor/skills/`:

| File | Current `name` | New `name` |
|------|---------------|------------|
| `bmad-rbtv-create-component/SKILL.md` | `create-component` | `bmad-rbtv-create-component` |
| `bmad-rbtv-designer/SKILL.md` | `designer` | `bmad-rbtv-designer` |
| `bmad-rbtv-doc/SKILL.md` | `doc` | `bmad-rbtv-doc` |
| `bmad-rbtv-quality-review/SKILL.md` | `quality-review` | `bmad-rbtv-quality-review` |
| `bmad-rbtv-tone-extraction/SKILL.md` | `tone-extraction` | `bmad-rbtv-tone-extraction` |
| `bmad-rbtv-visual-design-extraction/SKILL.md` | `visual-design-extraction` | `bmad-rbtv-visual-design-extraction` |
| `bmad-rbtv-web-research/SKILL.md` | `web-research` | `bmad-rbtv-web-research` |

Change ONLY the `name:` line in each file. Do not modify any other content.

### Phase: Validate

1. Verify each file has `name:` matching its parent folder name
2. Verify no other content was modified in any file

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| 7 updated SKILL.md files | `_config/.cursor/skills/bmad-rbtv-*/SKILL.md` | YAML frontmatter change only |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
