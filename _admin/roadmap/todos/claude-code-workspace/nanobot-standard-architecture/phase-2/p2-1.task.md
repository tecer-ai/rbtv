---
task_id: p2-1
status: pending
phase: understand
complexity_score: 9
human_review: optional
---

# Task p2-1: Create bmad-compat.yaml

## Goal

Create `bmad-compat.yaml` at RBTV root listing every RBTV→BMAD dependency (installer writes, agent references, task references, structure assumptions).

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/claude-code-workspace/nanobot-standard-architecture/prd-config-bmad-compatibility-check.md` | Full PRD 3 spec with proposed bmad-compat.yaml structure |
| `_admin/roadmap/todos/claude-code-workspace/nanobot-standard-architecture/shape.md` | Execution context and constraints |
| `agents/ana.md` | Source of agent_references touchpoints (BMAD workflow paths) |
| `_config/install-rbtv.py` | Source of installer_writes touchpoints |
| `workflows/doc-compound-learning/workflow.md` | Source of task_references touchpoints |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review PRD 3 proposed `bmad-compat.yaml` structure.
4. Identify all RBTV→BMAD touchpoints by scanning context files.

### Phase: Execute

1. Compile touchpoints into four categories: `installer_writes`, `agent_references`, `task_references`, `structure_assumptions`.
2. Verify each touchpoint path exists in the updated BMAD mirror (Beta.8).
3. Write `bmad-compat.yaml` at RBTV root following PRD 3 format.
4. Include `bmad_target_version` and `bmad_min_version` fields matching `_config/config.yaml`.

**Discovery Handling:**
- If new touchpoints are found beyond what PRD 3 lists: add them and document in shape.md.
- If any listed touchpoints no longer exist in Beta.8 mirror: document as discovery.

### Phase: Validate

1. Verify all touchpoint paths exist in the BMAD mirror.
2. Verify file is valid YAML.
3. Confirm `bmad-compat.yaml` is NOT referenced in any agent, rule, or workflow loaded during normal sessions.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user. Do not mark complete until user approves.
3. After approval, set todo to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Compatibility manifest | `bmad-compat.yaml` (RBTV root) | YAML |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
