---
task_id: p2-2
status: pending
phase: understand
complexity_score: 8
human_review: optional
---

# Task p2-2: Create Compatibility Check Task

## Goal

Create `tasks/check-bmad-compat.xml` — a structured agent task for evaluating new BMAD releases against RBTV's touchpoints.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/claude-code-workspace/nanobot-standard-architecture/prd-config-bmad-compatibility-check.md` | PRD 3 spec — task flow definition |
| `bmad-compat.yaml` | Touchpoints data consumed by this task |
| `tasks/` | Existing task files for RBTV conventions (XML structure) |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review PRD 3 task flow specification (Section B).
4. Review existing RBTV task file conventions (XML with `<task>` root, `<objective>`, `<llm>`, `<flow>`).

### Phase: Execute

1. Design task flow: read bmad-compat.yaml → accept BMAD version input → check touchpoints → produce report.
2. Write `tasks/check-bmad-compat.xml` following RBTV task conventions.
3. Reference `bmad-compat-report-template.md` as output template.

**Discovery Handling:**
- If existing task conventions have evolved since PRD was written: adapt and document in shape.md.

### Phase: Validate

1. Verify XML structure follows RBTV task conventions.
2. Verify task references `bmad-compat.yaml` correctly.
3. Verify task output points to report template.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user. Do not mark complete until user approves.
3. After approval, set todo to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Compatibility check task | `tasks/check-bmad-compat.xml` | XML (RBTV task format) |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
