---
task_id: p3-2
status: pending
phase: understand
complexity_score: 8
human_review: optional
---

# Task p3-2: Integrate Pre-Flight Version Check

## Goal

Add PRD 3 enforcement layer to unified installer — pre-flight version comparison before installation, with warn-only behavior and `--skip-version-check` flag.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/claude-code-workspace/nanobot-standard-architecture/prd-config-bmad-compatibility-check.md` | PRD 3 spec — Section C: Installer Pre-Flight Check |
| `_admin/roadmap/todos/claude-code-workspace/nanobot-standard-architecture/shape.md` | Execution context |
| `bmad-compat.yaml` | Version data consumed by pre-flight check |
| `_config/install-rbtv.py` | Unified installer — target for modification |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review PRD 3 Section C pre-flight flow (6-step decision logic).
4. Identify insertion point in unified installer for pre-flight check.

### Phase: Execute

1. Add function to read `bmad-compat.yaml` and extract version fields.
2. Add function to read target BMAD's `_bmad/_config/manifest.yaml`.
3. Implement version comparison logic (matches target → silent; >= min → warn; < min → strong warn).
4. Add `--skip-version-check` CLI flag to bypass.
5. Call pre-flight check before any installation logic in IDE and sync modes.

**Discovery Handling:**
- If `manifest.yaml` structure differs from expected: adapt and document.

### Phase: Validate

1. Verify silent pass when BMAD version matches `bmad_target_version`.
2. Verify warning when version >= `bmad_min_version` but != target.
3. Verify strong warning when version < `bmad_min_version`.
4. Verify `--skip-version-check` bypasses all checks.
5. Verify admin mode does NOT run pre-flight check (no BMAD project to check against).

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user. Do not mark complete until user approves.
3. After approval, set todo to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated installer | `_config/install-rbtv.py` | Python (added pre-flight function) |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
