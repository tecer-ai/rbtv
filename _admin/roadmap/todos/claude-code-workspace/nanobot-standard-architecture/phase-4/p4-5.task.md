---
task_id: p4-5
status: pending
phase: understand
complexity_score: 8
human_review: optional
---

# Task p4-5: Simplify Path Resolution Documentation

## Goal

Simplify the path resolution table in 3 mirrored files to reference config path variables instead of conditional lookup. All 3 files MUST be updated together to maintain consistency.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/claude-code-workspace/nanobot-standard-architecture/prd-reduce-path-resolution-hops.md` | PRD 5 spec — migration path |
| `_admin/roadmap/todos/claude-code-workspace/nanobot-standard-architecture/shape.md` | Execution context |
| `CLAUDE.md` | Primary path resolution table (mirrored source) |
| `.cursor/rules/admin-rbtv-bmad-mirror.mdc` | Mirrored copy of path resolution |
| `workflows/build-rbtv-component/data/admin-restrictions.md` | Mirrored admin restrictions |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Identify current path resolution table content in all 3 files.
4. Determine which content can be simplified vs must be preserved.
5. Verify p4-4 (path migration) is complete — docs should reflect post-migration state.

### Phase: Execute

1. UPDATE `CLAUDE.md` path resolution section to reference `{bmad_core}`, `{bmad_bmm}` variables from config instead of conditional table.
2. UPDATE `.cursor/rules/admin-rbtv-bmad-mirror.mdc` with identical changes.
3. UPDATE `workflows/build-rbtv-component/data/admin-restrictions.md` with identical changes.
4. Preserve `{project-root}` documentation for cases where full BMAD root is needed.
5. Keep the table structure but simplify content to reflect direct variable substitution.

**Discovery Handling:**
- If mirrored sections have drifted out of sync: fix all 3, document in shape.md.

### Phase: Validate

1. Verify all 3 files contain identical path resolution content.
2. Verify documentation correctly describes post-migration variable usage.
3. Verify `{project-root}` remains documented for full-root cases.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user. Do not mark complete until user approves.
3. After approval, set todo to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Simplified docs | `CLAUDE.md` | Markdown (path resolution section) |
| Mirrored copy | `.cursor/rules/admin-rbtv-bmad-mirror.mdc` | MDC rule file |
| Mirrored copy | `workflows/build-rbtv-component/data/admin-restrictions.md` | Markdown |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
