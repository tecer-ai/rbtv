---
task_id: p1-1
status: completed
phase: close
complexity_score: 11
human_review: optional
---

# Task p1-1: Provision Hetzner VPS Baseline

## Goal

Provision a Hetzner VPS with API-manageable project setup and SSH key access ready for secure hardening.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_bmad-output/robotville-v4.0/bmad/prd.md` | Confirm security and runtime expectations for prototype |
| `_bmad-output/robotville-v4.0/bmad/architecture.md` | Align VPS setup with architecture constraints and FR25 scope |
| `../shape.md` | Respect plan boundaries and shaping decisions |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. In the plan file, set `p1-1` to `in_progress`.
3. Confirm region, instance size, and SSH key source with user.
4. Confirm Hetzner API token handling approach.

### Phase: Execute

1. Create Hetzner project and VPS instance according to approved settings.
2. Attach SSH key-based access and record host details.
3. Verify SSH connectivity from operator workstation.

### Phase: Validate

1. Confirm VPS is reachable over SSH.
2. Confirm provider API can retrieve server metadata and state.
3. Confirm baseline host details are complete for documentation handoff.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user and request approval.
3. After approval, set `p1-1` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Provisioned VPS + access details | Hetzner + local operator notes | Running server + verified SSH access |
