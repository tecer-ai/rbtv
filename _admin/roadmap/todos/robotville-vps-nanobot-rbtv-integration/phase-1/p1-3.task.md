---
task_id: p1-3
status: completed
phase: close
complexity_score: 10
human_review: optional
---

# Task p1-3: Apply VPS Security Baseline

## Goal

Harden the VPS with SSH restrictions, firewall policy, and basic intrusion protection aligned to project constraints.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_bmad-output/robotville-v4.0/bmad/prd.md` | Validate security NFR expectations |
| `_bmad-output/robotville-v4.0/bmad/architecture.md` | Enforce no-public-service network posture |
| `_bmad/rbtv/_admin/docs/mobile/robotville-vps-access.md` | Keep access policy and recovery notes consistent |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. In the plan file, set `p1-3` to `in_progress`.
3. Confirm allowed admin IP ranges and SSH policy with user.

### Phase: Execute

1. Configure SSH hardening (keys only, least privilege settings).
2. Apply host firewall policy and deny unnecessary inbound traffic.
3. Configure baseline intrusion protections (e.g., fail2ban class controls).

### Phase: Validate

1. Verify SSH access still works from approved origin.
2. Verify blocked inbound paths are enforced.
3. Verify settings are persisted across reboot.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user and request approval.
3. After approval, set `p1-3` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Hardened host configuration | VPS system config | Applied and validated security baseline |
