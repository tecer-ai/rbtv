---
task_id: p6-3
status: pending
phase: understand
complexity_score: 8
human_review: required
---

# Task p6-3: CREATE Initial robotville.ai Home Page

## Goal

Create the robotville.ai home page based on founder documents, with navigation structure supporting `/docs/{project}` and `/app/{project}` sections. Deploy to live site.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/founder/project-memo.md` | Product description, solution, tenets, target customer — primary content source |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/bmad/prd.md` | Executive summary, differentiator, user journeys — secondary content source |
| `../shape.md` | Execution log — p6-1/p6-2 hosting decisions and deploy method |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for hosting platform decision and deploy method.
4. Extract key content from founder documents for the home page:
   - What Robotville is (executive summary / solution description)
   - Who it's for (target customer)
   - How it works (high-level pipeline description)
   - Key differentiator (end-to-end thinking-to-building)

### Phase: Execute

1. **CREATE** home page content as static HTML/CSS (or framework appropriate to hosting platform). Must include:
   - Hero section: What Robotville is, one-line value proposition
   - About section: Extracted from project-memo solution description and PRD executive summary
   - Navigation menu with links to:
     - `/docs/` — Project documentation (placeholder listing, populated by deployments)
     - `/app/` — Project applications (placeholder, populated by mentor M4-M6 workflows)
   - Footer with minimal branding
2. **CREATE** `/docs/` index page — placeholder that will list deployed project docs.
3. **CREATE** `/app/` index page — placeholder noting "Projects deployed here by the Mentor workflow."
4. **DEPLOY** home page to robotville.ai using the method established in p6-1/p6-2.
5. **Store** site source files in an appropriate location within the RBTV repo (e.g., `_mobile/site/` or a dedicated website repo — follow p6-1 decision).

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify `https://robotville.ai` displays the home page with correct content.
2. Verify navigation links to `/docs/` and `/app/` resolve to their placeholder pages.
3. Verify content accurately represents Robotville based on founder documents.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Home page live at robotville.ai | Hosting provider | HTML/CSS |
| `/docs/` index page | Hosting provider | HTML/CSS |
| `/app/` index page | Hosting provider | HTML/CSS |
| Site source files | RBTV repo (location per p6-1 decision) | Static site files |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
