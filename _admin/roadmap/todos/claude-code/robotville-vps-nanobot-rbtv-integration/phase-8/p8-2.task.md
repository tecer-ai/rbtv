---
task_id: p8-2
status: pending
phase: understand
complexity_score: 9
human_review: optional
---

# Task p8-2: Research and Configure Prompt Caching

## Goal

Research whether Nanobot's LiteLLM integration supports Anthropic prompt caching, and configure it if available — so the ~7,250-token system prompt is cached and subsequent API calls use cached tokens (separate rate limit bucket, 90% cost reduction).

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/cp-nanobot-token-optimization-prompt-caching.md` | Full token optimization analysis — Part B details prompt caching approach |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/bmad/prd.md` | PRD NFR1-NFR3 performance requirements, NFR13 reconnection behavior |
| `_admin/docs/mobile/server-env-template.md` | Current VPS config patterns — update target |
| `../shape.md` | Execution log — p8-1 model switch context |

---

## Tools

| Tool | Mode | Purpose |
|------|------|---------|
| web-research | skill | Research LiteLLM Anthropic prompt caching configuration and Nanobot provider passthrough |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for p8-1 model switch context.
4. From the compound doc, extract the three research questions:
   - Does Nanobot expose LiteLLM provider configuration for `cache_control` headers?
   - Does LiteLLM auto-apply caching for Anthropic system messages when configured?
   - If manual configuration required, what is the exact config surface?

### Phase: Execute

1. **Research** LiteLLM Anthropic prompt caching:
   - Read `web-research` skill, then use web search to check current LiteLLM docs for Anthropic prompt caching support
   - Check if `cache_control` can be set via environment variable, config file, or API parameter
   - Check Nanobot source code (HKUDS/nanobot) for LiteLLM provider passthrough configuration
2. **Document** findings: what is supported, what is not, what configuration is needed.
3. **If caching is supported:**
   - Configure on VPS (update `config.json` or environment variables)
   - Restart gateway
   - Verify caching is active (check API response headers for `cache_read_input_tokens`)
4. **If caching is NOT supported:**
   - Document the limitation
   - Note what would need to change in Nanobot for support (for future reference)
5. **UPDATE** `server-env-template.md` with caching configuration guidance regardless of outcome.

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. If configured: verify API responses show cached token usage.
2. If not configurable: verify documentation captures the limitation and workarounds.
3. Verify server-env-template is updated.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Research findings and configuration | shape.md | Append-only execution entry |
| Updated server-env-template | `_admin/docs/mobile/server-env-template.md` | Markdown |
| VPS config changes (if applicable) | VPS `/srv/nanobot/.nanobot/config.json` | JSON |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
