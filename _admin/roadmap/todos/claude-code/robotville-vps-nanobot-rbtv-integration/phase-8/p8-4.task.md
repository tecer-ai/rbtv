---
task_id: p8-4
status: pending
phase: understand
complexity_score: 6
human_review: none
---

# Task p8-4: UPDATE Deploy Docs with Token Optimization Guidance

## Goal

Update the deploy runbook, server environment template, and smoke checklist to include model selection, prompt caching configuration, and token budget validation guidance — so future operators and system updates maintain token-efficient operations.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/cp-nanobot-token-optimization-prompt-caching.md` | Acceptance criteria — full list of doc updates required |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/bmad/prd.md` | PRD NFR1-NFR3 performance requirements for context |
| `_admin/docs/mobile/deploy-runbook.md` | Update target — add model selection and caching config |
| `_admin/docs/mobile/server-env-template.md` | Update target — add recommended model and caching settings |
| `_admin/docs/mobile/smoke-checklist.md` | Update target — add token budget validation item |
| `../shape.md` | Execution log — p8-1/p8-2/p8-3 context and findings |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for p8-1 (model switch), p8-2 (caching research), p8-3 (TOOLS.md slimming) outcomes.
4. From the compound doc acceptance criteria, identify required doc updates.

### Phase: Execute

1. **UPDATE** `deploy-runbook.md`:
   - Add model selection guidance (Sonnet default, when to consider Opus)
   - Add prompt caching configuration section (based on p8-2 findings)
   - Add bootstrap file token budget awareness note
2. **UPDATE** `server-env-template.md`:
   - Add recommended model setting in config template
   - Add caching configuration fields (if supported, per p8-2)
3. **UPDATE** `smoke-checklist.md`:
   - Add token budget validation item: estimate total system prompt size, verify it stays within rate limit for configured model
   - Add multi-turn conversation test item

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify all three docs are updated with accurate guidance based on p8-1/p8-2/p8-3 outcomes.
2. Verify compound doc acceptance criteria for doc updates are addressed.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated deploy runbook | `_admin/docs/mobile/deploy-runbook.md` | Markdown |
| Updated server-env-template | `_admin/docs/mobile/server-env-template.md` | Markdown |
| Updated smoke checklist | `_admin/docs/mobile/smoke-checklist.md` | Markdown |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
