---
task_id: p3-2
status: pending
phase: understand
complexity_score: 9
human_review: optional
---

# Task p3-2: Create LiteLLM Retry Logic Patch

## Goal

Create a Python patch script (`_mobile/ops/patches/add-litellm-retries.py`) that modifies Nanobot's `litellm_provider.py` to add `litellm.num_retries = 3` as a module-level setting. This prevents user-facing errors on transient rate limit responses.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_mobile/ops/patches/update-nanobot-model.py` | Reference pattern for patch script structure |
| `.cursor/plans/optimize-nanobot-token-usage/shape.md` | Key Decisions: retry logic rationale |
| `.cursor/plans/optimize-nanobot-token-usage/cp-nanobot-token-optimization-prompt-caching.md` | Foundational analysis with retry implementation details |

**Remote source (fetch during Execute phase):**

| URL | Purpose |
|-----|---------|
| `https://raw.githubusercontent.com/HKUDS/nanobot/main/nanobot/providers/litellm_provider.py` | Current Nanobot source — target for patching |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. In the plan file (`optimize-nanobot-token-usage.plan.md`), set this task's todo to `status: in_progress`.
3. Review shape.md Decisions and Discoveries for prior task context.
4. Study `litellm_provider.py` module-level settings pattern: existing code sets `litellm.suppress_debug_info = True` and `litellm.drop_params = True`.

### Phase: Execute

1. Fetch the current `litellm_provider.py` source from GitHub (may reuse from p3-1 if already fetched).
2. Create `_mobile/ops/patches/add-litellm-retries.py` following the existing patch script pattern.
3. The patch must:
   - Read `litellm_provider.py` from the Nanobot installation
   - Locate the existing module-level `litellm.` settings block
   - Add `litellm.num_retries = 3` alongside existing settings
   - Write the modified file back
   - Include clear error messages if the expected code pattern is not found (upgrade resilience)

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify the patch script follows the same pattern as `update-nanobot-model.py`.
2. Verify the patch correctly targets the module-level settings in `litellm_provider.py`.
3. Verify error handling exists for when the expected source pattern is not found.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Retry logic patch script | `_mobile/ops/patches/add-litellm-retries.py` | Python script following existing patch pattern |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Decisions and Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
