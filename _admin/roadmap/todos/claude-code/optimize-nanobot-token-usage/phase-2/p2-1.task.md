---
task_id: p2-1
status: pending
phase: understand
complexity_score: 9
human_review: optional
---

# Task p2-1: Slim TOOLS.md

## Goal

Remove per-agent workflow tables from `_mobile/TOOLS.md` to reduce bootstrap token load by ~800 tokens. Preserve command routing table, skills reference, and tool reference.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_mobile/TOOLS.md` | Current content — identify sections to remove vs. preserve |
| `.cursor/plans/optimize-nanobot-token-usage/shape.md` | Key Decisions: TOOLS.md strategy rationale and scope boundaries |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. In the plan file (`optimize-nanobot-token-usage.plan.md`), set this task's todo to `status: in_progress`.
3. Review shape.md Decisions and Discoveries for prior task context.
4. Identify the per-agent workflow tables (~lines 26-73, ~800 tokens). These tables duplicate content already present in each agent's own file, loaded on demand during activation.
5. Identify content to preserve: command routing table (3 rows), skills section, tool reference section.

### Phase: Execute

1. Remove per-agent workflow tables from TOOLS.md.
2. Verify remaining content includes: command routing table, skills reference, tool reference.
3. Ensure all `{project-root}` placeholders are preserved in all paths.

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify deliverable meets goal statement.
2. Estimate token count of slimmed TOOLS.md (target: ~500-600 tokens).
3. Verify no essential routing information was removed — all command activation paths must remain functional.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Slimmed TOOLS.md | `_mobile/TOOLS.md` | Markdown, target ~500-600 tokens |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Decisions and Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
