---
task_id: p1-1
status: pending
phase: understand
complexity_score: 7
human_review: optional
---

# Task p1-1: Add PS Lite Action Block to Dom Cobb

## Goal

UPDATE `agents/domcobb.md` — add an `<action id="ps-lite">` block that implements conversational problem structuring with Socratic questioning, complexity monitoring, escalation triggers, and clean resolution format.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `agents/domcobb.md` | Target file — understand current structure, persona, and where action blocks belong |
| `_admin/roadmap/todos/compound-ps-lite-domcobb.md` | Complete behavior specification: entry prompt, conversation loop, resolution format, escalation triggers, escalation prompt, context transfer |
| `workflows/problem-structuring/workflow.md` | Escalation target — understand [PS] entry point so escalation carries context correctly |
| `.cursor/plans/ps-lite-domcobb/shape.md` | Planning decisions and constraints |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file (`.cursor/plans/ps-lite-domcobb/ps-lite-domcobb.plan.md`), set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Study the existing agent XML structure: identify where `<action>` blocks are placed (after `</menu>` or before `<rules>` — check pattern in other agents if unclear).
5. Study the compound PRD's "Action Behavior Specification" section for exact behavioral requirements: entry prompt, conversation loop, simple resolution format, escalation triggers, escalation prompt, context transfer.
6. Study `workflows/problem-structuring/workflow.md` to understand how [PS] initializes — the escalation must read this file via exec handler so context carries forward naturally.

### Phase: Execute

1. Craft the `<action id="ps-lite">` block (~15-20 lines) containing:
   - **Entry:** Ask "What are you trying to figure out?" — no framework jargon
   - **Conversation loop:** Socratic clarifying questions, organic structure surfacing, complexity assessment after each exchange
   - **Simple resolution (2-3 exchanges):** Clean problem statement + 2-3 actionable next steps + confirmation question
   - **Escalation triggers:** 3+ distinct problem dimensions, competing priorities/stakeholders, dependencies needing decomposition, user explicitly asks for more structure
   - **Escalation prompt:** Offer to switch to [PS] with gathered context
   - **On escalation:** Read and follow `{project-root}/_bmad/rbtv/workflows/problem-structuring/workflow.md`, carrying conversation context as initial input
2. Insert the action block in `agents/domcobb.md` at the correct location within the XML structure (between `</menu>` and `</agent>`, or wherever the agent pattern dictates).

### Phase: Validate

1. Verify action block maintains Dom Cobb's persona: Socratic, structured, warm.
2. Verify no framework jargon in the conversational phase (MECE, Pyramid, Problem Trees belong to [PS] only).
3. Verify escalation path references the correct workflow file with `{project-root}` placeholder.
4. Verify the action block is compact (15-20 lines target).
5. Count total agent file lines — must remain under ~110 lines.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Action block added to agent | `agents/domcobb.md` | XML `<action id="ps-lite">` block |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
