---
task_id: p2-3
status: pending
phase: understand
complexity_score: 10
human_review: none
---

# Task p2-3: Update IDE Mode Flow

## Goal

UPDATE `_config/install-rbtv.py` — rewrite the IDE mode installation flow to copy from `_config/.claude/` as source and derive `.cursor/` artifacts.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_config/install-rbtv.py` | Full installer — understand current IDE mode flow |
| `_admin/roadmap/todos/_claude-code-workspace/claude-native-config-source/prd-claude-native-config-source.md` | PRD section "Update IDE mode flow" — exact new flow |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review shape.md execution log for prior task context.
4. Identify the IDE mode entry point function and trace its flow.
5. Map the current flow (copy `.cursor/` → replicate to `.claude/`) to the new flow (copy `.claude/` → replicate to `.cursor/`).

### Phase: Execute

1. Update the IDE mode function to:
   - Copy `_config/.claude/` → `{root}/.claude/` (instead of `_config/.cursor/` → `{root}/.cursor/`)
   - Call `ide_replicate_commands_to_cursor` (`.claude/commands/` → `.cursor/commands/`)
   - Call `ide_replicate_rules_to_cursor` (`.claude/rules/` → `.cursor/rules/`, with conversion)
   - Call `ide_replicate_agents_to_cursor` (`.claude/agents/` → `.cursor/agents/`, with conversion)
   - Call `ide_replicate_skills_to_cursor` (`.claude/skills/` → `.cursor/skills/`, with stripping)
   - MCP merge unchanged: both `.cursor/mcp.json` and `.claude/.mcp.json`
2. Update source directory constants to reference `_config/.claude/` instead of `_config/.cursor/`.
3. Preserve cleanup logic (both `.cursor/` and `.claude/` directories are already covered).

### Phase: Validate

1. Verify IDE mode function references `_config/.claude/` as source.
2. Verify replication calls go `.claude/` → `.cursor/` (not reverse).
3. Verify MCP merge logic is unchanged.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user (max 2000 chars).
3. After approval, set task to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated IDE mode flow in installer | `_config/install-rbtv.py` | Python |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
