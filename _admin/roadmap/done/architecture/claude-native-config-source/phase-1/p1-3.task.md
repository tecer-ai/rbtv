---
task_id: p1-3
status: pending
phase: understand
complexity_score: 9
human_review: none
---

# Task p1-3: Convert Agents to Claude-Native Frontmatter

## Goal

CREATE `_config/.claude/agents/` containing all agent files converted from Cursor frontmatter to Claude Code native frontmatter format.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_config/.cursor/agents/` (all agent `.md` files) | Source agents to convert |
| `.claude/agents/bmad-rbtv-quality-review.md` | Reference for target Claude agent format (installed copy) |
| `_config/install-rbtv.py` (function `_convert_agent_frontmatter_to_claude`, ~line 431) | Existing conversion logic for field mapping reference |
| `_admin/roadmap/todos/_claude-code-workspace/claude-native-config-source/prd-claude-native-config-source.md` | PRD section on agent frontmatter mapping |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review shape.md execution log for prior task context.
4. Study Cursor agent frontmatter fields: `readonly`, `description`.
5. Study Claude agent frontmatter fields: `permissionMode`, `description`, `tools`, `maxTurns`, `hooks`, `memory`, `mcpServers`, `skills`.
6. Map conversions: `readonly: true` → `permissionMode: plan`. Fields not representable in Cursor (`tools`, `hooks`, etc.) can now be authored natively.

### Phase: Execute

1. CREATE directory `_config/.claude/agents/`.
2. For each agent file in `_config/.cursor/agents/`:
   - Read the source file.
   - Convert frontmatter: `readonly: true` → `permissionMode: plan`. Remove Cursor-only fields.
   - For v1, do NOT add new Claude-only fields — just convert existing fields accurately.
   - Write converted file to `_config/.claude/agents/{name}.md`.
3. Preserve all body content exactly as-is.

### Phase: Validate

1. Verify all agent files exist in `_config/.claude/agents/`.
2. Verify frontmatter uses Claude-native fields (`permissionMode` instead of `readonly`).
3. Verify no Cursor-specific fields remain.
4. Verify body content is identical to source.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user (max 2000 chars).
3. After approval, set task to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Converted agent files | `_config/.claude/agents/*.md` | Claude Code `.md` with native frontmatter |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
