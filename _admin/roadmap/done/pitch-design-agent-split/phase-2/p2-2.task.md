---
task_id: p2-2
status: pending
phase: understand
complexity_score: 9
human_review: none
---

# Task p2-2: Create Thin Loader Stack for Design Agent

## Goal

Create the command, skill, and cursor sub-agent files for the design agent following the existing RBTV thin loader pattern.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_config/.cursor/commands/bmad-rbtv-create-investor-pitch.md` | Reference command loader pattern |
| `_config/.cursor/skills/create-investor-pitch/SKILL.md` | Reference skill loader pattern |
| `_config/.cursor/agents/bmad-rbtv-create-investor-pitch.md` | Reference cursor sub-agent pattern |
| `agents/designer.md` | The design agent created in p2-1 — verify paths and entry points |
| `_config/tools-manifest.csv` | Understand manifest structure for p2-3 |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Study the thin loader pattern — how commands reference agents, how skills wrap commands, how cursor sub-agents delegate.

### Phase: Execute

1. CREATE `_config/.cursor/commands/bmad-rbtv-designer.md`
   - Follow command pattern: YAML frontmatter with name/description
   - Agent activation block pointing to `{project-root}/_bmad/rbtv/agents/designer.md`
   - Load full agent file, read contents, follow activation, display menu, wait for input

2. CREATE `_config/.cursor/skills/designer/SKILL.md`
   - Follow skill pattern from reference
   - Reference the design agent file
   - Include skill metadata

3. CREATE `_config/.cursor/agents/bmad-rbtv-designer.md`
   - Follow cursor sub-agent pattern from reference
   - Route to design agent activation

### Phase: Validate

1. Verify all three files exist and follow their respective patterns
2. Verify agent path references use `{project-root}/_bmad/rbtv/agents/designer.md`
3. Verify naming convention: `bmad-rbtv-designer` prefix
4. Verify YAML frontmatter is complete in each file

### Phase: Close

1. Append execution entry to shape.md.
2. **MUST** present summary of created files to user.
3. After user approval, set this task's todo to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Command loader | `_config/.cursor/commands/bmad-rbtv-designer.md` | Markdown command file |
| Skill file | `_config/.cursor/skills/designer/SKILL.md` | Markdown skill file |
| Cursor sub-agent | `_config/.cursor/agents/bmad-rbtv-designer.md` | Markdown agent file |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
