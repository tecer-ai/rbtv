---
task_id: p1-2
status: pending
phase: understand
complexity_score: 5
human_review: none
---

# Task p1-2: Create Verification Instructions for {project-root} in Milestone Paths

## Goal

CREATE instructions for the user on how to verify that {project-root} (or equivalent project-root placeholder) appears in all milestone workflow output paths, using file content search. Output: add a section to shape.md or a small doc in the plan folder so the user can run the check without AI token-heavy sweep.

---

## Context Files

| File | Purpose |
|------|---------|
| shape.md | Where to add instructions or reference to doc |
| _bmad/rbtv/workflows/bi-business-innovation/data/founder-process.md | Defines output folder structure |
| _bmad/rbtv/workflows/bi-m1/workflow.md | Example milestone outputFolder frontmatter |
| _bmad/rbtv/workflows/bi-m4/workflow.md | Example milestone outputFolder |

---

## Execution Flow

### Phase: Understand

1. Identify all milestone workflows: bi-m1, bi-m2, bi-m3, bi-m4 (and bi-m5, bi-m6 when they exist)
2. Identify frontmatter/output path patterns: outputFolder, projectMemo, any paths referencing projects

### Phase: Execute

1. CREATE a "Verification: Output Paths" section (in shape.md or CREATE docs/verify-output-paths.md in plan folder) containing:
   - List of file paths to check: e.g. _bmad/rbtv/workflows/bi-m*/workflow.md, _bmad/rbtv/workflows/bi-m*-*/workflow.md and steps-c/*.md that reference output paths
   - Exact search instruction: e.g. "Search for `projects` or `outputFolder` or `projectMemo` in these paths; confirm each uses `{project-root}` (or equivalent) so multiple projects in projects/founder/ are supported"
   - Optional: grep/search pattern the user can run (e.g. pattern for "missing project-root in path")
2. Ensure instructions are copy-pasteable and clear for file content search in IDE

### Phase: Validate

1. Instructions are actionable: user can follow without AI
2. All milestone workflow paths are covered

### Phase: Close

1. Append execution entry to shape.md Execution Log
2. Mark task completed in plan YAML if updating plan state

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Verification instructions | shape.md section or .cursor/plans/bi-m4-follow-up/docs/verify-output-paths.md | Markdown: paths to check, search pattern, success criteria |

---

## Revolving Plan Rules

- If more milestone paths are found: add them to the instructions, document in shape
- **MANDATORY:** In output message, state any tasks added or removed
