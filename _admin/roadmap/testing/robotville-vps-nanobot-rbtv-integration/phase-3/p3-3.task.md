---
task_id: p3-3
status: pending
phase: understand
complexity_score: 10
human_review: optional
---

# Task p3-3: Create Canonical Command Router

## Goal

Create routing module that maps canonical commands (`mentor`, `domcobb`, `doc`) to existing RBTV workflow entrypoints.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_bmad-output/robotville-v4.0/bmad/architecture.md` | Enforce routing and compatibility constraints |
| `_bmad/rbtv/agents/mentor.md` | Align mentor command behavior expectations |
| `_bmad/rbtv/_mobile/integration/nanobot-gateway-bridge.ts` | Consume normalized bridge payload contract |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. Set `p3-3` to `in_progress` in the plan file.
3. Confirm canonical command set and unknown-command behavior.

### Phase: Execute

1. CREATE router module under `_mobile/routing/`.
2. Implement command-to-agent/workflow dispatch mapping.
3. Implement fallback handling for unsupported commands.

### Phase: Validate

1. Verify canonical commands route to expected workflow targets.
2. Verify unsupported commands are handled safely and clearly.
3. Verify router stays free of direct transport/provider logic.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user for approval.
3. After approval, set `p3-3` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Command router module | `_bmad/rbtv/_mobile/routing/command-router.ts` | TypeScript routing contract |
