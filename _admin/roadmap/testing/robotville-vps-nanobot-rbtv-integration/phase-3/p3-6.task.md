---
task_id: p3-6
status: completed
phase: close
complexity_score: 10
human_review: optional
---

# Task p3-6: Wire Harness Bootstrap into Gateway Path

## Goal

Update runtime bootstrap path so Nanobot gateway execution loads `_mobile` bridge, routing, state, and security modules.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_bmad-output/robotville-v4.0/bmad/architecture.md` | Preserve boundary rules and flow contract |
| `_bmad/rbtv/_mobile/integration/nanobot-gateway-bridge.ts` | Ensure bridge is called correctly |
| `_bmad/rbtv/_mobile/routing/command-router.ts` | Ensure routing integration is complete |
| `_bmad/rbtv/_mobile/security/allowlist-gate.ts` | Ensure gate executes before routing |
| `_bmad/rbtv/_mobile/state/project-memo-adapter.ts` | Ensure canonical state adapter is used |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. Set `p3-6` to `in_progress` in the plan file.
3. Identify exact bootstrap entrypoint file to update.

### Phase: Execute

1. UPDATE bootstrap entrypoint to invoke `_mobile` flow in correct order.
2. Connect bridge -> allowlist gate -> command router -> workflow/state adapter path.
3. Ensure integration preserves existing workflow contracts.

### Phase: Validate

1. Verify gateway startup uses harness path.
2. Verify allowlist gate and command routing both execute in observed flow.
3. Verify `project-memo` state path remains canonical and intact.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user for approval.
3. After approval, set `p3-6` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Bootstrap wiring update | Runtime bootstrap file under `_bmad/rbtv/` | Updated integration path and invocation order |
