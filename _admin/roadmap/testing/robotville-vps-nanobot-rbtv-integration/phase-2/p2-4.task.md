---
task_id: p2-4
status: pending
phase: understand
complexity_score: 9
human_review: optional
---

# Task p2-4: Validate Gateway Startup and Slack Handshake

## Goal

Verify that the configured gateway starts successfully and can exchange messages with Slack from an allowlisted account.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `projects/robotville-v4.0/bmad/prd.md` | Confirm expected interaction behavior |
| `_bmad/rbtv/_admin/docs/mobile/server-env-template.md` | Ensure runtime variables are complete |
| `_bmad/rbtv/_admin/docs/mobile/robotville-vps-access.md` | Use approved remote execution method |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. Set `p2-4` to `in_progress` in the plan file.
3. Confirm test accounts and allowlist entries are ready.

### Phase: Execute

1. Start gateway process using configured runtime account.
2. Send Slack test command from allowlisted user.
3. Capture initial runtime logs and Slack response evidence.

### Phase: Validate

1. Confirm gateway starts without blocking errors.
2. Confirm allowlisted command receives response.
3. Confirm non-allowlisted behavior remains restricted as configured.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user for approval.
3. After approval, set `p2-4` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Connectivity validation evidence | `_bmad/rbtv/_admin/docs/mobile/` | Recorded handshake result summary |
