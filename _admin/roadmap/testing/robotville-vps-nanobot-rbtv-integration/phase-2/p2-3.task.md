---
task_id: p2-3
status: in_progress
phase: execute
complexity_score: 10
human_review: optional
---

# Task p2-3: Configure Nanobot Slack and LLM Connectivity

## Goal

Configure Nanobot with Slack Socket Mode, allowlist, and selected LLM provider credentials for controlled operation.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `projects/robotville-v4.0/bmad/prd.md` | Validate command/channel requirements |
| `projects/robotville-v4.0/bmad/architecture.md` | Ensure auth and integration boundaries are preserved |
| `_bmad/rbtv/_admin/docs/mobile/server-env-template.md` | Reuse approved credential and environment patterns |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. Set `p2-3` to `in_progress` in the plan file.
3. Confirm Slack app tokens, allowlist IDs, and provider credentials availability.

### Phase: Execute

1. Apply Slack channel configuration with Socket Mode tokens.
2. Apply LLM provider configuration and model defaults.
3. Configure allowlist policy for authorized users only.

### Phase: Validate

1. Verify configuration loads cleanly.
2. Verify unauthorized identity is blocked by policy.
3. Verify provider/auth configuration does not expose secrets in repository files.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user for approval.
3. After approval, set `p2-3` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Active Slack + provider config | VPS Nanobot configuration | Validated runtime configuration |
