---
task_id: p4-5
status: pending
phase: understand
complexity_score: 5
human_review: required
---

# Task p4-5: VALIDATE Multi-Turn Mentor Conversation

## Goal

Confirm that a multi-turn "mentor" conversation in Slack completes without `litellm.RateLimitError` after all optimization phases are deployed — proving the three-part fix (Sonnet, prompt caching, bootstrap slimming + memory window) achieves stable operation within rate limits.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `.cursor/plans/optimize-nanobot-token-usage/shape.md` | Prior task outcomes — confirm all patch scripts deployed and bootstrap files updated on VPS |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. In the plan file (`optimize-nanobot-token-usage.plan.md`), set this task's todo to `status: in_progress`.
3. Verify from shape.md that all prior phases are complete and deployed on VPS: model switch (p1-1), memory window reduction (p1-2), TOOLS.md slimming (p2-1), AGENTS.md slimming (p2-2), caching patch (p3-1), retry patch (p3-2).
4. If any phase is not yet deployed, stop and escalate to user before proceeding.

### Phase: Execute

1. Open Slack and send `mentor` to the Nanobot gateway.
2. Confirm mentor activation (RBTV mentor agent responds, not Nanobot default).
3. When the mentor presents a menu, select an option requiring at least one framework step (e.g., "N" for new project or continuing an existing milestone).
4. Send at least 3 follow-up messages to progress the conversation across multiple API calls.
5. Monitor Nanobot gateway logs (`journalctl -u nanobot-gateway -f`) on VPS for any `RateLimitError` occurrences.

**Pass criteria:**
- No `litellm.RateLimitError` during the conversation.
- Mentor correctly reads and updates `project-memo.md` state.
- Agent responses remain coherent and workflow-aligned across turns.

**Fail criteria:**
- Any `RateLimitError` → document model/window config on VPS, add remediation task.
- Mentor fails to activate → check workspace path and bootstrap file presence.

### Phase: Validate

1. Confirm pass/fail verdict against pass criteria above.
2. If pass: document evidence (brief log excerpt or confirmation) in shape.md.
3. If fail: document error details and add a new remediation task before marking complete.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) with pass/fail verdict and evidence. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Validation result | shape.md | Append-only execution entry with pass/fail and evidence |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Decisions and Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
