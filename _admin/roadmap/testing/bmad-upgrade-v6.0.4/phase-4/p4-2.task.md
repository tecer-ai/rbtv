---
task_id: p4-2
status: pending
phase: understand
complexity_score: 10
human_review: optional
---

# Task p4-2: Update bmad-architecture.md for v6.0.4

## Goal

Update the BMAD architecture reference document to reflect v6.0.4 patterns, ensuring new RBTV components are built with current BMAD conventions.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| workflows/build-rbtv-component/data/bmad-architecture.md | Current architecture doc to update |
| _admin/docs/BMAD-v6.0.4/_bmad/_config/manifest.yaml | v6.0.4 module versions and structure |
| .cursor/plans/bmad-upgrade-v6.0.4/shape.md | Prior execution context and findings |

---

## Tools

**Available tools:** Read `_bmad/rbtv/_config/tools-manifest.csv` — id, skill_path, subagent_path, description columns.

| Tool | Mode | Purpose |
|------|------|---------|
| context-distill | subagent | Distill v6.0.4 structural patterns from BMAD-v6.0.4 directory |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for all prior findings about v6.0.4 structure.
4. Compare current architecture doc against v6.0.4 reality.

### Phase: Execute

1. Use context-distill subagent to extract v6.0.4 structural patterns from `_admin/docs/BMAD-v6.0.4/`:
   - Directory structure conventions
   - Frontmatter standards
   - Module version info
   - Workflow naming patterns
2. Update bmad-architecture.md with v6.0.4 patterns:
   - Updated module versions (bmb 0.1.6, cis 0.1.8, tea 1.5.2)
   - Updated frontmatter standards if changed
   - Updated workflow conventions (split pattern, naming)
   - Updated path conventions

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify architecture doc accurately reflects v6.0.4 structure.
2. Verify no stale Beta.4 patterns remain.

### Phase: Close

1. Append execution entry to shape.md.
2. **MUST** present summary of architecture doc changes.
3. After user approval, set this task's todo to `status: completed` in the plan file.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated architecture reference | workflows/build-rbtv-component/data/bmad-architecture.md | Markdown |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
