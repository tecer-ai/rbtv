---
task_id: p2-1
status: pending
phase: understand
complexity_score: 10
human_review: required
---

# Task p2-1: Define Content Ownership Mapping Per Milestone

## Goal

UPDATE `founder-process.md` to add a content ownership mapping that defines which framework owns each concept (customer, problem, segment, value proposition, etc.) per milestone. This mapping becomes the reference for Prior Context sections and deduplication verification.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_bmad/rbtv/workflows/bi-business-innovation/data/founder-process.md` | Target file — understand current structure and framework sequence |
| `_bmad/rbtv/_admin/roadmap/todos/_claude-code-workspace/bi-workflow/prd-no-content-duplication-in-milestones.md` | Specification: content ownership rule, concept list, ownership logic |
| `../shape.md` | Prior decisions and constraints |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Identify the complete list of concepts that get repeated across frameworks (from the PRD): customer definition, problem statement, customer segment, value proposition, competitive landscape, jobs-to-be-done, assumptions.
5. For M1, determine the natural ownership order: Working Backwards defines customer/problem first, JTBD adds jobs, Competitive Landscape adds competitors, PSF synthesizes fit, Lean Canvas structures the model, Five Whys deepens root causes.

### Phase: Execute

1. UPDATE `_bmad/rbtv/workflows/bi-business-innovation/data/founder-process.md` — add `## Content Ownership Mapping` section with:
   - Purpose statement: "Defines which framework owns each concept. Later frameworks reference the owning framework's definition and add only deltas."
   - M1 ownership table: `| Concept | Owning Framework | Later Frameworks Reference + Add |`
   - M2 ownership table (if applicable — M2 frameworks are validation-focused, may have fewer shared concepts)
   - Rule statement: "When a framework needs to reference a concept it does not own, it MUST use the `## Prior Context` section format rather than restating the definition."

### Phase: Validate

1. Verify every M1 framework has at least one owned concept.
2. Verify no concept has multiple owners within a milestone.
3. Verify the mapping is consistent with the actual framework execution sequence.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present the ownership mapping to the user for review. This is a design decision that affects all downstream tasks.
3. After user approval, set this task's todo to `status: completed` in the plan file.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Content ownership mapping | `_bmad/rbtv/workflows/bi-business-innovation/data/founder-process.md` | Markdown table per milestone |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
