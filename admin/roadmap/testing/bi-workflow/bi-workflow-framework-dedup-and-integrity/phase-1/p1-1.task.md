---
task_id: p1-1
status: pending
phase: understand
complexity_score: 7
human_review: optional
---

# Task p1-1: Add Canonical Assumption Inventory to Project-Memo Template

## Goal

UPDATE `project-memo.md` template to include a `## Canonical Assumption Inventory` section with tiered categories (Existential / High / Lower / Founder Convictions), establishing the single source of truth for assumptions across all frameworks.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_bmad/rbtv/workflows/bi-business-innovation/templates/project-memo.md` | Current template — identify insertion point |
| `_bmad/rbtv/_admin/roadmap/todos/_claude-code-workspace/bi-workflow/cp-workflow-bi-default-structures.md` | Specification: canonical assumption inventory structure and tier definitions |
| `../shape.md` | Prior decisions and constraints |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Identify the correct insertion point in project-memo template (after `## Solution` / `## Tenets`, before `## Progress`).
5. Review the CP specification for the exact tier structure and section format.

### Phase: Execute

1. UPDATE `_bmad/rbtv/workflows/bi-business-innovation/templates/project-memo.md` — insert `## Canonical Assumption Inventory` section with:
   - Brief purpose statement (one line)
   - Subsections for each tier: `### Existential Assumptions`, `### High-Risk Assumptions`, `### Lower-Risk Assumptions`, `### Founder Convictions`
   - Each tier has a placeholder table: `| ID | Assumption | Source Framework | Status | Evidence |`
   - ID format: `A-{number}` (e.g., A-1, A-2)

### Phase: Validate

1. Verify the template is valid markdown with no syntax errors.
2. Verify the section is positioned logically within the document flow.
3. Verify tier categories match the CP specification.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated project-memo template | `_bmad/rbtv/workflows/bi-business-innovation/templates/project-memo.md` | Markdown with assumption inventory section |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
