---
task_id: p3-3
status: pending
phase: understand
complexity_score: 8
human_review: none
---

# Task p3-3: Add Deduplication Verification to Synthesis Steps

## Goal

UPDATE all 22 framework synthesis step files (M1-M4) to include a deduplication verification instruction that checks for restatement of concepts owned by prior frameworks before writing the synthesis output.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_bmad/rbtv/workflows/bi-business-innovation/data/founder-process.md` | Content ownership mapping (created in p2-1) — defines which concepts to check for duplication |
| `_bmad/rbtv/_admin/roadmap/todos/_claude-code-workspace/bi-workflow/prd-no-content-duplication-in-milestones.md` | Specification: dedup check logic, what constitutes duplication vs. delta |
| `../shape.md` | Prior decisions, p3-1 and p3-2 insertion patterns |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log — especially p3-1 and p3-2 insertion patterns for structural consistency.
4. Read the content ownership mapping from founder-process.md to understand which concepts are checked per framework.
5. Read 1-2 synthesis files to verify insertion point (before the assumption merging block — dedup check should happen before writing output, not after).

### Phase: Execute

1. For each of the 22 synthesis files, INSERT a deduplication verification instruction. Position it BEFORE the main synthesis output write (it's a pre-write check):
   ```
   ### Deduplication Verification

   Before writing the synthesis output, verify:
   1. Read the content ownership mapping in `founder-process.md` for this milestone.
   2. For each concept this framework does NOT own: confirm the synthesis output references the owning framework's definition rather than restating it.
   3. New insights and deltas are permitted — full restatements are not.
   4. If duplication is found, rewrite the affected section to use the `## Prior Context` reference format.
   ```

2. For the first framework in each milestone (the concept originator), adapt the instruction: "As the first framework in this milestone, you define foundational concepts. Later frameworks will reference your definitions."

### Phase: Validate

1. Verify all 22 files contain the dedup verification instruction.
2. Verify first-framework files have the originator variant.
3. Verify non-first-framework files have the checker variant.
4. Verify the instruction references `founder-process.md` ownership mapping correctly.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| 22 updated synthesis step files | Various paths within `_bmad/rbtv/workflows/` | Markdown with deduplication verification block |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
