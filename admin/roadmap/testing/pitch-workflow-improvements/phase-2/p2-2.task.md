---
task_id: p2-2
status: pending
phase: understand
complexity_score: 11
human_review: optional
---

# Task p2-2: Create step-10-pdf-validation.md

## Goal

CREATE `step-10-pdf-validation.md` — a new pitch workflow step that exports HTML to PDF via Decktape, takes page screenshots via Playwright MCP, reviews layout quality, and loops fixes until all pages pass visual QA.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_bmad/rbtv/_admin/roadmap/todos/_claude-code-workspace/pitch-workflow/cp-workflow-pitch-output-and-pdf-validation.md` | Source requirements: Decktape config, Playwright QA loop, acceptance criteria |
| `_bmad/rbtv/workflows/pitch/steps-c/step-09-synthesis.md` | Preceding step — understand output format and frontmatter structure to follow |
| `_bmad/rbtv/workflows/pitch/steps-c/step-07-generate.md` | Understand HTML output that feeds into PDF export |
| `_bmad/rbtv/workflows/pitch/workflow.md` | Understand step registration format and workflow architecture |
| `../shape.md` | Prior execution context |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file (the `.plan.md` in this plan's directory), set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Study step-09 structure (frontmatter, mandatory sequence, menu pattern, success criteria) — step-10 must follow the same conventions.
5. Confirm Decktape command format: `npx decktape generic [options] <input-html> <output-pdf>`.
6. Confirm Playwright MCP is available for screenshots.

### Phase: Execute

1. CREATE `step-10-pdf-validation.md` at the verified pitch workflow steps path, following the structure of existing steps (frontmatter, MANDATORY SEQUENCE, menu, success criteria).
2. Step frontmatter must include: `stepNumber: 10`, `stepName: 'pdf-validation'`, appropriate `nextStepFile` (none — this is the final step, or link back to synthesis completion).
3. Mandatory sequence sections:
   - **Export to PDF** — run Decktape via Shell tool against the HTML output from step-07
   - **Screenshot pages** — use Playwright MCP to capture each PDF page as an image
   - **Visual QA review** — review screenshots for layout issues (overflow, cut-off text, logo problems, alignment)
   - **Fix loop** — if issues found: fix HTML → re-export → re-screenshot → re-review (max 3 iterations)
   - **Completion** — present final PDF path to user
4. Include menu pattern consistent with other steps (Continue/Exit at minimum).
5. Include success criteria section.

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify step file follows existing step conventions (frontmatter fields, section ordering, menu format).
2. Verify Decktape command is syntactically correct.
3. Verify QA loop has explicit iteration limit (max 3).
4. Cross-check acceptance criteria from compound doc: step-10 runs after step-09, exports to PDF, screenshots pages, reviews layout, loops until clean.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| step-10-pdf-validation.md | `_bmad/rbtv/workflows/pitch/steps-c/step-10-pdf-validation.md` | Markdown — RBTV micro-file step format |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
