---
task_id: p4-4
status: pending
phase: understand
complexity_score: 8
human_review: optional
---

# Task p4-4: Migrate Cross-Module Path References

## Goal

Migrate ~60 files referencing `{project-root}/_bmad/core/` and `{project-root}/_bmad/bmm/` patterns to use `{bmad_core}` and `{bmad_bmm}` path variables from config.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/_claude-code-workspace/nanobot-standard-architecture/prd-reduce-path-resolution-hops.md` | PRD 5 spec — migration path and scope |
| `_admin/roadmap/todos/_claude-code-workspace/nanobot-standard-architecture/shape.md` | Execution context |
| `_config/config.yaml` | Verify paths: section exists with new variables |

---

## Tools

**Available tools:** Read `_config/tools-manifest.csv` for full list.

| Tool | Mode | Purpose |
|------|------|---------|
| context-distill | subagent | Find all files containing cross-module path patterns |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Audit: search for all occurrences of `{project-root}/_bmad/core/` and `{project-root}/_bmad/bmm/` across the codebase.
4. Catalog findings: file path, line number, current pattern, replacement pattern.
5. Exclude files under `_admin/docs/BMAD-mirror/` (read-only).

### Phase: Execute

1. Replace `{project-root}/_bmad/core/` with `{bmad_core}/` in all identified files.
2. Replace `{project-root}/_bmad/bmm/` with `{bmad_bmm}/` in all identified files.
3. Preserve `{project-root}/_bmad/rbtv/` references unchanged (self-resolving).
4. Process in batches by directory to manage context.
5. Maintain backwards compatibility: if any file is loaded without config, `{bmad_core}` must still be interpretable from context.

**Discovery Handling:**
- If file count differs significantly from ~60 estimate: document actual count in shape.md.
- If patterns are found in unexpected locations: assess impact before migrating.

### Phase: Validate

1. Search for remaining `{project-root}/_bmad/core/` and `{project-root}/_bmad/bmm/` patterns — expect zero results outside BMAD mirror.
2. Verify migrated files use `{bmad_core}/` and `{bmad_bmm}/` correctly.
3. Spot-check 5-10 migrated files for correctness.

### Phase: Close

1. Append execution entry to shape.md with actual file count migrated.
2. Present summary to user. Do not mark complete until user approves.
3. After approval, set todo to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Migrated files | ~60 files across RBTV repo | Updated path references |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
