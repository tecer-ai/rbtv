---
task_id: p5-8
status: pending
phase: understand
complexity_score: 6
human_review: none
---

# Task p5-8: Create M3 Milestone Workflow Complete Files

## Goal

CREATE the missing milestone workflow structure for M3 so that `workflow.md` + `steps-c/` is complete and `nextStep` resolves. The milestone workflow must present the framework menu (entry points to framework workflows); referral logic between milestone and framework workflows must be maintained.

---

## Referral Logic (MUST Maintain)

| Layer | Responsibility |
|-------|----------------|
| **Milestone workflow** | Entry points to framework workflows (menu). User selects a framework → load that framework's workflow. |
| **Framework workflow (last step)** | Update project_memo (stepsCompleted, synthesis); instruct user to return to milestone menu to pick next framework or [B] Back. |
| **project_memo** | Single source of truth for state (stepsCompleted, current milestone, synthesis from each framework). |

Do not break this flow: milestone = menu; framework synthesis = update project_memo + return to milestone for next choice.

---

## Context Files

| File | Purpose |
|------|---------|
| `workflows/bi-business-innovation/bi-m3/workflow.md` | Existing M3 milestone workflow (has nextStep pointing to missing file) |
| `workflows/bi-business-innovation/bi-m1/steps-c/step-01-init.md` | Reference: M1 milestone step pattern (after p3-8 complete) |
| `workflows/build-rbtv-component/templates/workflow-template.md` | BMAD workflow template (steps-c structure) |

---

## Execution Flow

### Phase: Execute

1. Create `workflows/bi-business-innovation/bi-m3/data/milestone-overview.md` (or equivalent) that:
   - Documents the recommended execution order from brand_process.md Steps 2-7
   - Order: Brand Archetypes → Brand Prism → Golden Circle → Brand Positioning → Messaging Architecture → Tone of Voice
   - Each framework includes "Recommended Order" field (1st-6th)
   - Includes framework dependencies and success criteria

2. Create `workflows/bi-business-innovation/bi-m3/steps-c/step-01-init.md` (or equivalent) that:
   - Section 1: Loads project state from project-memo.md frontmatter
   - Section 2: Analyzes progress with deterministic recommendation logic mapping stepsCompleted → recommended next framework
   - Section 3: Presents status summary with "Next Recommended" based on analysis
   - Section 4: Displays framework routing menu with `← Recommended next` indicator next to suggested framework
   - Shows prerequisite completion status (✓/✗) for each framework
   - Routes user selection to the corresponding framework workflow or [S] Status / [B] Back
   - Does not duplicate framework logic — only menu and routing

3. Ensure bi-m3/workflow.md frontmatter `nextStep: ./steps-c/step-01-init.md` resolves to this file.

4. Verify each M3 framework workflow's synthesis step updates project_memo and instructs return to M3 milestone menu (not direct load of another framework).

**Pattern Reference:** See shape.md "Decision: Add Recommended Next Step Indicator to Framework Menu" and M1 implementation at `workflows/bi-business-innovation/bi-m1/steps-c/step-01-init.md`

### Phase: Validate

1. nextStep resolves (file exists).
2. Referral logic intact: framework last steps → project_memo + return to milestone menu.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| M3 knowledge file | `workflows/bi-business-innovation/bi-m3/data/milestone-overview.md` | Markdown with recommended execution order |
| M3 milestone step (menu) | `workflows/bi-business-innovation/bi-m3/steps-c/step-01-init.md` | Markdown with recommendation logic, routing table, `← Recommended next` indicator, HALT for user input |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- **MANDATORY**: In output message, clearly state any tasks added or removed
