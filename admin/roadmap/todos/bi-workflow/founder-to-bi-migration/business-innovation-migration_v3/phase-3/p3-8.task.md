---
task_id: p3-8
status: pending
phase: understand
complexity_score: 6
human_review: none
---

# Task p3-8: Create M1 Milestone Workflow Complete Files

## Goal

CREATE the missing milestone workflow structure for M1 so that `workflow.md` + `steps-c/` is complete and `nextStep` resolves. The milestone workflow must present the framework menu (entry points to framework workflows); referral logic between milestone and framework workflows must be maintained.

---

## Referral Logic (MUST Maintain)

| Layer | Responsibility |
|-------|----------------|
| **Milestone workflow** | Entry points to framework workflows (menu). User selects a framework → load that framework's workflow. |
| **Framework workflow (last step)** | Update project_memo (stepsCompleted, synthesis); instruct user to return to milestone menu to pick next framework or [B] Back. |
| **project_memo** | Single source of truth for state (stepsCompleted, current milestone, synthesis from each framework). |

Do not break this flow: milestone = menu; framework synthesis = update project_memo + return to milestone for next choice.

---

## Context Files

| File | Purpose |
|------|---------|
| `workflows/bi-business-innovation/bi-m1/workflow.md` | Existing M1 milestone workflow (has nextStep pointing to missing file) |
| `workflows/bi-business-innovation/steps-c/step-03-milestone-select.md` | Reference: how master workflow routes to milestone |
| `workflows/build-rbtv-component/templates/workflow-template.md` | BMAD workflow template (steps-c structure) |

---

## Execution Flow

### Phase: Execute

1. Create `workflows/bi-business-innovation/bi-m1/steps-c/step-01-init.md` (or equivalent) that:
   - Displays the framework routing menu (same options as in bi-m1/workflow.md body)
   - Routes user selection to the corresponding framework workflow or [S] Status / [B] Back
   - Does not duplicate framework logic — only menu and routing
2. Ensure bi-m1/workflow.md frontmatter `nextStep: ./steps-c/step-01-init.md` resolves to this file.
3. Verify each M1 framework workflow's synthesis step updates project_memo and instructs return to M1 milestone menu (not direct load of another framework).

### Phase: Validate

1. nextStep resolves (file exists).
2. Referral logic intact: framework last steps → project_memo + return to milestone menu.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| M1 milestone step (menu) | `workflows/bi-business-innovation/bi-m1/steps-c/step-01-init.md` | Markdown with routing table, HALT for user input |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- **MANDATORY**: In output message, clearly state any tasks added or removed
