---
task_id: p1-1
status: pending
phase: understand
complexity_score: 11
human_review: required
---

# Task p1-1: Update BMAD Mirror from Beta.4 to Beta.8

## Goal

Replace BMAD mirror content (`_admin/docs/BMAD-mirror/`) from 6.0.0-Beta.4 to 6.0.0-Beta.8, preserving the empty `` slot and any RBTV-owned metadata files.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/_claude-code-workspace/nanobot-standard-architecture/shape.md` | Constraints and scope boundaries |
| `_admin/roadmap/todos/_claude-code-workspace/nanobot-standard-architecture/prd-config-bmad-version-declaration.md` | PRD 4 spec — prerequisite action: update mirror |
| `_admin/docs/BMAD-mirror/_config/manifest.yaml` | Current mirror version (Beta.4) |

---

## Tools

**Available tools:** Read `_config/tools-manifest.csv` for full list.

| Tool | Mode | Purpose |
|------|------|---------|
| web-research | subagent | Get Beta.8 release content from GitHub |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review shape.md execution log for prior context.
4. Identify current mirror structure and version.
5. Access BMAD-METHOD Beta.8 release at `https://github.com/bmad-code-org/BMAD-METHOD/releases/tag/v6.0.0-Beta.8`.

### Phase: Execute

1. Download or access Beta.8 release content.
2. Replace `_admin/docs/BMAD-mirror/` content with Beta.8 files.
3. Preserve the empty `_admin/docs/BMAD-mirror/` directory (RBTV's slot).
4. Preserve any RBTV-owned metadata files already in the mirror folder.
5. Verify `_admin/docs/BMAD-mirror/_config/manifest.yaml` shows Beta.8 version.

**Discovery Handling:**
- If Beta.8 introduces structural changes not anticipated in shape.md: document in shape.md, assess impact.
- If Beta.7 workflow splitting is confirmed: document for p1-2 consumption.

### Phase: Validate

1. Confirm manifest.yaml version reads `6.0.0-Beta.8`.
2. Verify `` slot remains empty.
3. Verify no RBTV files were accidentally overwritten.
4. Spot-check key BMAD paths referenced in shape.md dependency graph.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user (max 2000 chars). Do not mark complete until user approves.
3. After approval, set todo to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated BMAD mirror | `_admin/docs/BMAD-mirror/` | Directory tree matching Beta.8 release |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
