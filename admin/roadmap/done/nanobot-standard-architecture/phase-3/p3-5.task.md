---
task_id: p3-5
status: pending
phase: understand
complexity_score: 9
human_review: optional
---

# Task p3-5: Implement Sync Mode

## Goal

Implement the sync mode in the unified installer — patches BMAD configs for RBTV only. Does NOT generate entry_points, create IDE config, or modify nanobot files.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/_claude-code-workspace/nanobot-standard-architecture/structured-problem-2026-02-22.md` | Sync scope definition (Section: Tooling 2.1, 2.2, 2.3) |
| `_admin/roadmap/todos/_claude-code-workspace/nanobot-standard-architecture/shape.md` | Execution context, sync script decisions |
| `_config/install-rbtv.py` | Unified installer — target for sync mode implementation |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above.
2. Set this task's todo to `status: in_progress` in the plan file.
3. Review sync mode scope from structured problem: BMAD config patching only.
4. Identify which shared functions sync mode calls (BMAD config updates, output-path normalization, help catalog).
5. Confirm sync mode does NOT: generate entry_points.md, create .cursor/.claude config, run admin-specific logic.

### Phase: Execute

1. Implement sync mode entry point in unified installer.
2. Sync mode calls shared functions only: BMAD config updates, output-path normalization, version check.
3. Add CLI argument/flag for sync mode selection.
4. Sync mode must work from nanobot workspace context (RBTV is at `` relative to workspace root).

**Discovery Handling:**
- If sync mode needs additional BMAD config patches not covered by shared functions: implement and document.

### Phase: Validate

1. Verify sync mode patches `core/config.yaml` output paths.
2. Verify sync mode patches `bmm/config.yaml` output paths.
3. Verify sync mode adds RBTV entry to `_config/bmad-help.csv`.
4. Verify sync mode does NOT create .cursor/, .claude/, or .vscode/ artifacts.
5. Verify sync mode runs pre-flight version check.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user. Do not mark complete until user approves.
3. After approval, set todo to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated installer | `_config/install-rbtv.py` | Python (sync mode implemented) |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
