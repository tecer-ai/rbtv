---
task_id: p2-1
status: pending
phase: understand
complexity_score: 5
human_review: none
---

# Task p2-1: UPDATE plan-creation-rules.md

## Goal

Replace hardcoded subagent types list in the "Agent Invocation in Tasks" section with a reference to `tools-manifest.csv`.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `.cursor/plans/rbtv-tools-manifest/shape.md` | Scope and decisions |
| `workflows/plan-lifecycle/data/plan-creation-rules.md` | File to update |
| `tools-manifest.csv` | New manifest to reference |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file (the `.plan.md` in this plan's directory), set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Locate the "Agent Invocation in Tasks" section (lines ~117-131) in plan-creation-rules.md.
5. Identify the hardcoded subagent types list (`judge`, `generalPurpose`, `explore`).

### Phase: Execute

1. Replace the hardcoded "Available subagent types" line with a reference to `tools-manifest.csv` for the complete list of tool ids.
2. Add instruction: skills are invoked by reading the skill_path; subagents via Task tool with `subagent_type='<id>'`.
3. Preserve the existing rules about explicit mechanisms and avoiding ambiguous verbs.

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify deliverable meets goal statement.
2. Confirm no hardcoded tool lists remain in the file.
3. Confirm the manifest reference path is correct.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated rules file | `workflows/plan-lifecycle/data/plan-creation-rules.md` | Markdown |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
