---
task_id: p2-1
status: pending
phase: understand
complexity_score: 8
human_review: optional
---

# Task p2-1: CREATE Universal Shape Template

## Goal

Create a universal shape template at `_shared/templates/shape-template.md` by generalizing the plan-lifecycle shape template. The universal template must work for any interactive session (not just plans) with conditional plan-specific sections.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `workflows/plan-lifecycle/templates/shape-template.md` | Current plan-specific shape template — base to generalize |
| `_admin/roadmap/todos/_claude-code-workspace/context-preservation/cp-rule-context-preservation.md` | CP compound sections: "Shape.md as Universal Fallback" and "How This Affects Plan-Lifecycle" — universal template requirements |
| `../shape.md` | Plan shaping decisions and constraints |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Identify which sections of the current shape template are plan-specific vs. universal.
5. Review the CP compound's shape.md section table for required universal sections.

### Phase: Execute

1. CREATE directory `_shared/templates/` (if it does not exist).
2. CREATE `_shared/templates/shape-template.md` with:
   - Universal sections that apply to any interactive session (Scope Definition, Key Decisions, Constraints, User Inputs, Collaborative Decisions, Decisions and Discoveries, References)
   - Plan-specific sections marked as conditional (Standards Applied, Tool Mode Selection) with clear markers indicating when to include them
   - The CP compound's shape.md section table as the authoritative section list
   - Living document principle and append-only rules preserved
3. Ensure the template handles the edge case of freeform session naming (sessions without a named output) — include guidance for naming convention (e.g., `{date}-{topic}-shape.md`).

### Phase: Validate

1. Verify all universal sections from the CP compound's shape.md table are present.
2. Verify plan-specific sections are clearly marked as conditional.
3. Verify the template is self-contained (usable without reading any other file).
4. Verify freeform session naming convention is documented.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Universal shape template | `_shared/templates/shape-template.md` | Markdown template with conditional sections |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
