---
task_id: p2-2
status: pending
phase: understand
complexity_score: 11
human_review: required
---

# Task p2-2: CREATE Context Preservation Cursor Rule

## Goal

Create `.cursor/rules/bmad-rbtv-context-preservation.mdc` — a standalone Cursor rule implementing the Detect→Discover→Confirm→Capture sequence for preserving rich session context across interactive sessions.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/_claude-code-workspace/context-preservation/cp-rule-context-preservation.md` | Full CP compound — Proposed Solution (all 4 behaviors), detection signals, shape.md fallback, living document principle, quality review observations |
| `_admin/roadmap/todos/_claude-code-workspace/context-preservation/prd-rbtv-memory-system.md` | Memory System PRD — understand relationship (complementary, not overlapping) for the rule's "Relationship to Memory System" section |
| `.cursor/rules/bmad-rbtv-chat-discipline.mdc` | Existing rule — reference for `.mdc` file structure and formatting conventions |
| `_shared/templates/shape-template.md` | Universal shape template created in p2-1 — reference for shape.md fallback instructions |
| `../shape.md` | Plan shaping decisions and constraints |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Study the CP compound's "Proposed Solution" section — all 4 behaviors (Detect, Discover, Confirm, Capture).
5. Note the 5 quality review observations (compound lines 314-327) — observations 4 and 5 are edge cases to address in the rule.

### Phase: Execute

1. CREATE `.cursor/rules/bmad-rbtv-context-preservation.mdc` with these sections:
   - **Detection signals:** 8 explicit context accumulation signals (table from compound). Trigger threshold: 2+ signals across one or more turns (addresses QR observation 5 — changed from "multiple turns" to "one or more turns")
   - **Non-trigger criteria:** Single-turn tasks, simple edits, sessions with only task instructions
   - **Discover:** Target system identification, read conventions (CLAUDE.md, memory system, documentation standards), map context to destinations
   - **Confirm:** Agent proposes capture mechanism to user, user approves/redirects/declines
   - **Capture:** Continuous writes to confirmed destination(s), follow target system format, shape.md fallback using universal template at `_shared/templates/shape-template.md`
   - **Living document principle:** Table of required update behaviors (from compound)
   - **Shape.md naming convention:** `{output-name}-shape.md` for workflow outputs; `{date}-{topic}-shape.md` for freeform sessions (addresses QR observation 4)
   - **Relationship to Memory System:** Complementary table (from compound) — memory captures reusable knowledge, CP captures session context
2. Ensure the rule is standalone — fully interpretable without reading any other file.

### Phase: Validate

1. Verify rule covers all 13 acceptance criteria from the CP compound (lines 274-286).
2. Verify rule uses mandatory language per atomic files rule.
3. Verify rule follows `.mdc` conventions from the reference rule.
4. Verify both QR edge cases are addressed (observations 4 and 5).
5. Verify rule explicitly states it does NOT trigger for low-context sessions.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Context preservation rule | `.cursor/rules/bmad-rbtv-context-preservation.mdc` | Cursor rule file (`.mdc`) |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
