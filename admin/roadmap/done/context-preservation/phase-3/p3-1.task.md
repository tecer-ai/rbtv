---
task_id: p3-1
status: pending
phase: understand
complexity_score: 8
human_review: optional
---

# Task p3-1: UPDATE Plan-Lifecycle Step-04 for Shape.md Merge Logic

## Goal

Modify `plan-lifecycle/steps-c/step-04-generate-artifacts.md` to check for an existing shape.md before creating one. If shape.md already exists (created by the context preservation rule during planning), merge planning context into it instead of overwriting.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `workflows/plan-lifecycle/steps-c/step-04-generate-artifacts.md` | Current step-04 — understand existing shape.md creation flow (section 4: "Write shape.md") |
| `_admin/roadmap/todos/_claude-code-workspace/context-preservation/cp-rule-context-preservation.md` | CP compound section "How This Affects Plan-Lifecycle" — Conflict 1 resolution: merge logic spec |
| `_shared/templates/shape-template.md` | Universal template created in p2-1 — the new template source |
| `../shape.md` | Plan shaping decisions and constraints |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Study step-04's section 4 ("Write shape.md") to understand the current creation flow.
5. Study the CP compound's "Conflict 1: Creation timing" resolution.

### Phase: Execute

1. UPDATE step-04's section 4 ("Write shape.md") to add merge logic:
   - **Check first:** Before creating shape.md, check if one already exists in the plan folder
   - **If exists:** Read existing shape.md, merge planning context (Original Shaping, Standards Applied) into it — preserve any content already written by the context preservation rule
   - **If not exists:** Create from the universal template (update template reference to `_shared/templates/shape-template.md`)
2. UPDATE the frontmatter `shapeTemplateFile` reference if step-04 uses it directly.

### Phase: Validate

1. Verify merge logic handles both cases (shape.md exists vs. doesn't exist).
2. Verify the template reference points to the universal template, not the old plan-specific one.
3. Verify no other sections of step-04 are broken by the changes.
4. Verify the step's success criteria still hold.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Modified step-04 | `workflows/plan-lifecycle/steps-c/step-04-generate-artifacts.md` | Markdown — modified in place |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
