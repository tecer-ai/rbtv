---
task_id: p1-4
status: pending
phase: understand
complexity_score: 8
human_review: optional
---

# Task p1-4: UPDATE bootstrap.py to Protect Memory Directory

## Goal

Modify `_config/bootstrap.py` to ensure the RBTV installer does not overwrite, delete, or modify the `.claude/memory/` directory during install, sync, or any other operation.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_config/bootstrap.py` | Installer source — understand install/sync flow, identify where protection logic belongs |
| `_admin/roadmap/todos/_claude-code-workspace/context-preservation/prd-rbtv-memory-system.md` | PRD sections: "Non-Committed, User-Specific Storage" and "Dependencies" — protection requirements |
| `../shape.md` | Plan shaping decisions and constraints |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Map the installer's operation flow: identify which operations touch `.claude/` and could affect `memory/`.
5. Identify the safest insertion point for protection logic.

### Phase: Execute

1. UPDATE `_config/bootstrap.py` to:
   - Skip `.claude/memory/` during any file sync, copy, or clean operations
   - Preserve the directory and all its contents across install/sync/update runs
   - Follow existing patterns in bootstrap.py for exclusion logic (if any exist)

### Phase: Validate

1. Verify `.claude/memory/` is explicitly excluded from installer operations.
2. Verify no other installer code path could inadvertently affect the memory directory.
3. Verify the protection logic follows existing bootstrap.py patterns and conventions.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Modified installer | `_config/bootstrap.py` | Python — modified in place |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
