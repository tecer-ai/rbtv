---
task_id: p1-2
status: pending
phase: understand
complexity_score: 8
human_review: optional
---

# Task p1-2: CREATE Reorganize Memory Skill

## Goal

Create a "reorganize memory" skill that implements the 7-step maintenance protocol from the Memory System PRD. The skill is invocable when the user says "reorganize memory."

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/_claude-code-workspace/context-preservation/prd-rbtv-memory-system.md` | PRD "Maintenance Command" section — the 7-step protocol |
| `.cursor/rules/bmad-rbtv-memory-system.mdc` | Memory rule created in p1-1 — understand the system the skill operates on |
| `_config/tools-manifest.csv` | Existing tool registry — understand registration patterns for skills |
| `../shape.md` | Plan shaping decisions and constraints |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Study the PRD's "Maintenance Command" section for the exact 7 steps.
5. Study the tools-manifest.csv to understand skill registration patterns (skill_path column, naming conventions).

### Phase: Execute

1. CREATE the reorganize memory skill file at the appropriate location following RBTV skill conventions.
2. The skill MUST implement these 7 steps in order:
   - Read all memory files
   - Remove duplicates and outdated entries
   - Merge entries that belong together
   - Split files that cover too many topics
   - Re-sort entries by date within each file
   - Update `memory.md` index
   - Show a summary of what changed
3. UPDATE `_config/tools-manifest.csv` to register the new skill (if registration is required by the manifest pattern).

### Phase: Validate

1. Verify the skill implements all 7 maintenance steps from the PRD.
2. Verify the skill follows existing RBTV skill conventions (SKILL.md format, directory structure).
3. Verify registration in tools-manifest.csv if applicable.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Reorganize memory skill | Location determined by RBTV skill conventions | SKILL.md + supporting files |
| Tools manifest update | `_config/tools-manifest.csv` | CSV row addition (if pattern requires) |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
