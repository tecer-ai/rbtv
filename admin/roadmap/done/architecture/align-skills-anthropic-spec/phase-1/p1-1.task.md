---
task_id: p1-1
status: pending
phase: understand
complexity_score: 7
human_review: none
---

# Task p1-1: UPDATE ide-loader-template.md Skill Section with Anthropic-Aligned Spec

## Goal

Update the skill template in `ide-loader-template.md` to document Anthropic's optional YAML frontmatter fields, enforce name == folder name, and note optional subdirectories.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `workflows/build-rbtv-component/templates/ide-loader-template.md` | Current template to update |
| `../shape.md` | Plan context and decisions |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Identify the exact sections in ide-loader-template.md that need changes: Section 2 (Skill Templates), Naming Conventions table, Size Guidelines, Critical Rules.

### Phase: Execute

1. In Section 2 (Skill Templates), update both skill template variants:
   - Change `name: {name}` to `name: {system}-{module}-{name}` with a comment that name MUST match parent folder name
   - Add optional fields block after description showing: `license`, `compatibility`, `metadata` (with `author`, `version`), `allowed-tools`
   - Add a comment noting these fields are optional per Anthropic Claude Skills spec
2. Add a note about optional subdirectories (`scripts/`, `references/`, `assets/`) below the skill templates, stating RBTV skills are thin loaders by default but these dirs are allowed
3. In Critical Rules section, add three new rules:
   - `name` field MUST match parent folder name exactly (e.g., folder `bmad-rbtv-doc` → `name: bmad-rbtv-doc`)
   - No `README.md` inside skill folders
   - No XML angle brackets (`< >`) in YAML frontmatter (security restriction)
4. In Naming Conventions table, add note that skill `name` field must equal the folder name

### Phase: Validate

1. Verify the updated template is syntactically valid markdown
2. Verify both skill template variants (Agent and Workflow/Task) include the new fields
3. Verify critical rules section contains all three new rules
4. Verify no content repetition with other files

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated skill template | `workflows/build-rbtv-component/templates/ide-loader-template.md` | Markdown |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
