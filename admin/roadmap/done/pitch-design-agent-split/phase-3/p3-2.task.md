---
task_id: p3-2
status: pending
phase: understand
complexity_score: 10
human_review: none
---

# Task p3-2: Update Step 07 for Design Agent Invocation

## Goal

Update the merged pitch workflow's step-07-generate.md so it is invoked by the design agent instead of the narrative agent, adding design persona reinforcement and removing narrative agent assumptions.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `workflows/pitch-creation/steps-c/step-07-generate.md` | Current merged step file to update |
| `workflows/pitch-creation/steps-c/step-06-structure.md` | Handoff boundary — verify step 06 output is consumed correctly |
| `agents/designer.md` | Design agent persona — extract reinforcement text |
| `workflows/_shared/pitch-data/html-patterns.md` | Design knowledge already loaded by step 07 |
| `workflows/_shared/pitch-data/html-components.md` | Design knowledge already loaded by step 07 |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context — especially p3-1 handoff instruction.
4. Identify all narrative agent-specific content in step-07 (persona references, stress-testing language, VC/procurement framing).

### Phase: Execute

1. UPDATE `workflows/pitch-creation/steps-c/step-07-generate.md`:
   - **Add design persona reinforcement** at the top of the step (role reminder consistent with designer.md persona)
   - **Remove narrative agent assumptions** — any references to "as the VC partner" or "as the procurement VP" or similar
   - **Preserve all design knowledge loading** — html-patterns.md, html-components.md references stay
   - **Preserve all HTML generation logic** — slide structure, CSS, print optimization, icon libraries
   - **Add context loading instruction** — step 07 must load the structure document output from step 06 as its primary input
   - **Ensure `pitch_type` conditionals** from the merge are preserved
   - **Update step navigation** — verify nextStepFile points to step-08

### Phase: Validate

1. Verify no narrative agent persona references remain
2. Verify design persona reinforcement is present
3. Verify all design knowledge file references are intact
4. Verify step 06 output (structure document) is explicitly loaded
5. Verify `pitch_type` conditionals still work

### Phase: Close

1. Append execution entry to shape.md.
2. **MUST** present summary of changes to user.
3. After user approval, set this task's todo to `status: completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated step-07 | `workflows/pitch-creation/steps-c/step-07-generate.md` | Markdown step file with design agent framing |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
