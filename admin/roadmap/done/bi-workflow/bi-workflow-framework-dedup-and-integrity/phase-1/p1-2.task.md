---
task_id: p1-2
status: pending
phase: understand
complexity_score: 11
human_review: required
---

# Task p1-2: Bulk Rename `founder/` to `business-innovation/` Across BI Workflows

## Goal

UPDATE all BI workflow files — rename `founder/` path references to `business-innovation/` across 40+ files. Replacements must be path-specific only; preserve non-path uses of "founder" (e.g., "founder/team" in interview context, "Founder Convictions" label).

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/_claude-code-workspace/bi-workflow/cp-workflow-bi-default-structures.md` | Specification: folder rename details |
| `../shape.md` | Prior decisions, constraints, file list from exploration |

---

## Tools

| Tool | Mode | Purpose |
|------|------|---------|
| explore | subagent | Re-verify complete list of files containing `founder/` before bulk replacement6 |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Use Task tool with `subagent_type='explore'` to grep for `founder/` across `` — capture every file path and line number.
5. Classify each match:
   - **Path reference** (replace): patterns like `{bmad_output}/{project-name}/founder/`, `{output_folder}/founder/`, `outputFolder: '...founder'`
   - **Non-path usage** (preserve): patterns like `founder/team`, `Founder Convictions`, prose mentions

### Phase: Execute

File groups to update (all within ``):

1. **Main BI workflow:** `workflows/bi-business-innovation/workflow.md` — frontmatter `outputFolder`
2. **Project setup:** `workflows/bi-business-innovation/steps-c/step-01-project-setup.md` — folder paths and mkdir commands
3. **Milestone select:** `workflows/bi-business-innovation/steps-c/step-02-milestone-select.md` — `projectMemo` frontmatter
4. **Founder process:** `workflows/bi-business-innovation/data/founder-process.md` — path references
5. **Project-memo template:** `workflows/bi-business-innovation/templates/project-memo.md` — `{output_folder}/founder/` references
6. **M1 workflows:** `bi-m1/workflow.md`, `bi-m1/steps-c/step-01-init.md`, all 6 framework `workflow.md` files
7. **M2 workflows:** `bi-m2/workflow.md`, `bi-m2/steps-c/step-01-init.md`, all 6 framework `workflow.md` files, `bi-m2-leap-of-faith/steps-c/step-01-init.md`
8. **M3 workflows:** `bi-m3/workflow.md`, `bi-m3/steps-c/step-01-init.md`, all 7 framework `workflow.md` files
9. **M4 workflows:** `bi-m4/workflow.md`, `bi-m4/steps-c/step-01-init.md`, all 4 framework `workflow.md` files, heuristic-evaluation steps
10. **Pitch workflow:** `workflows/pitch/steps-c/step-02-context-gather.md`
11. **Config/tasks:** `_config/bootstrap.py`, `tasks/mentor-help.xml`, `tasks/quality-review.xml`, `tasks/update-bmad-config.xml`, `tasks/data/quality-evaluator-atomic-files.md`

For each file: replace path-pattern `founder/` with `business-innovation/` and `founder'` (end-of-value in YAML) with `business-innovation'`. Skip non-path occurrences.

### Phase: Validate

1. Grep `` for remaining `founder/` — every match must be a non-path usage (prose, labels).
2. Grep for `business-innovation/` to verify replacements landed correctly.
3. Spot-check 5 files from different groups to verify YAML frontmatter is syntactically valid.

### Phase: Close

1. Append execution entry to shape.md with count of files modified and any non-path occurrences preserved.
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| 40+ updated files | Various paths within `` | Path references changed from `founder/` to `business-innovation/` |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
