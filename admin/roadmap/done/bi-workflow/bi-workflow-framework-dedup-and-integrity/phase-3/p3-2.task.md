---
task_id: p3-2
status: pending
phase: understand
complexity_score: 10
human_review: optional
---

# Task p3-2: Add Conditional Consistency Gate to Synthesis Steps

## Goal

UPDATE all 22 framework synthesis step files (M1-M4) to include a conditional non-blocking recommendation to run a Party Mode cross-framework consistency review when ≥3 frameworks are completed in the current milestone.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/_claude-code-workspace/bi-workflow/cp-workflow-bi-cross-framework-consistency-gate.md` | Full specification: trigger, behavior, prompt template, conditional display logic |
| `workflows/bi-business-innovation/templates/project-memo.md` | `stepsCompleted` frontmatter field used for framework count |
| `../shape.md` | Prior decisions, list of all 22 synthesis files |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context — especially p3-1's insertion pattern.
4. Review the CP specification for the exact conditional display logic and copy-paste prompt.
5. Read 1-2 synthesis files to verify insertion point (after assumption merging block added in p3-1, before completion menu).

### Phase: Execute

1. For each of the 22 synthesis files, INSERT a conditional consistency gate block after the assumption merging instruction (from p3-1):
   ```
   ### Cross-Framework Consistency Gate

   **Condition:** Display this section only when ≥3 frameworks are marked completed in the project-memo `stepsCompleted` array for the current milestone.

   > **Recommended:** You have completed 3+ frameworks in this milestone. Consider running a cross-framework consistency review in a fresh context to detect drift between framework outputs.
   >
   > **How:** Open a new conversation and paste the following prompt:
   >
   > "Review the following framework outputs for [project-name] milestone [M-number] and identify contradictions, unstated assumption changes, or concept drift between frameworks: [list completed framework output files]"
   >
   > This is non-blocking — you may continue without running the review.
   ```

2. Adapt the conditional display instruction per file to match the file's existing structure and voice.

### Phase: Validate

1. Verify all 22 files contain the consistency gate block.
2. Verify the gate block appears after assumption merging and before completion menu.
3. Verify the conditional trigger references `stepsCompleted` correctly.
4. Verify the prompt template is consistent across all files.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| 22 updated synthesis step files | Various paths within `workflows/` | Markdown with conditional consistency gate block |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
