---
task_id: p4-1
status: pending
phase: understand
complexity_score: 8
human_review: none
---

# Task p4-1: Update bmad-compat.yaml for v6.0.4

## Goal

Update all compatibility metadata in `bmad-compat.yaml` to reflect v6.0.4 reality: version, paths, touchpoint entries, and output folder configurability.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| bmad-compat.yaml | Current compatibility metadata — all sections to update |
| _admin/roadmap/todos/_claude-code-workspace/bmad-upgrade-v6.0.4/prd-bmad-upgrade-v6.0.4.md | PRD change #10 — update requirements |
| .cursor/plans/bmad-upgrade-v6.0.4/shape.md | Phase 1 investigation findings and Phase 2-3 execution context |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for all investigation findings and execution decisions.
4. Identify every section of bmad-compat.yaml that needs updating.

### Phase: Execute

1. Update version fields:
   - `bmad_target_version` → `6.0.4`
   - `bmad_min_version` → `6.0.0`
2. Update all path references that changed per Phase 1 findings (BMM workflow paths, advanced elicitation path).
3. Update agent_references and task_references entries to match v6.0.4 paths.
4. Document that output folder name is user-configurable (no longer hardcoded).
5. Update any format_assumption entries that changed (e.g., bmad-help.csv schema).

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify all version references show 6.0.4.
2. Verify all documented paths match v6.0.4 structure.
3. Verify no stale Beta.4 references remain.

### Phase: Close

1. Append execution entry to shape.md.
2. **MUST** present summary of all sections updated.
3. After user approval, set this task's todo to `status: completed` in the plan file.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated compatibility metadata | bmad-compat.yaml | YAML |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
