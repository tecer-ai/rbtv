---
task_id: p3-5
status: pending
phase: understand
complexity_score: 10
human_review: none
---

# Task p3-5: Bulk Replace Literal projects in Workflow and Task Files

## Goal

Replace all literal `projects` references in 54 workflow files and 2 task files with the `{bmad_output}` path variable.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| _admin/roadmap/todos/_claude-code-workspace/bmad-upgrade-v6.0.4/prd-bmad-upgrade-v6.0.4.md | PRD change #11 — scope of literal projects references |
| .cursor/plans/bmad-upgrade-v6.0.4/shape.md | Prior execution context, decisions about replacement strategy |

---

## Tools

**Available tools:** Read `_config/tools-manifest.csv` — id, skill_path, subagent_path, description columns.

| Tool | Mode | Purpose |
|------|------|---------|
| explore | subagent | Find all files containing literal `projects` |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for prior decisions about output folder variable naming.
4. Confirm the replacement pattern: literal `projects` → `{bmad_output}` path variable.

### Phase: Execute

1. Use explore subagent to find all files under `` containing literal `projects` (excluding `_admin/docs/` directories which are reference copies).
2. For each file found, replace literal `projects` occurrences with `{bmad_output}` using the appropriate path variable syntax for the file context.
3. Handle context-sensitive replacements:
   - In path strings: `projects` → `{bmad_output}`
   - In YAML values: `"{project-root}/projects"` → `"{bmad_output}"`
   - Preserve surrounding path structure (e.g., `/{project-name}` suffixes)

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Re-search for any remaining literal `projects` in workflow and task files (excluding _admin/docs/).
2. Verify replacement count matches expected ~80+ occurrences across ~56 files.
3. Spot-check 3-5 files to confirm replacements are contextually correct.

### Phase: Close

1. Append execution entry to shape.md.
2. **MUST** present summary: files modified count, occurrences replaced, any anomalies.
3. After user approval, set this task's todo to `status: completed` in the plan file.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated workflow files | workflows/**/*.md | Markdown with path variables |
| Updated task files | tasks/**/*.xml | XML/Markdown with path variables |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
