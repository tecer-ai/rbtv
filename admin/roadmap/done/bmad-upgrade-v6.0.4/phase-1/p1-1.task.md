---
task_id: p1-1
status: pending
phase: understand
complexity_score: 7
human_review: none
---

# Task p1-1: Inspect v6.0.4 Directory Structures vs Mirror

## Goal

Compare BMM workflow paths, advanced elicitation path, and core directory structure between the Beta.4 mirror and BMAD v6.0.4 to determine which RBTV references are broken.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| _admin/roadmap/todos/_claude-code-workspace/bmad-upgrade-v6.0.4/prd-bmad-upgrade-v6.0.4.md | PRD sections 1-3 (touchpoints at risk) — specific paths to verify |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Note the 4 specific path groups to compare:
   - 3 BMM workflow paths referenced in ana.md
   - 1 advanced elicitation path referenced in doc-compound-learning workflow

### Phase: Execute

1. **BMM Workflow Paths** — List contents of these directories in BOTH mirror and v6.0.4:
   - `_admin/docs/BMAD-v6.0.4/bmm/workflows/1-analysis/create-product-brief/`
   - `_admin/docs/BMAD-v6.0.4/bmm/workflows/2-plan-workflows/create-prd/`
   - `_admin/docs/BMAD-v6.0.4/bmm/workflows/2-plan-workflows/create-ux-design/`
   - Same 3 paths in `_admin/docs/BMAD-mirror/`
   - Document: Does `workflow.md` still exist in v6.0.4, or was it renamed/split?

2. **Advanced Elicitation Path** — Check both locations:
   - `_admin/docs/BMAD-v6.0.4/core/workflows/advanced-elicitation/`
   - `_admin/docs/BMAD-mirror/core/workflows/advanced-elicitation/`
   - Document: Does `workflow.xml` still exist, or was it converted to `.md`?

3. **Core Directory Structure** — Verify top-level directories still exist:
   - `_admin/docs/BMAD-v6.0.4/_config/`
   - `_admin/docs/BMAD-v6.0.4/core/`
   - `_admin/docs/BMAD-v6.0.4/bmm/`

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. For each path checked, document whether it: exists unchanged, was renamed, was split, or was removed.
2. Flag any paths that require RBTV updates.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user of findings for each path group.
3. After user approval, set this task's todo to `status: completed` in the plan file.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Path comparison findings | shape.md execution log entry | Append-only entry with results per path group |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
