---
task_id: p2-1
status: pending
phase: understand
complexity_score: 12
human_review: optional
---

# Task p2-1: Refactor normalize_bmad_output_paths()

## Goal

Refactor the `normalize_bmad_output_paths()` function in `install-rbtv.py` to read and preserve the user-chosen output folder name instead of hardcoding `projects`.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| _config/install-rbtv.py | Current installer code — focus on `normalize_bmad_output_paths()` function |
| _admin/roadmap/todos/_claude-code-workspace/bmad-upgrade-v6.0.4/prd-bmad-upgrade-v6.0.4.md | PRD section 8 (Output Folder Name Preservation) — detailed requirements |
| .cursor/plans/bmad-upgrade-v6.0.4/shape.md | Prior execution context from Phase 1 investigation |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for Phase 1 investigation findings.
4. Understand the current `normalize_bmad_output_paths()` implementation:
   - How it reads `core/config.yaml` and `bmm/config.yaml`
   - What regex patterns it uses to rewrite `output_folder`, `planning_artifacts`, `implementation_artifacts`
   - Where it hardcodes `projects`

### Phase: Execute

1. Refactor `normalize_bmad_output_paths()` to:
   - Read the existing `output_folder` value from BMAD's config
   - Extract the folder name from the path (e.g., `projects` from `{project-root}/projects`)
   - Use the extracted folder name in all replacement strings
   - Never hardcode any folder name
2. Verify the function handles edge cases:
   - Folder name with hyphens (e.g., `projects`)
   - Folder name without underscore prefix (e.g., `projects`)
   - Path already containing `/{project-name}` (idempotent re-run)

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify the refactored function preserves the folder name from v6.0.4 config (`projects`).
2. Verify the function still correctly injects `/{project-name}` for multi-project support.
3. Verify no remaining hardcoded `projects` strings in the function.

### Phase: Close

1. Append execution entry to shape.md.
2. **MUST** present a brief summary to the user of what was changed.
3. After user approval, set this task's todo to `status: completed` in the plan file.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Refactored function | _config/install-rbtv.py | Python code |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
