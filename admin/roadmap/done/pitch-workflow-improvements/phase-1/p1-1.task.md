---
task_id: p1-1
status: pending
phase: understand
complexity_score: 9
human_review: optional
---

# Task p1-1: Add Asset Handling Patterns to html-patterns.md

## Goal

UPDATE `html-patterns.md` with three new sections — Logo Pattern, Background Image Pattern, and Design Constraints rows 13–16 — so the generation step has explicit rules preventing PDF overflow, logo sizing issues, and texture overuse.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

**Path format:** External files (outside this plan folder) use project-root-relative paths. Internal files (within this plan folder) use file-relative paths (`./`, `../`).

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/_claude-code-workspace/pitch-workflow/cp-workflow-pitch-asset-handling-and-layout-overflow.md` | Source requirements: logo patterns, background constraints, design constraint rows, acceptance criteria |
| `workflows/pitch/data/html-patterns.md` | Target file — understand existing structure, constraint numbering, pattern format |
| `workflows/pitch/data/html-components.md` | Reference — understand component structure for logo and background usage |
| `../shape.md` | Prior execution context |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file (the `.plan.md` in this plan's directory), set this task's todo to `status: in_progress`.
3. Review shape.md execution log for prior task context.
4. Map existing pattern and constraint numbering in `html-patterns.md` to identify insertion points.
5. Identify how logos and background images are currently referenced in `html-components.md`.

### Phase: Execute

1. Add **Logo Pattern** section to `html-patterns.md`:
   - Single logo sizing rules (max dimensions, aspect ratio preservation)
   - Multi-logo layout pattern (grid/row alignment, equal sizing)
   - Color inversion rule: logos on dark backgrounds must use light/white variants
2. Add **Background Image Pattern** section to `html-patterns.md`:
   - Max 3 textured/background-image slides per deck
   - Subtlety rules (opacity limits, no full-bleed photos behind text)
   - When to use solid color vs. texture
3. Add **Design Constraints** rows 13–16 to existing constraints table:
   - Row 13: Content overflow — all slide content must fit single landscape page
   - Row 14: Logo sizing — enforce max dimensions and color inversion
   - Row 15: Texture limit — max 3 textured slides per deck
   - Row 16: Content-fit validation — self-check before delivery

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify all three new sections exist and follow existing file formatting conventions.
2. Confirm constraint row numbering is sequential (no gaps, no duplicates).
3. Cross-check acceptance criteria from compound doc: Logo Pattern section, Background Image Pattern section, Design Constraints rows 13–16 all present.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Updated html-patterns.md | `workflows/pitch/data/html-patterns.md` | Markdown — new sections following existing file conventions |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
