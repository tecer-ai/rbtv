---
task_id: p6-6
status: pending
phase: understand
complexity_score: 8
human_review: required
---

# Task p6-6: BUILD robotville.ai Production Home Page

## Goal

Build the real robotville.ai website sourced from the founder documents and PRD. The page must communicate what Robotville is, who it's for, and how it works — with a waitlist email capture preserved from the current page. Include navigation linking to `/docs/` and `/app/` sections with placeholder pages. Apply the artprize-shadows design tokens to establish Robotville's visual identity.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/bmad/prd.md` | Executive summary, differentiators, user journeys, feature requirements — primary product definition |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/founder/project-memo.md` | Product description, solution, tenets, target customer — primary content source |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/founder/lean-canvas.md` | Problem, solution, unique value proposition, customer segments — messaging source |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/founder/working-backwards.md` | Press release framing, customer benefits, quotes — storytelling source |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/founder/problem-solution-fit.md` | Problem validation, solution fit evidence — credibility source |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/founder/m4-prototypation/design-brief-artprize-shadows.md` | Design direction — color strategy, typography, spacing, visual identity |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/founder/m4-prototypation/design-tokens/artprize-shadows.tokens.json` | Canonical design tokens (colors, typography, spacing, layout, breakpoints) |
| `_mobile/_docs/netlify-placeholder/index.html` | Current waitlist landing page — to be replaced |
| `_mobile/_docs/netlify-placeholder/success.html` | Current success page — update for visual consistency |
| `../shape.md` | Execution log — p6-1 through p6-5 decisions |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Synthesize the product story from founder documents:
   - What Robotville is (executive summary / solution description)
   - Who it's for (target customer / customer segments)
   - How it works (pipeline description / user journey)
   - Key differentiator (end-to-end thinking-to-building)
   - Value proposition (lean canvas UVP, working-backwards press release framing)
4. Review design brief and tokens — adapt the artprize-shadows aesthetic to Robotville's product identity. The inspiration site is a premium art exhibition; Robotville is a product-building AI mentor. The visual language must translate (minimal, confident, spacious) without copying the domain.

### Phase: Execute

1. **BUILD** `_mobile/_docs/netlify-placeholder/index.html` — production home page:
   - Navigation bar: Home, Docs (`/docs/`), Apps (`/app/`).
   - Hero section: value proposition headline, supporting subtitle.
   - What it is: Robotville product description sourced from PRD executive summary and project-memo.
   - How it works: high-level pipeline (idea → structured concept → validated plan → deployed product).
   - Who it's for: target customer description.
   - Waitlist section: **keep** the existing Netlify Forms email capture (`data-netlify="true"`, honeypot, `action="/success"`). The waitlist remains — this is a real site that is not yet open for sign-ups.
   - Footer with minimal branding.
   - Apply design tokens: color palette, typography scale, spacing, border-radii, transitions from `artprize-shadows.tokens.json`. Adapt — not copy — the aesthetic.
   - Responsive: readable and usable on mobile.
2. **CREATE** `_mobile/_docs/netlify-placeholder/docs/index.html` — `/docs/` placeholder:
   - Same navigation bar as home page.
   - Heading: "Project Documentation"
   - Body: "Project documents appear here when deployed via the `deploy docs` command. No projects deployed yet."
   - Same visual style as home page.
3. **CREATE** `_mobile/_docs/netlify-placeholder/app/index.html` — `/app/` placeholder:
   - Same navigation bar as home page.
   - Heading: "Project Applications"
   - Body: "Project applications appear here when deployed via the Mentor workflow. No projects deployed yet."
   - Same visual style as home page.
4. **UPDATE** `_mobile/_docs/netlify-placeholder/success.html` for visual consistency with the new home page.
5. **No external dependencies.** System fonts only (or self-hosted if design tokens specify), no JavaScript required, no build step. The site deploys as a static directory via `netlify deploy --dir=... --prod`.

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. All four pages render correctly: `/` (home), `/docs/`, `/app/`, `/success`.
2. Navigation links resolve between pages.
3. Waitlist form still submits correctly (Netlify Forms attributes preserved).
4. Design tokens are applied and the site has a cohesive, production-quality visual identity.
5. Content accurately represents Robotville based on founder documents and PRD.
6. Responsive on mobile (stacks, readable, usable).

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Production home page | `_mobile/_docs/netlify-placeholder/index.html` | HTML/CSS |
| Docs placeholder page | `_mobile/_docs/netlify-placeholder/docs/index.html` | HTML/CSS |
| App placeholder page | `_mobile/_docs/netlify-placeholder/app/index.html` | HTML/CSS |
| Updated success page | `_mobile/_docs/netlify-placeholder/success.html` | HTML/CSS |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
