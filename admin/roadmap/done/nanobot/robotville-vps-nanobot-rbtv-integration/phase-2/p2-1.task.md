---
task_id: p2-1
status: completed
phase: close
complexity_score: 9
human_review: optional
---

# Task p2-1: Install Nanobot Runtime on VPS

## Goal

Install Nanobot runtime and prepare dedicated service user/workspace on the VPS for gateway execution.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `projects/robotville-v4.0/bmad/prd.md` | Confirm runtime and integration expectations |
| `projects/robotville-v4.0/bmad/architecture.md` | Keep runtime choices aligned with architecture boundaries |
| `_admin/docs/mobile/robotville-vps-access.md` | Use approved server access method |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. Set `p2-1` to `in_progress` in the plan file.
3. Confirm runtime install method (source/uv/pip) and service user naming.

### Phase: Execute

1. Install runtime dependencies on VPS.
2. Install Nanobot runtime in approved manner.
3. Create and permission dedicated service user/workspace.

### Phase: Validate

1. Verify Nanobot CLI commands are available for service user.
2. Verify workspace paths are writable and restricted appropriately.
3. Verify initial status command runs without fatal errors.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user for approval.
3. After approval, set `p2-1` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Installed Nanobot runtime | VPS runtime environment | Operational CLI/runtime base |
