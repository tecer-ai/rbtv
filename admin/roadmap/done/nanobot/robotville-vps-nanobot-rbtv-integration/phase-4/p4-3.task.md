---
task_id: p4-3
status: completed
phase: close
complexity_score: 9
human_review: optional
---

# Task p4-3: Enable FR25 Auto-Restart Policy

## Goal

Enable and validate service-level auto-restart for gateway runtime on VPS to satisfy FR25 scope.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `projects/robotville-v4.0/bmad/architecture.md` | Confirm FR25 requirement and deferred FR boundaries |
| `_mobile/ops/systemd/nanobot-gateway.service` | Apply approved service configuration |
| `_admin/docs/mobile/deploy-runbook.md` | Keep operational procedure aligned |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. Set `p4-3` to `in_progress` in the plan file.
3. Confirm service user and runtime paths before enabling service.

### Phase: Execute

1. Deploy and register system service definition.
2. Enable restart policy and boot startup behavior.
3. Start service and capture service status details.

### Phase: Validate

1. Verify service restarts automatically after controlled termination.
2. Verify service remains stable after reboot.
3. Verify behavior is documented in operations docs.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user for approval.
3. After approval, set `p4-3` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| FR25 restart policy active | VPS service manager + docs | Validated auto-restart behavior |
