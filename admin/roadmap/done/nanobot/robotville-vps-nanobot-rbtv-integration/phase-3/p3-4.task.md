---
task_id: p3-4
status: pending
phase: understand
complexity_score: 11
human_review: optional
---

# Task p3-4: Create Project Memo Adapter

## Goal

Create state adapter module that centralizes canonical `project-memo.md` read/write operations for harness workflows.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `projects/robotville-v4.0/bmad/architecture.md` | Enforce canonical-state and atomic-write rules |
| `projects/robotville-v4.0/founder/project-memo.md` | Validate expected memo structure and keys |
| `_bmad/rbtv/_mobile/README.md` | Keep adapter scope and boundaries consistent |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. Set `p3-4` to `in_progress` in the plan file.
3. Confirm minimum memo schema and update operations needed.

### Phase: Execute

1. CREATE memo adapter module under `_mobile/state/`.
2. Implement canonical read and controlled update interfaces.
3. Implement atomic write behavior and schema guardrails.

### Phase: Validate

1. Verify adapter reads existing memo frontmatter without mutation.
2. Verify writes preserve schema integrity and avoid duplicate state sources.
3. Verify failure behavior is explicit and safe.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user for approval.
3. After approval, set `p3-4` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Memo adapter module | `_bmad/rbtv/_mobile/state/project-memo-adapter.ts` | TypeScript state adapter with guarded updates |
