---
task_id: p3-2
status: pending
phase: understand
complexity_score: 10
human_review: optional
---

# Task p3-2: Create Nanobot Gateway Bridge Module

## Goal

Create `_mobile` integration bridge module that normalizes gateway input and forwards to RBTV routing contract.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `projects/robotville-v4.0/bmad/architecture.md` | Preserve adapter boundary and non-duplication constraints |
| `readme.md` | Align with RBTV module conventions |
| `_mobile/README.md` | Respect harness conventions established in p3-1 |

---

## Execution Flow

### Phase: Understand

1. Read all context files.
2. Set `p3-2` to `in_progress` in the plan file.
3. Confirm bridge responsibilities (normalize, session key derivation input, handoff contract).

### Phase: Execute

1. CREATE bridge module file under `_mobile/integration/`.
2. Implement normalized inbound payload contract for routing layer consumption.
3. Add guardrails for malformed payload rejection.

### Phase: Validate

1. Verify bridge output matches router input contract.
2. Verify malformed payloads fail safely.
3. Verify no direct workflow/state mutation is performed in bridge.

### Phase: Close

1. Append execution entry to shape.md.
2. Present summary to user for approval.
3. After approval, set `p3-2` to `completed`.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Gateway bridge module | `_mobile/integration/nanobot-gateway-bridge.ts` | TypeScript module with normalization contract |
