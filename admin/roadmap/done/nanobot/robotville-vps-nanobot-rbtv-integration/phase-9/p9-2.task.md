---
task_id: p9-2
status: pending
phase: understand
complexity_score: 10
human_review: required
---

# Task p9-2: VALIDATE Mentor Long Workflow with State Management

## Goal

Verify that Nanobot Mentor can follow a long multi-step RBTV workflow (multiple framework steps, multiple sessions) while maintaining state through the project-memo read/write cycle, framework progression, and context window consolidation events.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/bmad/prd.md` | PRD FR6 (sequential framework execution), FR11-FR16 (session/state management), PSF-8 (consolidation risk) |
| `_mobile/SOUL.md` | RBTV core rules — read-before-respond contract, project-memo update rules, context window resilience |
| `agents/paul.md` | Mentor agent — workflow structure, menu handling, state expectations |
| `../shape.md` | Execution log — prior discoveries about workspace path, bootstrap loading |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for all prior Nanobot behavior discoveries.
4. Identify the test protocol — what constitutes "following a long workflow correctly":
   - **State persistence:** project-memo frontmatter reflects current position after each step
   - **Framework progression:** Mentor advances through framework steps in correct order
   - **Menu handling:** Mentor presents menus, waits for input, processes selections
   - **Cross-session continuity:** User can leave and return; Mentor picks up from project-memo state
   - **Consolidation resilience:** After Nanobot's memory consolidation (at ~50 messages), Mentor still knows where user is via project-memo read

### Phase: Execute

1. **TEST: Framework step progression**
   - Start (or continue) a business innovation framework through Mentor in Slack
   - Progress through at least 3-4 steps
   - After each step, verify project-memo frontmatter is updated (check via VPS filesystem)
2. **TEST: Cross-session continuity**
   - After progressing several steps, wait for a period (or start a new Slack message thread)
   - Invoke Mentor again and verify it resumes from the correct position
   - Check that Mentor reads project-memo before responding (not relying on conversation memory alone)
3. **TEST: Consolidation resilience** (if feasible)
   - If conversation has reached ~50+ messages, observe consolidation behavior
   - After consolidation, verify Mentor still tracks framework position via project-memo
   - If consolidation hasn't triggered naturally, note this as a deferred validation
4. **Document** all findings — successes, failures, behavioral gaps.

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Mentor progresses through framework steps in correct order.
2. Project-memo frontmatter accurately reflects current position after each step.
3. Cross-session resume works — Mentor reads project-memo, not just conversation history.
4. Consolidation resilience verified (or documented as deferred if not triggered).

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Validation evidence (test results, project-memo state, behavioral observations) | shape.md | Append-only execution entry |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
