# ZERO-ROW INVENTORY — COMPLETE, not unstarted. Authored by task 7.647, 2026-08-10, under the
# mandatory first-party tool inventory
# (`1-projects/rbtv-sb-merge-refactor-core-build/decisions.md#d-tool-inventory-exposure-rows`
# + FIRST-PARTY scope correction + `#d-tool-inventory-rbtv-cli-column`).
#
# JUDGMENT, stated per the ruling so a reader can tell this apart from an unstarted file:
# admin ships NO first-party tool with a module-resident command surface. What it DOES ship:
#   · admin/install/installer/*.py — the OLD installer's implementation PACKAGE. Its command
#     surface (the wrapper an agent actually invokes) is `install.py` at the REPO ROOT, outside
#     every module tree — and by the ignite worked example's one-tool-one-row-at-the-wrapper
#     rule the row belongs at the wrapper, which no module-relative entry point can reach.
#     (`install2.py`, also repo-root, belongs to core's rbtv-install lineage, not here.)
#   · admin/install/{defaults.json,module-manifest.json} — data files, not invocables.
#   · admin/install/installer/test_*.py — test runners, excluded by the boundary rule.
#   · admin/roadmap/ — documents only.
# When the CMP-5 component tree materializes and the installer wrapper gains a module-resident
# home, the row lands there in the same change.
part-id,part-kind,method,rbtv-cli,entry-point,description,write-roots
