---
task_id: p2-3
status: pending
phase: understand
complexity_score: 8
human_review: none
---

# Task p2-3: Create Init Step (Mode Selection)

## Goal

CREATE `_bmad/rbtv/workflows/bi-business-innovation/steps-c/step-01-init.md` — initialization step with new/continue project mode selection.

---

## Context Files

| File | Purpose |
|------|---------|
| `_bmad/rbtv/workflows/build-rbtv-component/templates/step-template.md` | BMAD step template |
| `_bmad/rbtv/workflows/plan-lifecycle/steps-c/step-01-init.md` | Reference: existing init step |

---

## Tools

| Tool | Mode | Purpose |
|------|------|---------|
| Read | direct | Load template and reference |
| Write | direct | Create the step file |
| Shell | direct | Create steps-c folder if needed |

---

## Execution Flow

### Phase: Understand

1. Read BMAD step template for structure
2. Read existing init step for pattern
3. Review shape.md for any prior context
4. Note: this step determines new vs continue mode

### Phase: Execute

1. Create folder: `_bmad/rbtv/workflows/bi-business-innovation/steps-c/`
2. CREATE `step-01-init.md` with:
   - Frontmatter: stepNumber, stepName, nextStepFile
   - Step Goal: Detect project state, determine mode
   - Mandatory Execution Rules
   - Mandatory Sequence:
     1. Check for existing project_memo in context
     2. If project_memo found → Continue mode (suggest project, confirm)
     3. If no project_memo → New project mode
     4. Present mode selection menu
   - Menu options:
     - [N] New Project → proceed to step-02-project-setup
     - [C] Continue Project → load project_memo, identify current state
     - [X] Exit

3. Include context detection logic:
   - Check if user referenced project_memo
   - Suggest project from context if available

### Phase: Validate

1. Verify step follows BMAD template structure
2. Verify file is under 250 lines
3. Verify mode detection logic is clear
4. Verify menu options route correctly

### Phase: Close

1. Append execution entry to shape.md
2. Mark task status as completed in plan YAML
3. Notify user of completion

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Init step file | `_bmad/rbtv/workflows/bi-business-innovation/steps-c/step-01-init.md` | Markdown with YAML frontmatter |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed
