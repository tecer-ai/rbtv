---
task_id: p8-3
status: pending
phase: understand
complexity_score: 6
human_review: optional
---

# Task p8-3: Slim TOOLS.md — Move Workflow Tables to On-Demand

## Goal

Reduce `_mobile/TOOLS.md` from ~1,375 tokens to ~500 tokens by moving per-agent workflow detail tables to on-demand loading, reducing baseline system prompt token cost for every API call.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/roadmap/todos/cp-nanobot-token-optimization-prompt-caching.md` | Part C details exactly what to keep and what to move — lines to remove, token targets |
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/bmad/prd.md` | PRD Must-Have: "Nanobot bootstrap files — Agent identity, routing, and rules" — behavioral fidelity must be preserved |
| `_mobile/TOOLS.md` | Current file to modify |
| `_mobile/AGENTS.md` | Agent file — verify workflow details are already present in agent files |
| `../shape.md` | Execution log |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for p8-1 and p8-2 context.
4. From the compound doc Part C, confirm what to keep and what to remove:
   - **Keep:** Top-level command routing table (3 rows), routing rules, skills reference table, Nanobot tool reference table
   - **Remove:** Mentor Agent Workflows table, DomCobb Agent Workflows table, Doc Agent Workflows table
5. Verify the removed tables are redundant — the same information exists in each agent's `.md` file which is loaded on demand during agent activation.

### Phase: Execute

1. **EDIT** `_mobile/TOOLS.md`:
   - Remove per-agent workflow detail tables (Mentor ~10 lines, DomCobb ~12 lines, Doc ~18 lines)
   - Keep the top-level command routing table, routing rules, skills reference, and Nanobot tool reference
   - Add a brief note: "Per-agent workflow details are loaded on demand from the agent files during activation."
2. **Estimate** resulting token count to confirm it's near the ~500 target.
3. **If p6-5 has already added deploy command routing to TOOLS.md:** Preserve those additions.

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Verify TOOLS.md retains all command routing capability.
2. Verify the removed content is genuinely available in the agent files loaded on demand.
3. Verify estimated token count is ~500 (down from ~1,375).

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Slimmed TOOLS.md | `_mobile/TOOLS.md` | Markdown |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
