---
task_id: p9-1
status: pending
phase: understand
complexity_score: 7
human_review: required
---

# Task p9-1: VALIDATE Mentor Outputs Framework Docs to Correct Folder

## Goal

Verify that when a user completes a business innovation framework via Nanobot Mentor in Slack, the output document is saved to the correct project output folder — matching the Cursor IDE behavior where outputs go to `_bmad-output/{project-name}/founder/`.

---

## Context Files

**MUST read every file in the table below before any execution phase.** Do not proceed to Phase: Understand until all are loaded and read.

| File | Purpose |
|------|---------|
| `_admin/docs/mobile/robotville-v4.0-business-innovation-run/bmad/prd.md` | PRD FR7 (framework produces markdown to project output folder), FR30 (preserve RBTV file output structure) |
| `_mobile/SOUL.md` | RBTV core rules — project-memo state tracking, output location conventions |
| `agents/mentor.md` | Mentor agent — workflow file references, output path expectations |
| `../shape.md` | Execution log |

---

## Execution Flow

### Phase: Understand

1. **MUST** read every file in the Context Files table above. Do not continue until all are read.
2. In the plan file, set this task's todo to `status: in_progress`.
3. Review shape.md for prior context.
4. Identify the expected output path convention:
   - Cursor behavior: `_bmad-output/{project-name}/founder/{milestone}/{framework-output}.md`
   - Nanobot behavior: should match — same workflow files, same output instructions
5. Identify potential failure modes:
   - Nanobot workspace root differs from Cursor workspace root
   - Relative paths resolve differently
   - `write_file` tool behavior differs from Cursor's Write tool

### Phase: Execute

1. **TEST via Slack:** Invoke Mentor, start or continue a framework, and complete it (or use an existing in-progress framework that is near completion).
2. **VERIFY** the output file:
   - Was it written to the correct path on VPS filesystem?
   - Does the path match `_bmad-output/{project-name}/founder/{milestone}/{filename}.md`?
   - Is the content complete and well-formed markdown?
3. **VERIFY** project-memo was updated with the framework completion.
4. **If path is wrong:** Identify root cause (workspace root mismatch, path construction issue, or workflow instruction gap) and document.

**Discovery Handling:**
- If simple discovery (<5 min to resolve): Resolve immediately, document in shape.md
- If complex discovery: Add new task to plan, notify user of task addition

### Phase: Validate

1. Framework output file exists at correct path on VPS.
2. Project-memo frontmatter updated to reflect completion.
3. Output content quality matches Cursor-produced equivalent.

### Phase: Close

1. Append execution entry to shape.md (never modify existing entries).
2. **MUST** present a brief summary to the user (max 2000 characters) of what was done. Do not mark complete until the user approves.
3. After user approval, set this task's todo to `status: completed` in the plan file.
4. Notify user of completion and any plan changes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Validation evidence (file path, content check) | shape.md | Append-only execution entry |

---

## Revolving Plan Rules

**When discoveries occur:**
- Append discovery to shape.md Execution Discoveries section
- If work is complex (>5 min), add new task to plan with next available ID
- If work is simple, perform immediately and document
- **MANDATORY**: In output message, clearly state any tasks added or removed

**Task change notification format:**
```
PLAN MODIFIED:
- Added: {task-id} - {description}
- Removed: {task-id} - {reason}
```
