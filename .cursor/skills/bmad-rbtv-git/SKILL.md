---
name: git
description: Git commit workflow with Conventional Commits messages. Use when committing changes, writing commit messages, or working with git operations.
---

# Git Skill

**Purpose:** Load and execute the BMAD git commit workflow.

**When to use:**
- Committing code changes
- Writing commit messages
- Following Conventional Commits standard
- User asks to commit or mentions git operations

---

## Activation

Load and follow the git commit workflow:

```
workflows/git-commit/workflow.md
```

This workflow provides:
- Conventional Commits message format
- Commit type selection guidance
- Scope and description best practices
- Breaking change documentation

> **ADMIN MODE:** Before proceeding, load and read `.cursor/rules/admin-rbtv-bmad-mirror.mdc` for path resolution and config values. Key: `.cursor/` and `tasks/` are at workspace root.
