2026-08-26 · issue · Uncaged staff seats never got their declared PATH · every instrument promised by name resolved command-not-found in live leader/goal-master/channel-master sittings → composeCageFor's uncaged early return precedes resolveLocalBinGrant and the PATH composition, so local-bin: true had no consumer on any door · af326d61 · meta-leader · ⚠
2026-08-26 · issue · Direct-created daemon goal skipped forever, in silence · scaffold --lane daemon wrote no taskforce.csv and lane-watch's skip was a bare continue marked `Quiet` (cli-tools-reachability-report: 0 daemon mentions ever) → scaffold now REFUSES that lane without --materialize-follows, and the skip names the goal · 4ed8acc8 · operator,planning · ⚠
2026-08-26 · issue · Leader chair resolution fell back to seats[0] · goal-memory-management's WORKER printed as "leader" on every pass, woken for judgment and named for the room rebuild → leaderSeat returned seats[0] with no `leader` row (and the literal 'leader' on an unreadable taskforce) → now {seat:null,why} and every consumer refuses loudly · 4ed8acc8 · — · ⚠
2026-08-26 · issue · three sites still claimed no ruling instrument exists · impl-leader-verbs (commit d7841291, 2026-08-26) wired the leader's ruling acts (`supervise accept`, → `reconcile.js`'s `nontermPayload` wakes the leader for rows with an ENDING nothing can advance on · f3aa3f16 · chat,operator,engine · ⚠
2026-08-27 · issue · Daemon-spawned seats acted as ignite-daemon · A daemon-spawned seat's every coordination act recorded `by ignite-daemon`, not the seat (live leader, 3/3 acts, 2026-08-27). → `composeCageFor` never emitted `COORD_AGENT`, so identity fell past an absent pane to the F16 lane's constant. · 8a4b7cab · coord,team-kit · ⚠
2026-08-27 · issue · Daemon-lane goal's first room had no opener · `scratch-cli-reach-report` was created 2026-08-27 ~14:43Z through the goal-creation → `ignite/supervisor/seeding.js#seedGoal` reads `deriveLease({workspaceRoot, goal}).live` · 9cdb472e · runtime,operator · ⚠
2026-08-27 · issue · Summoned goal-master chair seeded cold, then ended the goal · `scratch-cli-reach-report` FINISHED with 3 of 5 plan seats never enqueued → `readyFromEndings` rebuilds the frontier off the ending ledger and reads NO coord `verdict`, so D24s IDLE never reached seeding; `finish-goal` gated nobody and stamped `from: leader` · 91da4a8d · coord,meta-master · ⚠
2026-08-27 · issue · New staff mail counted as a retry, and the disarm was silent · scratch-tool-reach-note's planning chain stalled: classA was REPLACED each hop (plan-understander→designer→drafter) so three FIRST attempts reached N=3 → the mail-#7 pass took skip-disarmed, which had no say() and, with engine.endingStore unset anywhere, no stamp and no ask either · 348ebf7e · runtime,chat · ⚠
2026-08-28 · issue · Pane census deferred every daemon-lane seat forever · On 2026-08-28 05:16Z the acceptance wave induced a seat death on the DAEMON-LANE goal → `supervisor/launch.py`'s capacity term (7.278/7.363/7.406) reads `<goal>/state.json` — the TMUX · fa6bd7a0 · coord · ⚠
