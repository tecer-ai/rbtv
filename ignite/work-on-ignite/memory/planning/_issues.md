2026-08-25 · issue · Chair gate left half-migrated off awaiting-close · `materialize-seats.py` aborted with `NameError: name 'debts' is not defined` at → ONE gate existed as TWO copies, and only one was migrated. · e8f4a487 · coord,state-store · ⚠
2026-08-26 · issue · Uncaged staff seats got the worker cage's write surface · Every uncaged staff seat's assembled `seat.md` ended with a derived section that told it the → `render_descriptors` in `ignite/planning/materialize-seats.py` appended `_WRITE_SURFACE_BLOCK` to · 179310e8 · supervisor,meta-master · ⚠
2026-08-26 · issue · seat guidance told uncaged seats a false cage story · `ignite/planning/materialize-seats.py`'s `_SEAT_GUIDANCE_MD` — the body written into every seat → `_write_seat_guidance` rendered `_SEAT_GUIDANCE_MD` with a single fixed template for every seat — · f3aa3f16 · — · ⚠
2026-08-27 · issue · The approve-to-birth path, five refusals deep · The acceptance wave's re-run #8 (`scratch-tool-inventory-8`, 2026-08-27) got the plan-console → Five independent causes on one path, each in `ignite/planning/path_b.py`. · cf79c881 · coord,meta-planning,meta-leader · ⚠
2026-08-27 · issue · The writer accepted a goal name the scaffold refuses · While proving `cf79c881`'s birth path end to end on a scratch vault under /tmp (2026-08-27), an → Two rules for one field, and the writer re-spoke only the wider one. `SAFE_NAME_RE` · 4d06233b · meta-planning · ⚠
2026-08-31 · issue · Path-B birth writes its own envelope.json · Owner-flagged 2026-08-30 (`owner-flagged-birth-writes-no-envelope`, filed by → `approve_package.py`'s `OPTIONAL_KEYS` carries no fill-in field at all · 7d8cb4a2 · envelope · ⚠
2026-08-31 · issue · uncast_in_sheet did not merge sheet defaults · `wrapper.py#uncast_in_sheet` did not merge the casting sheet's `defaults` block. → The gate never read the field the loader downstream treats as legal. · 2e533bb1 · — · ⚠
2026-08-31 · issue · Path-B birth carries the plan's declared execution-mode · Filed `G-plan-drafter-0828-1848`: every Path-B birth (approve→scaffold) wrote → `goal_cli.py`'s `scaffold` verb already accepted `--execution-mode` correctly and derives nothing · 835f52b0 · — · ⚠
2026-08-31 · issue · bound-commit committed inside the tree it names · git commit -- planning/ snapshots working tree so the pointer rides inside the named hash → planning_bind.py unlinks it for the commit then writes the hash; frozen after approve-package.json · 81e51627 · meta-leader,supervisor · ⚠
2026-08-31 · issue · path-exposed CLI got no write-roots, skill-routed did · `resolve_cli_write_roots` (`ignite/planning/materialize-seats.py`) left `plan["cli_write_roots"]` → `resolve_cli_write_roots`'s per-part loop opened with `if method != "skill": continue`, so a · f55dc3a3 · — · ⚠
2026-08-31 · issue · Wait-trap rule reached no already-authored seat · Seven agent sittings in the `redesign-continue-1` plan were lost to the headless-wait trap → The reference document was correct and linked from `build.md`, both plan workflows, and · c48d2fa1 · meta-planning · ⚠
