2026-08-28 · issue · pause wrote a store the lane gate never reads · The owner's Slack `pause {goal}` was INERT and `resume {goal}` lied. Measured 2026-08-28 → Two of the three are one line each in `ignite/state-store/heart/pause-resume.js`, minted five · 919be192 · runtime,supervisor,gateway · ⚠
2026-08-30 · issue · asks read from the private store, not the ending one · `inspect asks` (the daemon target the bridge's 2-hourly system digest reads through, and `ignite → Two writers/readers of `open_asks`, and two of the three call sites never checked which store they · 361a56f2 · runtime,supervisor,chat · ⚠
