# 20260827-i-creation-route-sweep-closed-in — Creation-route sweep closed in the last three master files

kind: issue
component: meta-master
date: 2026-08-27
commit: 81d04a1c
deployed: no
pin: NONE
components: meta-planning

## Observed

`1c49ffc4` fixed the CM creation route in two references and was walled from three more files carrying the SAME stale ranking.

It (2026-08-27) re-ranked the channel master's goal-creation route by LANE in `meta/master/references/master-scaffold-flow.md` §3 and repaired `master-bootstrap.md` §3, and its own Consequences paragraph recorded that the fix was PARTIAL BY WALL: the identical false ranking survived in three files that seat could not touch. Measured on `ignite/core-daemon` at `68554623` with `grep -rn 'DIRECTLY\|fallback\|EROFS' meta/master/`. `references/master-instruments.md:125-129` § Goals bullet 1 read "D49 opened the goals ROOT to every caged master: run it DIRECTLY. The goal-creation request tool remains the fallback if a write still returns EROFS (old cage, undeployed daemon)" — and `master-instruments` is an `exposes: skill:` grant on BOTH `channel-master-prompt` and `console-master-prompt`, so a channel-master sitting loaded the corrected `master-scaffold-flow` and this stale ranking in the SAME sitting. `tasks/serve-master-traffic.md:28` — the `SCAFFOLDED` clause of `<done-contract>`, which `materialize-seats` renders verbatim into `.rbtv/goals/_channel-master/seat.md:259` as the seat's OWN definition of done — read "a goal folder exists via the goal CLI run DIRECTLY (D49: the goals root is writable; the daemon-executed-job fallback is only if a write still returns EROFS)". That is the worst of the three: a reference can be argued with, a definition of done is what the seat measures itself against. The same file carried at `:11` and `:31` the "plan workflow already present in the goal folder" premise that `1c49ffc4` had just struck from `master-bootstrap.md`. `prompts/console-master-prompt.md:175` and `:180` carried "**This seat runs the command DIRECTLY, always** … so the channel seat's daemon-executed-job fallback never applies here" — lower harm, since that seat genuinely does run direct for a console-lane goal, but false the moment the owner answers `daemon`, which `:180`'s own next bullet obliges it to ask about.

## Mechanism

Each of the three restated, as prose nothing re-derives, a fact `goal_cli.py` owns — and the worst of them renders into the seat's own definition of done.

All three restate, in prose the code does not generate, a fact `ignite/operator/goals-tree/tool/goal_cli.py:660-671` owns: `cmd_scaffold` refuses `--lane daemon` before its first write with code `daemon-lane-unmaterialized`, because it writes no `taskforce.csv` and the daemon adopts only a daemon-lane goal that has one. Each of the three was written when D49 (2026-08-21) had just opened the goals root, so each picked WRITABILITY as the discriminator and demoted the request route to an EROFS-conditional fallback. `4ed8acc8` (2026-08-26) then made the lane the discriminator and swept the code callers and the tool README only. Nothing in the tree re-derives master prose from the tool, so all four surfaces drifted together and were repaired one at a time.

The specific reason the descriptor clause mattered more than the reference: `serve-master-traffic.md` is SHARED — `meta/master/seats.csv` rows 2 and 3 both name it as the task for `channel-master` and `console-master`. A CM-specific rewrite would have made the console seat's done-criterion wrong in the opposite direction, so the clause had to become lane-conditional rather than channel-master-specific. That sharing is also why the defect was invisible from the channel-master prompt: the prompt (`channel-master-prompt.md` §3) is CLEAN and delegates to `master-scaffold-flow`, so a reader auditing the prompt tree alone finds nothing.

One live contradiction was created by the repair itself and had to be handled in the same change: the new done-clause tells the owner the goal is "queued for the daemon's next drain pass", while `<scope>` seventeen lines above carried the ruled OQ-22 wording "NOTHING is queued and nothing is delayed". Those are two different subjects — OQ-22 (owner, 2026-08-26) struck the master hand-enqueuing a `<goal>-workflow-start` LAUNCH job; the drain is the daemon's own periodic job, queue row 281, `interval_seconds 300`, which the master does not enqueue.

## Attempts

Second attempt on this defect, and the first on these three files. `1c49ffc4` (2026-08-27) is attempt one: it fixed `master-scaffold-flow.md` §3 and `master-bootstrap.md` §3 and held — `git log --oneline -- meta/master/references/master-scaffold-flow.md` shows no revision after it — but it was scoped by an explicit seat wall to `references/**` and named all three survivors in its own Consequences paragraph and in its `_issues.md` entry `20260827-i-cm-creation-route-ranked-by-wr`, so nothing here is a re-fix of something that failed. `052b0042` (2026-08-26, the CP-1 ruling sweep) DID reach `serve-master-traffic.md` — it struck the "job queued 10 minutes out" clause from the task file and the descriptors — but it was aimed at the queue wording and left the `DIRECTLY`/EROFS ranking in the same paragraph standing; that is why its own entry `20260826-i-master-material-contradicts-th` carries the ATTENTION bullet "A tool deletion or a component move must sweep references/**, prompts/** AND tasks/** in the same change". `f3aa3f16` (2026-08-26) swept two stale rulings-note lines out of `goal-master-prompt.md` and never read the console prompt's permissions unit. `d62a07a7` and `578547a9` (2026-08-27) repaired the two dangling `exposes:` refs that had frozen this seat's `--refresh` door; both were declaration sweeps that read no bodies. Build-memory grep floor over every `_issues.md`/`_creations.md` for `meta-master|master-instruments|serve-master-traffic|console-master-prompt`, plus two `rbtv embed-search` passes (the symptom, then the three file paths), surfaced exactly the five entries named above and nothing that had touched these files' route prose.

## Fix

Commit `81d04a1c`, three prose files, no code and no tool change — the tool is the deployed truth all three must agree with, and it is already correct.

`master-instruments.md` § Goals bullet 1 is rewritten to the shape `master-scaffold-flow.md` §3 already landed: it states that the verb writes no `taskforce.csv` and therefore refuses `--lane daemon` with `daemon-lane-unmaterialized`, then declares "WHICH ROUTE IS YOURS FOLLOWS FROM THE LANE, never from what you may write: writability decides nothing here and there is no EROFS fallback to wait for", and splits into a CHANNEL MASTER arm (the goal-creation request, always — validate, stage in the seat folder's `requests/` inbox, stop; the daemon drains with `scaffold-and-queue` every 300 seconds; never run `scaffold-and-queue`, never run `scaffold-seats`) and a CONSOLE MASTER arm (the verb directly for a console-lane goal; on a daemon answer, either the request route or the refusal's three-act sequence in one sitting). The two bans that the 2026-08-27 sitting's improvisation earned — never hand-queue a daemon job running the scaffold command, never pass `--materialize-follows` to get past the refusal — close the bullet, and the full sequence is pointed at rather than duplicated, because this file is the instrument roster and `master-scaffold-flow` is the procedure. The tool's full path and the closed seven-field payload table are untouched: they are what this file is FOR, and the defect was the ranking, not the inventory.

`tasks/serve-master-traffic.md`'s `SCAFFOLDED` disposition is rewritten LANE-CONDITIONAL rather than channel-master-specific, because the file is shared by both cold seats. The clause now opens by naming the refusal, then forks: DAEMON lane holds when a VALIDATED request is STAGED in the seat folder's `requests/` inbox AND the owner has been told the goal is queued for the daemon's next drain pass (every 300 seconds) — with the explicit negative "a goal folder produced HERE by running the creation verb directly is NOT this disposition — it is the defect this clause replaced"; CONSOLE lane keeps the verb run directly with the lane read back, and carries the three-act handover for a daemon answer with "not before all three are done". The explicit negative was kept rather than trimmed because the observed failure was a seat filling a gap, not a seat lacking a route. The struck "plan workflow already present in the goal folder" premise is replaced at both `<scope>` and the bootstrap done-clause with what a workflow actually is — a catalog artifact at `<catalog-root>/<component>/workflows/<name>/`, found by walking the component tree — matching the wording `1c49ffc4` landed in `master-bootstrap.md`. And OQ-22's "NOTHING is queued and nothing is delayed" is kept VERBATIM and qualified in place ("meaning NO launch job is enqueued and NO start is timed … NOT the goal-creation request's own drain wait, which is the daemon's, not a job this seat enqueues"). Deleting the ruled clause was rejected — it is an owner ruling — and leaving it unqualified was rejected because it would have put a flat contradiction of the new done-clause seventeen lines above it in the same rendered descriptor.

`prompts/console-master-prompt.md` is edited at exactly two lines, no restructuring. `:180`'s "**This seat runs the command DIRECTLY, always**" becomes "**for a CONSOLE-lane goal**", and the reasoning is inverted to match the tool: "no write ever refuses it for permission. Permission is not the bound: the verb writes no `taskforce.csv` and so REFUSES `--lane daemon` outright … for this seat exactly as for the channel seat", followed by the daemon branch (request route, or the three acts). `:175`'s output schema gains the same fork. Both new pointers read `master-scaffold-flow` § 3; an earlier draft wrote `(§3a)`, which is WRONG in this prompt — `## 3a` there is the bootstrap arm, and `## 3` is the dispositions section that hands off to the reference. A wider rewrite of the console prompt was rejected: that seat's direct route is correct for the lane its owner usually picks, and the defect is one missing branch.

## Consequences

The three-file sweep `1c49ffc4` named is now closed, and its "partial by wall" caveat is discharged: `grep -rn 'DIRECTLY\|fallback\|EROFS' meta/master/ meta/leader/` returns no goal-creation hit stating the old ranking. One stale hit REMAINS and was outside this change's wall — `meta/master/component.md:31-35`, "**The ONE genuine asymmetry is the daemon-executed-job fallback** … the channel seat's Slack cage may refuse the direct goal-folder write (measured 2026-08-08), so it queues a daemon-executed job running the same command; the console seat launches at the install root with no cage and always scaffolds DIRECTLY." That paragraph is a build-record explanation of a shared-unit ruling, is read by builders and not by a live master sitting, and now contradicts both the prompt and the task file it describes. It is the last surface of this defect class and is left knowingly, not missed.

`master-instruments.md`'s § Goals bullet 3 ("`rbtv goal lane <goal>` with NO `--set` READS the marker back … that read is the verification") and the payload table are unchanged and remain correct on both arms. No frontmatter `description:` changed on any of the three, so no `exposure.csv` row moves and the docs-in-sync obligation is discharged by inspection. `component_lint --component meta/master` is byte-identical before and after — 10 checks run, 2 skipped, 0 blocked, 5 findings, 0 info, all five pre-existing (two `stools` grants with no `<resources>` bullet on `channel-master-prompt` and `goal-master-prompt`, one 324-character `work-on-ignite` bullet over the 280 ceiling).

## Verification

Commit `81d04a1c` on `ignite/core-daemon`, committed by pathspec (`git commit -F <msg> -- <three paths>`); `git show --stat` carries exactly those three files, +28/-10, and `git status --porcelain meta/` is empty afterwards. Sweep proof: `grep -rn 'DIRECTLY\|fallback\|EROFS'` over `meta/master/`, `meta/leader/` (no hits at all) and `ignite/coord/` — every surviving hit dispositioned, none stating the old ranking in a goal-creation context. `component_lint` before/after was measured, not assumed: the three edited files were copied to `/tmp`, `git checkout --` restored them to HEAD, the lint tail captured, the edits copied back and re-proven with `sha256sum -c`; `diff before after` is empty. The lint was re-run once more after a trailing-whitespace trim and is unchanged. Descriptor refresh through the door, composed exactly as `spawn.js#refreshSeatDescriptor` composes it (`ignite/supervisor/spawn/spawn.js:637-641`), catalog root taken from the descriptor's own `component:` line: apply `EXIT=0` (`ok: True, dry_run: False, refusal: None, writes: 19, taskforce_rows_appended: 0`), dry-run `EXIT=0` (`ok: True, dry_run: True, writes: 19`), re-apply `EXIT=0` — NOTE the ordering: the first invocation was run WITHOUT `--dry-run` by mistake, so the apply preceded the dry-run; the dry-run and two further applies are byte-idempotent and the pre-image sha256 `164b0662744da4e7936a0265b2be4de2b75c9fb7efc3ff59b7a6fcea2aeed75d` was captured before any of them. `diff` of the pre-image against the rendered descriptor shows exactly three hunks, all three of them this change's clauses, at `.rbtv/goals/_channel-master/seat.md:242`, `:259-263` and `:266`. The four ruled-correction regression greps on that descriptor all return 0: "Secrets stay UNREADABLE", "queued 10 minutes", "you do not file", "QUEUED JOB". NO restart and NO deploy: the seat reaches `master-instruments` through a thin skill loader naming the absolute SOURCE path, and `refreshSeatDescriptor` re-renders the descriptor before every spawn at `spawn.js:1551` and `:1919`. Nothing was staged into the live `requests/` inbox and no goal was created.

## ATTENTION

- A seat's DEFINITION OF DONE outranks the reference it is told to follow. `tasks/*.md` renders verbatim into `.rbtv/goals/**/seat.md`'s `<done-contract>`, so a corrected reference and a stale done-criterion in the same sitting resolve toward the done-criterion — that is what the seat measures itself against. Sweep `tasks/**` FIRST when a route changes, not last.
- `meta/master/tasks/serve-master-traffic.md` is SHARED by `channel-master` and `console-master` (`meta/master/seats.csv` rows 2 and 3). Any correction there must be lane-conditional or it fixes one seat by breaking the other. Check `seats.csv` before editing any `meta/*/tasks/` file.
- The channel-master PROMPT is clean and delegates to `master-scaffold-flow`, so auditing the prompt tree alone finds nothing. This defect lived entirely in the shared task file and in a reference exposed to both seats — an audit that follows the prompt's own `exposes:` list and its `seats.csv` task column is the one that finds it.
- OQ-22's "nothing is queued and nothing is delayed" is about the master NOT hand-enqueuing a launch job. It is NOT about the goal-creation request's drain wait, which is the daemon's own periodic job (queue row 281, `interval_seconds 300`). Any edit that introduces the drain wait beside that ruled clause must qualify it in place, or the descriptor carries a flat self-contradiction seventeen lines apart.
- `##3a` in `console-master-prompt.md` is the BOOTSTRAP arm, not the creation route; the creation route is `## 3`, which hands off to `master-scaffold-flow` § 3. A cross-reference written from memory lands the reader in the wrong section of the right file — cite `<reference-name> § <n>`, never a bare `§n`.
- tasks/*.md renders into the seat's own definition of done — sweep tasks/** first when a route changes
