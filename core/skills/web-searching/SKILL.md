---
name: rbtv-web-searching
description: "Navigate the web — preview link metadata, extract clean content from URLs, and conduct rigorous multi-source research with source evaluation and citation standards. Use when the user provides a URL to read, asks to research a topic, or any task requires web information."
---

# Web Search

> **DEPRECATED (2026-08-18).** Superseded in rbtv2 by the `web/` module: the `web/research`
> component's reference carries the research procedure and standards; the `web/browse` component
> carries tool routing (page extraction, browser driving, link-metadata preview). Do not extend this
> skill. It stays functional until every referrer (source-mining, idea-sparring, writing,
> orchestrating's routing/dispatch-wrapper cards, module docs, install manifest) is repointed —
> that retirement sweep is a tracked Phase-6 task, not a same-day edit.

**CRITICAL — Execute these steps in order.**

1. Read and execute the workflow at `{rbtv_path}/core/workflows/web-search/workflow.md`.
