# Compounding

When the user corrects agent behavior, fix the immediate problem AND evaluate if a structural fix prevents recurrence.

Structural fixes: rule update, CLAUDE.md change, new routing rule, new memory entry, workflow adjustment.

If a structural fix would prevent recurrence, explain what it is and ask if the user wants to implement it. Never silently skip the structural evaluation.
