# Columns are the seven of `concepts/exposure-manifest.md` § file schema, in its order — routed,
# not invented; `#`-led lines are dropped before the header is read.
#
# TWO MANDATORY TOOL INVENTORY rows (first-party CLIs, `d-tool-inventory-exposure-rows`):
# `team-monitor` (CMP-20) is the sensor; `ctx-monitor` is CMP-20's per-pane sensing ENGINE, shipped
# in the same tool/ — NOT a second component (R24 retired the name to alias status; leader ruling,
# core-build 7.40 #852) but a separately runnable first-party invocable, so it carries its own
# inventory row. `description` empty by ruling on `tool` rows — both self-document via `-h`.
# Rows relocated here 2026-08-23 from the retired module-root `orchestration/exposure.csv` in the
# owner-directed component-shape migration; the wrapper part-ids (`rbtv-team-monitor`,
# `rbtv-ctx-monitor`) died with the wrappers, and the dangling `dispatch-scaffold` row (entry point
# `skills/orchestrating/scripts/scaffold.py`, a path that existed nowhere) was dropped, not moved.
part-id,part-kind,method,rbtv-cli,entry-point,description,write-roots
team-monitor,tool,path,,tool/team_monitor.py,,
ctx-monitor,tool,path,,tool/ctx_monitor.py,,
