probe-dispatch-resolve — task 7.54

  PASS  P1                   the committed config exists at the path this lane reads
        /home/henri/ht-wkdir/second-brain/3-resources/tools/rbtv/ignite/config/spawn-profiles.yaml
  PASS  P2                   BARE load of the fixture REFUSES (so the stub is load-bearing, not decorative)
        REFUSED with E_CONFIG_LOAD — as required
  PASS  P3                   fx-seat REALLY declares sandbox.SeatBinds (else §1s no-cage check is vacuous)
        fx-seat.sandbox.SeatBinds declared in the fixture this probe wrote
  PASS  P4                   WITH the stub the whole fixture loads (all 4 profiles)
        fx-seat, fx-claude, fx-codex, fx-no-dial
  PASS  1-NO-DENYLIST        the seat-binds deny-list is GONE from the resolve path (surface + behaviour)
        fx-seat (SeatBinds-declaring) resolves: claude -p
  PASS  1-NO-CAGE-IN-ARGV    the resolved result carries NO cage — the stub has nothing to leak
        fx-seat declares SeatBinds; nothing of it appears in argv or the result
  PASS  2a-RED               a dispatch with NO work-target is REFUSED
        REFUSED with E_ADD_DIR_ABSENT — as required
  PASS  2b-RED               a RELATIVE work-target is REFUSED
        REFUSED with E_ADD_DIR_RELATIVE — as required
  PASS  2-GREEN              an ABSOLUTE work-target passes the bound (it is not blanket-refusing)
        /tmp admitted
  PASS  3-GREEN              fx-claude pre-flights CLEAN against the live installed claude
        checked ["-p","--model","--output-format","--verbose","--effort"] missing []
  PASS  3-RED                a pinned flag absent from live --help goes RED (the green discriminates)
        REFUSED with E_PINNED_FLAG_ABSENT — as required
  PASS  4                    abstract effort is translated per-profile, never hand-written
        abstract 'high' -> claude '--effort high' AND codex '-c model_reasoning_effort=high'
  PASS  4-RED                an effort level outside the closed vocabulary is REFUSED
        REFUSED with E_UNKNOWN_EFFORT — as required
  PASS  4-NO-DIAL            a profile with NO effort table refuses the pair rather than dropping it (G-270)
        REFUSED with E_UNKNOWN_EFFORT — as required
  PASS  5-RED                a slot the profile does not declare is REFUSED (no raw-flag smuggling)
        REFUSED with E_RAW_FLAG — as required
  PASS  6-STDERR-HELP-GAP    help on STDERR at exit 0 still reads as unavailable (tripwire — flip when fixed)
        REFUSED with E_PREFLIGHT_UNAVAILABLE — as required
  PASS  6-STDERR-HELP-GREEN  the SAME help on STDOUT is read fine (the red is the stream, not the flag)
        checked ["--fx-pinned-flag"] missing [] — only the STREAM differs from the red above
  PASS  LIVE-1               the shipped config still loads through this consumer's stub
        14 shipped profiles loaded
  PASS  LIVE-2               NO seat-binds deny-list in the resolve path: every shipped profile loads and admits
        14/14 shipped profiles are SeatBinds-bearing and none is refused
  PASS  LIVE-3               NO manifest records a launch_profile — the field is RETIRED (owner ruling 2026-08-11)
        8 manifests scanned, 0 launch_profile values — retired as ruled
  PASS  LIVE-4               every CLI package keeps its invocation manual (now the ONLY dispatch home)
        all 4 CLI package manuals intact

21 passed, 0 failed
