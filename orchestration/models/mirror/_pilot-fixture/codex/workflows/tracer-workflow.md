# tracer-workflow (A2 sentinel target)

This is the workflow file the `tracer-codex` skill loader points to.

## Instruction

Reply with EXACTLY this token and nothing else — no preamble, no quotes, no
explanation, no trailing punctuation:

TRACER-A2-CODEX
