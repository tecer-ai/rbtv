# tracer-qwen workflow

This is the workflow file the `tracer-qwen` loader points to.

When this workflow runs, the worker MUST reply with EXACTLY the following sentinel string and nothing else:

TRACER-A2-QWEN

Do not add quotes, punctuation, explanation, or any other text. Output only that single line.
