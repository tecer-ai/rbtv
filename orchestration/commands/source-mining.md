Read and execute the workflow at `{rbtv_path}/orchestration/workflows/source-mining/workflow.md`.

If the user invoked the command with `yolo` as an argument (e.g., `/source-mining yolo`), pass `yolo_mode = true` into step-01. Otherwise `yolo_mode = false`.
