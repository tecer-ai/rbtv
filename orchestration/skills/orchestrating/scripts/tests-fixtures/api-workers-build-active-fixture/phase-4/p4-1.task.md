---
task_id: p4-1
status: pending
phase: understand
complexity_score: 8
human_review: required
executor: { model: claude, variant: opus }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - orchestration/skills/orchestrating/cards/dispatch-wrapper.md   # rbtv repo root
orchestrator_executed: false
---

# Task p4-1: UPDATE dispatch-wrapper.md — API-worker transport row

## Goal

UPDATE `orchestration/skills/orchestrating/cards/dispatch-wrapper.md` to add the **API-worker transport** to the §3 transport table (the runner writes output files + `return.json` to disk; the conductor reads the output folder + `return.json`), and note the all-`[INLINED]` rule for API workers (an API worker has nothing to `[FULL READ]` with). The five fields never change.

## Context Files

> Work-dir = rbtv repo root. After this edit, manuals MUST be re-rendered (plan inline render task).

| File | Purpose |
|------|---------|
| `1-projects/rbtv-evolution/orchestration/api-workers/api-workers-design.md` | §6 file-access / inlining; §8 transport row |
| `orchestration/skills/orchestrating/cards/dispatch-wrapper.md` | The file to edit (read §1, §3, the RENDER markers first) |
| `../specs/api-text-worker-spec.md` | The runner's return contract |

## Allowlist

- ✎ modify: `orchestration/skills/orchestrating/cards/dispatch-wrapper.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. Add the API-worker row to §3's transport table (runner writes files + `return.json`; "disk" = the output folder, not a git repo).
2. Add the §1 inlining note: for an API-worker dispatch every reference is `[INLINED]` (bounded by `context_window`); a dispatch that won't fit must be split, not handed off as a path.

### Phase: Validate
1. The five-field schema is unchanged (only the transport carrier row is added).
2. RENDER markers intact (the render step consumes them) — atomic, additive.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Dispatch-wrapper with API transport row | `orchestration/skills/orchestrating/cards/dispatch-wrapper.md` | Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
