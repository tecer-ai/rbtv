---
task_id: p4-3
status: pending
phase: understand
complexity_score: 5
human_review: optional
executor: { model: claude, variant: sonnet }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - orchestration/skills/orchestrating/core-protocol.md   # rbtv repo root
orchestrator_executed: false
---

# Task p4-3: UPDATE core-protocol.md — capability roster

## Goal

UPDATE `orchestration/skills/orchestrating/core-protocol.md` to add per-package capability lines (recall only) for `claude` (Agent-tool), `deepseek`, `gemini`, `openai`, `manus` to the static roster table. The `ORCH:AVAILABILITY` block is written by `install.py` (p4-4) — do NOT hand-edit between its markers here.

## Context Files

> Work-dir = rbtv repo root.

| File | Purpose |
|------|---------|
| `orchestration/skills/orchestrating/core-protocol.md` | The file to edit (read the Capability summary + the AVAILABILITY markers) |
| `orchestration/models/{deepseek,gemini,openai,manus,claude}/manifest.yaml` | The packages whose one-line recall is added |

## Allowlist

- ✎ modify: `orchestration/skills/orchestrating/core-protocol.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. Add roster recall lines: `claude` (Agent-tool tiers, in-session default carrier), `deepseek`/`gemini`/`openai` (API text-workers; Gemini web), `manus` (API agentic/autonomous-web worker).
2. Leave the `ORCH:AVAILABILITY` marker region untouched (installer-owned).

### Phase: Validate
1. The roster table is recall-only (a line ≠ routability); the AVAILABILITY markers are intact.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode).

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Core-protocol roster lines | `orchestration/skills/orchestrating/core-protocol.md` | Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
