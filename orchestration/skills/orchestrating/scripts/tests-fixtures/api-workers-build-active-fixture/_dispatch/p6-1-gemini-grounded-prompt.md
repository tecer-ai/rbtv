Using web search (your responses are search-grounded), answer this single lookup:

What are DeepSeek's CURRENT official API text models and their prices, per DeepSeek's official API pricing documentation?

Report, in plain text:

1. The official model ids, verbatim as the pricing page lists them (e.g. the standard chat model and the reasoning model).
2. For each model: input price (cache hit AND cache miss, if the page lists both) and output price, in USD per 1M tokens.
3. The context window each model supports, if listed.
4. The source URL(s) of the official page(s) your answer is grounded in.
5. If the page notes an effective date or a recent price change, state it.

Answer compactly — a small table plus the source list is ideal. Only use DeepSeek's official documentation as the source of truth; if search surfaces third-party blogs, prefer the official page.
