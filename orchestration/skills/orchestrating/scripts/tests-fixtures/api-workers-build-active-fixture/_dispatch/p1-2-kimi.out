**status:** DONE

**landed:** `orchestration/models/_api/clients/base.py` created — synchronous provider base abstract class with `ProviderName`, `Message`, `TokenUsage`, `Response`, `ProviderConfig`, `RequestOptions` dataclasses, and `ProviderClient(ABC)` exposing `structured_output: bool = True`, abstract `name`, `initialize`, and `chat`. No commit — conductor commits.

**validation:**
- `python -c "import ast; ast.parse(open('orchestration/models/_api/clients/base.py').read())"` → EXIT 0, WALL_MS ~120ms
- `NO_BAD_PATTERNS: PASS` (no async, await, aiohttp, AsyncIterator) → EXIT 0, WALL_MS ~80ms
- `structured_output: PASS` / `abstract name: PASS` / `abstract initialize: PASS` / `abstract chat: PASS` / `no call_with_tools: PASS` / `no stream: PASS` / `no validate_api_key: PASS` / `no get_models: PASS` → EXIT 0, WALL_MS ~90ms
- `SKIPPED_COUNT: 0`

**concerns:** None.

**open_questions:** None.
