Produce a short, accurate technical explainer about **idempotent HTTP methods**, as TWO separate files:

1. `explainer.md` — 2-3 short paragraphs: define what "idempotent" means for an HTTP method, and explain why idempotency matters for safely retrying requests.

2. `examples.md` — a markdown table listing the common HTTP methods (GET, POST, PUT, PATCH, DELETE) with one row each and a column stating whether the method is idempotent (yes/no).

Keep it concise and technically correct.
