---
task_id: p3-2
status: pending
phase: understand
complexity_score: 11
human_review: required
executor: { model: claude, variant: opus }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - orchestration/skills/orchestrating/cards/routing.md   # rbtv repo root
orchestrator_executed: false
---

# Task p3-2: UPDATE routing.md — deterministic selector + carrier + API-worker hooks

## Goal

UPDATE `orchestration/skills/orchestrating/cards/routing.md` with: the **deterministic `(model, variant)` selector** (enumerate→filter→rank, total tiebreak, never-collapse), the **§4 Claude carrier resolution** (Agent-tool `claude` vs process `claude-cli`), the **api-key availability** check (§1), the **text-leaf** clause (§2), the **folder-scan skip** for `_api/`, and the **§6 web note** (Gemini light). One coherent routing-card edit. STAKES: this is the live routing card — surgical, precise.

## Context Files

> Work-dir = rbtv repo root. Edit the live card that every dispatch depends on.

| File | Purpose |
|------|---------|
| `../specs/deterministic-routing-spec.md` | The selector behavior + total order + edge cases |
| `orchestration/skills/orchestrating/cards/routing.md` | The file to edit (read §1, §2, §4, §6, §8 first) |
| `orchestration/models/manifest-schema.md` | The variant fields the selector reads (after p1-1) |
| `1-projects/rbtv-evolution/orchestration/api-workers/api-workers-design.md` | §7 routing integration; §9 key availability |

## Allowlist

- ✎ modify: `orchestration/skills/orchestrating/cards/routing.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. **§1 availability:** add the api-key availability test (key resolves = available) alongside folder-presence; add the explicit **skip underscore/infra dirs** (`_api/`) in the folder-scan.
2. **§2:** add the **deterministic selector** (enumerate all `(model,variant)` → filter capable → rank `cost_class` → `evidence_status` → `reasoning_tier` fit → name; pins/stakes apply AFTER) + the **never-collapse** rule (name the pair, never a bare provider) + the **text-leaf** clause (API workers carry `code_competence: none` → compete on text leaves).
3. **§4 carrier resolution:** name the two Claude carriers — `claude` (Agent-tool, default) vs `claude-cli` (process, sub-conductor/native-rules); both enumerated.
4. **§6:** add the web note (API web → Gemini grounding only; rigorous research → `rbtv-web-searching`/qwen).

### Phase: Validate
1. The selector text is a total order (no tie survives all keys) — trace two sample leaves.
2. No invariant in `core-protocol.md` is contradicted; cross-references resolve.
3. Atomic-files: additive, self-contained, no content duplication.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Routing card with selector + carrier + API hooks | `orchestration/skills/orchestrating/cards/routing.md` | Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
