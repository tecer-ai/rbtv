---
task_id: p3-1
status: pending
phase: understand
complexity_score: 8
human_review: required
executor: { model: claude, variant: opus }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/claude/manifest.yaml   # rbtv repo root
    - orchestration/models/claude/delta.md
orchestrator_executed: false
---

# Task p3-1: CREATE the Agent-tool Claude package

## Goal

CREATE `orchestration/models/claude/manifest.yaml` + `delta.md` — the **Agent-tool Claude tiers** (`opus`, `sonnet`; no haiku) as routable `(model, variant)` rows, **sibling to** `models/claude-cli/` (process). This is the absence that caused the "Claude collapses to one entity" gap.

## Context Files

> Work-dir = rbtv repo root. This is the keystone of the Claude-collapse fix.

| File | Purpose |
|------|---------|
| `../specs/deterministic-routing-spec.md` | The Context Snapshot defines every variant field value |
| `orchestration/models/claude-cli/manifest.yaml` | The sibling (process) package — mirror the opus/sonnet capability data, differ on transport/auth/guidance/swarm |
| `orchestration/models/manifest-schema.md` | Schema + the repurposed-field semantics |
| `orchestration/skills/orchestrating/cards/routing.md` | §4 carrier distinction (Agent-tool vs process) this package formalizes |

## Allowlist

- ✚ create: `orchestration/models/claude/manifest.yaml`
- ✚ create: `orchestration/models/claude/delta.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.
2. Note the carrier difference: Agent-tool (in-session, no native `CLAUDE.md` load, nesting wall, `auth.method: none`) vs claude-cli (process, loads `CLAUDE.md`).

### Phase: Execute
1. `manifest.yaml`: `model: claude`, `evidence_status: validated`; variants `opus` (`reasoning_tier: top`, `cost_class: high`, ctx 1M, `code_competence: strong`) and `sonnet` (`mid`/`mid`/strong). `web_access: false` native (web is via the `rbtv-web-searching` skill, not native API). `auth: {required: false, method: none, interactive: false}`. Repurpose `headless` (in-session), `guidance_file` omitted, `confinement.write_enforcement: git-diff-vs-allowlist`, `swarm_support` nesting-wall. NO haiku variant (sonnet floor; note why).
2. `delta.md`: how the conductor dispatches an Agent-tool sub-agent (no CLI invocation); the model-transport-note (the five fields ARE the sub-agent's final reply).

### Phase: Validate
1. YAML parses; required fields per variant; distinct from `claude-cli` on transport/auth/guidance.
2. A selector run (p3-2) can enumerate `claude:opus` / `claude:sonnet`.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Agent-tool Claude package | `orchestration/models/claude/{manifest.yaml,delta.md}` | YAML + Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
