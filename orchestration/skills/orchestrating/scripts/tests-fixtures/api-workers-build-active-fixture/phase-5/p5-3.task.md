---
task_id: p5-3
status: pending
phase: understand
complexity_score: 6
human_review: required
executor: { model: kimi, variant: default }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - orchestration/models/_api/run.py   # rbtv repo root — SHARED FILE: serialize after p1-4
orchestrator_executed: false
---

# Task p5-3: UPDATE run.py — agentic-profile handling (minimal)

## Goal

UPDATE `orchestration/models/_api/run.py` to handle the agentic profile generically: a **per-client timeout** (Manus needs minutes) and reading the client's `structured_output` flag to route `False` clients through the **existing raw-dump path**. NO Manus-specific branch — the flag drives it.

## Context Files

> Work-dir = rbtv repo root. SHARED FILE — `run.py` was created in p1-4; this is the only later edit to it. Keep the change minimal (flag + timeout).

| File | Purpose |
|------|---------|
| `../specs/manus-worker-spec.md` | The agentic profile + the `structured_output:false`→fallback rule |
| `../specs/api-text-worker-spec.md` | The existing raw-dump fallback this reuses |
| `orchestration/models/_api/run.py` | The file to edit (the client-dispatch + fallback code) |
| `orchestration/models/_api/clients/manus.py` | The client whose flag/timeout this honors (p5-1) |

## Allowlist

- ✎ modify: `orchestration/models/_api/run.py`

## Execution Flow

### Phase: Understand
1. Read every Context File. Confirm the current run.py fallback path on disk. Mark in-progress in the three locations.

### Phase: Execute
1. Add a per-client timeout (read from the client/manifest; long for Manus).
2. Route clients declaring `structured_output: False` straight to the raw-dump path (the chat-envelope parse is skipped). Generic — driven by the flag, not the provider name.

### Phase: Validate
1. `python -c "import ast; ast.parse(...)"` → EXIT 0.
2. `git diff orchestration/models/_api/run.py` shows ONLY the timeout + flag-routing change (no provider-name special-case). Behavior proven at `p5-checkpoint`.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| run.py with generic agentic handling | `orchestration/models/_api/run.py` | Python |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.

## Errata

### ADX-1 (per D-exec-6) — ALSO wire a generic grounding / extra-params pass-through
In addition to the per-client timeout + `structured_output`-routing above, ADD a **generic** grounding pass-through to run.py: a `--grounded` flag (and/or `--extra-params <json>`) that populates `RequestOptions.extra_params` (e.g. `{"grounding": true}`) on the `client.chat(...)` call. `gemini.py` ALREADY reads `extra_params["grounding"]` to enable Google search-grounding (which switches its return to a raw-dump — grounding is mutually exclusive with JSON mode, so the existing raw-dump fallback handles it). The pass-through MUST be GENERIC (flag → `extra_params`), NEVER provider-specific. This is the Phase-5 wiring that makes Gemini grounding reachable through the runner — it was DEFERRED from Phase 2 (D-exec-6: Phase 2 shipped gemini.py grounding-capable but run.py un-wired). Extend the `git diff` validation (Phase: Validate step 2) to allow this flag + `extra_params` change alongside the timeout/flag-routing change.
