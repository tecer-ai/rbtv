---
task_id: p5-4
status: pending
phase: understand
complexity_score: 6
human_review: required
executor: { model: claude, variant: opus }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - orchestration/skills/orchestrating/cards/routing.md   # rbtv repo root — SHARED FILE: serialize after p3-2
orchestrator_executed: false
---

# Task p5-4: UPDATE routing.md — the autonomous-web leaf (Manus)

## Goal

UPDATE `orchestration/skills/orchestrating/cards/routing.md` §6 to add the **autonomous-web leaf**: route autonomous/browser/multi-step-data-collection tasks to **Manus**; light grounding stays **Gemini**; rigorous multi-source research stays `rbtv-web-searching`/qwen. Distinguish the three web tiers.

## Context Files

> Work-dir = rbtv repo root. SHARED FILE — routing.md was edited in p3-2; serialize after it. Additive §6 edit only.

| File | Purpose |
|------|---------|
| `../specs/manus-worker-spec.md` | Manus's autonomous-web capability |
| `orchestration/skills/orchestrating/cards/routing.md` | The file to edit (read §6 research leaf, post-p3-2) |
| `orchestration/models/manus/manifest.yaml` | Manus's `web_access: true` agentic profile (p5-2) |

## Allowlist

- ✎ modify: `orchestration/skills/orchestrating/cards/routing.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Confirm p3-2's selector edit is present. Mark in-progress in the three locations.

### Phase: Execute
1. §6: add the three-tier web routing — Manus (autonomous/browser), Gemini (light grounding), `rbtv-web-searching`/qwen (rigorous). Manus carries `code_competence: none` so the code tree already steers away.

### Phase: Validate
1. The §6 addition composes with the p3-2 selector (Manus is enumerated for web leaves); no contradiction.
2. Atomic, additive.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Routing card with autonomous-web leaf | `orchestration/skills/orchestrating/cards/routing.md` | Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
