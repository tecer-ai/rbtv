---
task_id: p5-1
status: pending
phase: understand
complexity_score: 8
human_review: required
executor: { model: kimi, variant: default }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/_api/clients/manus.py   # rbtv repo root
orchestrator_executed: false
---

# Task p5-1: CREATE the Manus agentic client

## Goal

CREATE `orchestration/models/_api/clients/manus.py` — a synchronous client that submits an autonomous **task** to Manus, polls to completion (up to a long timeout), and returns the agent output. Declares `structured_output: False` (no JSON envelope → the runner raw-dumps generically).

## Context Files

> Work-dir = rbtv repo root. This rides the SAME run.py; do not special-case it in run.py (p5-3 adds only the timeout + flag read).

| File | Purpose |
|------|---------|
| `../specs/manus-worker-spec.md` | The contract — task/poll/raw-dump, timeout, failure |
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/providers/manus_client.py` | Source to adapt (async → sync; keep POST /tasks + poll GET /tasks/{id}) |
| `orchestration/models/_api/clients/base.py` | The base it subclasses |

## Allowlist

- ✚ create: `orchestration/models/_api/clients/manus.py`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. Implement `chat()` as: create task (`POST /tasks` with the prompt as `description`) → poll (`GET /tasks/{id}`) until `completed`/`failed`/timeout → return the agent output.
2. Make it synchronous/one-shot; keep retry/backoff on task creation; honor a long (minutes) timeout.
3. Set `structured_output = False`.

### Phase: Validate
1. `python -c "import ast; ast.parse(...)"` → EXIT 0. Behavior proven at `p5-checkpoint`.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Manus agentic client | `orchestration/models/_api/clients/manus.py` | Python |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
