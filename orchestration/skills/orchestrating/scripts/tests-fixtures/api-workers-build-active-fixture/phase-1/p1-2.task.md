---
task_id: p1-2
status: pending
phase: understand
complexity_score: 6
human_review: required
executor: { model: kimi, variant: default }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/_api/clients/base.py   # rbtv repo root
orchestrator_executed: false
---

# Task p1-2: CREATE the synchronous provider base

## Goal

CREATE `orchestration/models/_api/clients/base.py` — the synchronous provider abstraction the shared runner and every API client build on (adapted from the Manus async `base.py`, made one-shot/synchronous).

## Context Files

> Work-dir = rbtv repo root (`3-resources/tools/rbtv/`). Build exactly to the spec — do not invent behavior.

| File | Purpose |
|------|---------|
| `../specs/api-text-worker-spec.md` | The contract — Context Snapshot defines the base interface |
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/providers/base.py` | The async source to adapt (drop `call_with_tools`/`stream`; add `structured_output`) |

## Allowlist

- ✚ create: `orchestration/models/_api/clients/base.py`

## Execution Flow

### Phase: Understand
1. Read both Context Files. Mark in-progress in the plan list, this frontmatter, and `../deliverables.md`.
2. Note: synchronous (`requests`/`httpx`), not `aiohttp`; one-shot per dispatch.

### Phase: Execute
1. Build the dataclasses + abstract `ProviderClient` (`name`, `initialize`, `chat`) per the spec's Context Snapshot. Add a `structured_output: bool` attribute on the base.
2. No tool-loop / streaming methods.

### Phase: Validate
1. `python -c "import ast; ast.parse(open('orchestration/models/_api/clients/base.py').read())"` → EXIT 0.
2. The interface matches the spec exactly (a chat client and the Manus client can both subclass it).

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Synchronous provider base | `orchestration/models/_api/clients/base.py` | Python |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
