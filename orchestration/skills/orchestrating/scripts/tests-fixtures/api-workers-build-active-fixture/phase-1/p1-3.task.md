---
task_id: p1-3
status: pending
phase: understand
complexity_score: 7
human_review: required
executor: { model: kimi, variant: default }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/_api/clients/deepseek.py   # rbtv repo root
orchestrator_executed: false
---

# Task p1-3: CREATE the DeepSeek client

## Goal

CREATE `orchestration/models/_api/clients/deepseek.py` — a synchronous DeepSeek client (OpenAI-compatible, JSON/structured-output mode) subclassing `base.py`, declaring `structured_output: True`, with retry/backoff on rate-limit/5xx.

## Context Files

> Work-dir = rbtv repo root. Re-verify model ids against DeepSeek docs at build (the Manus values are hardcoded 2026 ids).

| File | Purpose |
|------|---------|
| `../specs/api-text-worker-spec.md` | The contract — envelope, JSON mode, error behavior |
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/providers/deepseek_client.py` | The source to adapt (sync; harden the error taxonomy) |
| `orchestration/models/_api/clients/base.py` | The base it subclasses (must exist — p1-2) |

## Allowlist

- ✚ create: `orchestration/models/_api/clients/deepseek.py`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.
2. Confirm the base interface from p1-2 on disk.

### Phase: Execute
1. Implement `chat()`: build the request with JSON/structured-output mode, parse the response, surface `finish_reason`. Harden retry/backoff (rate-limit/5xx) beyond the thin Manus `validate_api_key` ping.
2. Declare `structured_output = True`.

### Phase: Validate
1. `python -c "import ast; ast.parse(...)"` → EXIT 0.
2. Behavior verified end-to-end at the `p1-checkpoint` pilot (real call) — not here.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| DeepSeek client | `orchestration/models/_api/clients/deepseek.py` | Python |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
