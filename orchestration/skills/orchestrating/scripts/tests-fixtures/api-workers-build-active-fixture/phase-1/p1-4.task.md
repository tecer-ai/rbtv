---
task_id: p1-4
status: pending
phase: understand
complexity_score: 10
human_review: required
executor: { model: kimi, variant: default }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/_api/run.py   # rbtv repo root
orchestrator_executed: false
---

# Task p1-4: CREATE the shared runner `run.py`

## Goal

CREATE `orchestration/models/_api/run.py` — the single entry point the conductor invokes via Bash. It resolves the API key, calls the provider in JSON mode, writes the model's envelope files into `--output-folder`, and writes `return.json` (the five-field return). Key NEVER appears in any prompt/log/output.

## Context Files

> Work-dir = rbtv repo root. This is the architectural core — build EXACTLY to the spec.

| File | Purpose |
|------|---------|
| `../specs/api-text-worker-spec.md` | The full contract — behavior, edge cases, key discovery, fallback, sanitization, truncation |
| `1-projects/rbtv-evolution/orchestration/api-workers/api-workers-design.md` | §4 architecture diagram; §6 worker semantics; §9 key mechanism |
| `orchestration/models/_api/clients/base.py` | The base interface (p1-2) |
| `orchestration/models/_api/clients/deepseek.py` | The first client to dispatch to (p1-3) |

## Allowlist

- ✚ create: `orchestration/models/_api/run.py`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. CLI args: `--provider --model --prompt-file --output-folder` (+ optional `--target-file`).
2. **Dynamic client resolution** by `--provider` (import `clients/{provider}.py`) — NEVER hardcode a per-provider branch.
3. Key discovery: walk up to `rbtv.json`, read `env_file`; OS-env first, then file.
4. Read/inline `--prompt-file` and any `--target-file`; call `client.chat(...)` in JSON mode.
5. Parse envelope → write files (path-sanitize, truncation guard); on no-envelope/`structured_output False` → raw-dump fallback.
6. Write `return.json` (`status`/`landed`/`validation`/`concerns`/`open_questions`), creating the output folder if absent.

### Phase: Validate
1. `python -c "import ast; ast.parse('orchestration/models/_api/run.py'...)"` → EXIT 0.
2. `python orchestration/models/_api/run.py --help` → EXIT 0, shows the four flags.
3. End-to-end behavior proven at `p1-checkpoint` (real call) — not here.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Shared API runner | `orchestration/models/_api/run.py` | Python (synchronous) |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
