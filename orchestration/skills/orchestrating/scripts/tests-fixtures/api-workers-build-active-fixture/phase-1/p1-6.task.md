---
task_id: p1-6
status: pending
phase: understand
complexity_score: 3
human_review: none
executor: { model: kimi, variant: no-thinking }
reviewer: { model: claude, variant: sonnet }
allowlist:
  create:
    - orchestration/docs/routing-matrix-reference.md   # rbtv repo root
orchestrator_executed: false
---

# Task p1-6: CREATE the routing-matrix reference doc

## Goal

CREATE `orchestration/docs/routing-matrix-reference.md` — the harvested Manus routing matrix as **reference data** (per-provider models, prices, modes), for the conductor's within-tier tiebreaking. Reference only — NOT a router.

## Context Files

> Work-dir = rbtv repo root. Mechanical transform: harvest + reframe, no new analysis.

| File | Purpose |
|------|---------|
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/docs/routing_matrix.md` | The source matrix (Portuguese) to harvest |

## Allowlist

- ✚ create: `orchestration/docs/routing-matrix-reference.md`

## Execution Flow

### Phase: Understand
1. Read the source. Mark in-progress in the three locations.

### Phase: Execute
1. Harvest the DeepSeek/Gemini/OpenAI/Manus (and Claude/Kimi for context) rows into an English reference doc: models, prices, context, modes, web capability.
2. Add a header stating this is REFERENCE DATA (a within-tier tiebreaker), never the master routing cut (boundedness-first stays the cut).

### Phase: Validate
1. The four in-scope providers' models + prices are present and English.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode).

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Routing-matrix reference | `orchestration/docs/routing-matrix-reference.md` | Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
