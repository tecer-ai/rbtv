---
task_id: p6-1
status: completed
phase: validate
complexity_score: 10
human_review: required
orchestrator_executed: true
hard_halt: true   # real paid API + Manus calls through the live conductor
---

# Task p6-1: Live orchestrated pilot — the "earn their keep" test

## Goal

Through the **real conductor**, route a genuine **text leaf** to an API chat worker (e.g. DeepSeek synthesis over inlined sources) AND a genuine **autonomous leaf** to Manus, end-to-end: the deterministic selector picks the `(model, variant)`, the lighter verification gate fires, evidence is captured. This is the cross-strategy-seam pilot and the test of whether the API/agentic workers actually get routed to and win.

## Context Files

| File | Purpose |
|------|---------|
| `../decisions.md` | The full shaping + the "do they earn their keep?" risk |
| `../specs/{api-text-worker,manus-worker,deterministic-routing}-spec.md` | The three contracts exercised together |
| `3-resources/tools/rbtv/orchestration/skills/orchestrating/cards/routing.md` | The selector + leaves that pick the workers |
| `3-resources/tools/rbtv/orchestration/skills/orchestrating/cards/verification.md` | The lighter gate that fires |

## Tools

| Tool | Purpose |
|------|---------|
| `rbtv-orchestrating` | This task IS an orchestrated run — invoke the skill and follow its core protocol; it dispatches the workers and runs the gates |

## Execution Flow

### Phase: Understand
1. Read every Context File + `decisions.md`. Mark in-progress in the three locations.
2. Choose ONE real text leaf and ONE real autonomous leaf where an API/Manus worker plausibly beats an in-session Claude sub-agent (cost or capability) — name the leaf explicitly.

### Phase: Execute
1. **HARD HALT** — surface projected spend for both real calls; get go-ahead.
2. Run the orchestrated dispatch: the conductor enumerates `(model, variant)`, routes the text leaf to a chat worker and the autonomous leaf to Manus, packages each via the dispatch-wrapper, and reconciles each return through the lighter gate (files-exist + `landed` + reviewer-reads-content).
3. Capture evidence files (selection trace, output folders, `return.json`, reviewer verdicts) on disk.

### Phase: Validate
1. Both leaves produced real, reconciled output; the selector named pairs; the gate certified non-development returns correctly.
2. Record the result on the unvalidated cross-strategy seam (run-log), honestly — whether the API/Manus worker actually won its leaf.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block (include the honest "earn their keep" verdict).

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Live-pilot evidence + seam verdict | `1-projects/rbtv-evolution/coding/done-gate-evidence/rbtv/{date}-orchestrated-pilot/` + evidence sheet | Files + Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
