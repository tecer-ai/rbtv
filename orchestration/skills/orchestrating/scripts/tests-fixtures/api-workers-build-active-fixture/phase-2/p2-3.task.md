---
task_id: p2-3
status: pending
phase: understand
complexity_score: 6
human_review: optional
executor: { model: kimi, variant: default }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/_api/clients/openai.py   # rbtv repo root
orchestrator_executed: false
---

# Task p2-3: CREATE the OpenAI client

## Goal

CREATE `orchestration/models/_api/clients/openai.py` — a synchronous OpenAI-compatible client (JSON/structured-output mode) subclassing `base.py`, `structured_output: True`.

## Context Files

> Work-dir = rbtv repo root. Adding this client must NOT edit `run.py`. Re-verify model ids at build.

| File | Purpose |
|------|---------|
| `../specs/api-text-worker-spec.md` | The contract |
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/providers/openai_client.py` | Source to adapt (sync; harden) |
| `orchestration/models/_api/clients/base.py` | The base it subclasses |

## Allowlist

- ✚ create: `orchestration/models/_api/clients/openai.py`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. Implement `chat()` with JSON/structured-output mode; surface `finish_reason`; harden retry/backoff.

### Phase: Validate
1. `python -c "import ast; ast.parse(...)"` → EXIT 0. Behavior proven at `p2-checkpoint`.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode).

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| OpenAI client | `orchestration/models/_api/clients/openai.py` | Python |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
