---
task_id: p4-7
status: pending
phase: understand
complexity_score: 6
human_review: required
executor: { model: claude, variant: sonnet }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - README.md                              # rbtv repo root
    - admin/install/module-manifest.json
    - modules/<orchestration-module>.md      # resolve the exact orchestration module file at build
orchestrator_executed: false
---

# Task p4-7: UPDATE docs-sync surfaces (README + module + manifest)

## Goal

UPDATE the rbtv-repo docs-sync surfaces so the new installable components are registered (the rbtv `CLAUDE.md` hard rule): `README.md` (if it documents the inventory/install), the owning **orchestration module file** in `modules/`, and `admin/install/module-manifest.json` (the runner, `base.py` + the clients, the 5 model packages, the reference doc).

## Context Files

> Work-dir = rbtv repo root. Resolve the exact orchestration module filename from `admin/install/module-manifest.json` at build.

| File | Purpose |
|------|---------|
| `3-resources/tools/rbtv/CLAUDE.md` | The docs-sync hard rule (what MUST be updated together) |
| `README.md` | Inventory/install docs to refresh if affected |
| `admin/install/module-manifest.json` | The installable-file list to extend |
| `modules/` | The orchestration module file (component list) |

## Allowlist

- ✎ modify: `README.md`
- ✎ modify: `admin/install/module-manifest.json`
- ✎ modify: the orchestration module file under `modules/`

## Execution Flow

### Phase: Understand
1. Read every Context File. Resolve the orchestration module filename. Mark in-progress in the three locations.

### Phase: Execute
1. Register the new installable files (runner, `_api/clients/*`, `models/{deepseek,gemini,openai,manus,claude}/`, the reference doc) in `module-manifest.json`.
2. Update the orchestration module's component list; update `README.md` inventory/install steps if affected.

### Phase: Validate
1. `python -c "import json; json.load(open('admin/install/module-manifest.json'))"` → EXIT 0.
2. Every new component appears in exactly the right surface; no stale/duplicate entries.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Synced docs/module/manifest | `README.md`, `modules/<orchestration>.md`, `admin/install/module-manifest.json` | Markdown + JSON |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
