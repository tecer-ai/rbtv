---
task_id: p4-2
status: pending
phase: understand
complexity_score: 9
human_review: required
executor: { model: claude, variant: opus }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - orchestration/skills/orchestrating/cards/verification.md   # rbtv repo root
orchestrator_executed: false
---

# Task p4-2: UPDATE verification.md — lighter gate for text-worker returns

## Goal

UPDATE `orchestration/skills/orchestrating/cards/verification.md` so an **API text-worker return** is classified **non-development** (clears at §1 + a content review, no git reconciliation, no test suite): reconcile **files-exist + non-empty against the runner's `landed` list** (deliverable-scoped — not "folder non-empty", since the destination may pre-exist) + envelope-valid, then a reviewer Claude reads the output for quality.

## Context Files

> Work-dir = rbtv repo root.

| File | Purpose |
|------|---------|
| `1-projects/rbtv-evolution/orchestration/api-workers/api-workers-design.md` | §8 lighter verification gate |
| `orchestration/skills/orchestrating/cards/verification.md` | The file to edit (read §1c reconciliation, §1d, the "development work" definition) |
| `../specs/api-text-worker-spec.md` | The return + `landed` semantics |

## Allowlist

- ✎ modify: `orchestration/skills/orchestrating/cards/verification.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. In the "development work defined" boundary, classify API text returns as **non-development** (clear at §1 + content review; §2 review reads content, §3 cold verifier does not fire for non-development text).
2. In §1c, add the **deliverable-scoped reconciliation**: reconcile against the runner's `landed` list (specific paths), NOT "folder non-empty" — the destination may already hold files. Envelope-valid + `finish_reason == stop` are the field checks.
3. Keep the agentic-worker (Manus) note as a forward pointer (p5-5 adds its latency/cost specifics).

### Phase: Validate
1. No contradiction with the done-gate rule or core invariant 3 (disk = truth; here "disk" = the output folder).
2. Atomic, additive; cross-references resolve.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Verification card with lighter text gate | `orchestration/skills/orchestrating/cards/verification.md` | Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
