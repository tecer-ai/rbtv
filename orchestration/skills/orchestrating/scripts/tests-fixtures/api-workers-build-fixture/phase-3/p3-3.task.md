---
task_id: p3-3
status: pending
phase: understand
complexity_score: 7
human_review: required
executor: { model: claude, variant: opus }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - orchestration/skills/orchestrating/cards/intake.md   # rbtv repo root
orchestrator_executed: false
---

# Task p3-3: UPDATE intake.md — budget summary enumerates `(model, variant)`

## Goal

UPDATE `orchestration/skills/orchestrating/cards/intake.md` §4 (the budget question) so the provisional map + spend forecast **enumerate `(model, variant)` pairs** (reading per-variant `cost_class`), never bare-provider roles like "a top-tier Claude". This kills the collapse at the surface the user saw.

## Context Files

> Work-dir = rbtv repo root.

| File | Purpose |
|------|---------|
| `../specs/deterministic-routing-spec.md` | The never-collapse behavior (Behavior #5) |
| `orchestration/skills/orchestrating/cards/intake.md` | The file to edit (read §4 first) |
| `orchestration/skills/orchestrating/cards/routing.md` | The selector intake defers to (after p3-2) |

## Allowlist

- ✎ modify: `orchestration/skills/orchestrating/cards/intake.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. §4: change the provisional-map sketch to assign each work-cluster a **default `(model, variant)`** from the enumerated manifests (per the routing selector), and the spend forecast to read per-variant `cost_class` — flagging `probe-pending` API variants as ESTIMATE-ONLY.
2. State the rule: the budget summary names pairs (e.g. `claude:sonnet`), never a bare provider.

### Phase: Validate
1. A sample budget summary for a mixed build names pairs on every row.
2. No contradiction with routing §2 selector or core invariant 6.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Intake card with pair-enumerated budget summary | `orchestration/skills/orchestrating/cards/intake.md` | Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
