---
task_id: p2-4
status: pending
phase: understand
complexity_score: 6
human_review: optional
executor: { model: claude, variant: sonnet }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/openai/manifest.yaml   # rbtv repo root
    - orchestration/models/openai/delta.md
orchestrator_executed: false
---

# Task p2-4: CREATE the OpenAI model package (multi-variant)

## Goal

CREATE `orchestration/models/openai/manifest.yaml` + `delta.md` — OpenAI as `(model, variant)` rows by tier (`gpt-5.5`, `5.4-mini`, `5.4-nano`), capped to routing-distinct variants.

## Context Files

> Work-dir = rbtv repo root.

| File | Purpose |
|------|---------|
| `orchestration/models/manifest-schema.md` | Schema + repurposed-field semantics |
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/docs/routing_matrix.md` | OpenAI §5 — tiers, context |
| `orchestration/models/deepseek/manifest.yaml` | Sibling API package example |

## Allowlist

- ✚ create: `orchestration/models/openai/manifest.yaml`
- ✚ create: `orchestration/models/openai/delta.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. Variants: `gpt-5.5` (`cost_class: high`, `reasoning_tier: top`), `5.4-mini` (`cost_class: mid`, `reasoning_tier: mid`), `5.4-nano` (`cost_class: cheapest`, `reasoning_tier: mid`) — merge any that route identically. All `code_competence: none`, `web_access: false`, `resume_support: none`, `auth: api-key`, `evidence_status: probe-pending`.
2. `delta.md`: shared `run.py` invocation + API transport note.

### Phase: Validate
1. YAML parses; required fields per variant; each variant routes distinctly (else merge).

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode).

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| OpenAI package | `orchestration/models/openai/{manifest.yaml,delta.md}` | YAML + Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
