---
task_id: p2-1
status: pending
phase: understand
complexity_score: 7
human_review: optional
executor: { model: kimi, variant: default }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/_api/clients/gemini.py   # rbtv repo root
orchestrator_executed: false
---

# Task p2-1: CREATE the Gemini client

## Goal

CREATE `orchestration/models/_api/clients/gemini.py` — a synchronous Gemini client (its own REST shape + JSON mode + optional search-grounding) subclassing `base.py`, `structured_output: True`.

## Context Files

> Work-dir = rbtv repo root. Adding this client must NOT edit `run.py` (dynamic resolution). Re-verify model ids at build.

| File | Purpose |
|------|---------|
| `../specs/api-text-worker-spec.md` | The contract — envelope, JSON mode, fallback |
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/providers/gemini_client.py` | Source to adapt (sync; keep grounding) |
| `orchestration/models/_api/clients/base.py` | The base it subclasses |

## Allowlist

- ✚ create: `orchestration/models/_api/clients/gemini.py`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. Implement `chat()` against Gemini's REST shape with JSON mode; expose search-grounding as an option (the only web-capable chat client).
2. Map Gemini's `finish_reason` equivalent for the truncation guard.

### Phase: Validate
1. `python -c "import ast; ast.parse(...)"` → EXIT 0. Behavior proven at `p2-checkpoint`.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode).

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Gemini client | `orchestration/models/_api/clients/gemini.py` | Python |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
