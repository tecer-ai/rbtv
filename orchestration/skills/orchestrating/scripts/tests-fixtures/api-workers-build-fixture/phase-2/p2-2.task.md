---
task_id: p2-2
status: pending
phase: understand
complexity_score: 6
human_review: optional
executor: { model: claude, variant: sonnet }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/gemini/manifest.yaml   # rbtv repo root
    - orchestration/models/gemini/delta.md
orchestrator_executed: false
---

# Task p2-2: CREATE the Gemini model package (multi-variant)

## Goal

CREATE `orchestration/models/gemini/manifest.yaml` + `delta.md` — Gemini as multiple `(model, variant)` rows (`3.5-flash`, `3.1-flash-lite`, and `3.1-pro` only if it routes distinctly), each `web_access: true` (the only web-capable chat worker).

## Context Files

> Work-dir = rbtv repo root. Cap variants to routing-distinct ones.

| File | Purpose |
|------|---------|
| `orchestration/models/manifest-schema.md` | Schema + repurposed-field semantics |
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/docs/routing_matrix.md` | Gemini §4 — models, prices, grounding |
| `orchestration/models/deepseek/manifest.yaml` | The sibling API package as a filled example (p1-5) |

## Allowlist

- ✚ create: `orchestration/models/gemini/manifest.yaml`
- ✚ create: `orchestration/models/gemini/delta.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. Variants: `3.5-flash` (`cost_class: mid`, `reasoning_tier: top`), `3.1-flash-lite` (`cost_class: cheapest`, `reasoning_tier: mid`). Both `web_access: true`, `code_competence: none`, `resume_support: none`, `auth: api-key`, `evidence_status: probe-pending`. Repurpose CLI-shaped fields (`tool_surface.native: [web]`).
2. `delta.md`: the shared `run.py` invocation + the API transport note; flag grounding.

### Phase: Validate
1. YAML parses; required fields per variant; each variant routes distinctly.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode).

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Gemini package | `orchestration/models/gemini/{manifest.yaml,delta.md}` | YAML + Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
