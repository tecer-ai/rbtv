---
task_id: p5-2
status: pending
phase: understand
complexity_score: 7
human_review: required
executor: { model: claude, variant: opus }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/manus/manifest.yaml   # rbtv repo root
    - orchestration/models/manus/delta.md
orchestrator_executed: false
---

# Task p5-2: CREATE the Manus model package (agentic profile)

## Goal

CREATE `orchestration/models/manus/manifest.yaml` + `delta.md` — Manus as a distinct **agentic** worker: `web_access: true`, `code_competence: none`, `cost_class: high`, `resume_support: none` (poll is internal), `evidence_status: probe-pending`, single variant `manus-autonomous`. Routes to the autonomous-web leaf, never the code path.

## Context Files

> Work-dir = rbtv repo root. This is a NEW worker profile — its fields differ meaningfully from the chat workers.

| File | Purpose |
|------|---------|
| `../specs/manus-worker-spec.md` | The agentic worker contract |
| `orchestration/models/manifest-schema.md` | Schema + repurposed-field semantics |
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/docs/routing_matrix.md` | Manus §7 — autonomy, per-task cost, 5-min timeout |
| `orchestration/models/deepseek/manifest.yaml` | A filled API package for structure |

## Allowlist

- ✚ create: `orchestration/models/manus/manifest.yaml`
- ✚ create: `orchestration/models/manus/delta.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. `manifest.yaml`: single variant `manus-autonomous`; `web_access: true` (autonomous browser), `code_competence: none`, `cost_class: high`, `resume_support: none`, `auth: api-key`, `evidence_status: probe-pending`. Repurpose `tool_surface.native: [web, browser]`, `confinement` output-folder, `swarm_support` disabled. `rate_budget_notes`: per-task cost + minutes-scale latency.
2. `delta.md`: the `run.py --provider manus` invocation; the agentic transport note (always raw-dump; long timeout; non-development verification path).

### Phase: Validate
1. YAML parses; required fields present; profile clearly distinct from the chat workers (web+agentic+per-task).

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Manus package | `orchestration/models/manus/{manifest.yaml,delta.md}` | YAML + Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.

---

## ADX-3 erratum (conductor, 2026-06-09T17:04Z) — folds in the D-exec-3 schema amendment

**Allowlist EXPANDED — add to `modify`:**
- ✎ modify: `orchestration/models/manifest-schema.md` (rbtv repo root)

**Added Execute step (author the schema block BEFORE/ALONGSIDE filling `manifest.yaml` — the manifest must MATCH it):**

In `orchestration/models/manifest-schema.md` §2a, ADD a THIRD canonical repurposed-values block, titled for the **agentic-web worker class** — autonomous-agent task-API workers (e.g. Manus: submit a task → poll → retrieve the agent's output; the agent runs its OWN browser/tool loop **server-side**). Mirror the EXACT table format of the two existing §2a blocks (API chat workers; Agent-tool in-session workers). Define the four repurposed CLI-shaped fields with these values:

| Field | Agentic-web value | Router meaning |
|-------|-------------------|----------------|
| `headless` | `flags: ""` (runner non-interactive), `auto_approves_tools: false` (no approval surface — the agent's loop is server-side), `prompt_transport: arg` (prompt → runner via `--prompt-file`, inlined into the task `description`) | The runner is always non-interactive; no approval dialog exists. |
| `tool_surface` | `native: [web, browser]` (the agent's autonomous browser is its defining capability), `mcp: false`, `agent_file: false` | The agent executes its OWN server-side tool loop, NOT ours — `[web, browser]` gates the autonomous-web leaf. |
| `confinement` | `workspace_scope: "output-folder only — no local filesystem access"`, `tool_restriction: "none — the agent runs its own server-side tool loop, not ours"`, `write_enforcement: "runner writes only the agent's returned output into --output-folder; path-sanitized"` | Confinement is the runner's job; the conductor scopes no work-dir. |
| `swarm_support` | `subagents: false`, `nesting: none`, `default: disabled` | The agent cannot spawn OUR sub-workers; its internal autonomy is server-side and invisible to our topology/depth-cap. |

`manus/manifest.yaml` is the FIRST consumer of this block — fill its repurposed fields to MATCH the values you document here (the point of folding these into one task is that the schema pattern and its first instance stay consistent).

**Rationale:** D-exec-3 pre-authorized "fold the schema amendment into p5-2". `manifest-schema.md` is clean of the paused-mirror footprint → normal staging on commit. Do NOT add a `transport:` discriminator (the repurposed values suffice; §2a's escalation note governs).
