---
task_id: p5-6
status: pending
phase: understand
complexity_score: 4
human_review: optional
executor: { model: claude, variant: sonnet }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - README.md                              # rbtv repo root — SHARED: serialize after p4-7
    - admin/install/module-manifest.json
    - modules/<orchestration-module>.md
orchestrator_executed: false
---

# Task p5-6: UPDATE docs-sync for the Manus package

## Goal

UPDATE the docs-sync surfaces so the Manus package + client are registered (the rbtv `CLAUDE.md` hard rule): `module-manifest.json`, the orchestration module file, and `README.md` if affected.

## Context Files

> Work-dir = rbtv repo root. SHARED FILES — registered in p4-7; serialize after it (add only the Manus entries).

| File | Purpose |
|------|---------|
| `3-resources/tools/rbtv/CLAUDE.md` | The docs-sync hard rule |
| `admin/install/module-manifest.json` | Add `models/manus/` + `clients/manus.py` |
| `modules/` | The orchestration module component list |
| `README.md` | Inventory if affected |

## Allowlist

- ✎ modify: `README.md`
- ✎ modify: `admin/install/module-manifest.json`
- ✎ modify: the orchestration module file under `modules/`

## Execution Flow

### Phase: Understand
1. Read every Context File. Confirm p4-7's registrations are present. Mark in-progress in the three locations.

### Phase: Execute
1. Add `models/manus/{manifest.yaml,delta.md,manual.md}` + `_api/clients/manus.py` to `module-manifest.json` and the orchestration module list; update README if affected.

### Phase: Validate
1. `python -c "import json; json.load(open('admin/install/module-manifest.json'))"` → EXIT 0; Manus entries present, no duplicates of p4-7's.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode).

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Docs-sync for Manus | `README.md`, `modules/<orchestration>.md`, `admin/install/module-manifest.json` | Markdown + JSON |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
