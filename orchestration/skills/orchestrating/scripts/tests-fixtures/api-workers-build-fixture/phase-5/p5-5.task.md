---
task_id: p5-5
status: pending
phase: understand
complexity_score: 5
human_review: required
executor: { model: claude, variant: opus }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - orchestration/skills/orchestrating/cards/verification.md   # rbtv repo root — SHARED FILE: serialize after p4-2
orchestrator_executed: false
---

# Task p5-5: UPDATE verification.md — Manus latency + per-task cost

## Goal

UPDATE `orchestration/skills/orchestrating/cards/verification.md` so the §1b `WALL_MS` plausibility note accounts for the **agentic worker**: a Manus task taking minutes is PLAUSIBLE (not a "did not run" auto-reject like a sub-second chat call), and note the per-task (not per-token) cost for the budget tripwire.

## Context Files

> Work-dir = rbtv repo root. SHARED FILE — verification.md was edited in p4-2; serialize after it. Small additive note.

| File | Purpose |
|------|---------|
| `../specs/manus-worker-spec.md` | Manus's minutes-scale latency + per-task cost |
| `orchestration/skills/orchestrating/cards/verification.md` | The file to edit (read §1b WALL_MS, post-p4-2) |

## Allowlist

- ✎ modify: `orchestration/skills/orchestrating/cards/verification.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Confirm p4-2's lighter-gate edit is present. Mark in-progress in the three locations.

### Phase: Execute
1. Add the agentic-worker WALL_MS exception (minutes are plausible for a Manus autonomous task) so the plausibility tripwire does not false-reject a real long task.
2. Note per-task cost for the budget surface.

### Phase: Validate
1. The note composes with p4-2's lighter gate; no contradiction with the spec-template plausibility bars (it CALIBRATES them for this work class).

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Verification card with Manus latency/cost note | `orchestration/skills/orchestrating/cards/verification.md` | Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
