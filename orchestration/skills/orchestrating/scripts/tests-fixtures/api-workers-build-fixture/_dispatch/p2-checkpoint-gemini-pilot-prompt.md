Produce two short files for a quick connectivity test.

1. A file named `gemini-haiku.md` containing a three-line haiku about searching the web.
2. A file named `gemini-fact.md` containing exactly one sentence describing what the Google Gemini API does.

Keep both files short. Return them in the required JSON object envelope (the files array).
