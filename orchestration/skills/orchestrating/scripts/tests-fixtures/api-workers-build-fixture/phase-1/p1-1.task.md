---
task_id: p1-1
status: pending
phase: understand
complexity_score: 6
human_review: required
executor: { model: claude, variant: sonnet }
reviewer: { model: claude, variant: opus }
allowlist:
  modify:
    - orchestration/models/manifest-schema.md   # rbtv repo root
orchestrator_executed: false
---

# Task p1-1: Document the schema-fit (a) repurpose + api-key availability

## Goal

UPDATE `orchestration/models/manifest-schema.md` so the CLI-shaped required fields (`headless`, `tool_surface`, `confinement`, `swarm_support`) carry **API-meaningful repurposed semantics** for API/agentic workers, and so `auth.method: api-key` availability is defined as "the key resolves" (OS-env or `env_file`). NO enum change. NO `transport:` discriminator (that stays the documented escalation).

## Context Files

> Work-dir = rbtv repo root (`3-resources/tools/rbtv/`). External refs are vault-root-relative.

| File | Purpose |
|------|---------|
| `1-projects/rbtv-evolution/orchestration/api-workers/api-workers-design.md` | §0.4 schema-fit decision; §9 key mechanism |
| `../specs/deterministic-routing-spec.md` | The repurposed-field values the manifests will use |
| `orchestration/models/manifest-schema.md` | The file to edit (read §2 field list, §3 consumer table first) |

## Allowlist

- ✎ modify: `orchestration/models/manifest-schema.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the plan list, this frontmatter, and `../deliverables.md`.
2. Confirm the four CLI-shaped fields and the `auth` field shape in §2.

### Phase: Execute
1. Add a subsection documenting the **repurposed values** for API/agentic workers (no tools → `tool_surface.native: []`/`[web]`; output-folder confinement; swarm disabled; non-interactive headless). State `transport:` discriminator as the escalation-only fallback.
2. Add to §1/§3 the rule that for `auth.method: api-key`, **availability = the key resolves** (OS-env first, then `env_file`) — distinct from `cli-login`'s USER-EXECUTED login.
3. Add the variant field-count discipline note: a variant exists only where a mode changes a routing-relevant field.

### Phase: Validate
1. The four required fields remain required (no removal); the repurposed semantics are additive prose, not a schema-structure change.
2. Re-read as a fresh router: an API manifest filled per the new prose is unambiguous.

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode). `human_review: required` → emit the Human Review Presentation block.

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Schema doc with repurpose + api-key-availability prose | `orchestration/models/manifest-schema.md` | Markdown (additive) |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
