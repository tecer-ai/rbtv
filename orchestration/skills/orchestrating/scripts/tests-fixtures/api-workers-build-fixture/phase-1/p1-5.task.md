---
task_id: p1-5
status: pending
phase: understand
complexity_score: 6
human_review: optional
executor: { model: claude, variant: sonnet }
reviewer: { model: claude, variant: opus }
allowlist:
  create:
    - orchestration/models/deepseek/manifest.yaml   # rbtv repo root
    - orchestration/models/deepseek/delta.md
orchestrator_executed: false
---

# Task p1-5: CREATE the DeepSeek model package (multi-variant)

## Goal

CREATE `orchestration/models/deepseek/manifest.yaml` + `delta.md` — the routing metadata for DeepSeek as **multiple `(model, variant)` rows** (`v4-flash`, `v4-pro`), filling the CLI-shaped required fields with the repurposed API values (per p1-1).

## Context Files

> Work-dir = rbtv repo root. Re-verify model ids/prices at build.

| File | Purpose |
|------|---------|
| `orchestration/models/manifest-schema.md` | The schema + the repurposed-field semantics (after p1-1) |
| `orchestration/models/kimi/manifest.yaml` | Structural template (copy the SHAPE, not the CLI specifics) |
| `1-projects/rbtv-evolution/orchestration/api-workers/manus-orchestation-scripts/docs/routing_matrix.md` | DeepSeek §2 — `v4-pro`/`v4-flash` ids, prices, context, modes |
| `orchestration/models/_api/clients/deepseek.py` | What `invocation_pointer`/manual will describe |

## Allowlist

- ✚ create: `orchestration/models/deepseek/manifest.yaml`
- ✚ create: `orchestration/models/deepseek/delta.md`

## Execution Flow

### Phase: Understand
1. Read every Context File. Mark in-progress in the three locations.

### Phase: Execute
1. `manifest.yaml`: `model: deepseek`, `evidence_status: probe-pending`; variants `v4-flash` (`cost_class: cheapest`, `reasoning_tier: mid`) and `v4-pro` (`cost_class: low`, `reasoning_tier: top`). Both `code_competence: none`, `web_access: false`, `parallel_safe: true`, `resume_support: none`, `auth: {required: true, method: api-key, interactive: false}`, `invocation_pointer: ./manual.md`. Repurpose `headless`/`tool_surface`/`confinement`/`swarm_support` per p1-1.
2. `delta.md`: the `run.py --provider deepseek --model … --prompt-file … --output-folder …` invocation; the model-transport-note insertion (API return surface).

### Phase: Validate
1. YAML parses; every required schema field present per variant.
2. Each variant changes a routing-relevant field vs the other (else collapse to one).

### Phase: Close
Follow the microstep template Phase: Close (orchestration mode).

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| DeepSeek package | `orchestration/models/deepseek/{manifest.yaml,delta.md}` | YAML + Markdown |

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
