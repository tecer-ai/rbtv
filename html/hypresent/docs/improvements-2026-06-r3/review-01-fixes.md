# Hypresent v3 — Review-01 Fix Map (Session 3, improvements-2026-06-r3)

Resolution of every finding in `review-01.md` (F1–F6 + GENERAL SWEEP G1–G11) applied to the Session-3 planning document set ONLY (`spec.md`, `test-plan.md`). ZERO product/code files touched; zero git writes. The fix corrects CONTRACTS, TEST NUMBERS, and helper executability — every change-map ANCHOR in the spec was confirmed real by the review and is unchanged.

**Status legend:** FIXED (reviewer fix applied as written / orchestrator-directed) · REJECTED (ruled not-applicable, with reason).

**Verdict addressed:** REWORK. All six MUST-land items (F1, F3, F5, F6, G1, G2) applied; all SHOULD-land MINORs (G3, G4, G5, G6, G7, G8, G9, G10, G11) applied. Zero rejections.

---

## MUST-land findings (BLOCKER + MAJOR)

| # | Severity | Status | What changed | Where (doc · section) |
|---|----------|--------|--------------|------------------------|
| **F1** | BLOCKER | FIXED | Inverted R12 contract corrected to the doubled-dist / 1:1-dragged-edge / 2Δ-width model proven by `Ra`'s `b = 2/|direction − fixedDirection|` (verified offset 150863). **S3-6** rewritten: under `fixedDirection:[0,0]`, `e.dist[0]` arrives DOUBLED (`2·Δcursor`), `e.width = startWidth + 2Δ`; the `beforeRect+dist` write is UNCHANGED in code (dist is pre-doubled) but the spec now states width grows 2Δ. Added an **Anchor-relative `e.dist` distinction** block to R10 root-cause (default anchor `b=1` → 1:1; center anchor `b=2` → 2Δ). **R12 contract 1** rewritten: dragged (right) edge tracks cursor 1:1 (`|ΔrightEdge − Δcursor| ≤ 3`, the HEADLINE), left edge mirrors by Δ, width grows 2Δ (`|Δwidth − 2Δcursor| ≤ 3`). **R12 contracts 2/4** aligned. **E-R12-1** assertions → `right ≈ 60 (delta=3)` (headline 1:1), `left moves 60 (delta=3)`, `w ≈ 120 (delta=4)`. **E-R12-4** → `right ≈ 60`, `left ≈ 60`, `w ≈ 120`, basis = startRendered+120. Per-axis tolerance rationale corrected (no more "Δ/2-per-edge" framing). | `spec.md` (S3-6 · R10 root-cause anchor block · R12 contracts 1/2/4) · `test-plan.md` (tolerance rationale · E-R12-1 · E-R12-4) |
| **F3** | MAJOR | FIXED | Replaced the unsound index-based `resolveCloneNode` stamping with a **strip-stable transient-live-stamp** strategy (the review's recommended "approach 1 applied transiently"). **S3-13** fully respecified: in `serialize()` BEFORE `cloneNode` — (1) remove every live `data-hyp-agent` (clears reopened/stale residue), (2) stamp live elements by `matchAnchor` identity from `agentStampMap`; clone; `stripClone` with `data-hyp-agent` EXEMPTED this pass; `finally` removes all live `data-hyp-agent`. Steps 1+2 normalize the live attr set so the exemption is safe and idempotence holds (stale ids gone before clone). Change-map `serialize()`/`stripClone` rows rewritten with the exact sequence + the exemption test `attr.name !== "data-hyp-agent"`. Reconciled Finding line, contracts 4/6, edge-case rows (idempotence, live-residue), non-goals, and risk table to the new strategy. Added **E-R14-8** (stamp a NON-FIRST element `#p-arch`/`#li-2` with selection + markers/wrapper live; assert each `data-hyp-agent~=` selector resolves the exact element on reopen — the desync-killer). | `spec.md` (S3-13 · R14 Finding · R14 contracts 4/6 · R14 change-map `serialize`/`stripClone` · R14 edge-cases · non-goals · risk table) · `test-plan.md` (E-R14-8 · coverage matrix) |
| **F5** | MAJOR | FIXED | Excluded solver-derived flex-grow siblings from the R11 equal-size candidate cache. **S3-7** gained a precise exclusion rule: drop candidate `c` when `c.parentElement === el.parentElement` AND the shared parent's computed `display` is `flex`/`inline-flex` AND `getComputedStyle(c).flexGrow > 0` — applied to BOTH width and height candidate sets; out-of-flow / `flex-grow:0` / explicitly-sized peers remain. Added **E-R11-5** (slow-drag the accent through a frozen sibling start-width; assert NO snap and `hint == 0`; pairs with E-R11-2 which DOES snap to the out-of-flow `twin`). | `spec.md` (S3-7) · `test-plan.md` (E-R11-5 · coverage matrix) |
| **F6** | MAJOR | FIXED | Provided a dead-zone fixture so E-R10-2/-2b are TRUE red-first proofs. Added committed **`flow-grow-deadzone.html`** to spec §Synthetic fixtures (accent `flex-grow:8` + inline `flex-basis:1100px` → leftover-pinned, the genuine R10b condition; PRISTINE `flow-grow.html` has slack → only amplification). Rewrote **E-R10-2** and **E-R10-2b** to open the dead-zone fixture (no priming needed) and stated the expected PRE-fix master numbers from `evidence-summary.md` (G2/G3/G5/G8/G9: basis ±1px, rendered PINNED Δ≈0 → fails `delta=2` by ≈120px / `delta=3` by ≈100px). Added a binding **Red-first proof gate** to the regression section requiring the new RED tests be run against master and the failure numbers recorded in `red-first-log.md` BEFORE writing the fix (demonstrate, not assume). | `spec.md` (§Synthetic fixtures `flow-grow-deadzone.html` · S3-17 fixture count) · `test-plan.md` (E-R10-2 · E-R10-2b · Red-first proof gate · fixtures header) |
| **G1** | MAJOR | FIXED | Added `inlWidth:e.style.width, inlHeight:e.style.height` to the `rendered()` helper (E-R10-3/-4/-5 reference `inlWidth`; was a runtime `KeyError`). | `test-plan.md` (Shared helpers `rendered()`) |
| **G2** | MAJOR | FIXED | Inlined the concrete bodies for the two stubbed helpers. `bridge_get_selection(page)` transcribed verbatim from `test_r2_resize_real.py` E-R2-8 (postMessage `get-selection` round-trip). `edge_handle_screen(self, which)` fully defined with a `_HANDLE_SELECTOR` map for `{"e","se","ne","w"}` → the `.moveable-line`/`.moveable-control` selectors (mapping sourced from `test_r2_resize_real.py::_handle_center` + E-R2-8 edge-line probe + `test_f2_select_guides.py` line 185), with the in-viewport assertion. Added `parse_px`, `_iframe_origin`/`_rect_in_iframe`/`_scroll_into_view` provenance, and made `select_by_native_id` use them. | `test-plan.md` (Shared helpers block) |

---

## SHOULD-land findings (MINOR — all applied)

| # | Status | What changed | Where (doc · section) |
|---|--------|--------------|------------------------|
| **G3** | FIXED | R13 `runtime-main.js` change-map now mandates edit/delete validators require ONLY their listed fields and MUST NOT copy the `!payload.author` check from `reply-comment`/`add-comment` (the composer edit-mode passes no author). Added: validate `replyIndex` with `typeof !== "number"` (NOT a falsy check, or index 0 throws). | `spec.md` (R13 change-map `runtime-main.js`) |
| **G4** | FIXED | `editComment` undo now uses the explicit conditional `if (before.editedAt === undefined) delete thread.editedAt; else thread.editedAt = before.editedAt;` (DELETE the key when absent pre-edit, never set to `undefined`, to keep the in-memory store clean for later `toJson` consumers). `editReply` already restores via full-object replace (`replies[i] = before` from a spread snapshot) → G4-safe, no change needed. | `spec.md` (R13 change-map `editComment`) |
| **G5** | FIXED | `showSizeHint` change-map now requires the overlay position add `window.scrollX/scrollY` (mirror `positionMarker`) since the wrapper origin is document-relative but `getBoundingClientRect()` is viewport-relative. Added a position-overlap assertion to **E-R11-3** (`|hint.left − twin.left| ≤ 8` AND `|hint.top − twin.top| ≤ 8`) so the test catches the scroll-offset mispositioning the bare existence count would miss. | `spec.md` (R11 change-map `showSizeHint`) · `test-plan.md` (E-R11-3) |
| **G6** | FIXED | `agentStampMap` change-map now requires stamping ONLY on a high-confidence anchor — accept the element only if `matchAnchor` resolves via level 1 (unique `data-hyp-hook`) or level 2 (`nativeId`+path+`contentHash`); fall through (levels 3/4/5) → no stamp (the block's `context:` excerpt is the human fallback). Prevents a fuzzy re-resolution stamping a wrong sibling. | `spec.md` (R14 change-map `agentStampMap`) |
| **G7** | FIXED | Surfaced the `flex-shrink:0` D1-extension as an explicit owner-ack flag in R10 Non-goals: D1's locked text names `flex-grow` but not `flex-shrink`; the spec neutralizes shrink (needed for the overfull-row 1:1), scoped to the resized main axis and captured for undo; fallback stated (grow-only) if the owner rejects. | `spec.md` (R10 Non-goals / bounded cases) |
| **G8** | FIXED | Committed the `grid-healthy.html` fixture source to spec §Synthetic fixtures (`#grid-mid` centered middle column `408px 606px 408px`; `#grid-start` start-pinned `justify-items:normal`). Promoted the E-R10-4/-5 prose NOTE into two full numbered cases on the committed fixture (E-R10-4 grid middle `+61→+61 ±2`, x shifts Δ/2; E-R10-5 start-pinned `−160→−160 ±2`, x pinned; both assert NO flex props). | `spec.md` (§Synthetic fixtures `grid-healthy.html` · S3-17) · `test-plan.md` (E-R10-4 · E-R10-5 · coverage matrix · fixtures header) |
| **G9** | FIXED | R10 change-map `applyVisualResize` row now states the flex CROSS-axis write needs NO `top`/`left` compensation (the item is in normal flow; Moveable's `fixedDirection` holds the opposite edge) — the absolute-branch comp must NOT be added to the flex branch. | `spec.md` (R10 change-map `applyVisualResize`) |
| **G10** | FIXED | R12 `onResizeEnd` change-map now requires the `fixedDirection = false` reset survive an `onResize` throw — move the assignment ABOVE the `byId(activeHypId)` early-return guard (or wrap in `finally`) so a thrown gesture cannot leave the anchor stuck symmetric. | `spec.md` (R12 change-map `onResizeEnd`) |
| **G11** | FIXED | Provided the verbatim two-branch flex-write code block in the R10 change-map with the exact `mainDim` expression: `const mainDim = isFlexRow ? "width" : "height"; const mainAxisIdx = isFlexRow ? 0 : 1; el.style.flexGrow = "0"; el.style.flexShrink = "0"; el.style.flexBasis = (beforeRect[mainDim] + dist[mainAxisIdx]) + "px"; if (isFlexRow) { el.style.height = (beforeRect.height + dist[1]) + "px"; } else { el.style.width = (beforeRect.width + dist[0]) + "px"; }` — note `beforeRect[mainDim]` uses the literal STRING key, grow-neutralize MAIN axis only. Sequenced **E-R10-6**'s content-floor probe BEFORE the gesture (it mutates `style.height`, must not interleave mid-drag). | `spec.md` (R10 change-map `applyVisualResize`) · `test-plan.md` (E-R10-6) |

---

## F2 / F4 — confirmed-correct findings (no change required)

| # | Status | Ruling |
|---|--------|--------|
| **F2** | REJECTED (no-op) | The review CONFIRMS the spec's W-handle `dist[0]` claim is correct (`dist` is a signed bounding-box SIZE delta; `beforeRect.width + dist[0]` needs no handle special-casing; default W-anchor `b[0]=1`). The spec already states this (R10 change-map closing note + W-handle edge case) — kept as written. No edit. |
| **F4** | REJECTED (no-op) | The review CONFIRMS S3-4's undo-capture-of-absent-inline is correct (`applyStyleMap` maps `""`→`removeProperty`; `getPropertyValue` returns `""` pristine; capture-before/after both key through `captureSizingState`). G-R10-UNDO's `assertEqual(post["inlGrow"], "")` is implementable as written. The review's sub-note recommends camelCase `m.flexGrow`/`m.flexShrink` for capture consistency — the S3-4 change-map already captures via `s.getPropertyValue("flex-grow") || ""` keyed consistently through one `captureSizingState`, so the before/after key-sets match (the `onResizeEnd` change-detection loop is correct). No defect; no edit. |

---

## Coverage-hole additions (review §G10)

| Hole | Status | Resolution |
|------|--------|------------|
| No test for stamping a non-first element with markers/wrapper live (F3 desync) | FIXED | **E-R14-8** added (see F3). |
| No guard that R12 `fixedDirection` reset survives a mid-gesture error | FIXED | G10 spec change (reset above the early-return guard) + **E-R12-3** already exercises the post-Alt non-Alt one-sided path. |
| Overfull-row bounded deviation has no red-bar test | REJECTED (accepted bound) | Coverage matrix waives it as layout-forced (review concurs it is "acceptable"); the `flex-shrink:0` write's overfull behaviour is bounded by the documented edge case. No separate test added — consistent with the review's "acceptable" ruling. |

---

## Self-verification performed

- **F1 contract direction triple-checked against `Ra`** (`b = 2/|direction − fixedDirection|`, offset 150863): E-handle default `fixedDirection=[-1,0]` → `b[0]=2/2=1` (dist=Δ, dragged edge 1:1, opposite fixed); Alt `fixedDirection=[0,0]` → `b[0]=2/1=2` (dist=2Δ, dragged edge STILL 1:1, opposite mirrors, width 2Δ). The `beforeRect+dist` write is identical in code; only the asserted magnitude differs. R10 contracts 1–5 (DEFAULT anchor) correctly remain Δ; R12 contracts (Alt) correctly state 2Δ. The dragged-edge-tracks-cursor property is the headline assertion in E-R12-1 and E-R12-4.
- **F3 idempotence traced end-to-end** against `serializer.js::serialize()` (clone at line 253) + `stripClone` Phase B (attr strip 180–184) + `comments.js::matchAnchor`: the live-stamp must precede `cloneNode`; the `finally` unstamp guarantees zero live residue; step (1) clears a reopened file's prior stamp BEFORE the clone so a stale id cannot survive (E-R14-6). The exemption (`attr.name !== "data-hyp-agent"`) is the ONLY `stripClone` change; `stripIds`/`data-hyp-id` untouched.
- **G2 helper bodies verbatim-checked** against `test_r2_resize_real.py` (`_handle_center` corner selectors, E-R2-8 `get-selection` promise + edge-line `.moveable-line.moveable-e`) and `test_f2_select_guides.py:185` (`.moveable-control-box .moveable-e || .moveable-se`). The `_HANDLE_SELECTOR` map covers e/se/ne/w with `.moveable-line`-then-`.moveable-control` fallbacks.
- **G11 two-branch block** checked against live `interaction.js::applyVisualResize` (109–111): `isFlexRow` gates which dim is main; the verbatim block grow-neutralizes the MAIN axis only and writes the cross axis plain — matches the existing branch shape, no grow on both axes.
- **Ellipsis/TBD sweep:** `grep '\.\.\.|…'` over `test-plan.md` → ZERO matches; over `spec.md` → ONE match, the JavaScript object-spread `{...thread.replies[replyIndex]}` in the `editReply` change-map (load-bearing code, a shallow-clone snapshot required for G4-safe undo — NOT a placeholder gap). `grep 'TBD|TODO|stub|FIXME|to be (determined|defined)'` over both → ZERO. Every requirement remains falsifiable; no narrative gaps introduced.
- **Change-map anchors unchanged:** the review confirmed every `file:line`/quoted-content anchor exists in the live source; this round corrected CONTRACTS, TEST NUMBERS, FIXTURES, and HELPER BODIES only — no anchor was moved or invented.
