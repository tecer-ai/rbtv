# Run Log — shortcuts-copypaste

Append-only audit log. Each row: timestamp (UTC date) · event · detail. Disk = truth; every worker return is reconciled against `git log` / `git status` before it is logged here.

| When | Event | Detail |
|------|-------|--------|
| 2026-06-09 | INTAKE | Plan-ingest door. Plan `shortcuts-copypaste` (DEEP, orchestrated). Spine initialized in plan folder; `decisions.md` reused (no second decisions file). rbtv repo @ `master`, work-dir `html/hypresent`. |
| 2026-06-09 | CONFIG | Question round answered: run mode = end-to-end; context-refresh = suggest; budget = default tiers (kimi builds, opus reviews). Capsule finalized. |
| 2026-06-09 | PREFLIGHT | kimi 1.41.0 @ `C:\Users\henri\.local\bin\kimi.exe`; pinned-flag gate PASS (all flags present in live --help); AGENTS.md present at rbtv repo root. |
| 2026-06-09 | DISPATCH | W1 `p1-1` → kimi (default variant, thinking-on). Shape B stdin from `phase-1/p1-1.dispatch.md`, work-dir `html/hypresent`. Allowlist: text-format.js, runtime-main.js, test_format_repeat_wholebox.py. Reviewer pin: claude-opus. |
| 2026-06-09 | RETURN+GATE | `p1-1` returned DONE, commit `0c70ed4`. §1 gate PASS: hash in git log; diff = exactly the 3 allowlist files (no out-of-allowlist writes); validation node --check x2 EXIT 0, pytest EXIT 0 (10 passed, ~18.5s), SKIPPED_COUNT 0; convention string present. Disk reconciles. Kimi concerns: test uses `.slide-subtitle` (fixture CSS bolds `.slide-title`); scaleWholeBox handles explicit inline sizes only. |
| 2026-06-09 | REVIEW | `p1-1` → Opus review gate (Agent-tool, pin: Opus reviews all external-CLI code). Pre-flagged: editing-path offset capture/restore, transient contenteditable mgmt, scaleWholeBox undo, ES-cycle avoidance. |
| 2026-06-09 | REVIEW PASS | `p1-1` review CLEAN — no defects, no fixes. Opus re-ran: node --check x2 EXIT 0; pytest 10 passed (17.5s); +16 compat (test_r7_alignment, test_r3_delete) green. All 5 suspect areas + kimi's 2 concerns verified. Forward note → decisions.md: p2-1 keyboard handlers must prime the format-snapshot for the editing-path repeat-fix. |
| 2026-06-09 | CHECKPOINT EXERCISE | p1-checkpoint: conductor exercised C1/C2 headed (real Chrome, real deck, server :8799 w/ HYP_TEST_DIALOG seam). C1 whole-box bold(subtitle)/italic(title)/A+ proportional+undo all HELD; C2 editing-path repeat HELD (selection preserved across both presses). 10 captures + sheet at done-gate-evidence/hypresent/2026-06-08-shortcuts-copypaste.md. held-surprising: bold-on-CSS-bold-text toggles execCommand state (pre-existing quirk). |
| 2026-06-09 | HARD HALT | p1-checkpoint awaiting owner approval before Phase 2 (hard-halt registry; non-overridable). |
| 2026-06-09 | APPROVED | Owner approved p1-checkpoint (option A). Phase 1 COMPLETE. p1-1 + p1-checkpoint flipped ✅. |
| 2026-06-09 | DISPATCH W2 | `p2-1 ∥ p2-2` → kimi, PARALLEL, disjoint allowlists (p2-1: shortcuts.js/runtime-main.js/test_shortcuts.py; p2-2: app/js/shell/shortcuts-help.js, app/index.html, app/css/shell.css, app/js/main.js, test_cheatsheet.py). Both UNCOMMITTED per W2 decision (conductor wave-commits). Shape B stdin. |
| 2026-06-09 | RETURN W2 | p2-2 DONE (named-field, clean). p2-1 DONE but **return was PROSE not the five named fields** (drift instance — logged; disk = truth used). DRIFT noted; reconciled against disk. |
| 2026-06-09 | DISK NOTE | Shared rbtv repo moved during run: HEAD `0c70ed4`→`ccb4332` (parallel session committed orchestration source edits). Tree carries foreign uncommitted files (admin/install, orchestration/*, modules/core.md, core/rules/*). p1-1 `0c70ed4` confirmed still ancestor. |
| 2026-06-09 | RECONCILE+GATE | §1 re-run on disk: node --check x4 EXIT 0; pytest p2-1 (test_shortcuts 5 + test_r3_delete 7 = 12 passed, 20.1s), p2-2 (test_cheatsheet 4 passed, 2.75s). All present in tree. FLAG: p2-1 added out-of-spec `window.__deleteComponentById` test hook (Ctrl+Delete-into-iframe drivability gap) — referred to review. |
| 2026-06-09 | WAVE COMMIT | Conductor staged EXACTLY the 8 W2 files (foreign tree changes excluded), commit `e7e482a` `[p2-1][p2-2]`. |
| 2026-06-09 | REVIEW | Phase 2 → Opus review gate (one review, both tasks; they interact via the `shortcut` bridge contract). Pre-flagged: `window.__deleteComponentById` hook (keep-as-seam vs remove), ES-cycle in shortcuts.js, deleteComponentById guard parity, preventDefault coverage, overlay listener-leak, Ctrl+Alt+C≠Ctrl+Shift+C. |
| 2026-06-09 | REVIEW PASS | Phase 2 review: 1 defect fixed in place — removed out-of-spec `window.__deleteComponentById` global, rewrote delete test to drive real `delete-element` bridge command (commit `87144a5` `[p2-review]`, 2 files). All other areas clean (ES-cycle absent, guard parity byte-identical, preventDefault complete, no listener leak, Ctrl+Alt+C correct, bridge action strings match). Reconciled on disk: hook gone, 16 passed (22.3s). Testability gap + P3 drivability note logged (registry + decisions.md). |
| 2026-06-09 | CHECKPOINT EXERCISE | p2-checkpoint: conductor exercised C3/C4/C5 headed (real Chrome, real deck, server :8799). C3 comment shortcut (iframe + shell focus) HELD; C4 delete (Ctrl+Del via shell focus deletes; Undo restores; plain Delete/Backspace inert; Ctrl+Del-while-editing blocked) HELD; C5 cheat-sheet (Ctrl+/ + ? button open, Esc close, all 3 groups/25 keys) HELD. 7 captures + sheet rows filled. held-surprising: Ctrl+/ opened with iframe focus → Esc won't close (shell Esc listener); natural ?-button flow closes fine. |
| 2026-06-09 | HARD HALT | p2-checkpoint awaiting owner approval before Phase 3 (hard-halt registry; non-overridable). |
| 2026-06-09 | APPROVED (B) | Owner approved p2-checkpoint with condition B: fix the Esc-while-iframe-focus quirk before Phase 3. Owner also requested STOP + full-context capsule for an orchestrator refresh before Phase 3. |
| 2026-06-09 | DISPATCH | `p2-fix` → kimi (single dispatch, self-commit authorized). Fix: scrim `tabindex=-1` + `scrim.focus()` in open() so Esc closes regardless of focus. Allowlist: app/js/shell/shortcuts-help.js, tests/e2e/test_cheatsheet.py. |
| 2026-06-09 | RETURN+COLLISION | `p2-fix` DONE, commit `a3e217d`, fix present (tabindex + scrim.focus), test 5 passed. ⚠️ COLLISION: `a3e217d` swept in 5 FOREIGN files pre-staged in the shared index by a parallel session (admin/install/*, orchestration/mirror/driver/*, cards/routing.md) — `git commit` commits the whole index. No data lost; history NOT rewritten (parallel-session discipline). Logged → learnings.md L1 + decisions.md. Future commits: pathspec-limited only. |
| 2026-06-09 | REVIEW PASS | `p2-fix` Opus review CLEAN (no defects; node --check EXIT 0, test_cheatsheet 5 passed). Note: `close()` doesn't restore focus to opener (pre-existing modal-a11y nicety, out of scope, NOT blocking). |
| 2026-06-09 | CHECKPOINT VERIFY | p2-fix exercised headed: focus in iframe → `Ctrl+/` opens overlay AND focus moves to `.shortcuts-scrim` → `Esc` closes. C5 held-surprising RESOLVED → plain held. Evidence `c5-03-*.png`. p2-checkpoint fully APPROVED; Phase 2 COMPLETE. |
| 2026-06-09 | REFRESH POINT | Stopping before Phase 3 at owner request for orchestrator refresh. Full handoff written to state-capsule.md. |
| 2026-06-09 | RESUME | Fresh orchestrator resumed at the Phase 3 boundary. Re-read capsule + run-log tail + decisions + deliverables + copypaste-spec + W3 task files. Disk reconciled: all 4 prior commits present, HEAD `a490b89` `[spine]`, branch `master`, INDEX CLEAN (only foreign unstaged/untracked files: slide-expand spine, core-protocol.md, how-it-works.html — none touch P3 files). kimi 1.41.0 pinned-flag gate PASS. |
| 2026-06-09 | DISPATCH W3 | `p3-1 ∥ p3-2` → kimi, PARALLEL, disjoint allowlists (p3-1: ✚ `runtime/js/clipboard.js`; p3-2: ✎ `runtime/js/commands.js`). Both UNCOMMITTED per wave override (conductor wave-commits at boundary; honors hazard #1 pathspec-commit discipline). Shape B stdin, `--quiet`, work-dir `html/hypresent`. Reviewer pin: claude-opus. |
| 2026-06-09 | RETURN W3 | Both DONE, named-field clean (no prose drift). p3-1 sess `44f5cdcc…`, p3-2 sess `52c97600…`. Both `commit: none` (wave override honored). Field-check tripwires PASS (no commit claimed; node --check EXIT 0; SKIPPED_COUNT 0). |
| 2026-06-09 | RECONCILE+GATE | §1 re-run on disk: `node --check` clipboard.js EXIT 0 + commands.js EXIT 0 (conductor re-ran, not trusting kimi). commands.js diff = EXACTLY the `paste` factory (+24, after `deleteElement`); no existing factory altered. clipboard.js contract-faithful (deep clone, stripIds + full data-hyp-* sweep on clone+descendants, wasRegion, 4 exports, original never mutated). |
| 2026-06-09 | WAVE COMMIT | Conductor staged EXACTLY the 2 W3 files (explicit pathspec; foreign tree changes excluded — parallel writer had added `orchestration/models/claude/` + modified `cards/routing.md` mid-wave). Commit `d66021c` `[p3-1][p3-2]`, 2 files / 103 insertions. No foreign sweep (W2 collision NOT repeated). |
| 2026-06-09 | REVIEW PASS | W3 (p3-1+p3-2) → Opus review gate (Agent-tool, model opus; one review — the two share the clone→insert seam). CLEAN: 0 defects, no fixes. All 7 pre-flagged suspect areas held; reviewer independently re-ran `git show`/`diff` (commit = exactly the 2 files, no existing factory altered) + `node --check` x2 EXIT 0. Three forward bindings for p3-3 carried → decisions.md (re-tag after insert; `parentHypId` "hyp-N"\|null only; clipboard `stripAllHyp` sweep is load-bearing). p3-1+p3-2 ✅. |
| 2026-06-09 | DISPATCH W4 | `p3-3` → kimi (single task, complexity 10). ✚ `runtime/js/paste.js` (float/insert/grid-fallback/whole-slide; imports clipboard.js+commands.paste, mirrors interaction.js FLIP). UNCOMMITTED per commit-override (foreign writer active → conductor pathspec-commits). Header carries 3 inherited p3-3 bindings + inlined spec rows 3–11 + FLIP anchor (HALT on anchor mismatch). Shape B stdin, `--quiet`. Reviewer pin: claude-opus. |
| 2026-06-09 | RETURN W4 | `p3-3` DONE, named-field clean. sess `9b014fbd…`. commit none (override honored). Concerns (all "matches spec verbatim"): pasteRegion has no slot-guard (only called post-guard); float target w/o data-hyp-id → body; **FLIP anchor matched interaction.js exactly** (worker verified, did not guess). Field-checks PASS (node --check EXIT 0, SKIPPED 0). |
| 2026-06-09 | RECONCILE+COMMIT | §1 on disk: `node --check paste.js` EXIT 0 (conductor re-ran); `git status` runtime/js = only `?? paste.js`. Content contract-faithful (3 fns + regionInView; tag-before-idOf; getAttribute parentHypId; FLIP mirrors interaction.js). Conductor pathspec-commit `92a0423` `[p3-3]`, 1 file/170 ins (no foreign sweep). Flagged for review: pasteRegion EXPORTED (contract listed 2 public exports) + no `slot` null-guard. |
| 2026-06-09 | REVIEW PASS | `p3-3` → Opus review gate (Agent-tool, opus). DONE_WITH_NOTES: 1 fix in place — un-exported `pasteRegion` (commit `6c7a129`; public surface now = `pasteFloat`/`pasteIntoLayout` only). HIGH-VALUE CHECKS: all 7 cross-module imports confirmed vs real exports (line-cited); `history.push` confirmed SYNCHRONOUS → FLIP snapshot/play timing correct; `git show 92a0423` = only paste.js (interaction.js untouched, no import cycle). Conductor re-verified diff (1-line) + node --check EXIT 0. Forward notes for p3-4 wiring + p3-checkpoint → decisions.md. p3-3 ✅. |
| 2026-06-09 | DISPATCH W5 | `p3-4` → kimi (single SERIAL task — LAST writer of shared `shortcuts.js` + `runtime-main.js`; complexity 7). ✎ shortcuts.js (pointer tracker + Ctrl+C/V/Shift+V cases w/ D4 guards), ✎ runtime-main.js (imports + initShortcuts handlers + 3 bridge commands), ✎ docs/spec/03-module-map.md, ✚ tests/e2e/test_copy_paste.py. UNCOMMITTED per commit-override. **E2E DRIVABILITY OVERRIDE:** e2e drives `copy`/`paste`/`paste-into-layout` bridge commands (production path), NOT synthetic Ctrl+C/V (won't cross iframe), NEVER a window.* hook. Validation incl. pytest. Shape B stdin, `--quiet`. Reviewer pin: claude-opus. |
| 2026-06-09 | BLOCKED W5 | `p3-4` did NOT execute — kimi returned a **429 rate-limit** ("usage limit for this period; refreshed next period"), exit **75**. Disk reconciled: NO p3-4 files landed (`git status` of all 4 allowlist files EMPTY), HEAD unchanged `6c7a129`, no `[p3-4]` commit. NOT the exit-75 disk-recovery case (trigger requires uncommitted allowlist work — none) → clean BLOCKED, zero work lost. kimi session `7de9bfd0-2ed5-45c7-a4a0-0f4b66cc85b6` (resumable when quota refreshes). p3-4 is the LAST kimi build task. HALT to owner: executor decision (wait kimi / degrade codex / in-session Claude). |
| 2026-06-09 | OWNER DECISION | Owner: "kimi has reset, continue with it." → re-dispatch p3-4 to kimi (Option C-resolved; original approved executor, lowest risk). |
| 2026-06-09 | RE-DISPATCH W5 | kimi pre-flight re-PASS (1.41.0; pinned flags present). Disk re-reconciled: my 3 code commits `d66021c`/`92a0423`/`6c7a129` CONFIRMED ancestors of HEAD (parallel writer advanced HEAD to foreign `af494e1` `[p3-3] orchestration` WITHOUT disturbing them — fix-forward, no history rewrite); spine edits intact; p3-4's 4 files still absent. L1 deterministic retry — re-dispatch SAME `p3-4.dispatch.md` prompt file (clean block, nothing produced). New kimi session. |
| 2026-06-09 | RETURN+RECONCILE W5 | `p3-4` re-dispatch DONE, named-field clean. sess `6a363094…`. Disk reconciled: `git status` = my 4 files; `node --check` ×2 EXIT 0 (conductor re-ran); **pytest RE-RUN by conductor: 4 passed 6.37s EXIT 0** (disk=truth confirmed, not trusting kimi's claim). Diffs faithful: pointer tracker + Ctrl+C/V/Shift+V w/ D4 guards (existing p2-1 cases byte-intact); imports ONLY `pasteFloat`/`pasteIntoLayout`; 3 bridge commands `copy`/`paste`/`paste-into-layout`; e2e drives the real bridge postMessage protocol (NO window.* hook, NO synthetic keys) w/ measured getBoundingClientRect for C6/C7/C9. |
| 2026-06-09 | COMMIT W5 | Conductor pathspec-commit `3aca3f6` `[p3-4]`, EXACTLY 4 files / 347 ins / 2 del (no foreign sweep; HEAD had moved to foreign `af494e1` — explicit pathspec isolated my files). Phase 3 BUILD complete (all of p3-1..p3-4 landed). |
| 2026-06-09 | REVIEW PASS | `p3-4` → Opus review gate (Agent-tool, opus). CLEAN — DONE, 0 defects, no edits. All 6 suspect areas verified: imports resolve (`pasteRegion` NOT imported); `editing` in scope + existing p2-1 cases byte-intact; D4 guards correct (Ctrl+Alt+C handled before copy; V vs Shift+V split on shiftKey); e2e drives real bridge protocol (no window.* hook); bridge shapes correct; module map accurate. Reviewer re-ran `node --check` ×2 + pytest (4 passed) + **regression `test_shortcuts` (5 passed — existing cases unregressed)**. 3 forward notes for p3-checkpoint → decisions.md (exercise HEADED w/ measured geometry; real Ctrl+C/V keystroke = headed-only owner-UAT; tighten C7-grid assert). p3-4 ✅. **PHASE 3 BUILD COMPLETE & REVIEWED.** Next: p3-checkpoint (HARD halt). |
| 2026-06-09 | CHECKPOINT EXERCISE | p3-checkpoint: conductor exercised C6–C9 headed (chrome-devtools MCP, server :8799, real deck + flow-grow/grid-healthy fixtures). **C6** float-no-reflow (REAL `Ctrl+C`/`Ctrl+V` — keys DO cross the iframe under CDP; originals moved 0.00px) HELD; **C7** flex insert-reflow (REAL `Ctrl+Shift+V`; `hyp-4` +197.8/`hyp-5` +82.4px) + grid→float fallback (grid intact, 3) HELD; **C8** whole-slide dup (region 11→12, new `SECTION.slide--soft`) HELD; **C9** undo-removes + serialize(100,106 chars) + no-comment-threads HELD but **whole-slide REDO FAILED** (over-adds untracked orphan regions; component redo clean per e2e). 5 captures + sheet C6–C9 rows filled. New finding: real Ctrl+C/V/Shift+V cross the iframe under CDP `press_key`. |
| 2026-06-09 | HARD HALT | p3-checkpoint awaiting owner decision. C6/C7/C8 + C9 (undo/serialize/no-comments) HELD; C9 whole-slide REDO `failed` → recommend a small `p3-fix` (`pasteRegion` tag-on-redo bug) before Phase 3 approval, or accept/defer the whole-slide-redo edge. Non-overridable hard halt. |
| 2026-06-09 | OWNER DECISION + DEBUG | Owner: "fix the redo bug now." → top-tier Opus debug+fix agent (§4 debug role). Root cause (corrects initial hypothesis): paste command `do(){cmd.do();tag();}` RE-RUNS `tag()` on redo → stamps the editor's own untagged body UI artifacts (selection ring / Moveable wrapper / color hexProbe) as `data-hyp-id` "regions"; `undo()` removes only the clone → orphan leak. Fix: one-time `tagged` guard (tag only at initial paste) on ALL 3 paste paths. + regression `test_region_paste_undo_redo_roundtrip` + `two-regions.html` fixture (fails-before/passes-after). |
| 2026-06-09 | RECONCILE+COMMIT p3-fix | Disk: diff = 3-path `tagged` guard (minimal, commented); `node --check` EXIT 0; **pytest conductor-rerun 5 passed 8.91s** (incl. new region-redo case). Conductor pathspec-commit `a27c401` `[p3-fix]`, EXACTLY 3 files / 121 ins (no foreign sweep). |
| 2026-06-09 | C9 RE-EXERCISE (HEADED) | Conductor re-exercised whole-slide redo round-trip headed (real deck, :8799): baseline 10 sec → paste 11 → undo 10 → **redo 11 (exactly +1, NO orphans)** → undo 10 (artifact-inclusive body 11→12→11→12→11). C9 defect RESOLVED → verdict **held**. Evidence `c9-02`. **ALL C6–C9 now HELD.** |
| 2026-06-09 | HARD HALT | p3-checkpoint re-presented: C6–C9 ALL held (C9 after `p3-fix` `a27c401`). Awaiting owner approval to complete Phase 3 → final phase (p4-1 cold-verify / p4-refs / p4-compound / p4-checkpoint). |
| 2026-06-09 | APPROVED | Owner approved p3-checkpoint. **PHASE 3 COMPLETE** (p3-1..p3-4 + p3-fix; C6–C9 all held headed). p3-checkpoint + p3-fix flipped ✅/[x]. |
| 2026-06-09 | REFRESH POINT | Owner requested a context refresh at this clean phase boundary (Phase 3 approved). Spine finalized as the resume contract; fresh conductor resumes from `state-capsule.md` → starts the FINAL phase at `p4-1` (independent cold-verifier, C1–C9 from contract + compat regression). Spine-protect commit made (pathspec, plan folder). |
| 2026-06-09 | RESUME | Fresh conductor resumed at the FINAL phase (`p4-1`). Re-read capsule → run-log tail → decisions → deliverables → plan + p4 task files. Disk reconciled: `d66021c`/`92a0423`/`6c7a129`/`3aca3f6`/`a27c401` + spine-protect `a490b89` ALL ancestors of HEAD `9c0bbaa`; branch `master`. Parallel writer keeps advancing HEAD with foreign orchestration commits (expected, hazard #1). No p4 work in flight. |
| 2026-06-09 | COMPAT (p4-1) | Conductor ran the compat regression UN-piped: `pytest test_r3_delete test_r8_font_size_repeat test_r7_alignment tests/unit -q` → **39 passed, EXIT 0** (incl. `test_no_keyboard_delete` + serializer units). Disk = truth (conductor re-ran, not a claim). |
| 2026-06-09 | DISPATCH (p4-1 cold-verify) | Independent cold verifier → Agent-tool Claude (opus), w/ chrome-devtools, **contract-only**. Given ONLY: C1–C9 verbatim (design §11) + app-launch recipe + real deck + fixtures. Builder tests/claims/sheet WALLED OFF; isolation prohibitions explicit. Own sheet+captures to `2026-06-09-…-coldverify[.md]` (distinct from builder sheet). chrome-devtools skill named per `rbtv-sub-agents`. |
| 2026-06-09 | RETURN+RECONCILE (p4-1 cold-verify) | DONE_WITH_NOTES. Verdicts C1-C4/C6-C9 **held**, **C5 `failed`** (builder/verifier mismatch). Reconciled: C5 is a **FALSE NEGATIVE** — the verifier's "visible?" boolean read the scrim's opacity:0/pointer-events:none/display:flex/inset:0 hidden-but-present node as visible. **DRIFT instance** (message ≠ disk): the verifier's OWN screenshots (`c5pw-after-outsideclick.png`, `c5-after-esc-still-open.png`) show the overlay CLOSED → pixels win. Verifier restored deck byte-identical. Not an integrity-tripwire (honest heuristic bug, not fabrication). |
| 2026-06-09 | EXIT PROBES (p4-1, §5) | Conductor's own headed pass (chrome-devtools MCP, :8799, real deck). **E1 (C5):** open via "?" + via Ctrl+/ → Escape → measured `opacity 1→0`, `is-open true→false` both routes → **held** (resolves the C5 false-neg). **E2 (cross-feature):** select→Ctrl+B whole-box bold→Ctrl+C→Ctrl+V float-paste (copy under cursor, **0px** sibling reflow)→Undo×2 restores (slide 8 boxes, bold reverted) → C1+C6+C9 integration **held**. Reproduced known out-of-scope flag: paste `tag()` stamps 16 editor-UI artifacts (moveable-*/interaction-wrapper/hexProbe) that undo doesn't clear — the `p3-fix`-flagged `element-registry` hardening; owner-observable C9 unaffected; serialize strips them. Deck file byte-identical (never saved); 0 source files touched. |
| 2026-06-09 | CONSOLIDATION (p4-1) | C1–C9 **ALL held** (C5 reconciled from a verifier false-negative). Compat green. Consolidated sheet updated (`2026-06-08-shortcuts-copypaste.md` § p4-1) with the cold-verify pass + reconciliation + exit probes + consolidated table. Owner-decision rows for p4-checkpoint: C5 reconciliation, C1 held-surprising (CSS-bold text), known out-of-scope orphan-UI-tag. p4-1 ✅. Next: `p4-refs` (link audit). |
| 2026-06-09 | p4-refs (LINK AUDIT) | Conductor scanned the plan folder. LIVE deliverable docs (decisions/deliverables/learnings/plan/design/specs/task files) **CLEAN**: internal refs use `./`/`../`; external refs app-root/vault-root-relative; no absolute paths; no `../../../` escape; no abs/project-root self-ref links. The only plan-folder paths found are work-dir-relative labels in `phase-*/*.dispatch.md` (e.g. `docs/plan/…/specs/X-spec.md`) — CORRECT for the consuming worker's `--work-dir html/hypresent` (rewriting to `../` would BREAK worker resolution), NOT violations. No fixes needed. p4-refs ✅. Next: `p4-compound`. |
| 2026-06-09 | p4-compound (DRAFTED — write-gated) | Processed `learnings.md` (2 compound-ready entries). Drafted 2 system-improvement proposals; per the task Gate they are NOT written — owner approval required before writing any compound PRD. **(1)** L1 → `rbtv-orchestrating` pathspec-commit discipline for CLI workers in shared repos (target: kimi delta commit-auth + dispatch-wrapper commit-discipline + routing commit-pin); proposed PRD `.user/compounds/rbtv-orchestrating/cp-rbtv-orchestrating-pathspec-commit-shared-repo.md`. **(2)** L2 → done-gate/cold-verifier user-perceived-visibility check (target: `rbtv-done-gate` § Fidelity Floor + verification card §3); proposed PRD folder is an owner routing choice (`rbtv-done-gate/` new vs `rbtv-orchestrating/`). Both surfaced for p4-checkpoint approval; an eval task in `2-areas/compounds/compounds-tasks.md` also pends approval. Next: `p4-checkpoint` (HARD halt). |
