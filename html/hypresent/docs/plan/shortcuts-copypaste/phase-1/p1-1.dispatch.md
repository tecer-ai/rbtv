# DISPATCH — p1-1 (kimi executor)

You are a bounded code executor. Implement EXACTLY the task below. You do NOT reason about intent, design, or alternatives — every decision is pre-resolved in the task. If you hit any ambiguity the task does not resolve, HALT and return `DOUBT_ESCALATED` with the precise question — never guess.

## Binding obligations (you MUST honor every one)

- **Allowlist boundary:** Create/modify ONLY these files (work-dir-relative): `runtime/js/text-format.js` (✎), `runtime/js/runtime-main.js` (✎), `tests/e2e/test_format_repeat_wholebox.py` (✚). Any other file write is a violation.
- **No agentic exploration:** Your read/write universe is the work-dir `html/hypresent` plus the inlined context below. Do NOT read other task files, the plan, or decisions.md. The spec you need is inlined under "INLINED CONTEXT". The design rationale is available at `docs/plan/shortcuts-copypaste/shortcuts-copypaste-design.md` §5 `[FULL READ]` — read it ONLY if escalating a doubt; the task anchors + inlined spec are authoritative.
- **Stray-file ban:** NEVER write scratch/log/summary files into the repo root or anywhere outside the allowlist.
- **Commit discipline:** After validation passes, commit LOCALLY on `master` (`commit_policy: local-only`). Subject MUST start with `[p1-1]`. Stage ONLY the 3 allowlist files — NEVER `git add -A`. NEVER push, force-reset, or amend. Return the hash; it MUST match `git log`.
- **Doubt policy:** `halt`. **Swarm policy:** `disabled` — do NOT launch any subagent.
- **Forbidden ops:** git push · writes outside allowlist · destructive git reset · external production API calls.
- **Evidence:** capture validation command output as you go; the `validation` field cites each command with its EXIT and WALL_MS.

## Return schema — return EXACTLY these five named fields (not prose)

- `status`: one of `DONE` · `DONE_WITH_NOTES` · `BLOCKED` · `DOUBT_ESCALATED` · `NEEDS_CONTEXT`
- `landed`: files created/modified/deleted + the commit hash
- `validation`: each command run, its `EXIT`, its `WALL_MS`; `SKIPPED_COUNT` + per-skip reason
- `concerns`: risks/smells/partial-confidence/adjacent issues noticed but not fixed
- `open_questions`: anything unresolved bearing on this or downstream work (the precise blocker if you halt)

## Decisions pointer

Worker-facing decisions live at `docs/plan/shortcuts-copypaste/decisions.md`. The load-bearing ones for this task are already inlined in the task body (PR1: `format` handler passes `current()` to avoid a `selection.js ↔ text-format.js` ES-module cycle). Do NOT open decisions.md.

---

## INLINED CONTEXT — `docs/plan/shortcuts-copypaste/specs/formatting-spec.md` (authoritative behavior + test plan)

# Spec — Whole-box formatting + repeat-fix

## Goal

The owner selects a text box (single click, NOT editing) and presses Bold / Italic / A+ / A− (button or shortcut): the **whole box** toggles bold/italic, or scales every text size proportionally — repeatably, without re-selecting. While **editing** text, selecting a word and pressing Bold toggles just that word, and pressing again toggles it back without re-selecting (the repeat bug, fixed).

## Context Snapshot

All current code is in `runtime/js/text-format.js` unless noted. Exact anchors live in the backing task (below).

- **`apply(op)`** (op ∈ `bold|italic|fontInc|fontDec`) currently returns `false` unless `getActiveEditElement()` finds an element with `contenteditable="true"` as `document.activeElement`. Bold/italic call `document.execCommand`; `fontInc/fontDec` call `adjustFontSize(el, ±2)`. On any innerHTML change it builds `format(hypId, beforeHtml, afterHtml)` (a `commands.js` innerHTML-swap command) and `push`es it.
- **The repeat bug (design §5.1):** `history.push(cmd)` runs `cmd.do()` → `el.innerHTML = afterHtml`, which re-parses the subtree and **collapses the live selection**. Font-size survives because `adjustFontSize` re-finds its target span by the `data-hyp-fontspan` marker each press; **bold/italic have no such fallback and never restore the selection**, so the second press acts on a collapsed caret.
- **`selection.current()`** → `{hypId, role, rect, isText, alignCaps}`; `isText` is `true` when the element is text-editable. `selection.js` keeps the `hyp-` ring; it has no exported "give me the selected element node" helper yet.
- **`commands.format(hypId, beforeHtml, afterHtml)`** = `{do(){el.innerHTML=afterHtml}, undo(){el.innerHTML=beforeHtml}, label:"format"}`.
- **`text-edit.js`** owns the user-driven edit lifecycle: `enterEdit` sets `contenteditable=true`, `focus()`, `suspendInteraction()`, emits `edit-state {editing:true}`, and commits on blur/Esc. Whole-box formatting MUST NOT route through it.

## Behavior Specification

| # | When (gesture) | Then (observable result) |
|---|----------------|--------------------------|
| 1 | Editing text, a word selected, press Bold | the word bolds; the selection is **restored** from character offsets so a second Bold un-bolds the SAME word — no re-selection |
| 2 | Element selected (NOT editing, `current().isText` true), press Bold | ALL the element's text bolds via the existing `execCommand` path; one `format` command pushed; the ring stays; press again → all un-bolds |
| 3 | Element selected (not editing), press Italic | same as #2 for italic |
| 4 | Element selected (not editing), press A+ / A− | every text size in the box is multiplied by ONE factor (the factor that moves the element's base `font-size` by the existing ±2px step), including individually-sized descendant spans; one `format` command; ratios preserved |
| 5 | Whole-box op finishes | the element's `contenteditable` is restored to its prior value; the `hyp-` selection ring is intact; the box is NOT left in an editing session |

**Mechanism (binding):**
- **Repeat-fix (editing path):** before the op, capture the selection as character offsets (start,end) by walking the editable's text nodes; after `push`, restore a `Range` from those offsets (offsets are stable because bold/italic do not change characters).
- **Whole-box (selected path):** when `apply()` finds no active edit but `selection.current().isText` is true, operate on that element. For bold/italic, manage a transient `contenteditable` **locally** (set → select all → `execCommand` → capture `format` command → restore prior `contenteditable`), never via `text-edit.enterEdit`. For A+/A−, a new helper `scaleWholeBox(el, dir)` multiplies the base and every descendant explicit `font-size` by one factor; do NOT route whole-box sizing through `adjustFontSize`.

## Edge Cases & Error Behavior

| Case | Required behavior |
|------|-------------------|
| Whole-box Bold on an already-all-bold box | toggles OFF (one `format` command) |
| Whole-box A+ on mixed sizes | proportional scale keeps every ratio (design §5.3/D10) |
| Selected element is not text-editable (`isText` false) | whole-box op is a no-op (`apply` returns false) |
| No active edit AND no selection | `apply` returns false (unchanged) |
| Editing-path font-size repeat | unchanged — `test_r8_font_size_repeat` must still pass |
| Transient `contenteditable` | always restored to the prior value; the ring selection remains; `interaction` is never suspended by the whole-box path |

## Out of Scope

- New dedicated font-size keyboard shortcuts (A+/A− stay button/Ctrl-routed via the format command; only their whole-box behaviour changes here).
- Any change to alignment (`computeAlignCaps`/`applyAlign`) or the editing-path font-size logic beyond the §5.2 selection restore.

## Test Plan

Build-phase e2e may be headless (`tests/e2e/`, playwright, `conftest_helpers`). Mirror the existing harness.

| # | Criterion (owner-observable) | Gesture | Expected result |
|---|------------------------------|---------|-----------------|
| C1 | Whole-box formatting | select a text box (not editing); press Bold button; press again; A+/A− | every press toggles/scales the WHOLE box; bold/italic repeat without re-selecting; A+/A− scale proportionally (ratio unchanged) |
| C2 | Repeat bug fixed (editing path) | double-click to edit; select a word; Bold; with it still selected, Bold again | the second press un-bolds the same word — no re-selection |

---

## TASK PAYLOAD — `phase-1/p1-1.task.md` (verbatim — implement exactly this)

---
task_id: p1-1
status: pending
phase: understand
complexity_score: 9
human_review: optional
execution_kind: code
executor: kimi
reviewer: claude-opus
workspace: 3-resources/tools/rbtv
work_dir: html/hypresent
allowed_workdir: html/hypresent
branch: master
allowlist:
  - runtime/js/text-format.js
  - runtime/js/runtime-main.js
  - tests/e2e/test_format_repeat_wholebox.py
commit_policy: local-only
test_command: node --check runtime/js/text-format.js && node --check runtime/js/runtime-main.js && python -m pytest tests/e2e/test_format_repeat_wholebox.py tests/e2e/test_r8_font_size_repeat.py -q
forbidden_ops:
  - git push
  - writes outside allowlist
  - destructive git reset
  - external production API calls
doubt_policy: halt
swarm_policy: disabled
max_kimi_subagents: 0

# Task p1-1: Formatting — repeat-fix + whole-box

## Goal

Make bold/italic **repeat** on the same text while editing (the repeat bug), and make bold/italic/A+/A− act on the **whole text box** when it is selected but not being edited. Behaviour source of truth: the INLINED formatting-spec above (criteria C1, C2). Do NOT restate behaviour — build to that spec.

All work is in `html/hypresent` (the work dir). Run commands from there. Touch ONLY the three allowlist files.

## Acceptance criteria (owner-observable)

1. **C1 — Whole-box.** A text box selected (not editing): `Ctrl+B` / Bold button bolds ALL its text; press again un-bolds; repeatable without re-selecting. Same for `Ctrl+I`. A+/A− scale the whole box proportionally (mixed sizes keep their ratios). The selection ring stays; the box is not left in an edit session.
2. **C2 — Repeat (editing path).** While editing, select a word, `Ctrl+B` bolds it; with it still selected, `Ctrl+B` again un-bolds it — no re-selection.

(`Ctrl+B/Ctrl+I` key handling itself is p2-1; this task makes the underlying `format` path whole-box-aware and repeatable. Exercise C1/C2 here via the Bold/Italic **buttons** and a direct `apply()` call in the e2e; the keys are added in Phase 2.)

## Current code — exact excerpts to anchor edits

### A. `runtime/js/text-format.js` — import line (locate by exact text)
```js
import { idOf } from "./element-registry.js";
```
Change to also import `byId`:
```js
import { idOf, byId } from "./element-registry.js";
```

### B. `runtime/js/text-format.js` — `apply(op)` (locate by exact text)
```js
export function apply(op) {
  const el = getActiveEditElement();
  if (!el) return false;

  const hypId = idOf(el);
  const beforeHtml = el.innerHTML;

  // Ensure focus for execCommand
  el.focus();

  switch (op) {
    case "bold":
      document.execCommand("bold", false, null);
      break;
    case "italic":
      document.execCommand("italic", false, null);
      break;
    case "fontInc":
      adjustFontSize(el, +2);
      break;
    case "fontDec":
      adjustFontSize(el, -2);
      break;
    default:
      return false;
  }

  const afterHtml = el.innerHTML;
  if (afterHtml !== beforeHtml) {
    const cmd = format(hypId, beforeHtml, afterHtml);
    push(cmd);
  }

  return true;
}
```

Required change — make it `apply(op, selInfo)` with two paths:

1. **Editing path (active `contenteditable`, `getActiveEditElement()` truthy):** unchanged for `fontInc/fontDec` (keep `adjustFontSize`). For `bold`/`italic`, ADD the **repeat-fix**: before `execCommand`, capture the live selection as character offsets within `el` (walk its text nodes to a `{start,end}` pair); after `push(cmd)`, restore a `Range` from those offsets and re-apply it to the live `Selection`. Offsets are stable because bold/italic do not change characters (spec §5.2).
2. **Whole-box path (no active edit, but `selInfo && selInfo.isText`):** resolve `const box = byId(selInfo.hypId)`. If `box` is null, return false.
   - `bold`/`italic`: manage a transient `contenteditable` LOCALLY — record `box.getAttribute("contenteditable")`; set it `"true"`; `box.focus()`; select ALL of `box`'s text via a `Range` over its contents; `document.execCommand(op)`; capture before/after `innerHTML` and `push(format(selInfo.hypId, before, after))`; then restore the prior `contenteditable` (remove the attribute if it had none). Do NOT call `text-edit.enterEdit` (its blur listener + `suspendInteraction` are for user edits). Leave the `hyp-` ring intact (do not call `selection.clear`/`select`).
   - `fontInc`/`fontDec`: call the new helper `scaleWholeBox(box, op === "fontInc" ? +1 : -1)` (below).
   - return true.

### C. `runtime/js/text-format.js` — `getActiveEditElement()` (context — do NOT change)
```js
function getActiveEditElement() {
  const active = document.activeElement;
  if (!active) return null;
  if (active.getAttribute("contenteditable") !== "true") return null;
  const hypId = idOf(active);
  return hypId ? active : null;
}
```

### D. New helper `scaleWholeBox(box, dir)` (add to `text-format.js`)
Proportional whole-box font scaling (spec §5.3 / D10):
- `base = parseFloat(getComputedStyle(box).fontSize)`; `newBase = Math.max(8, Math.round(base + dir*2))`; `factor = newBase / base`.
- Capture `beforeHtml = box.innerHTML` AND `beforeSize = box.style.fontSize`.
- Set `box.style.fontSize = newBase + "px"`. For EVERY descendant of `box` that carries an explicit inline `font-size` (`descendant.style.fontSize`), multiply it: `parseFloat(d.style.fontSize) * factor` (rounded, min 8) → `d.style.fontSize`. Non-explicitly-sized text inherits the new base, so all ratios are preserved.
- Capture `afterHtml = box.innerHTML` AND `afterSize = box.style.fontSize`.
- **Undo correctness (binding):** `commands.format` captures ONLY `innerHTML`; the box's OWN `font-size` change would not be undone by it. Therefore `push` a command that restores BOTH — build it inline in this module (you already import `push`):
  ```js
  push({
    do() { const b = byId(selInfo.hypId); if (b) { b.style.fontSize = afterSize; b.innerHTML = afterHtml; } },
    undo() { const b = byId(selInfo.hypId); if (b) { b.style.fontSize = beforeSize; b.innerHTML = beforeHtml; } },
    label: "format",
  });
  ```
  (`scaleWholeBox` is called from `apply`, so `selInfo.hypId`/`box` are in scope; pass what it needs.)

### E. `runtime/js/runtime-main.js` — `format` handler (locate by exact text)
```js
  register("format", (payload) => {
    if (!payload || !payload.op) {
      throw new Error("format: missing op");
    }
    const ok = applyFormat(payload.op);
    return { ok };
  });
```
Change ONLY the call to pass the current selection (note `current` is ALREADY imported at the top of the file: `import { select, clear, current, onSelectionChange } from "./selection.js";`):
```js
    const ok = applyFormat(payload.op, current());
```
This avoids `text-format.js` importing `selection.js` (which would create a `selection.js ↔ text-format.js` ES-module cycle — `selection.js` already imports `computeAlignCaps` from `text-format.js`; the codebase deliberately breaks such cycles, `interaction.js` R09).

## Self-verifiable checks (run before returning)
- `node --check runtime/js/text-format.js && node --check runtime/js/runtime-main.js` → EXIT 0.
- `python -m pytest tests/e2e/test_format_repeat_wholebox.py tests/e2e/test_r8_font_size_repeat.py -q` → passes. `test_r8` is the compat guard (font-size repeat in edit mode must still work); the new file proves C1/C2.
- Manually confirm whole-box A+ then Undo fully restores the prior sizes (the inline command above).

## E2E test — `tests/e2e/test_format_repeat_wholebox.py` (CREATE)
Mirror the existing harness (`tests/e2e/test_r8_font_size_repeat.py`, `conftest_helpers.py`: `start_server(port, test_dialog=True)`, `open_via_dialog_ui`, `wait_runtime_ready`, `doc_eval`). Use a UNIQUE port `8810`. Cover: (a) whole-box bold on a selected-not-editing text box bolds all text and repeats (toggles off) without re-selecting; (b) whole-box A+ multiplies two differently-sized descendants by the same factor (assert the ratio is unchanged via `doc_eval` on computed `font-size`); (c) editing-path bold on a selected word toggles and repeats with the selection preserved. Select elements via a real mouse click at the element's screen rect (the `test_r3_delete` pattern).

## Execution Flow
- **Understand:** read the Context Files (the inlined spec + the live `text-format.js`/`runtime-main.js`); confirm the anchors match the live files (if any excerpt does not match, HALT with the mismatch in `open_questions`).
- **Execute:** apply edits A–E; create the e2e test.
- **Validate:** run the two `node --check` + the two pytest files; all EXIT 0.
- **Close:** commit locally on `master` (below); return the five-field contract.

## Forbidden paths / ops
- Touch ONLY the 3 allowlist files. Do NOT modify `commands.js`, `selection.js`, `text-edit.js`, or any other module. Do NOT add a `selection.js` import to `text-format.js`.
- Do NOT write scratch/log files at the repo root. No `git push`, force-reset, or amend. No external API calls.

## Commit rule
After both `node --check` and both pytest files pass (all EXIT 0): commit locally on `master`. Subject starts with `[p1-1]` (e.g. `[p1-1] fix(hypresent): whole-box formatting + bold/italic repeat`). Stage ONLY the 3 allowlist files — never `git add -A`. Local commit only; NEVER push. Return the hash in `landed`; it MUST match `git log`.

## Return contract
Return exactly: `status` (DONE/BLOCKED) · `landed` (files + commit hash) · `validation` (each command + EXIT + WALL_MS; skips with reasons) · `concerns` · `open_questions` (precise blocker if you halt).
