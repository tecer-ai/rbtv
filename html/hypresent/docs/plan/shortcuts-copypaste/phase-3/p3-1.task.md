---
task_id: p3-1
status: pending
phase: understand
complexity_score: 4
human_review: optional
execution_kind: code
executor: kimi
reviewer: claude-opus
workspace: 3-resources/tools/rbtv
work_dir: html/hypresent
allowed_workdir: html/hypresent
branch: master
allowlist:
  - runtime/js/clipboard.js
commit_policy: local-only
test_command: node --check runtime/js/clipboard.js
forbidden_ops:
  - git push
  - writes outside allowlist
  - destructive git reset
  - external production API calls
doubt_policy: halt
swarm_policy: disabled
max_kimi_subagents: 0
# Human-readable allowlist (✚ create · ✎ modify):
#   ✚ runtime/js/clipboard.js
---

# Task p3-1: In-memory clipboard slot

## Goal

Create `runtime/js/clipboard.js` — a single in-memory clipboard slot for component copy/paste. Behaviour source of truth: `../specs/copypaste-spec.md` (Interface Contracts → Clipboard slot; behaviour rows 1–2). Parallel-safe with p3-2 (disjoint files).

## Context Files

| File | Purpose |
|------|---------|
| `../specs/copypaste-spec.md` | The clipboard-slot shape + copy behaviour (D2, D5, D6 context) |
| `runtime/js/element-registry.js` | `stripIds(clone)` (removes `data-hyp-id` from a clone) — imported here |

## Acceptance criteria
- `copy(el)` stores `{ node: <deep clone with EVERY `data-hyp-*` attribute stripped>, wasRegion, cascade: 0 }`; last copy wins.
- `wasRegion === (el.parentElement === document.body)`.
- The stored clone carries NO `data-hyp-id` and NO other `data-hyp-*` marker (so paste re-tags cleanly and carries no editor state, incl. comment anchors).

## Module contract (build to this)
```
import { stripIds } from "./element-registry.js";

export function copy(el)        // clone = el.cloneNode(true); stripIds(clone) THEN remove every remaining data-hyp-* attr from clone + all descendants; slot = { node: clone, wasRegion: el.parentElement === document.body, cascade: 0 }
export function get()           // → the slot object, or null
export function hasContent()    // → boolean
export function bumpCascade()   // slot.cascade++ (no-op if empty) — used by repeated float-paste (D7)
```

Stripping ALL `data-hyp-*` (not just ids): after `stripIds(clone)`, walk `clone` + its descendants (`clone.querySelectorAll('*')`, plus `clone` itself) and for each, remove every attribute whose name starts with `data-hyp-`. (`stripIds` only handles `data-hyp-id`; markers like `data-hyp-fontspan`/`data-hyp-agent`/`data-hyp-hook`/`data-hyp-text` must also go.)

## Self-verifiable checks
- `node --check runtime/js/clipboard.js` → EXIT 0.
- (Functional copy/paste is exercised by p3-4's e2e and p3-checkpoint; this module has no standalone e2e.)

## Execution Flow
- **Understand:** read the Context Files + the spec's clipboard contract.
- **Execute:** create `clipboard.js` to the contract.
- **Validate:** `node --check` EXIT 0; re-read the file to confirm every `data-hyp-*` is stripped (not only ids).
- **Close:** local commit on `master`; return the contract.

## Forbidden paths / ops
- Touch ONLY `runtime/js/clipboard.js`. No scratch files at repo root. No `git push`/reset/amend. No external API calls.

## Commit rule
After `node --check` passes: local commit on `master`, subject starting `[p3-1]`. Stage ONLY `runtime/js/clipboard.js`. Never push. Return the hash in `landed`.

## Return contract
`status` · `landed` (file + commit hash) · `validation` (command + EXIT + WALL_MS) · `concerns` · `open_questions`.

> `decisions.md` entries: decision + rationale + scope ONLY — never file-lists or N→M narratives; supersede by appending, never rewrite.
