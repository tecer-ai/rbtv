---
task_id: T18
status: pending
phase: validate
complexity_score: 11
human_review: required
---

# Task T18: End-to-End Verification via Chrome DevTools MCP

## Goal

Execute the full verification suite against BOTH test HTML files: every V-case, the regression checklist, and the Save-As validity gate. Produce a results log.

---

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| ../../../spec/05-verification-plan.md | THE test spec — every V-case, §3 regression checklist, §4 Save-As gate. |
| ../../../spec/04-implementation-plan.md | T18 row + all task acceptance IDs being verified. |
| ../../../spec/01-architecture.md | Expected behaviors (isolation, namespacing, serialization). |
| ../../decision-log.md | D1–D6 — the behaviors the gate enforces. |

---

## Tools

| Tool | Purpose |
|------|---------|
| Chrome DevTools MCP | navigate, snapshot, click, evaluate_script, take_screenshot, wait_for, list_console_messages against both fixtures. |

---

## Execution Flow

### Phase: Understand
1. MUST read every Context File.
2. Mark T18 in progress in BOTH locations.
3. Start the server (`python server/server.py`); confirm `/app/` healthy.

### Phase: Execute
1. Run every V-case in `05` §2 on DECK and on REPORT; record PASS/FAIL with evidence.
2. Run the `05` §3 regression checklist after the feature suite.
3. Run the `05` §4 Save-As validity gate on both fixtures (open the saved file WITHOUT the editor; assert chrome-free, document-JS runnable, island inert/invisible, round-trip re-editable).
4. CREATE a results log under `docs/` capturing per-case outcomes + screenshots references.

**Discovery Handling:** any FAIL → log as a discovery in shape.md; if it requires code changes, add a fix task and notify (`PLAN MODIFIED:`).

### Phase: Validate
1. All V-* PASS on both fixtures.
2. §3 regression checklist clean.
3. §4 gate green on DECK and REPORT (hard release blocker).

### Phase: Close
1. Append results summary + any discoveries to shape.md (standalone) or defer (orchestration).
2. `human_review: required` → emit the Human Review Presentation block (format + flag criteria per `3-resources/tools/rbtv/orchestration/workflows/planning/templates/plan-task-microstep-template.md` § Human Review Presentation and `3-resources/tools/rbtv/orchestration/workflows/_shared/authoring/human-review-criteria.md`), pointing at any FAILs and the gate result; if all green, write "None identified" + one-line rationale.
3. Mark T18 complete in BOTH locations after acceptance passes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Verification results log | `docs/verification-results.md` | Per-case PASS/FAIL table + gate result |

---

## Revolving Plan Rules

- Append discoveries to shape.md; FAIL needing code → add fix task; `PLAN MODIFIED:` in output.
