---
task_id: T14
status: pending
phase: execute
complexity_score: 11
human_review: required
---

# Task T14: Recolor — Palette Tokens + Per-Element + Inline-Style (D6)

## Goal

CREATE recolor: (a) palette editor mutating `:root` theme tokens; (b) per-element color override; (c) mutation of inline `style=` colors that sit OUTSIDE the variable system. Palette read filters to color tokens.

---

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| ../../../spec/04-implementation-plan.md | T14 row: contract, acceptance (V-COL-1..5). |
| ../../../spec/02-html-convention.md | H2 variable colors, H9 palette manifest + fallbacks (filter non-color vars by value). |
| ../../../spec/03-module-map.md | §4 color + color-popover contracts. |
| ../../decision-log.md | D6 (both paths + inline-style colors). |
| ../shape.md | Constraint: inline-`style` colors exist outside `:root` (REPORT legend swatches/spans). |

---

## Execution Flow

### Phase: Understand
1. MUST read every Context File.
2. Mark T14 in progress in BOTH locations.
3. Confirm: per-element overrides apply as inline style ON the element (so they serialize cleanly), NOT via an injected stylesheet.

### Phase: Execute
1. CREATE `runtime/js/color.js`: `readPalette()→{tokens[],inlineSites[]}` (enumerate `:root` color tokens, filter non-color vars by value per H9 fallback; discover inline color sites); `applyToken(name,value)` via `documentElement.style.setProperty`; `applyElement(hypId,prop,value)` inline; each builds a color command pushed to `history`.
2. CREATE `app/js/shell/color-popover.js`: wrap Coloris; render the token list + per-element input; dispatch `apply-color` bridge commands.

**Discovery Handling:** simple → resolve + shape.md; complex → add task, notify.

### Phase: Validate
1. V-COL-1 (DECK): change `--primary` → all bound elements recolor; one history entry.
2. V-COL-2: per-element override changes only that element.
3. V-COL-3 (REPORT): recolor an inline `style="background:…"` legend swatch → the inline value updates (not only a token).
4. V-COL-4: palette lists color tokens only (`--nav-w`/`--r` filtered out).
5. V-COL-5: undo a token recolor reverts all affected elements together.

### Phase: Close
1. Append decision/discovery to shape.md (standalone) or defer (orchestration).
2. `human_review: required` → emit the Human Review Presentation block (format + flag criteria per `3-resources/tools/rbtv/orchestration/workflows/planning/templates/plan-task-microstep-template.md` § Human Review Presentation and `3-resources/tools/rbtv/orchestration/workflows/_shared/authoring/human-review-criteria.md`) — D6's inline-style coverage is the easy-to-miss requirement.
3. Mark T14 complete in BOTH locations after acceptance passes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Color module | `runtime/js/color.js` | ES module per §4 contract |
| Color popover UI | `app/js/shell/color-popover.js` | ES module wrapping Coloris |

---

## Revolving Plan Rules

- Append discoveries to shape.md; complex → add task; `PLAN MODIFIED:` in output.
