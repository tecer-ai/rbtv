---
task_id: T13
status: pending
phase: execute
complexity_score: 10
human_review: required
---

# Task T13: Move — Transform Translate (D2)

## Goal

CREATE move: mount Moveable in drag mode inside the iframe; write ONLY `transform: translate()`; compute and emit out-of-flow status after each drag.

---

## Context Files

**MUST read every file in the table below before any execution phase.**

| File | Purpose |
|------|---------|
| ../../../spec/04-implementation-plan.md | T13 row: contract, acceptance (V-MOV-1/2/3/4). |
| ../../../spec/01-architecture.md | §2 (Moveable in iframe, A5); §3 move-commit + `out-of-flow` event. |
| ../../../spec/03-module-map.md | §4 move contract. |
| ../../decision-log.md | D2 + Move/Resize Reconciliation (out-of-flow flag, absolute targets keep transform). |
| ../shape.md | Constraint: move must not reflow siblings; coexists with document JS. |

---

## Execution Flow

### Phase: Understand
1. MUST read every Context File.
2. Mark T13 in progress in BOTH locations.
3. Confirm: write ONLY `transform: translate()` (never `top`/`left`/`margin`); even for absolute elements, move uses transform so the original anchor is preserved.

### Phase: Execute
1. CREATE `runtime/js/move.js`: `begin(hypId)` mounts Moveable (draggable); `end()` writes `transform: translate(...)`, builds `commands.move(before,after)` pushed to `history`, emits `geometry-changed`; after each drag compute whether the translated bounding box overflows the pre-transform flow rect and emit `out-of-flow {bool}`.
2. UPDATE `app/js/shell/toolbar.js`: add the move tool toggle (`set-tool move`).

**Discovery Handling:** simple → resolve + shape.md; complex → add task, notify.

### Phase: Validate
1. V-MOV-1: only `transform` changed; siblings do NOT reflow.
2. V-MOV-2: moving past the flow box fires `out-of-flow true`; UI badge shows.
3. V-MOV-3: undo returns `transform` to its prior value exactly.
4. V-MOV-4 (REPORT): move applies; document's `.in` reveal toggling still functions.

### Phase: Close
1. Append decision/discovery to shape.md (standalone) or defer (orchestration).
2. `human_review: required` → emit the Human Review Presentation block (format + flag criteria per `3-resources/tools/rbtv/orchestration/workflows/planning/templates/plan-task-microstep-template.md` § Human Review Presentation and `3-resources/tools/rbtv/orchestration/workflows/_shared/authoring/human-review-criteria.md`) — D2 is a locked behavior; verify transform-only + out-of-flow flag.
3. Mark T13 complete in BOTH locations after acceptance passes.

---

## Output Requirements

| Output | Location | Format |
|--------|----------|--------|
| Move module | `runtime/js/move.js` | ES module per §4 contract |
| Toolbar move tool | `app/js/shell/toolbar.js` (UPDATE) | ES module |

---

## Revolving Plan Rules

- Append discoveries to shape.md; complex → add task; `PLAN MODIFIED:` in output.
- NOTE: `toolbar.js` shared (see T12 note + `04` Parallelization note).
