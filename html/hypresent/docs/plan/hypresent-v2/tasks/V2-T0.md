# V2-T0 — Create branch `hypresent-v2`

You are Kimi, a code executor. Do EXACTLY what this file says. Read nothing else.

## Goal
Create and check out a git branch named `hypresent-v2` in the working directory's repo.

## Context
The repo root for git is the rbtv repo (the parent of `hypresent/`). The working directory passed via `--work-dir` is the `hypresent/` folder. The sample file `hypresent/tecer-gsmm-introduction.html` is intentionally gitignored (rule `hypresent/tecer-gsmm-introduction*.html` in the repo's `.gitignore`) — it must remain UNTRACKED. Do NOT stage it; `git add` on it would be refused as ignored, and forcing it (`-f`) is forbidden.

## Steps (run these git commands)
1. Create and switch to the branch:
   ```
   git checkout -b hypresent-v2
   ```
   If the branch already exists, switch to it instead:
   ```
   git checkout hypresent-v2
   ```

## File allowlist
- NO source files are created or modified.
- Only git branch state changes. Nothing is staged.

## Acceptance (verify yourself)
- `git rev-parse --abbrev-ref HEAD` prints `hypresent-v2`.
- `git check-ignore hypresent/tecer-gsmm-introduction.html` (run from the repo root) prints the path — confirming the sample is ignored.
- `git status --porcelain` does NOT list `tecer-gsmm-introduction.html`.

## Do NOT
- Do not commit. Do not push. Do not modify any file content.
- Do not stage the sample file or any `tecer-gsmm-introduction*.html` variant (no `git add`, no `-f`).
