V3-T11 Evidence — R6 data: export rgbToHex + normalizeHex + token.hex
=====================================================================
Date: 2026-06-04

1. node --check runtime/js/color.js
   Result: PASS (syntax check succeeds, exit code 0)

2. rgbToHex export
   - Line 170: `export function rgbToHex(rgb) { ... }`
   - Body is UNCHANGED from the original private function.

3. normalizeHex export
   - Line 201: `export function normalizeHex(value) { ... }`
   - Added immediately after `rgbToHex`.

4. readPalette hex mapping
   - Line 149-155:
     ```js
     export function readPalette() {
       const tokens = extractRootVars().map((t) => ({ ...t, hex: normalizeHex(t.value) }));
       return {
         tokens,
         inlineSites: discoverInlineSites(),
       };
     }
     ```
   - Each token now carries a `hex` field normalized from `t.value`.

5. _hexProbe module-scope singleton (RV11)
   - Lines 190-200 create `_hexProbe` once at module scope.
   - Styles set: `visibility:hidden; position:absolute; pointer-events:none`.
   - Appended to `document.body` at module init (or on DOMContentLoaded if body not yet present).
   - `normalizeHex` lazily re-attaches if `_hexProbe.isConnected` is false.

6. var()-token resolution behavior
   - `normalizeHex('var(--x)')` resolves to a concrete `#rrggbb` value IF AND ONLY IF:
     a) `--x` is defined in the document CSS cascade (e.g. on `:root`), AND
     b) the probe element is ATTACHED to the live document.
   - A DETACHED probe would compute `''` for a `var(...)` reference because
     `getComputedStyle` on a detached node resolves only against the UA stylesheet.
     The attached probe inherits the document's `:root` cascade, so variable chains
     resolve to `rgb(...)` which `rgbToHex` then converts to `#rrggbb`.

7. Unchanged functions
   - `extractRootVars`    — unchanged
   - `discoverInlineSites` — unchanged
   - `applyToken`         — unchanged
   - `applyElement`       — unchanged
   - `readElementBorder`  — unchanged (still calls `rgbToHex`)
   - `readElementColors`  — unchanged

8. Git diff scope
   - Only `runtime/js/color.js` was modified by this task.
   - `changelog.md` shows as pre-existing modified in `git status` prior to this work.
